# SpecKiwi Appendix

| Field | Value |
|---|---|
| Document Type | appendix |
| Product | SpecKiwi |
| Product Version | 1.0.0 |
| Version | 1.0.0 |
| Last Updated | 2026-05-10 |

## 1. CLI Command Reference

### 1.1 Global Usage

```text
speckiwi <command> [options]
```

Common options:

| Option | Meaning |
|---|---|
| `--root <path>` | Project root. If omitted, SpecKiwi searches upward for `.git` or `docs/spec/00.index.md`. |
| `--json` | Print machine-readable JSON to stdout. |
| `--no-color` | Disable ANSI color. |
| `--quiet` | Reduce non-essential output. |
| `--help` | Print command help. |
| `--version` | Print package version. |

### 1.2 Commands

| Command | Mutates Files | Purpose |
|---|---:|---|
| `speckiwi init` | yes | Create SRS/rule directories, index, appendix, rules, and both agent files. |
| `speckiwi validate` | no | Validate SRS-MD documents. |
| `speckiwi extract` | no | Extract normalized requirement records. |
| `speckiwi list` | no | List requirements by filter. |
| `speckiwi show <id>` | no | Show one requirement. |
| `speckiwi targets` | no | Show target map and counts. |
| `speckiwi active-target` | no | Show the current active target and summary. |
| `speckiwi completed-work` | no | Show Completed Work Log rows. |
| `speckiwi scopes` | no | Show scope map and counts. |
| `speckiwi add-requirement` | yes | Add a new Requirement Block to a scope SRS document. |
| `speckiwi set-active-target <target>` | yes | Update `Active Target` metadata and Target Map active row. |
| `speckiwi add-completed-work` | yes | Append a Completed Work Log row. |
| `speckiwi update-status <id> <status>` | yes | Update metadata `Status` row. |
| `speckiwi check-ac <id> <ac-id...>` | yes | Mark Acceptance Criteria as checked. |
| `speckiwi uncheck-ac <id> <ac-id...>` | yes | Mark Acceptance Criteria as unchecked. |
| `speckiwi add-evidence <id>` | yes | Add Verification Evidence row. |
| `speckiwi add-trace <id>` | yes | Add Trace Links row. |
| `speckiwi summary --target <target>` | no | Print target readiness summary. |
| `speckiwi links check` | no | Check local links and requirement references. |
| `speckiwi mcp` | yes when workspace bootstrap is missing | Start stdio MCP server. Startup may auto-create the default SRS structure; MCP mutation tools may also modify files. |

### 1.3 Command Details

#### `speckiwi init`

```text
speckiwi init [--root <path>] [--target <target>] [--scope <name:prefix>] [--force]
```

Behavior:

- Checks `docs/`, `docs/spec/`, and `docs/rule/`, then creates missing directories.
- Creates `docs/spec/00.index.md` as an empty SRS index when absent.
- Creates `docs/spec/90.appendix.md` when absent.
- Creates `docs/rule/SRS-MD-Rules-v1.0.0.md` from the bundled rules template when absent.
- Creates or updates uppercase `AGENTS.md` and `CLAUDE.md`.
- Upserts the current SpecKiwi managed block by versioned heading and suffix marker; an existing rules link alone is not a skip condition.
- Does not overwrite existing files unless `--force` is set.

#### `speckiwi validate`

```text
speckiwi validate [--root <path>] [--strict] [--fail-on-warning] [--scope <scope>] [--target <target>] [--json]
```

Exit behavior:

- `0`: no errors.
- `1`: validation errors, or warnings with `--fail-on-warning`.
- `2`: invalid arguments.
- `3`: file I/O failure.
- `4`: fatal parse failure.

#### `speckiwi extract`

```text
speckiwi extract [--target <target>] [--status <status>] [--scope <scope>] [--type <type>] [--tag <tag>] [--json] [--include-markdown]
```

#### `speckiwi list`

```text
speckiwi list [--target <target>] [--status <status>] [--scope <scope>] [--type <type>] [--tag <tag>] [--format <plain|table|json>]
```

#### `speckiwi show`

```text
speckiwi show <RequirementID> [--json] [--markdown]
```

#### `speckiwi add-requirement`

```text
speckiwi add-requirement --type <Type> --scope <Scope> --target <Target> --title <Title> --requirement <Text> --ac <Text> [--ac <Text>...] [--checked-ac <Text>...] [--status <Status>] [--priority <Priority>] [--tags <Tags>] [--risk <Risk>] [--stability <Stability>] [--verification-method <Method>] [--rationale <Text>] [--implementation-notes <Text>] [--evidence <JSON>] [--trace <JSON>] [--dry-run] [--json]
```

Behavior:

- Generates the next Requirement ID from type prefix and scope prefix.
- Appends the generated Requirement Block to the end of the target scope document's `## 4. Requirements` section.
- Defaults `Status` to `planned` when omitted.
- Rejects `verified` unless every AC is checked and at least one Verification Evidence row has a non-empty reference.

#### `speckiwi update-status`

```text
speckiwi update-status <RequirementID> <Status> [--dry-run] [--reason <text>] [--json]
```

Allowed statuses:

```text
planned
in_progress
blocked
implemented
verified
discarded
```

`verified` guard:

- All Acceptance Criteria must be checked.
- Verification Evidence must contain at least one row.
- Evidence reference must not be empty.

#### `speckiwi check-ac`

```text
speckiwi check-ac <RequirementID> <AC-ID...> [--all] [--dry-run] [--json]
```

Examples:

```text
speckiwi check-ac FR-AUTH-001 AC-1 AC-2
speckiwi check-ac FR-AUTH-001 --all
```

#### `speckiwi add-evidence`

```text
speckiwi add-evidence <RequirementID> --type <Type> --ref <Reference> --covers <AC-ID|all> [--notes <text>] [--dry-run] [--json]
```

Example:

```text
speckiwi add-evidence FR-AUTH-001 --type Test --ref src/test/AuthSessionExpirationTest.ts --covers AC-1 --notes "세션 만료 API 테스트"
```

#### `speckiwi add-trace`

```text
speckiwi add-trace <RequirementID> --type <Type> --ref <Reference> --relation <Relation> [--notes <text>] [--dry-run] [--json]
```

#### `speckiwi summary`

```text
speckiwi summary --target <target> [--json] [--markdown]
```

#### `speckiwi mcp`

```text
speckiwi mcp [--root <path>] [--transport stdio]
```

Rules:

- Default transport is `stdio`.
- If `--root` is omitted, the server resolves the project root from the server process current working directory by searching upward for `.git` or `docs/spec/00.index.md`.
- If no project root can be found, the server treats the current working directory as a new project root and creates the default SRS structure before handling MCP calls.
- If a project root exists but `docs/spec/00.index.md` is missing, the server creates the default SRS structure before handling MCP calls.
- Explicit `--root <path>` must point to an existing directory. Missing explicit roots are rejected instead of being created.
- stdout is reserved for MCP JSON-RPC messages.
- stderr is used for logs.
- The server must not print banners or human-readable logs to stdout.

## 2. Exit Codes

| Code | Meaning |
|---:|---|
| `0` | Success |
| `1` | Validation error |
| `2` | Usage, argument, or root resolution error |
| `3` | File I/O error |
| `4` | Fatal parse error |
| `5` | Mutation denied, status transition denied, or verified guard failure |
| `6` | MCP server fatal error |

## 3. MCP Tool Reference

### 3.1 Tool Summary

| Tool | Mutates Files | Purpose |
|---|---:|---|
| `list_requirements` | no | List requirements by filter. |
| `get_requirement` | no | Return one normalized requirement record. |
| `validate_spec` | no | Run validation and return diagnostics. |
| `get_active_target` | no | Return the active target and summary. |
| `list_completed_work` | no | Return Completed Work Log rows. |
| `add_requirement` | yes | Add a new Requirement Block to a scope SRS document. |
| `set_active_target` | yes | Update `Active Target` metadata and Target Map active row. |
| `add_completed_work` | yes | Append a Completed Work Log row. |
| `init_project` | yes | Initialize SRS/rule directories, index, rules, and both agent files. |
| `update_status` | yes | Update requirement status. |
| `check_acceptance_criteria` | yes | Check or uncheck AC items. |
| `add_verification_evidence` | yes | Add Verification Evidence row. |
| `add_trace_link` | yes | Add Trace Links row. |
| `summarize_target` | no | Return target readiness summary. |

### 3.2 MCP Tool Input Shapes

`list_requirements` input:

```json
{
  "target": "v1.0.0",
  "status": "planned",
  "scope": "CLI",
  "type": "interface",
  "tag": "cli"
}
```

`get_requirement` input:

```json
{
  "id": "IR-CLI-010",
  "includeMarkdown": true
}
```

`add_requirement` input:

```json
{
  "type": "interface",
  "scope": "CLI",
  "target": "v1.0.0",
  "title": "add-requirement 명령",
  "requirement": "SpecKiwi는 새 Requirement Block을 생성해야 한다.",
  "acceptanceCriteria": [
    { "text": "필수 입력이 없으면 structured error를 반환한다.", "checked": false }
  ],
  "status": "planned",
  "priority": "high",
  "tags": ["cli", "requirement", "mutation"],
  "risk": "high",
  "stability": "stable",
  "verificationMethod": "test",
  "rationale": "요구사항 추가 형식을 자동화해야 한다.",
  "implementationNotes": ["새 block은 문서 끝에 추가한다."],
  "evidence": [],
  "traceLinks": [],
  "dryRun": false
}
```

`init_project` input:

```json
{
  "target": "v1.0.0",
  "scope": "Core:NODE",
  "force": false
}
```

Behavior:

- Checks `docs/`, `docs/spec/`, and `docs/rule/`, then creates missing directories.
- Creates `docs/spec/00.index.md` as an empty SRS index when absent.
- Creates `docs/spec/90.appendix.md` when absent.
- Creates `docs/rule/SRS-MD-Rules-v1.0.0.md` from the bundled rules template when absent.
- Creates or updates uppercase `AGENTS.md` and `CLAUDE.md`.
- Upserts the current SpecKiwi managed block by versioned heading and suffix marker; an existing rules link alone is not a skip condition.
- Returns created, skipped, updated path lists and diagnostics.

`update_status` input:

```json
{
  "id": "IR-CLI-010",
  "status": "implemented",
  "reason": "Implementation completed, verification pending",
  "dryRun": false
}
```

`check_acceptance_criteria` input:

```json
{
  "id": "IR-CLI-010",
  "acIds": ["AC-1", "AC-2"],
  "checked": true,
  "dryRun": false
}
```

`add_verification_evidence` input:

```json
{
  "id": "IR-CLI-010",
  "type": "Test",
  "reference": "test/cli/update-status.test.ts",
  "covers": "AC-1, AC-2",
  "notes": "Status row mutation test",
  "dryRun": false
}
```

`add_trace_link` input:

```json
{
  "id": "IR-CLI-010",
  "type": "Requirement",
  "reference": "REL-ARCH-001",
  "relation": "depends_on",
  "notes": "Status update must preserve minimal diff",
  "dryRun": false
}
```

`summarize_target` input:

```json
{
  "target": "v1.0.0"
}
```

## 4. MCP Resources

| Resource URI | Content |
|---|---|
| `speckiwi://index` | SRS index summary |
| `speckiwi://active-target` | Active target summary |
| `speckiwi://completed-work` | Completed Work Log rows |
| `speckiwi://completed-work/{target}` | Target-filtered Completed Work Log rows |
| `speckiwi://requirements/{id}` | Requirement detail |
| `speckiwi://targets/{target}` | Target summary |
| `speckiwi://scopes/{scope}` | Scope summary |

### 4.1 MCP Resource Payload Policy

v1.2.0 resources use an envelope-all payload policy. The domain-specific resource payload is always nested under `value`, and diagnostics are carried at the envelope level:

```json
{
  "ok": true,
  "value": {},
  "diagnostics": [],
  "diagnosticsSummary": { "errors": 0, "warnings": 0, "byCode": {} }
}
```

Compatibility note: `speckiwi://completed-work` had documented raw `{ completedWork }` behavior in v1.1.0-era requirements. During the v1.2.0 compatibility window, clients should treat `value.completedWork` as canonical and may support the raw shape only when talking to older v1.1.x servers.

## 5. Normalized Requirement Record

```json
{
  "id": "IR-CLI-010",
  "title": "update-status 명령",
  "file": "docs/spec/30.cli-interface.srs.md",
  "lineStart": 120,
  "lineEnd": 165,
  "type": "interface",
  "target": "v1.0.0",
  "status": "planned",
  "priority": "critical",
  "tags": ["cli", "status", "mutation"],
  "risk": "high",
  "stability": "stable",
  "requirement": "SpecKiwi는 ...",
  "acceptanceCriteria": [
    { "id": "AC-1", "checked": false, "text": "허용 status 외 값은 거부한다.", "line": 141 }
  ],
  "verificationEvidence": [],
  "traceLinks": []
}
```

## 6. Diagnostic Record

```json
{
  "code": "SRS-E004",
  "severity": "error",
  "title": "Type does not match requirement ID prefix",
  "message": "Type does not match ID prefix for IR-CLI-010",
  "filePath": "docs/spec/30.cli-interface.srs.md",
  "line": 128,
  "requirementId": "IR-CLI-010"
}
```

### 6.1 Target Status and Diagnostic Governance

Target Map status values:

| Status | Meaning |
|---|---|
| `planned` | Target is defined but work has not started. |
| `active` | Target is the current work target. |
| `frozen` | Target scope is frozen and requires explicit review for meaningful changes. |
| `completed` | Target requirements are complete but not necessarily released as an immutable baseline. |
| `released` | Target passed release-readiness and is treated as an immutable baseline. |
| `archived` | Historical target that is generally not modified. |

v1.2.0 planned hardening requires a code-level diagnostic registry. Registry entries must include code, severity, title, message template, source rule, and since fields, and the rule document diagnostic table must be contract-tested against that registry.

`SRS-E001` through future v1.2.0 diagnostic codes are governed by `src/core/diagnostic-registry.ts`; code emission through `diagnostic()` fails when a code is unknown or emitted with a severity that differs from the registry.

v1.2.0 planned hardening also requires index diagnostics for duplicate targets, duplicate scope prefixes, multiple active target rows, missing scope documents, unregistered scope SRS documents, Status Summary drift, and Requirement Type Summary drift.

### 6.2 Existing Requirements Needing Review Disposition

Research document section 7.3 identified existing verified requirements that need v1.2.0 review. The disposition matrix below records whether each requirement is kept as the baseline contract, superseded, split, demoted, or covered by v1.2.0 hardening requirements.

| Existing Area | Existing Requirement | Disposition | v1.2.0 Coverage |
|---|---|---|---|
| Parser heading format and forbidden heading content | FR-PARSE-002 | split | Keep base heading parsing; forbidden heading content and code-fence heading behavior are covered by FR-PARSE-011, FR-PARSE-012, and FR-PARSE-013. |
| Requirement section parsing and duplicate section behavior | FR-PARSE-004 | split | Keep section extraction; duplicate section diagnostics and block boundary rules are covered by FR-PARSE-011 and FR-PARSE-012. |
| Acceptance Criteria parsing and nested task list behavior | FR-PARSE-005 | split | Keep top-level AC parsing; nested AC diagnostics are covered by FR-PARSE-012. |
| Validation rule code definitions | FR-PARSE-009 | superseded | Diagnostic code governance is superseded by FR-PARSE-013 registry and rules-doc contract. |
| CLI summary and read payload shape | IR-CLI-015, IR-CLI-021 | covered_by v1.2.0 | Existing summary remains the user-facing baseline; expanded diagnostics and summary parity are covered by IR-CLI-021 and FR-FLOW-007. |
| MCP summary and read payload shape | FR-MCP-008, FR-MCP-013 | covered_by v1.2.0 | Existing summarize_target remains baseline; MCP parity is covered by FR-MCP-013 and envelope resource policy by FR-MCP-014. |
| Release readiness summary | FR-FLOW-003 | covered_by v1.2.0 | Existing release readiness summary is expanded by FR-FLOW-007 and FR-FLOW-008. |
| CI validation example | FR-FLOW-004 | demoted | Keep as example-level v1.0.0 documentation; enforced CI and coverage gates are covered by OPS-NODE-002. |
| Agent instruction managed block | FR-FLOW-005, FR-FLOW-006, FR-FLOW-009 | covered_by v1.2.0 | Existing managed block behavior is kept; canonical root file enforcement is covered by FR-FLOW-009. |
| Parser, validation, and workflow fixture corpus | FR-NODE-003, REL-NODE-001, OPS-NODE-002 | covered_by v1.2.0 | Existing fixture baseline is kept; code fence, duplicate section, nested AC, malformed table, stale evidence, broken local path, CRLF, and multi-scope drift coverage is assigned to FR-PARSE-011, FR-PARSE-012, FR-PARSE-014, FR-FLOW-008, and OPS-NODE-002. |

## 7. Node.js Package Layout

```text
package.json
tsconfig.json
src/
├─ cli/
│  ├─ index.ts
│  ├─ commands.ts
│  └─ formatters.ts
├─ mcp/
│  ├─ server.ts
│  ├─ tools.ts
│  ├─ resources.ts
│  └─ adapter.ts
├─ core/
│  ├─ project-root.ts
│  ├─ parser/
│  ├─ validator/
│  ├─ query/
│  ├─ mutation/
│  ├─ patch/
│  └─ types.ts
└─ index.ts

test/
├─ fixtures/
├─ parser/
├─ validator/
├─ cli/
└─ mcp/
```

## 8. Package Metadata Example

```json
{
  "name": "speckiwi",
  "version": "1.0.0",
  "type": "module",
  "bin": {
    "speckiwi": "./dist/cli/index.js"
  },
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "test:integration": "vitest run test/cli test/mcp",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint ."
  }
}
```

## 9. AGENTS.md / CLAUDE.md Addition

v1.2.0 planned hardening treats uppercase root `AGENTS.md` and `CLAUDE.md` as canonical agent instruction files. Init and MCP startup init must upsert the current managed block by versioned heading and suffix marker, preserving unrelated user content and replacing legacy or older SpecKiwi blocks. Lowercase `agents.md` is retained as a compatibility mirror only; new automation should read the uppercase canonical files first.

```md
# SpecKiwi SRS 워크플로 v1.1

This repository uses `docs/spec/` as the required source of truth for requirements.

Before making any code, test, CLI, MCP, or documentation change, agents MUST:
1. Read `docs/spec/00.index.md`.
2. Find the relevant Requirement ID in the scope SRS files.
3. Mention the Requirement ID in the work summary.
4. If no matching requirement exists, stop and ask whether to create/update an SRS requirement first.

Agents MUST NOT:
- Implement behavior that is not covered by an SRS requirement.
- Create an alternate requirements source outside `docs/spec/`.
- Change requirement IDs manually.
- Mark requirements as verified without evidence.

When SpecKiwi MCP tools are available, agents MUST use them for requirement lookup and safe SRS updates. If MCP is unavailable, use the `speckiwi` CLI.

Current work status workflow:
1. Read the active target with MCP `get_active_target`, or CLI `speckiwi active-target --json` if MCP is unavailable.
2. If `activeTarget` is empty, report that no active target is set and ask which target to use before making target-scoped changes.
3. Read open work with MCP `list_requirements` for `status=in_progress`, `status=blocked`, and `status=implemented`; CLI fallback is `speckiwi list --status <status> --json`.
4. Check missing verification evidence through `summary` or MCP `summarize_target` before saying work is complete.
5. Read recent completed work with MCP `list_completed_work`; CLI fallback is `speckiwi completed-work --json`.

Completed Work Log is a read-only summary for agents. Requirement Block status, Acceptance Criteria, Verification Evidence, and Change Notes remain the source of truth for completion.

<!-- /SpecKiwi SRS 워크플로 -->
```

## 10. Reference Basis

- [SRS-MD Authoring Rules v1.0.0](../rule/SRS-MD-Rules-v1.0.0.md)
- [Model Context Protocol transport specification](https://modelcontextprotocol.io/specification/2025-11-25/basic/transports)
- [Model Context Protocol SDK documentation](https://modelcontextprotocol.io/docs/sdk)
- [MCP TypeScript SDK repository](https://github.com/modelcontextprotocol/typescript-sdk)
- [Node.js Releases](https://nodejs.org/en/about/previous-releases)

## 11. Change Notes

| Date | Change | Reason |
|---|---|---|
| 2026-05-07 | 최초 작성 | CLI, MCP, Node.js implementation details 정의 |
| 2026-05-08 | `add-requirement` command와 `add_requirement` MCP tool reference 추가 | 새 Requirement Block 생성 도구 요구사항 반영 |
| 2026-05-08 | init command/tool reference와 rules path 갱신 | `docs/rule` canonical rules 위치와 MCP `init_project` 요구사항 반영 |
| 2026-05-09 | init reference에서 agent 파일 선택 입력 제거 | init은 항상 `AGENTS.md`와 `CLAUDE.md`를 생성 또는 갱신 |
| 2026-05-09 | root 없는 MCP server 실행 규칙 추가 | `speckiwi mcp`가 `--root` 없이 cwd 기준으로 project root를 결정하도록 명시 |
| 2026-05-09 | MCP startup 자동 init 규칙 추가 | root 또는 index가 없는 작업 디렉터리에서도 MCP 호출 전에 기본 SRS 구조 생성 |
| 2026-05-09 | versioned agent instruction block 추가 | `AGENTS.md`/`CLAUDE.md` prompt 변경을 deterministic replace로 관리 |
| 2026-05-10 | active target CLI/MCP reference 추가 | 현재 작업 목표 버전을 조회/갱신하는 interface 문서화 |
| 2026-05-10 | v1.2.0 hardening appendix 정책 추가 | released target status, diagnostic registry, agent upsert, MCP resource policy를 planned hardening으로 문서화 |
