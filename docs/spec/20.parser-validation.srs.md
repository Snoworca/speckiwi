# SpecKiwi Parser and Validation SRS

| Field | Value |
|---|---|
| Document Type | scope_srs |
| Product | SpecKiwi |
| Product Version | 1.0.0 |
| Scope | PARSE |
| Scope Name | Parser and Validation |
| Version | 1.0.0 |
| Last Updated | 2026-05-13 |
| Rules | [SRS-MD Authoring Rules v1.0.0](../rule/SRS-MD-Rules-v1.0.0.md) |

## 1. Scope Overview

이 문서는 SRS Markdown 문서 탐색, Requirement Block 파싱, normalized requirement record 생성, validation rule 적용을 정의한다.

## 2. Scope Boundaries

### In Scope

- SRS 파일 탐색
- Requirement heading 파싱
- metadata table 파싱
- Acceptance Criteria 파싱
- Verification Evidence 및 Trace Links 파싱
- validation diagnostics 생성

### Out of Scope

- Markdown 전체 렌더링
- 일반 문서 요약
- 외부 URL 네트워크 검증
- YAML 또는 다른 요구사항 포맷 파싱

## 3. Assumptions and Constraints

- SRS 문서는 SRS-MD Authoring Rules v1.0.0을 따른다.
- 파서는 가능한 한 부분 파싱을 제공한다.
- 기본 명령은 네트워크 호출을 하지 않는다.

## 4. Requirements

### FR-PARSE-001 — SRS 파일 탐색

| Field | Value |
| --- | --- |
| Type | functional |
| Target | v1.0.0 |
| Status | verified |
| Priority | critical |
| Tags | parser, glob, docs-spec |
| Risk | medium |
| Stability | stable |
| Verification Method | test |
| GitHub Issue | - |
| Related Docs | - |

#### Requirement

SpecKiwi는 `docs/spec/**/*.srs.md` 파일과 `docs/spec/00.index.md`를 탐색해야 한다.

#### Rationale

요구사항은 scope 문서에 분산되어 있으므로 모든 SRS 문서를 안정적으로 찾아야 한다.

#### Acceptance Criteria

- [x] AC-1: 기본 glob은 `docs/spec/**/*.srs.md`다.
- [x] AC-2: `00.index.md`는 target map과 scope map을 읽기 위해 별도로 탐색한다.
- [x] AC-3: `docs/rule/SRS-MD-Rules-v1.0.0.md`는 요구사항 파일로 파싱하지 않는다.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |
| VE-1 | test | test/release/srs-traceability.test.ts; npm test; npm run test:integration; npm run release:acceptance; npm run perf:srs | all | SpecKiwi v1.0.0 implementation verification on 2026-05-08 |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |

#### Research / Analysis

- -

#### Implementation Notes

- Node.js `fs`와 `path`를 사용한다.
- glob 의존성을 사용할 경우 Windows path를 지원해야 한다.

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-05-07 | 최초 작성 | SpecKiwi v1.0.0 제품 SRS baseline 정의 |



### FR-PARSE-002 — Requirement heading 파싱

| Field | Value |
| --- | --- |
| Type | functional |
| Target | v1.0.0 |
| Status | verified |
| Priority | critical |
| Tags | parser, heading, requirement-id |
| Risk | medium |
| Stability | stable |
| Verification Method | test |
| GitHub Issue | - |
| Related Docs | - |

#### Requirement

SpecKiwi는 `### {RequirementID} — {Title}` 형식의 heading을 Requirement Block 시작점으로 인식해야 한다.

#### Rationale

Requirement ID와 제목은 모든 조회, 상태 갱신, evidence 연결의 기준이다.

#### Acceptance Criteria

- [x] AC-1: em dash `—`를 사용한 heading을 정상 인식한다.
- [x] AC-2: hyphen 또는 colon을 사용한 heading은 validation warning 또는 error로 보고한다.
- [x] AC-3: heading 내부의 link, emoji, emphasis는 validation error로 보고한다.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |
| VE-1 | test | test/release/srs-traceability.test.ts; npm test; npm run test:integration; npm run release:acceptance; npm run perf:srs | all | SpecKiwi v1.0.0 implementation verification on 2026-05-08 |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |

#### Research / Analysis

- -

#### Implementation Notes

- heading regex는 SRS-MD 규칙 문서의 형식을 따른다.
- parser는 block start/end line number를 보존한다.

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-05-07 | 최초 작성 | SpecKiwi v1.0.0 제품 SRS baseline 정의 |



### FR-PARSE-003 — Metadata table 파싱

| Field | Value |
| --- | --- |
| Type | functional |
| Target | v1.0.0 |
| Status | verified |
| Priority | critical |
| Tags | parser, metadata, table |
| Risk | medium |
| Stability | stable |
| Verification Method | test |
| GitHub Issue | - |
| Related Docs | - |

#### Requirement

SpecKiwi는 Requirement heading 바로 아래의 `Field` / `Value` table을 metadata table로 파싱해야 한다.

#### Rationale

Target, Status, Type은 요구사항 목록과 상태 판정의 핵심 속성이다.

#### Acceptance Criteria

- [x] AC-1: `Type`, `Target`, `Status` field를 추출한다.
- [x] AC-2: `Priority`, `Tags`, `Risk`, `Stability`, `GitHub Issue`, `Related Docs` field를 있으면 추출한다.
- [x] AC-3: metadata table이 없으면 validation error를 생성한다.
- [x] AC-4: table cell 줄바꿈은 지원하지 않는다.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |
| VE-1 | test | test/release/srs-traceability.test.ts; npm test; npm run test:integration; npm run release:acceptance; npm run perf:srs | all | SpecKiwi v1.0.0 implementation verification on 2026-05-08 |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |

#### Research / Analysis

- -

#### Implementation Notes

- Field key는 trim하지만 원문 key도 보존한다.
- Tags는 comma-separated array로 정규화한다.

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-05-07 | 최초 작성 | SpecKiwi v1.0.0 제품 SRS baseline 정의 |



### FR-PARSE-004 — Requirement section 파싱

| Field | Value |
| --- | --- |
| Type | functional |
| Target | v1.0.0 |
| Status | verified |
| Priority | critical |
| Tags | parser, sections, block |
| Risk | medium |
| Stability | stable |
| Verification Method | test |
| GitHub Issue | - |
| Related Docs | - |

#### Requirement

SpecKiwi는 Requirement Block 내부의 고정 `####` section을 이름별로 추출해야 한다.

#### Rationale

요구사항 statement, 수용 기준, evidence, trace link는 서로 다른 검증 규칙을 갖는다.

#### Acceptance Criteria

- [x] AC-1: `Requirement`, `Rationale`, `Acceptance Criteria`, `Verification Evidence`, `Trace Links`, `Research / Analysis`, `Implementation Notes`, `Change Notes` section을 추출한다.
- [x] AC-2: 중복 section heading은 validation error로 보고한다.
- [x] AC-3: block 내부 `###` heading은 새 Requirement Block으로 간주한다.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |
| VE-1 | test | test/release/srs-traceability.test.ts; npm test; npm run test:integration; npm run release:acceptance; npm run perf:srs | all | SpecKiwi v1.0.0 implementation verification on 2026-05-08 |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |

#### Research / Analysis

- -

#### Implementation Notes

- section content는 원문 line range와 함께 보존한다.

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-05-07 | 최초 작성 | SpecKiwi v1.0.0 제품 SRS baseline 정의 |



### FR-PARSE-005 — Acceptance Criteria task list 파싱

| Field | Value |
| --- | --- |
| Type | functional |
| Target | v1.0.0 |
| Status | verified |
| Priority | critical |
| Tags | acceptance-criteria, parser, task-list |
| Risk | medium |
| Stability | stable |
| Verification Method | test |
| GitHub Issue | - |
| Related Docs | - |

#### Requirement

SpecKiwi는 `#### Acceptance Criteria` 아래의 `- [ ]` 및 `- [x]` task list를 AC record로 파싱해야 한다.

#### Rationale

verified 판정은 AC 체크 상태에 의존한다.

#### Acceptance Criteria

- [x] AC-1: unchecked AC는 `checked = false`로 추출한다.
- [x] AC-2: checked AC는 `checked = true`로 추출한다.
- [x] AC-3: `AC-1:` 형식의 identifier를 추출한다.
- [x] AC-4: nested task list는 validation error로 보고한다.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |
| VE-1 | test | test/release/srs-traceability.test.ts; npm test; npm run test:integration; npm run release:acceptance; npm run perf:srs | all | SpecKiwi v1.0.0 implementation verification on 2026-05-08 |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |

#### Research / Analysis

- -

#### Implementation Notes

- AC line number를 보존하여 `check-ac` 명령이 해당 줄만 수정할 수 있게 한다.

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-05-07 | 최초 작성 | SpecKiwi v1.0.0 제품 SRS baseline 정의 |



### FR-PARSE-006 — Verification Evidence table 파싱

| Field | Value |
| --- | --- |
| Type | functional |
| Target | v1.0.0 |
| Status | verified |
| Priority | high |
| Tags | evidence, parser, table |
| Risk | medium |
| Stability | stable |
| Verification Method | test |
| GitHub Issue | - |
| Related Docs | - |

#### Requirement

SpecKiwi는 `#### Verification Evidence` 아래의 evidence table을 파싱해야 한다.

#### Rationale

구현 완료와 검증 완료를 구분하려면 evidence의 존재와 범위를 확인해야 한다.

#### Acceptance Criteria

- [x] AC-1: `Evidence ID`, `Type`, `Reference`, `Covers`, `Notes` column을 인식한다.
- [x] AC-2: empty table은 evidence row 0개로 처리한다.
- [x] AC-3: `Covers` 값의 `AC-1`, `AC-2`, `all`, `-`를 정규화한다.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |
| VE-1 | test | test/release/srs-traceability.test.ts; npm test; npm run test:integration; npm run release:acceptance; npm run perf:srs | all | SpecKiwi v1.0.0 implementation verification on 2026-05-08 |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |

#### Research / Analysis

- -

#### Implementation Notes

- column 순서가 다르면 validation warning을 낼 수 있으나 v1.0.0에서는 권장 순서를 기준으로 한다.

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-05-07 | 최초 작성 | SpecKiwi v1.0.0 제품 SRS baseline 정의 |



### FR-PARSE-007 — Trace Links table 파싱

| Field | Value |
| --- | --- |
| Type | functional |
| Target | v1.0.0 |
| Status | verified |
| Priority | medium |
| Tags | traceability, parser, links |
| Risk | medium |
| Stability | stable |
| Verification Method | test |
| GitHub Issue | - |
| Related Docs | - |

#### Requirement

SpecKiwi는 `#### Trace Links` 아래의 trace table을 파싱해야 한다.

#### Rationale

요구사항과 issue, PR, code, test, docs, 다른 요구사항의 연결을 조회하고 검증해야 한다.

#### Acceptance Criteria

- [x] AC-1: `Type`, `Reference`, `Relation`, `Notes` column을 인식한다.
- [x] AC-2: `Requirement` trace reference가 다른 Requirement ID인 경우 존재 여부 검증에 사용한다.
- [x] AC-3: empty table은 trace link 0개로 처리한다.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |
| VE-1 | test | test/release/srs-traceability.test.ts; npm test; npm run test:integration; npm run release:acceptance; npm run perf:srs | all | SpecKiwi v1.0.0 implementation verification on 2026-05-08 |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |

#### Research / Analysis

- -

#### Implementation Notes

- trace link는 write command에서 추가 가능해야 한다.

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-05-07 | 최초 작성 | SpecKiwi v1.0.0 제품 SRS baseline 정의 |



### FR-PARSE-008 — Normalized Requirement Record 생성

| Field | Value |
| --- | --- |
| Type | functional |
| Target | v1.0.0 |
| Status | verified |
| Priority | critical |
| Tags | normalized-record, json, core |
| Risk | high |
| Stability | stable |
| Verification Method | test |
| GitHub Issue | - |
| Related Docs | - |

#### Requirement

SpecKiwi는 각 Requirement Block을 normalized requirement record로 변환해야 한다.

#### Rationale

CLI와 MCP가 같은 데이터를 사용하려면 정규화된 내부 표현이 필요하다.

#### Acceptance Criteria

- [x] AC-1: record는 `id`, `title`, `file`, `lineStart`, `lineEnd`, `type`, `target`, `status`, `priority`, `tags`, `requirement`, `acceptanceCriteria`, `verificationEvidence`, `traceLinks`를 포함한다.
- [x] AC-2: record는 JSON 직렬화가 가능해야 한다.
- [x] AC-3: 원문 Markdown block도 선택적으로 포함할 수 있어야 한다.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |
| VE-1 | test | test/release/srs-traceability.test.ts; npm test; npm run test:integration; npm run release:acceptance; npm run perf:srs | all | SpecKiwi v1.0.0 implementation verification on 2026-05-08 |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |

#### Research / Analysis

- -

#### Implementation Notes

- JSON schema는 `90.appendix.md`에 정의한다.

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-05-07 | 최초 작성 | SpecKiwi v1.0.0 제품 SRS baseline 정의 |



### FR-PARSE-009 — SRS validation rule registry

| Field | Value |
| --- | --- |
| Type | functional |
| Target | v1.0.0 |
| Status | verified |
| Priority | critical |
| Tags | validation, rules, registry |
| Risk | medium |
| Stability | stable |
| Verification Method | test |
| GitHub Issue | - |
| Related Docs | - |

#### Requirement

SpecKiwi는 SRS-MD 규칙 위반을 error와 warning으로 분류하는 validation rule registry를 제공해야 한다.

#### Rationale

규칙을 명확히 분리해야 CLI, MCP, CI에서 동일한 검증 결과를 얻을 수 있다.

#### Acceptance Criteria

- [x] AC-1: 중복 ID는 error로 보고한다.
- [x] AC-2: 필수 field 누락은 error로 보고한다.
- [x] AC-3: Status enum 위반은 error로 보고한다.
- [x] AC-4: verified 조건 위반은 error로 보고한다.
- [x] AC-5: Target 미등록은 warning으로 보고한다.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |
| VE-1 | test | test/release/srs-traceability.test.ts; npm test; npm run test:integration; npm run release:acceptance; npm run perf:srs | all | SpecKiwi v1.0.0 implementation verification on 2026-05-08 |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |

#### Research / Analysis

- -

#### Implementation Notes

- rule id는 `SRS-E001`, `SRS-W001` 형식을 따른다.

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-05-07 | 최초 작성 | SpecKiwi v1.0.0 제품 SRS baseline 정의 |



### FR-PARSE-010 — Target Map과 Scope Map 검증

| Field | Value |
| --- | --- |
| Type | functional |
| Target | v1.0.0 |
| Status | verified |
| Priority | high |
| Tags | target-map, scope-map, index |
| Risk | medium |
| Stability | stable |
| Verification Method | test |
| GitHub Issue | - |
| Related Docs | - |

#### Requirement

SpecKiwi는 `00.index.md`의 metadata table, Target Map, Scope Map을 읽고 요구사항 metadata와 비교해야 한다.

#### Rationale

target과 scope가 등록되지 않으면 release summary와 agent 탐색 흐름이 불안정해진다.

#### Acceptance Criteria

- [x] AC-1: 요구사항의 Target이 Target Map에 없으면 warning을 생성한다.
- [x] AC-2: 요구사항 ID의 scope segment가 Scope Map에 없으면 error를 생성한다.
- [x] AC-3: Scope SRS 문서가 index에 없으면 warning을 생성한다.
- [x] AC-4: index parser는 `Active Target` metadata row를 `activeTarget`으로 노출하고, row가 존재하지만 값이 비어 있으면 빈 문자열로 보존한다.
- [x] AC-5: `Active Target` metadata row가 누락된 legacy index에서만 Target Map의 `active` row와 첫 target fallback을 수행한다.
- [x] AC-6: 명시된 non-empty `Active Target`이 Target Map에 없으면 error를 생성하고, 해당 row의 status가 `active`가 아니면 warning을 생성한다.
- [x] AC-7: index parser는 `Completed Work Log` table을 `completedWork` 배열로 노출하고 `Requirement IDs` cell을 comma-separated ID 배열로 정규화한다.
- [x] AC-8: Completed Work Log의 invalid date, unknown target, unknown scope, unknown requirement ID, non-completed requirement reference는 warning으로 보고한다.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |
| VE-1 | test | test/release/srs-traceability.test.ts; npm test; npm run test:integration; npm run release:acceptance; npm run perf:srs | all | SpecKiwi v1.0.0 implementation verification on 2026-05-08 |
| VE-2 | test | test/core/parser/workspace-records.test.ts; test/core/validator/validation-rules.test.ts; test/core/query/completed-work.test.ts | AC-4, AC-5, AC-7, AC-8 | Active Target empty semantics and Completed Work Log parser/validator coverage on 2026-05-10 |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |

#### Research / Analysis

- -

#### Implementation Notes

- Index parser는 table heading text를 기준으로 필요한 표를 찾고 section number에 의존하지 않는다.
- Active Target row가 존재하지만 값이 비어 있으면 현재 작업 목표가 선택되지 않은 상태다.
- Completed Work Log는 완료 상태의 source of truth가 아니라 index-level summary다.

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-05-10 | active target parser 규칙 추가 | 현재 작업 목표 버전을 index metadata에서 읽기 위함 |
| 2026-05-10 | Completed Work Log parser와 warning 진단 추가 | 완료 작업 요약을 index에서 조회하기 위함 |
| --- | --- | --- |
| 2026-05-07 | 최초 작성 | SpecKiwi v1.0.0 제품 SRS baseline 정의 |



### FR-PARSE-011 — fenced-code-aware requirement block scanner

| Field | Value |
| --- | --- |
| Type | functional |
| Target | v1.2.0 |
| Status | verified |
| Priority | high |
| Tags | parser, block-scanner, code-fence |
| Risk | high |
| Stability | evolving |
| Verification Method | test |
| GitHub Issue | - |
| Related Docs | [docs/research/06.hardening-governance-research.md](../research/06.hardening-governance-research.md) |

#### Requirement

SpecKiwi parser는 fenced code block 내부의 heading-like text를 Requirement heading으로 해석하지 않고, Requirement Block을 문서화된 Requirements section boundary 안에서만 추출해야 한다.

#### Rationale

현재 line-based scanner는 code fence와 후속 section boundary를 충분히 구분하지 못해 정상 Markdown 예시를 requirement로 오인하거나 마지막 block에 문서 후속 section을 포함할 수 있다.

#### Acceptance Criteria

- [x] AC-1: fenced code block 안의 `### FR-...` 텍스트는 requirement record를 만들지 않는다.
- [x] AC-2: fenced code block 안의 malformed heading-like text는 malformed requirement heading diagnostic을 만들지 않는다.
- [x] AC-3: Requirement Block은 다음 valid requirement heading, 다음 관련 top-level `##` section, 또는 파일 끝에서 종료된다.
- [x] AC-4: parser fixture set maps to the research Parser hardening acceptance group with code fence heading, malformed heading inside code fence, and final block boundary regression cases.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |
| VE-1 | command | npm run build; npm run typecheck; npm run lint; npm test; npm run test:integration; npm run release:acceptance; npm run perf:srs | all | P9 bash verification passed on 2026-05-10 |
| VE-2 | command | npm run release:check | all | P9 release readiness passed on 2026-05-10 |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |
| Requirement | FR-PARSE-002 | depends_on | Requirement heading scanner 동작을 hardening한다. |
| Requirement | FR-PARSE-004 | depends_on | Requirement section extraction boundary를 hardening한다. |

#### Research / Analysis

- [SpecKiwi Hardening and Governance Research](../research/06.hardening-governance-research.md) sections 5.4, 5.5, and 7.2.

#### Implementation Notes

- v1.2.0 implementation sequencing은 diagnostic registry와 parser fixture를 먼저 추가한 뒤 scanner behavior를 변경한다.

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-05-10 | 최초 작성 | v1.2.0 parser boundary hardening target 반영 |



### FR-PARSE-012 — duplicate section, nested AC, heading content, and malformed table diagnostics

| Field | Value |
| --- | --- |
| Type | functional |
| Target | v1.2.0 |
| Status | verified |
| Priority | high |
| Tags | parser, validator, diagnostics |
| Risk | high |
| Stability | evolving |
| Verification Method | test |
| GitHub Issue | - |
| Related Docs | [docs/research/06.hardening-governance-research.md](../research/06.hardening-governance-research.md) |

#### Requirement

SpecKiwi는 SRS-MD structural violation인 duplicate requirement section, nested Acceptance Criteria task list, forbidden heading content, malformed table cell count mismatch를 diagnostic으로 보고해야 한다.

#### Rationale

SRS-MD rules는 제한형 Markdown을 요구하지만 현재 parser/validator coverage는 일부 위반을 조용히 덮어쓰거나 누락할 수 있다.

#### Acceptance Criteria

- [x] AC-1: 같은 Requirement Block 안의 중복 `#### Requirement`, `#### Acceptance Criteria`, `#### Verification Evidence`, `#### Trace Links` section은 error diagnostic으로 보고된다.
- [x] AC-2: nested task list로 작성된 AC는 top-level AC로 계산하지 않고 error diagnostic으로 보고된다.
- [x] AC-3: requirement heading title에 Markdown link, emphasis, inline code, emoji가 포함되면 error diagnostic으로 보고된다.
- [x] AC-4: metadata, Verification Evidence, Trace Links table의 row cell count mismatch는 table 종류에 맞는 error 또는 warning diagnostic으로 보고된다.
- [x] AC-5: 각 diagnostic은 file, line, requirementId가 가능한 경우 함께 제공된다.
- [x] AC-6: parser fixture set maps to the research Parser hardening acceptance group with duplicate section, nested AC, forbidden heading content, and malformed table cases.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |
| VE-1 | command | npm run build; npm run typecheck; npm run lint; npm test; npm run test:integration; npm run release:acceptance; npm run perf:srs | all | P9 bash verification passed on 2026-05-10 |
| VE-2 | command | npm run release:check | all | P9 release readiness passed on 2026-05-10 |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |
| Requirement | FR-PARSE-004 | depends_on | Section extraction rules를 확장한다. |
| Requirement | FR-PARSE-005 | depends_on | Acceptance Criteria parser hardening에 해당한다. |
| Requirement | FR-PARSE-009 | related_to | Validation diagnostics contract와 연결된다. |

#### Research / Analysis

- [SpecKiwi Hardening and Governance Research](../research/06.hardening-governance-research.md) sections 5.3, 5.5, and 7.2.

#### Implementation Notes

- Diagnostic code assignment는 FR-PARSE-013의 registry contract와 충돌하지 않아야 한다.

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-05-10 | 최초 작성 | v1.2.0 SRS-MD structural diagnostics target 반영 |



### FR-PARSE-013 — diagnostic registry and rules-doc contract

| Field | Value |
| --- | --- |
| Type | functional |
| Target | v1.2.0 |
| Status | verified |
| Priority | critical |
| Tags | diagnostics, registry, rules |
| Risk | high |
| Stability | evolving |
| Verification Method | test, inspection |
| GitHub Issue | - |
| Related Docs | [docs/research/06.hardening-governance-research.md](../research/06.hardening-governance-research.md), [docs/rule/SRS-MD-Rules-v1.0.0.md](../rule/SRS-MD-Rules-v1.0.0.md) |

#### Requirement

SpecKiwi는 diagnostic code definition을 code-level registry로 관리하고, rule document diagnostic table 및 tests가 같은 code meaning을 검증하도록 해야 한다.

#### Rationale

Diagnostic code 의미가 문서와 구현에서 달라지면 사람, agent, release gate가 validation output을 신뢰할 수 없다.

#### Acceptance Criteria

- [x] AC-1: 모든 emitted diagnostic code는 registry에 등록된 code여야 한다.
- [x] AC-2: registry entry는 code, severity, title, message template, source rule, since 정보를 포함한다.
- [x] AC-3: rules document diagnostic table은 registry와 contract-tested 또는 생성 기반으로 일치한다.
- [x] AC-4: tests는 대표 code가 하나의 의미만 갖는지 검증한다.
- [x] AC-5: 신규 parser/index/completed-work diagnostics는 registry 추가 없이는 사용할 수 없다.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |
| VE-1 | command | npm run build; npm run typecheck; npm run lint; npm test; npm run test:integration; npm run release:acceptance; npm run perf:srs | all | P9 bash verification passed on 2026-05-10 |
| VE-2 | command | npm run release:check | all | P9 release readiness passed on 2026-05-10 |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |
| Requirement | FR-PARSE-009 | depends_on | 기존 validation rule registry 요구사항을 hardening한다. |
| Requirement | OBS-FLOW-001 | related_to | Diagnostics summary는 registry code를 집계해야 한다. |

#### Research / Analysis

- [SpecKiwi Hardening and Governance Research](../research/06.hardening-governance-research.md) sections 5.3 and 7.2.

#### Implementation Notes

- v1.2.0 implementation sequencing은 parser fixture 추가와 registry contract 정립을 초기 단계로 둔다.

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-05-10 | 최초 작성 | v1.2.0 diagnostic governance target 반영 |



### FR-PARSE-014 — index consistency and rollup drift validation

| Field | Value |
| --- | --- |
| Type | functional |
| Target | v1.2.0 |
| Status | verified |
| Priority | high |
| Tags | index, target-map, scope-map, rollup |
| Risk | high |
| Stability | evolving |
| Verification Method | test |
| GitHub Issue | - |
| Related Docs | [docs/research/06.hardening-governance-research.md](../research/06.hardening-governance-research.md) |

#### Requirement

SpecKiwi validator는 index consistency를 검증하여 duplicate target, duplicate scope prefix, multiple active target rows, missing scope document, unregistered SRS document, Status Summary drift, Requirement Type Summary drift를 보고해야 한다.

#### Rationale

Index는 control plane 역할을 하므로 navigation table과 실제 parsed records 사이의 drift가 release 판단을 오염시키지 않아야 한다.

#### Acceptance Criteria

- [x] AC-1: Target Map의 duplicate target value와 multiple active row는 diagnostic으로 보고된다.
- [x] AC-2: Scope Map의 duplicate prefix와 missing primary document file은 diagnostic으로 보고된다.
- [x] AC-3: `docs/spec/**/*.srs.md` 중 Scope Map에 등록되지 않은 scope document는 diagnostic으로 보고된다.
- [x] AC-4: Status Summary count가 parsed requirement status count와 다르면 diagnostic으로 보고된다.
- [x] AC-5: Requirement Type Summary count가 parsed requirement type count와 다르면 diagnostic으로 보고된다.
- [x] AC-6: v1.2.0 hardening 전 기존 historical inconsistency는 정책에 따라 warning으로 분류할 수 있다.
- [x] AC-7: index consistency fixture set maps to the research Index consistency acceptance group with duplicate target, duplicate scope prefix, missing scope document, unregistered SRS document, stale Status Summary, stale Requirement Type Summary, and multi-scope drift cases.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |
| VE-1 | command | npm run build; npm run typecheck; npm run lint; npm test; npm run test:integration; npm run release:acceptance; npm run perf:srs | all | P9 bash verification passed on 2026-05-10 |
| VE-2 | command | npm run release:check | all | P9 release readiness passed on 2026-05-10 |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |
| Requirement | FR-PARSE-010 | depends_on | Target Map과 Scope Map validation을 확장한다. |
| Requirement | FR-FLOW-007 | related_to | Release readiness는 index consistency 결과를 사용한다. |

#### Research / Analysis

- [SpecKiwi Hardening and Governance Research](../research/06.hardening-governance-research.md) sections 6.1 and 7.2.

#### Implementation Notes

- Status/type rollup은 자동 생성으로 전환하기 전까지 validation drift detection으로 보호한다.

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-05-10 | 최초 작성 | v1.2.0 index consistency hardening target 반영 |



### FR-PARSE-015 — Requirement Stability lifecycle taxonomy and validation

| Field | Value |
| --- | --- |
| Type | functional |
| Target | v1.3.0 |
| Status | verified |
| Priority | high |
| Tags | parser, validation, stability, governance |
| Risk | high |
| Stability | evolving |
| Verification Method | test, inspection |
| GitHub Issue | - |
| Related Docs | [docs/research/07.requirement-stability-lifecycle-research.md](../research/07.requirement-stability-lifecycle-research.md) |

#### Requirement

SpecKiwi must define and validate a requirement `Stability` lifecycle independently from implementation `Status`, including `draft` semantics for requirements that are not yet ready to act as implementation contracts.

#### Rationale

`Status` answers implementation progress, while `Stability` answers whether the requirement text and acceptance criteria are mature enough for agents and release workflows to trust. Without this separation, `draft` can be confused with `planned` or `in_progress`.

#### Acceptance Criteria

- [x] AC-1: `Status` remains limited to implementation and verification progress, and `Stability` remains limited to requirement maturity and change-control maturity.
- [x] AC-2: The canonical `Stability` values are `draft`, `evolving`, `stable`, `frozen`, and `deprecated`.
- [x] AC-3: Unknown `Stability` values are validation errors.
- [x] AC-4: Legacy `volatile` values are handled by an explicit migration policy and are not generated for new Requirement Blocks.
- [x] AC-5: New Requirement Blocks default to `Stability=draft` unless an explicit stability value is provided.
- [x] AC-6: Non-discarded `draft` requirements in an active or release target are reported as stability diagnostics.
- [x] AC-7: `verified` requirements with `Stability=draft` are reported as diagnostics because verified requirements must have a trustworthy requirement contract.
- [x] AC-8: `frozen` requirement changes require Change Notes, and `deprecated` requirements remain searchable but are excluded from default new-work recommendations.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |
| VE-1 | command | npm test | all | 2026-05-11 full Vitest suite passed: 30 test files and 120 tests. |
| VE-2 | test | test/core/validator/validation-rules.test.ts | all | Validator fixture coverage for stability diagnostics, legacy volatile, draft, verified draft, and frozen Change Notes behavior. |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |
| Requirement | FR-PARSE-003 | depends_on | Metadata table parsing exposes `Stability`. |
| Requirement | FR-PARSE-009 | depends_on | Validation registry reports invalid stability values. |
| Requirement | FR-FLOW-010 | related_to | Agent and release workflows consume stability diagnostics. |

#### Research / Analysis

- [Requirement Stability Lifecycle Research](../research/07.requirement-stability-lifecycle-research.md)

#### Implementation Notes

- Update `STABILITY_LEVELS`, schema guards, mutation rendering defaults, CLI/MCP input documentation, fixtures, and validator tests together.
- Do not update generated defaults to `draft` without also updating validation, rules, and tests.
- Treat `volatile` as legacy compatibility, not as a value emitted by new project templates.

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-05-10 | 최초 작성 | `Stability=draft` 의미와 validator 처리 요구사항 반영 |



### REL-PARSE-001 — 잘못된 Markdown에 대한 부분 파싱

| Field | Value |
| --- | --- |
| Type | reliability |
| Target | v1.0.0 |
| Status | verified |
| Priority | medium |
| Tags | resilience, partial-parse, diagnostics |
| Risk | medium |
| Stability | stable |
| Verification Method | test |
| GitHub Issue | - |
| Related Docs | - |

#### Requirement

SpecKiwi는 일부 Requirement Block이 잘못되어도 나머지 유효한 block을 가능한 한 파싱하고 진단 정보를 제공해야 한다.

#### Rationale

문서 작성 중에는 일부 block이 불완전할 수 있으며, 전체 조회가 중단되면 개발 흐름이 막힌다.

#### Acceptance Criteria

- [x] AC-1: 한 파일의 한 block 오류가 전체 저장소 파싱 실패를 유발하지 않는다.
- [x] AC-2: 파싱 실패 위치와 이유를 diagnostics에 포함한다.
- [x] AC-3: 치명적 파일 I/O 오류는 별도 error로 보고한다.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |
| VE-1 | test | test/release/srs-traceability.test.ts; npm test; npm run test:integration; npm run release:acceptance; npm run perf:srs | all | SpecKiwi v1.0.0 implementation verification on 2026-05-08 |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |

#### Research / Analysis

- -

#### Implementation Notes

- parse result는 `requirements`와 `diagnostics`를 함께 반환한다.

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-05-07 | 최초 작성 | SpecKiwi v1.0.0 제품 SRS baseline 정의 |



### PERF-PARSE-001 — 일반 저장소 파싱 성능

| Field | Value |
| --- | --- |
| Type | performance |
| Target | v1.0.0 |
| Status | verified |
| Priority | medium |
| Tags | performance, parser, repository |
| Risk | medium |
| Stability | stable |
| Verification Method | test, analysis |
| GitHub Issue | - |
| Related Docs | - |

#### Requirement

SpecKiwi는 500개 Requirement Block을 포함한 저장소에서 기본 파싱과 validation을 2초 이내에 완료해야 한다.

#### Rationale

repo-local workflow는 자주 실행되므로 느리면 사용성이 떨어진다.

#### Acceptance Criteria

- [x] AC-1: 500개 block fixture에서 `speckiwi validate`가 2초 이내에 완료된다.
- [x] AC-2: 성능 테스트 환경은 `docs/analysis/parser-benchmark.md` 또는 test output에 기록된다.
- [x] AC-3: 기본 명령은 네트워크 호출을 수행하지 않는다.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |
| VE-1 | test | test/release/srs-traceability.test.ts; npm test; npm run test:integration; npm run release:acceptance; npm run perf:srs | all | SpecKiwi v1.0.0 implementation verification on 2026-05-08 |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |

#### Research / Analysis

- -

#### Implementation Notes

- Node.js LTS 기준으로 측정한다.
- MCP server는 초기 index build 후 request마다 필요한 경우 incremental refresh를 고려할 수 있다.

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-05-07 | 최초 작성 | SpecKiwi v1.0.0 제품 SRS baseline 정의 |



### FR-PARSE-016 — Completed Work Log report path parsing

| Field | Value |
| --- | --- |
| Type | functional |
| Target | v2.1.1 |
| Status | verified |
| Priority | high |
| Tags | parser, completed-work, report-paths |
| Risk | medium |
| Stability | evolving |
| Verification Method | test |
| GitHub Issue | - |
| Related Docs | [docs/rule/SRS-MD-Rules-v1.0.0.md](../rule/SRS-MD-Rules-v1.0.0.md), [docs/spec/90.appendix.md](./90.appendix.md) |

#### Requirement

SpecKiwi parser and validator must support an optional Completed Work Log report path list without breaking existing five-column Completed Work Log tables.

#### Rationale

Completion reports need to be discoverable from the same index-level summary that agents already read, but existing Completed Work Log rows must remain readable.

#### Acceptance Criteria

- [x] AC-1: Parser accepts both the legacy five-column Completed Work Log table and a six-column table with a trailing Report Paths column.
- [x] AC-2: Parser exposes Report Paths as a normalized string array on each CompletedWorkEntry and returns an empty array when the column or cell is absent.
- [x] AC-3: Report Paths cells are parsed as comma-separated path tokens after trimming surrounding whitespace; token order is preserved and duplicate tokens are not de-duplicated.
- [x] AC-4: Each report path token must be a repository-relative POSIX-style path that resolves under the project root and does not start with `/`, `./`, `../`, or a URL scheme.
- [x] AC-5: Report path tokens containing a `..` segment, backslash, Markdown table delimiter `|`, comma, CR/LF, fragment marker `#`, or an empty value after trimming are malformed.
- [x] AC-6: Validation reports malformed Completed Work Log report path values with a registered `SRS-W024` warning without changing the existing completion status source-of-truth semantics.
- [x] AC-7: Parser validation is lexical only for v2.1.1; it must not require referenced report files to exist and missing report files must not affect release readiness.
- [x] AC-8: SRS-MD rules and appendix schemas document the optional Report Paths column, parser compatibility with legacy five-column logs, and `SRS-W024`.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |
| VE-1 | test | test/core/parser/workspace-records.test.ts; test/core/validator/validation-rules.test.ts; test/core/diagnostic-registry.test.ts | all | - |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |
| Requirement | FR-NODE-010 | depends_on | Extends the Completed Work Log core data shape. |
| Requirement | FR-PARSE-010 | related_to | Index parsing and validation remain the basis for target and summary workflows. |
| Requirement | FR-FLOW-006 | related_to | Agents read Completed Work Log summaries during current-work status checks. |

#### Research / Analysis

- 2026-05-13 feasibility review concluded the parser architecture can support this by extending the header-based Completed Work Log row normalization.
- The review identified delimiter round-trip risk, so v2.1.1 forbids comma in report path tokens instead of defining escaping.
- File-existence validation is intentionally out of scope for v2.1.1 so Completed Work Log remains a summary surface rather than verification evidence.

#### Implementation Notes

- Append the new column after Summary so legacy rows preserve their existing Summary value. Missing or blank report path cells should normalize to an empty array.
- Register `SRS-W024` before emitting malformed report path diagnostics.

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-05-13 | Refined report path grammar and diagnostics | feasibility review found delimiter and validation semantics were underspecified |
| 2026-05-13 | Created | add-requirement |

### FR-PARSE-017 — Stability lifecycle mutation update_stability

| Field | Value |
| --- | --- |
| Type | functional |
| Target | v2.2.1 |
| Status | verified |
| Priority | high |
| Tags | mutation, stability, lifecycle, marker-policy, feasibility:high |
| Risk | medium |
| Stability | stable |
| Verification Method | test |
| GitHub Issue | - |
| Related Docs | - |

#### Requirement

SpecKiwi는 기존 Requirement Block 의 Stability 필드 (`draft`/`evolving`/`stable`/`frozen`/`deprecated`) 를 단일 line-patch 트랜잭션으로 변경하는 `update_stability` mutation core 를 제공해야 한다.

#### Rationale

FR-PARSE-015 가 정의한 Stability lifecycle 의 전이를 도구로 강제하지 않으면 frozen 의 Change Notes 의무 (AC-8) 와 §30.2 [DRAFT] 마커 (SRS-MD-Rules v1.1.0) 가 사용자 수기 책임으로 남는다. update-status.ts 가 이미 §30.1 [DISCARDED] 마커 trigger 를 구현한 패턴을 stability 전이에 동형 복제.

#### Acceptance Criteria

- [x] AC-1: `update_stability({ id, stability, reason?, dryRun? })` 입력을 받아 대상 REQ 의 metadata `Stability` row, heading marker, Change Notes 를 단일 apply-patch SHA256 atomic transaction 으로 갱신한다.
- [x] AC-2: Stability 전이는 유효하지 않은 전이 (예: draft→stable 건너뛰기, deprecated→draft 복귀) 에 대해 warning 을 반환하되 mutation 은 적용한다 (Warning only 정책).
- [x] AC-3: 현재 status 가 `verified` 이면서 새 stability 가 `draft` 인 전이는 FR-PARSE-015 AC-7 에 의거하여 mutation 거부한다.
- [x] AC-4: 새 stability 가 `frozen` 인 전이는 `reason` 인자 부재 시 mutation 을 거부한다 (FR-PARSE-015 AC-8 Change Notes 의무 강제).
- [x] AC-5: 새 stability 가 `draft` 인 전이 시 SRS-MD-Rules v1.1.0 §30.2 에 따라 heading 마커 `[DRAFT — pending decision]` 을 자동 부착하며, `conflicts_with` trace_link 가 존재하면 `[DRAFT — pending decision, see REQ-Y]` 또는 `+N` 으로 확장한다.
- [x] AC-6: stability 가 `draft` 에서 다른 값으로 전이하는 경우 해당 heading 의 `[DRAFT ...]` 마커를 자동 제거하여 원본 형태 `### REQ-ID — Title` 로 복원한다.
- [x] AC-7: `reason` 이 제공되면 Change Notes 표에 `| YYYY-MM-DD | Stability -> {new} | {reason} |` row 를 append 하며, `reason` 길이는 500 UTF-16 code unit 이하이고 제어문자 (TAB/LF/CR 제외) 를 거부한다.
- [x] AC-8: `dryRun: true` 시 patch operation 목록만 반환하고 파일은 업데이트하지 않으며, `dryRun: false` (default) 에서는 tmp+rename atomic write 로 적용한다.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |
| VE-1 | test | test/core/mutation/update-stability.test.ts; test/core/mutation/stability-transition.test.ts | AC-1, AC-2, AC-3, AC-4, AC-5, AC-6, AC-7, AC-8 | - |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |
| Code | src/core/mutation/update-status.ts:44-48 | addition_site | trace_intent: addition_site; new src/core/mutation/update-stability.ts to mirror update-status pattern |
| Requirement | FR-PARSE-015 | extends | FR-PARSE-015 의 Stability lifecycle taxonomy 위에 mutation trigger 를 추가 |
| Reference | docs/rule/SRS-MD-Rules-v1.1.0.md#302 | depends_on | §30.2 [DRAFT] marker auto-apply policy |

#### Research / Analysis

- -

#### Implementation Notes

- `src/core/mutation/update-stability.ts` 신설 — `update-status.ts:53-132` 패턴 동형. heading marker render 는 `src/core/parser/heading-render.ts` 에 `DRAFT` variant 추가 (이미 `DISCARDED` variant 존재). `findIncomingTraceRows` 호출 시 relation 인자를 `conflicts_with` 로 사용. transition 유효성은 별도 helper `isValidStabilityTransition(from, to)` 에서 warning 발행. frozen+no-reason 은 `mutationFail("USAGE", ...)` 로 거부. Status enum 에 `draft` 가 없는 SRS-MD-Rules v1.1.0 §30.2 본문의 표기 오류 (Status=draft → Stability=draft) 는 별도 in-place 정정.

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-05-15 | 최초 작성 | FR-PARSE-015 stability lifecycle 의 mutation trigger 미존재 gap 을 v2.2.1 patch 로 분리 |

### FR-PARSE-018 — Target Goal meta block parsing

| Field | Value |
| --- | --- |
| Type | functional |
| Target | v2.2.2 |
| Status | verified |
| Priority | medium |
| Tags | parser, target, governance |
| Risk | low |
| Stability | evolving |
| Verification Method | test |
| GitHub Issue | - |
| Related Docs | - |

#### Requirement

Workspace parser MUST recognise a Target Goal meta block placed after the Target Map table and expose the parsed goal text on the workspace records so read-side tools can surface per-target goals.

#### Rationale

Three-agent review (2026-05-16) rejected adding a Goal column to the Target Map table because the existing parser hardcodes column index order and renderRow positions; inserting a new column risks breaking backward compatibility on existing SRS workspaces. A separate meta block (heading-based) under or adjacent to the Target Map preserves the table schema and stays Markdown-native.

#### Acceptance Criteria

- [x] AC-1: AC-1: parser detects '### Target: <token>' subsection under the Target Map area in 00.index.md or 90.appendix.md and extracts the body until the next heading of equal or higher level.
- [x] AC-2: AC-2: a '**Goal:**' inline label inside the block is normalised into a single goal string (multi-paragraph preserved as newline-joined).
- [x] AC-3: AC-3: parser exposes the parsed goals via `ParsedWorkspace.index.targetGoals: Record<string, string>` keyed by target token (the `targetGoals` field lives on `IndexDocument`, reachable through `workspace.index.targetGoals`).
- [x] AC-4: AC-4: absence of the meta block does not generate a diagnostic; targetGoals stays empty.
- [x] AC-5: AC-5: an automated parser test reads a fixture with two target goal blocks and asserts both texts are extracted with codepoint equality (verbatim non-ASCII, tab, and escape preservation; multi-paragraph bodies are joined with single `\n`).
- [x] AC-6: AC-6: goal text accepts CR/LF/TAB but rejects other control characters at the mutation layer (consistency with reason policy).

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |
| VE-1 | test | test/core/parser/target-goal-block.test.ts; test/integration/target-goal-workspace.test.ts; npm test | AC-1, AC-2, AC-3, AC-4, AC-5, AC-6 | v2.2.2 implementation verification on 2026-05-17 |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |

#### Research / Analysis

- Architecture-agent recommended: '### Target: <version>' subsection with '**Goal:** ...' inline metadata; parser reads block by heading lookup. UX-agent recommended: 1-line summary in table + multi-paragraph body in heading block - SpecKiwi adopts heading-block-only to avoid table-cell multi-line rendering inconsistency. Governance-agent confirmed: goal field is purely descriptive and cannot trigger status/stability transitions.

#### Implementation Notes

- Extend src/core/parser/index-parser.ts to scan for '### Target:' subsections; surface on ParsedWorkspace and IndexDocument.

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-05-16 | Created | add-requirement |
| 2026-05-17 | AC-5 문구 정정: "byte-faithfully" → "codepoint equality (verbatim non-ASCII/tab/escape, multi-paragraph `\n` join)" | line-split 후 `\n` join 이 원본 byte (CRLF/LF 종결문자) 까지 보존하지 않음을 반영, 검증 어셔트와 명세 의미 정렬 |
| 2026-05-17 | AC-3 표기 정정: `ParsedWorkspace.targetGoals` → `ParsedWorkspace.index.targetGoals` | 실제 노출 경로가 IndexDocument 한 단계 깊이로 위치하며 SRS 본문이 정확한 객체 경로를 명시하지 않아 IEEE29148 unambiguity 충족을 위해 정정 |
| 2026-05-16 | Status -> implemented | v2.2.2 implementation + boost cycle 2 complete |
| 2026-05-16 | Status -> verified | AC 6/6 checked, evidence VE-1 attached, 345/345 PASS, A+B축 평가 통과 |

### FR-PARSE-019 — Normalized query fields for requirement search

| Field | Value |
| --- | --- |
| Type | functional |
| Target | v2.3.0 |
| Status | verified |
| Priority | high |
| Tags | v2.3.0, parser, query, filters, CAND-PARSE-RELATED-DOCS-FILTERS |
| Risk | medium |
| Stability | evolving |
| Verification Method | test |
| GitHub Issue | - |
| Related Docs | [docs/research/09.mcp-cli-tool-abandonment-research.md](../research/09.mcp-cli-tool-abandonment-research.md) |

#### Requirement

SpecKiwi parser and query records must normalize Related Docs, evidence references, trace references, stability, priority, and new-work-candidate data so CLI and MCP requirement search can filter without raw Markdown scans.

#### Rationale

The research found that existing list and extract filters are too narrow, which pushes agents back to grep over SRS Markdown. Search and compact discovery require normalized fields rather than ad hoc string scans.

#### Acceptance Criteria

- [x] AC-1: Requirement records expose normalized relatedDocs tokens, evidence references, trace references, stability, priority, scope, type, status, target, tags, and new-work-candidate eligibility for query code.
- [x] AC-2: Query filters can select by stability, priority, missing evidence, related docs reference, evidence reference, trace reference, and new-work-candidate state.
- [x] AC-3: Filter matching is deterministic for both exact metadata fields and normalized path-like references.
- [x] AC-4: Parser and query tests cover requirements with multiple related docs, multiple evidence references, multiple trace references, missing evidence, draft or deprecated stability, and active-target new-work eligibility.
- [x] AC-5: CLI and MCP search features consume these normalized fields instead of reparsing raw Markdown in adapters.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |
| VE-1 | plan | docs/plans/2026-06-29.specwkiki.v230-plan.plan.md#T-PH003-02 | all | - |
| VE-2 | test | npx vitest run test/core/parser/workspace-records.test.ts test/core/query/query-summary-links.test.ts test/cli/read-commands.test.ts test/mcp/read-tools-resources.test.ts test/cli/global-options-help.test.ts test/mcp/mutation-kind-contract.test.ts test/mcp/stdio-purity.test.ts; npm run typecheck; npm test | AC-1, AC-2, AC-3, AC-4, AC-5 | - |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |
| Requirement | OPS-FLOW-001 | related_to | Research anchor for raw Markdown search replacement. |
| Requirement | FR-PARSE-008 | extends | Extends normalized requirement records for richer query filters. |
| Requirement | FR-PARSE-015 | related_to | Stability fields feed new-work and implementation readiness filters. |
| Code | src/core/query/filter.ts:3-9 | related_to | Current exact filter implementation. |
| Code | src/core/query/records.ts:121 | related_to | Normalized record status and metadata parsing reference. |
| Task | T-PH003-02 | implements | - |
| Code | src/core/query/records.ts; src/core/query/filter.ts; src/cli/commands/read.ts; src/mcp/server.ts | implements | - |

#### Research / Analysis

- Derived from docs/research/09.mcp-cli-tool-abandonment-research.md sections 3.3, 4.5, 6, 7, and 9. This is the parser/query prerequisite for compact CLI and MCP search.

#### Implementation Notes

- Extend normalized records and query filter parsing before adding adapter commands. Avoid exposing filters that adapters compute independently.

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-06-26 | Created | add-requirement |
| 2026-06-30 | Status -> verified | v2.3.0 closeout: acceptance criteria and verification evidence reviewed; final validation gates pending. |

### REL-PARSE-002 — Deduplicated and line-anchored diagnostics

| Field | Value |
| --- | --- |
| Type | reliability |
| Target | v2.3.0 |
| Status | verified |
| Priority | critical |
| Tags | v2.3.0, diagnostics, validation, dedupe, CAND-PARSE-DIAGNOSTICS-DEDUPE |
| Risk | high |
| Stability | evolving |
| Verification Method | test |
| GitHub Issue | - |
| Related Docs | [docs/research/09.mcp-cli-tool-abandonment-research.md](../research/09.mcp-cli-tool-abandonment-research.md), [docs/research/10.kiwi-planner-jsonl-tooling-research.md](../research/10.kiwi-planner-jsonl-tooling-research.md) |

#### Requirement

SpecKiwi diagnostics aggregation must deduplicate repeated diagnostics and preserve row-level line anchors and structured details for parser, index, target map, scope map, summary, and completed-work validation findings.

#### Rationale

The research found duplicated diagnostics and weak index line anchors. Agents need precise, non-duplicated diagnostics to recover by editing the right row or invoking the right tool.

#### Acceptance Criteria

- [x] AC-1: Validation output applies a deterministic dedupe key using diagnostic code, file path, line when available, requirement ID when available, and message.
- [x] AC-2: Index diagnostics for Target Map, Scope Map, Status Summary, Requirement Type Summary, and Completed Work Log include the offending row line when the row exists.
- [x] AC-3: Human read output reports compact diagnostics when nonzero and JSON output preserves diagnostics and diagnosticsSummary consistently.
- [x] AC-4: Regression tests prove malformed table diagnostics are not double counted when parser diagnostics and validator diagnostics are combined.
- [x] AC-5: Diagnostics include structured details or recommended next action data for recoverable cases such as duplicate target rows, rollup drift, missing scope documents, and malformed completed-work report paths.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |
| VE-1 | plan | docs/plans/2026-06-29.specwkiki.v230-plan.plan.md#T-PH003-02 | all | - |
| VE-2 | test | npx vitest run test/core/validator/validation-rules.test.ts test/cli/read-commands.test.ts test/mcp/read-tools-resources.test.ts test/integration/cli-mcp-parity.test.ts test/core/diagnostic-registry.test.ts; npm run typecheck; npm test | AC-1, AC-2, AC-3, AC-4, AC-5 | - |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |
| Requirement | OPS-FLOW-001 | related_to | Research anchor for diagnostics quality findings. |
| Requirement | FR-PARSE-013 | extends | Extends diagnostic registry contract with dedupe and structured details. |
| Requirement | FR-PARSE-014 | extends | Extends index consistency diagnostics with row anchors. |
| Requirement | OBS-FLOW-001 | related_to | Diagnostics summaries consume these canonical counts. |
| Code | src/core/diagnostic.ts:24-41 | related_to | Diagnostics summary and split helpers. |
| Code | src/core/validator/validate-workspace.ts:6-9 | related_to | Validation aggregation entry point. |
| Task | T-PH003-02 | implements | - |
| Code | src/core/diagnostic.ts; src/core/validator/rules.ts; src/cli/commands/read.ts | implements | - |

#### Research / Analysis

- Derived from docs/research/09.mcp-cli-tool-abandonment-research.md sections 3.4, 6, 7, and 9. Also supports docs/research/10 workflow JSONL diagnostics and validator consistency.

#### Implementation Notes

- Add red tests for duplicated malformed-table diagnostics and index row anchors before changing the aggregator. Keep diagnostic code registration rules intact.

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-06-26 | Created | add-requirement |
| 2026-06-30 | Status -> verified | v2.3.0 closeout: acceptance criteria and verification evidence reviewed; final validation gates pending. |

### ~~FR-PARSE-020 — External Completed Work Log parsing~~ [DISCARDED → see FR-PARSE-021]

| Field | Value |
| --- | --- |
| Type | functional |
| Target | v2.3.0 |
| Status | discarded |
| Priority | high |
| Tags | v2.3.0, completed-work, external-log, parser, CAND-NODE-EXTERNAL-COMPLETED-WORK-LOG |
| Risk | medium |
| Stability | evolving |
| Verification Method | test |
| GitHub Issue | - |
| Related Docs | [docs/research/230.md](../research/230.md), [docs/research/11.v2.3.0-tooling-code-improvement-plan.md](../research/11.v2.3.0-tooling-code-improvement-plan.md) |

#### Requirement

SpecKiwi must parse an optional docs/spec/05.completed-work.md Completed Work Log file when that fixed path exists inside the project, while allowing docs/spec/00.index.md to link to it for navigation and preserving legacy Completed Work Log rows embedded in the index for backward compatibility.

#### Rationale

Doc230 found that long Completed Work Log tables in 00.index.md increase token cost and encourage manual index edits. Parsing must support an external log before core, CLI, or MCP can route reads and writes to it.

#### Acceptance Criteria

- [ ] AC-1: Parser discovers docs/spec/05.completed-work.md by fixed path when it exists inside the project; an index link is not required for discovery, and absence of the file produces no diagnostic.
- [ ] AC-2: Parser reads Completed Work Log rows from the external file with the same columns and report-path grammar used by the legacy index table.
- [ ] AC-3: Parser preserves source file path and row line for every Completed Work Log entry so diagnostics identify the row source.
- [ ] AC-4: If both docs/spec/00.index.md and docs/spec/05.completed-work.md contain non-empty Completed Work Log rows, validation emits an explicit duplicate-source or migration diagnostic instead of silently ignoring one source.
- [ ] AC-5: Report Paths validation remains lexical by default and any path-existence validation is exposed only through an opt-in validation mode.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |
| Requirement | OPS-FLOW-001 | depends_on | v2.3.0 tool improvement research anchor. |
| Requirement | FR-PARSE-016 | extends | Reuses Completed Work Log report path grammar. |
| Requirement | FR-PARSE-014 | extends | External completed-work diagnostics are index consistency diagnostics. |
| Code | src/core/parser/index-parser.ts:32-61 | related_to | Current legacy Completed Work Log parser embedded in index parser. |
| Code | src/core/parser/workspace-parser.ts:8-50 | related_to | Workspace parse flow that will discover the external completed-work file. |

#### Research / Analysis

- Derived from docs/research/230.md sections 14.1 through 14.7 and implementation plan phase 0. This is the parser side of the external completed-work log split.

#### Implementation Notes

- Historical draft only. Use FR-PARSE-021 as the canonical external Completed Work Log parser contract.

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-06-28 | Created | add-requirement |

### FR-PARSE-021 — External Completed Work Log parsing

| Field | Value |
| --- | --- |
| Type | functional |
| Target | v2.3.0 |
| Status | verified |
| Priority | high |
| Tags | v2.3.0, completed-work, external-log, parser, CAND-NODE-EXTERNAL-COMPLETED-WORK-LOG |
| Risk | medium |
| Stability | evolving |
| Verification Method | test |
| GitHub Issue | - |
| Related Docs | [docs/research/230.md](../research/230.md), [docs/research/11.v2.3.0-tooling-code-improvement-plan.md](../research/11.v2.3.0-tooling-code-improvement-plan.md) |

#### Requirement

SpecKiwi must parse an optional docs/spec/05.completed-work.md Completed Work Log file when that fixed path exists inside the project, while allowing docs/spec/00.index.md to link to it for navigation and preserving legacy Completed Work Log rows embedded in the index for backward compatibility.

#### Rationale

Doc230 found that long Completed Work Log tables in 00.index.md increase token cost and encourage manual index edits. Parsing must support an external log before core, CLI, or MCP can route reads and writes to it.

#### Acceptance Criteria

- [x] AC-1: Parser discovers docs/spec/05.completed-work.md by fixed path when it exists inside the project; an index link is not required for discovery, and absence of the file produces no diagnostic.
- [x] AC-2: Parser reads Completed Work Log rows from the external file with the same columns and report-path grammar used by the legacy index table.
- [x] AC-3: Parser preserves source file path and row line for every Completed Work Log entry so diagnostics identify the row source.
- [x] AC-4: If both docs/spec/00.index.md and docs/spec/05.completed-work.md contain non-empty Completed Work Log rows, validation emits an explicit duplicate-source or migration diagnostic instead of silently ignoring one source.
- [x] AC-5: Report Paths validation remains lexical by default and any path-existence validation is exposed only through an opt-in validation mode.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |
| VE-1 | plan | docs/plans/2026-06-29.specwkiki.v230-plan.plan.md#T-PH008-02 | all | - |
| VE-2 | test | npx vitest run test/core/query/completed-work.test.ts test/core/parser/workspace-records.test.ts test/core/validator/validation-rules.test.ts test/cli/read-commands.test.ts test/mcp/read-tools-resources.test.ts test/integration/cli-mcp-parity.test.ts test/core/diagnostic-registry.test.ts test/mcp/stdio-purity.test.ts; npm run typecheck; npm test | AC-1, AC-2, AC-3, AC-4, AC-5 | - |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |
| Requirement | OPS-FLOW-001 | depends_on | v2.3.0 tool improvement research anchor. |
| Requirement | FR-PARSE-016 | extends | Reuses Completed Work Log report path grammar. |
| Requirement | FR-PARSE-014 | extends | External completed-work diagnostics are index consistency diagnostics. |
| Code | src/core/parser/index-parser.ts:32-61 | related_to | Current legacy Completed Work Log parser embedded in index parser. |
| Code | src/core/parser/workspace-parser.ts:8-50 | related_to | Workspace parse flow that will discover the external completed-work file. |
| Requirement | FR-PARSE-020 | supersedes | - |
| Task | T-PH008-02 | implements | - |
| Code | src/core/parser/discover.ts; src/core/parser/index-parser.ts; src/core/parser/workspace-parser.ts; src/core/validator/rules.ts | implements | - |

#### Research / Analysis

- Derived from docs/research/230.md sections 14.1 through 14.7 and implementation plan phase 0. This is the parser side of the external completed-work log split.

#### Implementation Notes

- Canonical discovery trigger: fixed path docs/spec/05.completed-work.md existing inside the project is sufficient. The 00.index.md link is recommended navigation, not a prerequisite for parsing or writing.

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-06-28 | Created | add-requirement |
| 2026-06-30 | Status -> verified | v2.3.0 closeout: acceptance criteria and verification evidence reviewed; final validation gates pending. |

### REL-PARSE-003 — Grouped duplicate Requirement ID diagnostics for repair

| Field | Value |
| --- | --- |
| Type | reliability |
| Target | v2.3.0 |
| Status | verified |
| Priority | critical |
| Tags | v2.3.0, diagnostics, duplicate-id, collision-repair |
| Risk | high |
| Stability | evolving |
| Verification Method | test |
| GitHub Issue | - |
| Related Docs | [docs/research/230.md](../research/230.md) |

#### Requirement

SpecKiwi validation must report duplicate Requirement ID failures as grouped, repair-ready SRS-E002 diagnostics so agents can see every occurrence of a duplicate ID and invoke a guarded repair workflow without scanning raw Markdown manually.

#### Rationale

Doc230 merge-time collision research found that the current SRS-E002 duplicate-ID diagnostic proves a duplicate exists but does not expose all occurrences or stable block identity needed for safe repair planning.

#### Acceptance Criteria

- [x] AC-1: SRS-E002 diagnostics group records by duplicate Requirement ID and include every occurrence with filePath, headingLine, title, target, status, stability, marker state when available, and a stable blockHash or equivalent block identity.
- [x] AC-2: The diagnostics summary counts duplicate-ID failures deterministically without double-counting repeated aggregation while machine-readable output preserves the full occurrence list for each duplicate group.
- [x] AC-3: Grouped duplicate-ID diagnostics include a recoverable next-action hint that identifies when a selected occurrence is required before repair planning can write anything.
- [x] AC-4: Tests cover same-file duplicates, cross-file duplicates, discarded or draft marked duplicates, more than two occurrences for one ID, and the existing duplicate-id fixture upgraded to assert grouped diagnostic detail.
- [x] AC-5: CLI and MCP validation JSON preserve grouped SRS-E002 details through diagnostics and diagnosticsSummary without requiring raw Markdown bodies by default.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |
| VE-1 | plan | docs/plans/2026-06-29.specwkiki.v230-plan.plan.md#T-PH016-02 | all | - |
| VE-2 | test | npx vitest run test/core/validator/validation-rules.test.ts test/cli/read-commands.test.ts test/mcp/read-tools-resources.test.ts; npm run typecheck; npm test | AC-1, AC-2, AC-3, AC-4, AC-5 | - |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |
| Requirement | OPS-FLOW-001 | depends_on | v2.3.0 tool improvement research anchor. |
| Requirement | REL-PARSE-002 | extends | Adds grouped duplicate-ID repair detail to the existing deduplicated and line-anchored diagnostics contract. |
| Code | src/core/validator/rules.ts:164-168 | related_to | Current duplicate-ID diagnostic emits only the later occurrence line. |
| Code | src/core/diagnostic-registry.ts:13-18 | related_to | Current SRS-E002 diagnostic definition extension point. |
| Code | test/fixtures/workspaces/duplicate-id/docs/spec/10.product-architecture.srs.md:5-35 | related_to | Existing duplicate ID fixture to upgrade with grouped diagnostic assertions. |
| Task | T-PH016-02 | implements | - |
| Code | src/core/validator/rules.ts | implements | - |

#### Research / Analysis

- Derived from docs/research/230.md sections 16.2, 16.3, 16.4, 16.10, and 16.11. This requirement is the parser/validator prerequisite for guarded merge-time Requirement ID collision repair.

#### Implementation Notes

- Extend the existing validator and diagnostic registry rather than adding a separate duplicate scanner. Keep grouped diagnostics available to CLI and MCP readers through the shared diagnostics envelope.

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-06-29 | Created | add-requirement |
| 2026-06-30 | Status -> verified | v2.3.0 closeout: acceptance criteria and verification evidence reviewed; final validation gates pending. |

### DR-PARSE-001 — DiagnosticDefinition remediation field and runtime registry join

| Field | Value |
| --- | --- |
| Type | data |
| Target | v2.3.0.1 |
| Status | implemented |
| Priority | medium |
| Tags | - |
| Risk | low |
| Stability | evolving |
| Verification Method | test |
| GitHub Issue | - |
| Related Docs | - |

#### Requirement

The system SHALL define every diagnostic code in a single DiagnosticDefinition registry that is the source of truth for each code's severity, title, message template, source rule, since-version, and human-readable remediation guidance. Every DiagnosticDefinition SHALL carry a non-empty remediation string so the field is fully populated across all registered codes. The registry SHALL expose a runtime lookup that joins a diagnostic code to its complete definition, returning the definition for a registered code and failing for an unregistered one, and diagnostic emission SHALL join each emitted diagnostic to the registry by code and reject a severity that diverges from the registered severity. Consumers resolving a definition SHALL be able to surface the remediation guidance, rendered only when present so no empty or undefined remediation artifact is produced.

#### Rationale

-

#### Acceptance Criteria

- [ ] AC-1: Each diagnostic code is defined exactly once in the registry; every definition's code matches the SRS-[EW]NNN shape, its severity is one of error/warning/info, and its title, messageTemplate, sourceRule, and since fields are each non-empty.
- [ ] AC-2: Every DiagnosticDefinition in the registry carries a non-empty remediation string, so the remediation field is fully populated across all codes (e.g. SRS-E001 resolves to a non-empty remediation).
- [ ] AC-3: The runtime registry lookup returns the exact registered definition for a known code and throws an 'Unknown diagnostic code' error for a code that is not registered.
- [ ] AC-4: Diagnostic emission resolves the emitted code against the registry and throws a severity-mismatch error when the emitted severity differs from the code's registered severity.
- [ ] AC-5: When a resolved definition is surfaced to a consumer (e.g. the explain / validate --explain read surface), the remediation text is included verbatim when present and omitted cleanly when absent, in both human-readable and JSON output, never emitting an empty or 'undefined' remediation artifact.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |

#### Research / Analysis

- -

#### Implementation Notes

- -

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-07-08 | Created | add-requirement |

### FR-PARSE-022 — Normalized query fields for requirement search

| Field | Value |
| --- | --- |
| Type | functional |
| Target | v2.3.0.1 |
| Status | implemented |
| Priority | high |
| Tags | - |
| Risk | medium |
| Stability | evolving |
| Verification Method | test |
| GitHub Issue | - |
| Related Docs | - |

#### Requirement

SpecKiwi requirement records SHALL expose normalized, query-ready fields — related-doc references, verification-evidence references, and trace references (as deduplicated, path-normalized string arrays), together with stability, priority, and a derived boolean new-work-candidate flag — computed once during parsing from the Related Docs metadata, Verification Evidence rows, Trace Links rows, and status/stability metadata. Requirement queries SHALL filter records by these fields, supporting exact metadata matches (stability, priority, missing-evidence) and normalized path-like reference matches (related-doc, evidence, trace) so that equivalent reference spellings resolve to the same target. CLI list/search and MCP list_requirements SHALL consume these normalized record fields directly, without re-scanning or re-parsing raw SRS Markdown in the adapter layer.

#### Rationale

-

#### Acceptance Criteria

- [ ] AC-1: Parsing a requirement block yields a record exposing normalized string-array fields relatedDocs (from Related Docs metadata: markdown link targets, else comma/semicolon-split tokens, each path-normalized and deduped), evidenceReferences (from Verification Evidence row references), and traceReferences (from Trace Links row references), plus stability, priority, and a boolean newWorkCandidate. Verified: fixture FR-ARCH-001 yields relatedDocs=[docs/spec/00.index.md], evidenceReferences=[], traceReferences=[FR-ARCH-001], newWorkCandidate=true.
- [ ] AC-2: A requirement query filters records by stability, priority, missingEvidence (true selects records with zero verification-evidence rows), relatedDoc, evidenceReference, traceReference, and newWorkCandidate (boolean), returning only records that match every supplied filter.
- [ ] AC-3: Reference filters (relatedDoc/evidenceReference/traceReference) match against normalized path-like values so equivalent spellings resolve to the same target — e.g. a '../research/beta.md' filter matches records whose normalized relatedDocs contain 'docs/research/beta.md', and a trace filter distinguishes a code path ('src/core/query/filter.ts') from a requirement id ('FR-ARCH-010'). Matching is deterministic and order-stable.
- [ ] AC-4: The derived newWorkCandidate flag is true if and only if a requirement's status is planned, in_progress, or blocked AND its stability is neither draft nor deprecated; parser/query tests exercise records with multiple related docs, multiple evidence references, missing evidence, and draft or deprecated stability.
- [ ] AC-5: CLI 'list' (--stability, --priority, --related-doc, --evidence-reference, --trace-reference, --new-work-candidate) and MCP list_requirements (equivalent filter keys) apply these filters over the normalized record fields and return the normalized relatedDocs/evidenceReferences/traceReferences arrays, without re-parsing raw Markdown in the adapter.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |

#### Research / Analysis

- -

#### Implementation Notes

- -

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-07-08 | Created | add-requirement |

### FR-PARSE-023 — Workspace parser origin split into body and step records

| Field | Value |
| --- | --- |
| Type | functional |
| Target | v2.3.0.1 |
| Status | implemented |
| Priority | high |
| Tags | - |
| Risk | medium |
| Stability | evolving |
| Verification Method | test |
| GitHub Issue | - |
| Related Docs | - |

#### Requirement

The workspace parser (parseWorkspace) SHALL partition parsed requirement blocks by their file origin. Requirement blocks discovered in body scope files SHALL be flattened into ParsedWorkspace.records, while requirement blocks discovered in step files under docs/spec/steps/<name>/ SHALL be flattened into a separate ParsedWorkspace.stepRecords array and SHALL be excluded from both ParsedWorkspace.records and ParsedWorkspace.files. Each step record SHALL carry a stepName field derived from the <name> segment of its docs/spec/steps/<name>/ path. ParsedWorkspace.stepRecords SHALL always be present as an array (empty when no step files exist), and ParsedWorkspace.stateFile SHALL be exposed when a step state file is present, so that a requirement id may exist independently in both the body and step partitions without collision.

#### Rationale

-

#### Acceptance Criteria

- [ ] AC-1: Requirement blocks parsed from a step file under docs/spec/steps/<name>/ appear in ParsedWorkspace.stepRecords, and each such record carries a stepName equal to the <name> path segment (verified by parseWorkspace flattening step files and by FR-NODE-017 AC-2 asserting the loaded step record's stepName and steps/<name>/ filePath).
- [ ] AC-2: A requirement block sourced from a body scope file appears in ParsedWorkspace.records; a block sourced from a step file appears only in ParsedWorkspace.stepRecords and is never added to records, and step files are excluded from ParsedWorkspace.files. The same requirement id may exist independently in both partitions with distinct filePaths (verified by FR-NODE-017 precondition asserting the id in both workspace.records and workspace.stepRecords).
- [ ] AC-3: ParsedWorkspace.stepRecords is always present as an array (empty when the workspace has no step files), and ParsedWorkspace.stateFile is populated when docs/spec/steps/state.md exists (verified by FR-PARSE-023 AC-1 stateFile load and by consumers reading workspace.stepRecords ?? []).
- [ ] AC-4: A workspace containing no step files parses unchanged from prior behavior — records and files are unaffected and stepRecords defaults to an empty array — so existing fixtures parse without modification (verified by the valid-basic workspace fixtures and the FR-PARSE-021 backward-compatible-construction type test).

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |

#### Research / Analysis

- -

#### Implementation Notes

- -

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-07-08 | Created | add-requirement |

### FR-PARSE-024 — Optional origin and stepName provenance fields on RequirementRecord and ParsedWorkspace

| Field | Value |
| --- | --- |
| Type | functional |
| Target | v2.3.0.1 |
| Status | implemented |
| Priority | medium |
| Tags | - |
| Risk | low |
| Stability | evolving |
| Verification Method | test |
| GitHub Issue | - |
| Related Docs | - |

#### Requirement

The system SHALL extend the RequirementRecord type with two optional provenance fields — `origin`, constrained to the union `'body' | 'step'`, and `stepName` of type `string` — and SHALL extend the ParsedWorkspace type with three optional carrier fields — `stepRecords` of type `RequirementRecord[]`, `stepFiles` of type `TextFile[]`, and `stateFile` of type `TextFile | null`. These fields exist to let external step / Completed Work Log parsing attach step provenance to individual records and carry step-file and state-file artifacts through a parsed workspace. Every new field SHALL be declared optional so that existing constructors and fixtures that omit them continue to type-check unchanged (backward compatible).

#### Rationale

-

#### Acceptance Criteria

- [ ] AC-1: RequirementRecord exposes an optional `origin` field whose type is exactly `'body' | 'step' | undefined` and an optional `stepName` field whose type is exactly `string | undefined`; a record supplying only these two optional fields is assignable to `Pick<RequirementRecord, 'origin' | 'stepName'>` (i.e. both are truly optional).
- [ ] AC-2: ParsedWorkspace exposes an optional `stepRecords` field of type exactly `RequirementRecord[] | undefined`, an optional `stepFiles` field of type exactly `TextFile[] | undefined`, and an optional `stateFile` field of type exactly `TextFile | null | undefined`.
- [ ] AC-3: A RequirementRecord literal that omits `origin` and `stepName`, and a ParsedWorkspace literal that omits `stepRecords`, `stepFiles`, and `stateFile`, both remain assignable to their respective types, so existing single-constructor and fixture call sites type-check unchanged.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |

#### Research / Analysis

- -

#### Implementation Notes

- -

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-07-08 | Created | add-requirement |

### FR-PARSE-025 — SRS-E012 trace resolution over the union of body and step requirement records

| Field | Value |
| --- | --- |
| Type | functional |
| Target | v2.3.0.1 |
| Status | implemented |
| Priority | high |
| Tags | - |
| Risk | medium |
| Stability | evolving |
| Verification Method | test |
| GitHub Issue | - |
| Related Docs | - |

#### Requirement

The system SHALL resolve the SRS-E012 referential-existence check for Requirement trace targets against the union of body requirement records and step requirement records (ids = body records plus step records), so that a trace whose target id exists only in a step requirement (declared under docs/spec/steps/<name>/) does not raise SRS-E012. The system SHALL keep the SRS-E002 duplicate-id check scoped to body requirement records only, so that a step requirement reusing an existing body Requirement ID does not raise SRS-E002. The system SHALL exclude step files from the files-based scope-document discovery set (discoveredScopeDocuments), so that step files never raise SRS-W018 (unregistered scope document) or SRS-E025. As a consequence, a step file that declares an id matching an otherwise-unregistered scope SHALL produce zero whole-workspace diagnostics for that id.

#### Rationale

-

#### Acceptance Criteria

- [ ] AC-1: A Requirement trace whose target id exists only in a step requirement resolves cleanly — no SRS-E012 is emitted for that reference — because E012 id resolution uses the union of body and step records.
- [ ] AC-2: A step requirement that shares an identical Requirement ID with a body requirement does not trigger SRS-E002 duplicate-id, because the duplicate-id check iterates body records only (step records are excluded from the duplicate map).
- [ ] AC-3: Step files never emit SRS-W018 or SRS-E025 for an unregistered scope, because the scope-document discovery set is files-based over non-step scope files and excludes step files.
- [ ] AC-4: A step file that declares the same Requirement ID as an unregistered scope yields zero whole-workspace (HEAD) diagnostics for that id (composite of AC-1/AC-2/AC-3).

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |

#### Research / Analysis

- -

#### Implementation Notes

- -

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-07-08 | Created | add-requirement |

### FR-PARSE-026 — state.md step-state loader and parseStepState parser

| Field | Value |
| --- | --- |
| Type | functional |
| Target | v2.3.0.1 |
| Status | implemented |
| Priority | high |
| Tags | - |
| Risk | medium |
| Stability | evolving |
| Verification Method | test |
| GitHub Issue | - |
| Related Docs | - |

#### Requirement

The system SHALL load `docs/spec/steps/state.md`, when it exists, into the parsed workspace as an optional `stateFile` (relative path `docs/spec/steps/state.md`) using an access-then-read pattern, and SHALL yield an undefined `stateFile` without raising an error when the file is absent. The system SHALL expose a `parseStepState` parser that decomposes the step-state Markdown table into one `StepStateEntry` per row, populating the seven columns Step, Status, DependsOn, TouchesScope, TouchesReq, Created, and Updated. The parser SHALL reuse the shared `parseMarkdownTable` (opting into leading-heading skip) rather than introducing a separate table parser, and SHALL flag any Status value outside the enum {active, merging, merged, abandoned} while preserving the raw value.

#### Rationale

-

#### Acceptance Criteria

- [ ] AC-1: When `docs/spec/steps/state.md` exists, parsing the workspace exposes it as `ParsedWorkspace.stateFile` with relative path `docs/spec/steps/state.md` and text content that includes the state table (e.g. the `| Step | Status |` header).
- [ ] AC-2: `parseStepState` returns exactly one entry per table row, with all seven columns (step, status, dependsOn, touchesScope, touchesReq, created, updated) populated verbatim from that row's cells.
- [ ] AC-3: A Status cell whose value is outside {active, merging, merged, abandoned} is preserved as-is on the entry and marked with `invalidStatus === true`.
- [ ] AC-4: `parseStepState` reuses `parseMarkdownTable` (invoked with `skipNonTableLeading: true` to walk past the state.md heading): it yields the same number of rows in the same order, with each entry's Step cell equal to the corresponding `parseMarkdownTable` row's Step value.
- [ ] AC-5: When state.md is absent the loader yields a falsy/undefined `stateFile` without throwing, and when present the same loader yields a populated `stateFile` — the two cases are discriminated by the same access-then-read path.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |

#### Research / Analysis

- -

#### Implementation Notes

- -

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-07-08 | Created | add-requirement |

### FR-PARSE-027 — Step-local scoped validation pass with extended ValidationRule context

| Field | Value |
| --- | --- |
| Type | functional |
| Target | v2.3.0.1 |
| Status | implemented |
| Priority | high |
| Tags | - |
| Risk | medium |
| Stability | evolving |
| Verification Method | test |
| GitHub Issue | - |
| Related Docs | - |

#### Requirement

The system SHALL provide a single step-local validation pass, validateWorkspaceScoped(workspace, { step }), that runs over the parsed-workspace validation context extended to carry step state and step requirement records (ParsedWorkspace.stateFile and ParsedWorkspace.stepRecords). The pass SHALL return a split-diagnostics envelope with independent errors and warnings collections whose parse-time diagnostics are restricted to files under docs/spec/steps/<step>/, so a body-scope error never influences the named step's exit code while a step-anchored parse error does. On top of the scoped parse diagnostics the pass SHALL compose the step advisory warnings over the workspace step context without promoting any advisory to a gate-failing error.

#### Rationale

-

#### Acceptance Criteria

- [ ] AC-1: Calling validateWorkspaceScoped(workspace, { step }) returns a ValidationResult exposing separate errors and warnings arrays (a split-diagnostics envelope), each an array.
- [ ] AC-2: A parse-time structural error anchored under docs/spec/steps/<step>/ (e.g. SRS-E001 from a malformed step scope file) is surfaced in the returned step-local errors.
- [ ] AC-3: Diagnostics anchored to body scope (00.index.md and non-step scope files, e.g. SRS-E014) are excluded from the step pass; a body-only structural error never appears in the result and leaves the step's errors empty (length 0), so it cannot flip the step exit code.
- [ ] AC-4: The scoped pass composes the step advisory warnings (SRS-W044 step-shadows-body, SRS-W045 step-overload, STEP_* direct-conflict) over the workspace step context and returns them strictly in warnings, never in errors.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |

#### Research / Analysis

- -

#### Implementation Notes

- -

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-07-08 | Created | add-requirement |

### FR-PARSE-028 — Step-local advisory validators: W044 step-shadows-body, W045 step-overload, and STEP_* conflict namespace

| Field | Value |
| --- | --- |
| Type | functional |
| Target | v2.3.0.1 |
| Status | implemented |
| Priority | medium |
| Tags | - |
| Risk | low |
| Stability | evolving |
| Verification Method | test |
| GitHub Issue | - |
| Related Docs | - |

#### Requirement

The system SHALL, during the step-local validation pass (validateWorkspaceScoped), emit three step advisory validators as warning-severity diagnostics that never fail the step gate. It SHALL emit SRS-W044 when a step-scope requirement block reuses a requirement id that already exists in the canonical body records (step-shadows-body), SRS-W045 when a single step carries at least the overload threshold of requirement blocks (step-overload, threshold 7), and a STEP_* namespace advisory (STEP_DIRECT_CONFLICT) when the named step is itself active and shares a touched requirement with another active peer step per steps/state.md. All three advisories SHALL be produced as warnings placed in the warnings bucket so they inform agents without influencing the step exit code.

#### Rationale

-

#### Acceptance Criteria

- [ ] AC-1: Given a step whose requirement block reuses a requirement id already defined in the body (canonical) records, the step-local pass emits an SRS-W044 warning that names the shadowed requirement id and is anchored to the offending step block (filePath, heading line, requirementId).
- [ ] AC-2: Given a step that carries requirement blocks at or above the overload threshold (7), the step-local pass emits a single SRS-W045 warning reporting the step name and its requirement count, anchored to the step's first requirement block.
- [ ] AC-3: Given the named step is active and shares a TouchesReq requirement (per docs/spec/steps/state.md) with another active or merging peer step, the step-local pass emits a STEP_* namespace advisory (STEP_DIRECT_CONFLICT) that names the conflicting peer step; a step never conflicts with itself and non-active (non active/merging) steps do not participate.
- [ ] AC-4: All three advisories (SRS-W044, SRS-W045, STEP_*) are emitted with warning severity into the warnings bucket and never appear as errors, so they cannot flip the step validation exit code to failure.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |

#### Research / Analysis

- -

#### Implementation Notes

- -

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-07-08 | Created | add-requirement |

### FR-PARSE-029 — Strict tokenizer for compatibility-check Notes cell

| Field | Value |
| --- | --- |
| Type | functional |
| Target | v2.3.0.1 |
| Status | implemented |
| Priority | medium |
| Tags | - |
| Risk | low |
| Stability | evolving |
| Verification Method | test |
| GitHub Issue | - |
| Related Docs | - |

#### Requirement

The system SHALL provide a `parseCompatibilityNotes` tokenizer that parses the `checked_compatible` Trace Links "Notes" cell into structured fields per the compatibility Notes grammar. It SHALL split the cell into items on the "; " (semicolon-space) separator, split each item into a `key: value` pair on the ": " (colon-space) separator, accept only recognized lowercase keys (`fpv`, `self`, `peer`, `checked-at`), and constrain values to a restricted alphanumeric/hyphen/colon/dot charset so that ellipsis and other arbitrary characters allowed by the generic table-cell guard are rejected. The tokenizer SHALL return an explicit `{ ok, fields?, error? }` result (never throwing for malformed input), treating an empty cell as a valid empty result.

#### Rationale

-

#### Acceptance Criteria

- [ ] AC-1: Given a well-formed Notes string, the tokenizer splits items on the "; " separator, splits each item into a key/value pair on ": ", and returns ok:true with each pair populated in the fields object (e.g. "fpv: fpv1; self: abc123; peer: def456" yields fpv=fpv1, self=abc123, peer=def456).
- [ ] AC-2: The tokenizer returns ok:false when a key is outside the lowercase/hyphen key rule (e.g. an uppercase key such as "FPV") or when a value contains a character outside the restricted value charset (e.g. a space, as in "self: abc 123").
- [ ] AC-3: The tokenizer returns ok:false when a value contains an out-of-charset character such as an ellipsis ("abc…") or a forward slash ("ab/c").
- [ ] AC-4: The tokenizer recognizes the fpv, self, peer, and checked-at tokens as structured fields, returning ok:true and exposing each parsed value under its key (including checked-at with a date-shaped value such as "2026-06-17").
- [ ] AC-5: The tokenizer returns ok:false when a recognized key appears more than once in the input (e.g. "self: a; self: b"), rejecting the ambiguity rather than silently letting a later item overwrite an earlier one.
- [ ] AC-6: The tokenizer treats an empty (or whitespace-only) input as a valid, non-malformed cell, returning ok:true with an empty fields object.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |

#### Research / Analysis

- -

#### Implementation Notes

- -

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-07-08 | Created | add-requirement |

### FR-PARSE-030 — Compatibility cache staleness, coverage, and conflict-concealment validators with E012 relation template

| Field | Value |
| --- | --- |
| Type | functional |
| Target | v2.3.0.1 |
| Status | implemented |
| Priority | high |
| Tags | - |
| Risk | medium |
| Stability | evolving |
| Verification Method | test |
| GitHub Issue | - |
| Related Docs | - |

#### Requirement

The system SHALL validate the compatibility-check cache carried as `checked_compatible` Trace Links rows (self/peer semanticSha pins under the `fpv1` format contract) and emit distinct warning diagnostics for three cache defects: SRS-W041 when a stored self or peer pin no longer matches its endpoint's current semanticSha (stale pin), SRS-W042 when an expected compatibility row for an adjacent requirement pair is absent (missing coverage), and SRS-W043 when a recorded compatibility row conceals a detected conflict. The system SHALL also extend the SRS-E012 trace-reference diagnostic so its message renders the offending relation through a template placeholder. These validators SHALL be assigned codes that do not collide with the step validators SRS-W044 and SRS-W045.

#### Rationale

-

#### Acceptance Criteria

- [ ] AC-1: SRS-W041 is emitted when a stored self or peer pin differs from the referenced endpoint's current semanticSha (stale compatibility pin).
- [ ] AC-2: SRS-W042 is emitted when an expected compatibility row for an adjacent requirement pair is absent (missing compatibility coverage).
- [ ] AC-3: SRS-W043 is emitted when a compatibility row conceals a detected conflict between the two endpoints.
- [ ] AC-4: The SRS-E012 diagnostic message renders the relation via a template placeholder (relation is interpolated into the emitted message).
- [ ] AC-5: The W041, W042, and W043 codes do not collide with the step validator codes W044 or W045.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |

#### Research / Analysis

- -

#### Implementation Notes

- -

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-07-08 | Created | add-requirement |

### FR-PARSE-031 — Work-mode metadata parsing in parseStepState for Mode and Active Task with fail-open default

| Field | Value |
| --- | --- |
| Type | functional |
| Target | v2.3.0.1 |
| Status | implemented |
| Priority | medium |
| Tags | - |
| Risk | low |
| Stability | evolving |
| Verification Method | test |
| GitHub Issue | - |
| Related Docs | - |

#### Requirement

placeholder

#### Rationale

-

#### Acceptance Criteria

- [ ] AC-1: parseStepState returns `.mode` set to the value read from the state.md metadata block, constrained to the enum {sdd, vibe, wait} (e.g. `Mode: sdd` yields "sdd", `Mode: vibe` yields "vibe", `Mode: wait` yields "wait").
- [ ] AC-2: `.activeTask` is returned as the Active Task name only when `Mode` is `vibe`; for any non-vibe mode it is omitted (undefined) even when an `Active Task` line is present in the file.
- [ ] AC-3: A missing `Mode` line, empty input ([]), or unparseable/garbage input yields `.mode === "wait"` with `.activeTask` undefined and never throws.
- [ ] AC-4: A `Mode` value outside the enum {sdd, vibe, wait} (e.g. "turbo") is treated as `.mode === "wait"` and flagged via `.modeInvalid === true`, while a valid in-enum Mode leaves `.modeInvalid` falsy.
- [ ] AC-5: Regression guard (FR-PARSE-023): the same result preserves the StepStateEntry[] row contract — it is an array whose rows are populated (step, status, touchesReq) — so adding work-mode metadata does not regress the array semantics.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |

#### Research / Analysis

- -

#### Implementation Notes

- -

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-07-08 | Created | add-requirement |

## 5. Cross-scope Dependencies

| From | To | Relation | Notes |
|---|---|---|---|
| PARSE | ARCH | depends_on | Markdown-only 원본성과 Requirement Block 구조를 따른다. |
| CLI | PARSE | depends_on | CLI는 parser 결과를 사용한다. |
| MCP | PARSE | depends_on | MCP는 parser 결과를 사용한다. |

## 6. Open Questions

| ID | Question | Impact | Status |
|---|---|---|---|
| - | - | - | - |

## 7. Change Notes

| Date | Change | Reason |
|---|---|---|
| 2026-05-07 | 최초 작성 | SpecKiwi v1.0.0 scope 요구사항 정의 |
| 2026-05-10 | v1.3.0 stability lifecycle 요구사항 추가 | `Stability=draft`와 안정성 enum 정합성 요구 반영 |
| 2026-05-10 | active target parser 요구사항 추가 | `00.index.md` metadata에서 현재 작업 목표 버전을 노출하도록 명시 |
| 2026-05-10 | v1.2.0 parser/diagnostic/index hardening 요구사항 추가 | 연구 문서의 parser correctness, diagnostic registry, index consistency 권고 반영 |
| 2026-05-13 | v2.1.1 report path parser 요구사항 추가 | Completed Work Log 완료 보고서 경로 목록 파싱 계약 정의 |
