# SpecKiwi Workflow and Release SRS

| Field | Value |
|---|---|
| Document Type | scope_srs |
| Product | SpecKiwi |
| Product Version | 1.0.0 |
| Scope | FLOW |
| Scope Name | Workflow and Release |
| Version | 1.0.0 |
| Last Updated | 2026-05-19 |
| Rules | [SRS-MD Authoring Rules v1.0.0](../rule/SRS-MD-Rules-v1.0.0.md) |

## 1. Scope Overview

이 문서는 SpecKiwi를 사용하는 개발자와 AI coding agent의 구현 전후 workflow, release readiness, CI validation, baseline 운영을 정의한다.

## 2. Scope Boundaries

### In Scope

- 구현 전 SRS 탐색
- 구현 후 evidence 갱신
- release readiness summary
- CI validation 예시
- AGENTS.md 및 CLAUDE.md instruction
- baseline tag 운영

### Out of Scope

- 프로젝트 관리 도구 연동 자동화
- issue 자동 생성
- PR 자동 생성
- 배포 자동화

## 3. Assumptions and Constraints

- SRS는 Git에 저장된다.
- release 판단은 target/status/evidence summary를 기준으로 한다.
- agent는 CLI 또는 MCP를 통해 SRS를 사용할 수 있다.

## 4. Requirements

### FR-FLOW-001 — 구현 전 요구사항 탐색 workflow

| Field | Value |
| --- | --- |
| Type | functional |
| Target | v1.0.0 |
| Status | verified |
| Priority | high |
| Tags | workflow, agent, implementation |
| Risk | medium |
| Stability | stable |
| Verification Method | inspection |
| GitHub Issue | - |
| Related Docs | - |

#### Requirement

SpecKiwi는 구현자가 `00.index.md`에서 scope를 찾고 Requirement Block을 읽은 뒤 구현하도록 workflow를 정의해야 한다.

#### Rationale

요구사항과 무관한 구현을 줄이려면 작업 시작점이 명확해야 한다.

#### Acceptance Criteria

- [x] AC-1: AGENTS.md가 구현 전 SRS 탐색 순서를 포함한다.
- [x] AC-2: README가 CLI로 요구사항을 찾는 예를 포함한다.
- [x] AC-3: MCP tool 목록이 agent 사용 흐름을 포함한다.
- [x] AC-4: workflow는 `00.index.md`의 `Active Target`을 현재 작업 목표 버전으로 사용하도록 설명한다.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |
| VE-1 | test | test/release/srs-traceability.test.ts; npm test; npm run test:integration; npm run release:acceptance; npm run perf:srs | all | SpecKiwi v1.0.0 implementation verification on 2026-05-08 |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |

#### Research / Analysis

- -

#### Implementation Notes

- agent는 먼저 `list_requirements`, 이후 `get_requirement`를 호출하는 흐름을 권장한다.
- active target은 현재 진행 중이거나 다음에 진행해야 할 작업 범위를 판단하는 기본 target이다.

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-05-07 | 최초 작성 | SpecKiwi v1.0.0 제품 SRS baseline 정의 |



### FR-FLOW-002 — 구현 후 evidence 갱신 workflow

| Field | Value |
| --- | --- |
| Type | functional |
| Target | v1.0.0 |
| Status | verified |
| Priority | high |
| Tags | workflow, evidence, verification |
| Risk | medium |
| Stability | stable |
| Verification Method | inspection |
| GitHub Issue | - |
| Related Docs | - |

#### Requirement

SpecKiwi는 구현 후 test, code, PR, review evidence를 Requirement Block에 연결하는 workflow를 제공해야 한다.

#### Rationale

구현 완료 여부와 검증 완료 여부를 명확히 분리해야 한다.

#### Acceptance Criteria

- [x] AC-1: 구현만 완료되면 status를 `implemented`로 둔다.
- [x] AC-2: 모든 AC와 evidence가 충족되면 `verified`로 변경한다.
- [x] AC-3: evidence 추가 명령 또는 MCP tool 사용 예가 README에 포함된다.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |
| VE-1 | test | test/release/srs-traceability.test.ts; npm test; npm run test:integration; npm run release:acceptance; npm run perf:srs | all | SpecKiwi v1.0.0 implementation verification on 2026-05-08 |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |

#### Research / Analysis

- -

#### Implementation Notes

- PR 번호가 없으면 code path와 test evidence부터 연결할 수 있다.

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-05-07 | 최초 작성 | SpecKiwi v1.0.0 제품 SRS baseline 정의 |



### FR-FLOW-003 — release readiness summary

| Field | Value |
| --- | --- |
| Type | functional |
| Target | v1.0.0 |
| Status | verified |
| Priority | high |
| Tags | release, summary, readiness |
| Risk | medium |
| Stability | stable |
| Verification Method | test |
| GitHub Issue | - |
| Related Docs | - |

#### Requirement

SpecKiwi는 target이 release 가능한지 판단하는 summary를 제공해야 한다.

#### Rationale

작업 관리가 SRS 기반이면 release 판단도 target/status/evidence를 기준으로 해야 한다.

#### Acceptance Criteria

- [x] AC-1: blocked 요구사항 목록을 포함한다.
- [x] AC-2: planned 또는 in_progress 요구사항 목록을 포함한다.
- [x] AC-3: implemented but not verified 목록을 포함한다.
- [x] AC-4: critical/high priority 미검증 요구사항 목록을 포함한다.
- [x] AC-5: target이 생략되면 active target을 기준으로 summary를 제공한다.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |
| VE-1 | test | test/release/srs-traceability.test.ts; npm test; npm run test:integration; npm run release:acceptance; npm run perf:srs | all | SpecKiwi v1.0.0 implementation verification on 2026-05-08 |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |

#### Research / Analysis

- -

#### Implementation Notes

- CLI `summary --target`과 MCP `summarize_target`이 같은 결과를 반환한다.
- active target은 `00.index.md`의 `Active Target` metadata row를 우선 사용한다.

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-05-07 | 최초 작성 | SpecKiwi v1.0.0 제품 SRS baseline 정의 |
| 2026-05-10 | active target default 명시 | 현재 작업 목표 버전 기준으로 남은 작업을 판단하도록 함 |



### FR-FLOW-004 — CI validation 예시 제공

| Field | Value |
| --- | --- |
| Type | functional |
| Target | v1.0.0 |
| Status | verified |
| Priority | medium |
| Tags | ci, validation, github-actions |
| Risk | medium |
| Stability | stable |
| Verification Method | inspection |
| GitHub Issue | - |
| Related Docs | - |

#### Requirement

SpecKiwi는 저장소 CI에서 `speckiwi validate`를 실행하는 예시를 제공해야 한다.

#### Rationale

SRS 규칙 위반이 코드 리뷰 전에 발견되어야 한다.

#### Acceptance Criteria

- [x] AC-1: README 또는 docs에 CI 예시가 포함된다.
- [x] AC-2: CI 예시는 Node.js LTS를 사용한다.
- [x] AC-3: validation error 발생 시 CI가 실패하는 방식이 설명된다.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |
| VE-1 | test | test/release/srs-traceability.test.ts; npm test; npm run test:integration; npm run release:acceptance; npm run perf:srs | all | SpecKiwi v1.0.0 implementation verification on 2026-05-08 |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |

#### Research / Analysis

- -

#### Implementation Notes

- CI config 파일 생성은 v1.0.0에서 선택 기능이다.

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-05-07 | 최초 작성 | SpecKiwi v1.0.0 제품 SRS baseline 정의 |



### FR-FLOW-005 — Agent instruction 생성

| Field | Value |
| --- | --- |
| Type | functional |
| Target | v1.0.0 |
| Status | verified |
| Priority | high |
| Tags | agents-md, claude-md, instructions |
| Risk | medium |
| Stability | stable |
| Verification Method | inspection |
| GitHub Issue | - |
| Related Docs | - |

#### Requirement

SpecKiwi는 AGENTS.md와 CLAUDE.md에 넣을 수 있는 versioned SRS-enforcement agent instruction을 제공해야 한다.

#### Rationale

AI coding agent는 SRS-MD 구조와 CLI/MCP 사용 규칙을 명확히 알아야 한다.

#### Acceptance Criteria

- [x] AC-1: 문서에 `# SpecKiwi SRS 워크플로 v1.3` heading과 `<!-- /SpecKiwi SRS 워크플로 -->` suffix marker가 포함된 AGENTS.md / CLAUDE.md 추가 문장이 포함된다.
- [x] AC-2: `speckiwi init`이 해당 문장을 `AGENTS.md`와 `CLAUDE.md`에 생성, append, 또는 update한다.
- [x] AC-3: agent instruction은 `docs/spec/`을 필수 source of truth로 선언하고, 작업 전 `docs/spec/00.index.md` 조회, Requirement ID 확인, 작업 요약의 Requirement ID 언급을 요구한다.
- [x] AC-4: 일치하는 Requirement ID가 없으면 agent가 구현을 중단하고 SRS 요구사항 생성 또는 수정을 사용자에게 물어야 한다고 명시한다.
- [x] AC-5: updater는 `# SpecKiwi SRS 워크플로 v{version}` heading과 suffix marker로 managed block 범위를 찾고, 현재 버전과 다르면 전체 block을 교체한다.
- [x] AC-6: legacy unversioned `# SpecKiwi SRS workflow` block은 outdated managed block으로 보고 현재 versioned block으로 교체한다.
- [x] AC-7: versioned heading이 suffix marker 없이 존재하면 안전한 managed block으로 보지 않고 기존 text를 보존한 뒤 현재 block을 append한다. suffix marker는 다음 top-level Markdown heading 전에 나오는 경우에만 해당 heading의 marker로 인정한다.
- [x] AC-8: agent instruction은 Active Target, in-progress/blocked/implemented requirements, missing evidence, Completed Work Log를 함께 읽는 current work status workflow를 포함한다.
- [x] AC-9: agent instruction은 behavior change에 대해 실패 자동 테스트를 먼저 작성하는 TDD 원칙과 자동 테스트 예외 처리 절차를 포함한다.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |
| VE-1 | test | test/release/srs-traceability.test.ts; npm test; npm run test:integration; npm run release:acceptance; npm run perf:srs | all | SpecKiwi v1.0.0 implementation verification on 2026-05-08 |
| VE-2 | test | test/core/bootstrap/init-project.test.ts; test/mcp/stdio-purity.test.ts | AC-1, AC-5, AC-7, AC-8 | v1.1 managed agent instruction coverage on 2026-05-10 |
| VE-3 | test | test/core/bootstrap/init-project.test.ts; test/cli/mutation-commands.test.ts; test/mcp/mutation-tools-errors.test.ts; test/mcp/stdio-purity.test.ts; test/smoke/package.test.ts | AC-1, AC-2, AC-5, AC-7, AC-9 | v1.3 managed agent instruction TDD principle coverage on 2026-05-13 |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |

#### Research / Analysis

- -

#### Implementation Notes

- MCP 사용 가능 시 MCP tool을 우선 사용할 수 있음을 명시한다.
- `speckiwi init`은 별도 agent 파일 선택 입력 없이 `AGENTS.md`와 `CLAUDE.md`를 모두 갱신한다.
- 현재 agent instruction version은 `v1.3`이다.
- current work status workflow는 MCP 우선, CLI fallback 순서를 문서화한다.
- suffix marker는 updater가 block 범위를 안전하게 산정하기 위한 필수 종료 표식이다.
- versioned heading detection은 line-anchored 정규표현식으로만 수행하며 prose 중간의 heading prefix mention은 교체 대상이 아니다.

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-05-07 | 최초 작성 | SpecKiwi v1.0.0 제품 SRS baseline 정의 |
| 2026-05-09 | init agent instruction 정책 갱신 | `--agent-file` 제거와 항상 양쪽 agent 파일 갱신 요구 반영 |
| 2026-05-09 | versioned SRS-enforcement agent instruction 추가 | agent 파일에서 SRS 사용을 강제하고 향후 prompt 변경 시 deterministic update 보장 |
| 2026-05-10 | v1.1 current work status workflow 추가 | agent가 Active Target과 Completed Work Log를 함께 읽도록 함 |
| 2026-05-13 | v1.3 TDD 원칙 추가 | behavior change 구현 전 실패 테스트 작성을 agent workflow에 강제 |



### FR-FLOW-006 — current work status agent workflow

| Field | Value |
| --- | --- |
| Type | functional |
| Target | v1.1.0 |
| Status | verified |
| Priority | high |
| Tags | agents, workflow, active-target, completed-work |
| Risk | medium |
| Stability | stable |
| Verification Method | test, inspection |
| GitHub Issue | - |
| Related Docs | - |

#### Requirement

SpecKiwi agent instructions must define how coding agents answer current work status requests using Active Target, requirement status, missing evidence, and Completed Work Log.

#### Rationale

사용자가 "현재 작업 진행중인 목록과 상태"를 물으면 agent는 임의 추측 대신 SRS index와 Requirement Blocks에서 상태를 조합해야 한다.

#### Acceptance Criteria

- [x] AC-1: workflow는 MCP `get_active_target` 또는 CLI `speckiwi active-target --json`으로 Active Target을 먼저 읽도록 요구한다.
- [x] AC-2: Active Target이 비어 있으면 no active target 상태를 보고하고 target-scoped 변경 전 사용자 확인을 요구한다.
- [x] AC-3: workflow는 `in_progress`, `blocked`, `implemented` 요구사항을 조회하도록 요구한다.
- [x] AC-4: workflow는 summary 또는 `summarize_target`의 `missingEvidence`를 확인하도록 요구한다.
- [x] AC-5: workflow는 MCP `list_completed_work` 또는 CLI `speckiwi completed-work --json`으로 Completed Work Log를 읽도록 요구한다.
- [x] AC-6: Completed Work Log가 source of truth가 아니며 Requirement Block status/evidence가 우선임을 명시한다.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |
| VE-1 | test | test/core/bootstrap/init-project.test.ts; test/mcp/stdio-purity.test.ts | all | Current work status managed block coverage on 2026-05-10 |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |
| Requirement | FR-NODE-009 | depends_on | Bootstrap service renders the managed instruction block. |
| Requirement | FR-MCP-012 | supports | MCP tools provide completed work reads for agents. |
| Requirement | IR-CLI-020 | supports | CLI fallback provides completed work reads for agents. |

#### Research / Analysis

- The workflow keeps Completed Work Log as a summary and uses Requirement Blocks for final completion decisions.

#### Implementation Notes

- The managed block version is incremented whenever prompt content changes; the current version is `v1.3`.

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-05-10 | 최초 작성 | agent current work status workflow 요구사항 반영 |



### FR-FLOW-007 — release readiness core contract

| Field | Value |
| --- | --- |
| Type | functional |
| Target | v1.2.0 |
| Status | verified |
| Priority | critical |
| Tags | release, readiness, governance |
| Risk | high |
| Stability | evolving |
| Verification Method | test, inspection |
| GitHub Issue | - |
| Related Docs | [docs/research/06.hardening-governance-research.md](../research/06.hardening-governance-research.md) |

#### Requirement

SpecKiwi release readiness must be a core workflow contract that resolves target safely, reports blocking findings, and serves as the single source of truth for release scripts and CI.

#### Rationale

Release readiness cannot be only a script wrapper; it must prove whether the selected target is ready and explain why it is blocked.

#### Acceptance Criteria

- [x] AC-1: Release readiness uses an explicit target when provided; otherwise it uses the `Active Target` metadata value.
- [x] AC-2: If no explicit target exists and `Active Target` is empty, release readiness returns `ready=false` with a blocking reason.
- [x] AC-3: The summary includes `target`, `targetSource`, `ready`, `diagnosticsSummary`, `blocked`, `plannedOrInProgress`, `implementedNotVerified`, `criticalHighUnverified`, `missingEvidence`, `acCoverageGaps`, `missingEvidenceReferences`, `brokenTraceLinks`, `warnings`, and `baselineCommand` fields.
- [x] AC-4: Release check scripts and CI call the core release-readiness workflow rather than duplicating target readiness logic.
- [x] AC-5: Release readiness treats target status and validation diagnostics consistently with SRS target status rules.
- [x] AC-6: Release readiness exposes `commandEvidencePolicyViolations` in the result and populates it from the FR-FLOW-008 command evidence policy gate.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |
| VE-1 | command | npm run build; npm run typecheck; npm run lint; npm test; npm run test:integration; npm run release:acceptance; npm run perf:srs | all | P9 bash verification passed on 2026-05-10 |
| VE-2 | command | npm run release:check | all | P9 release readiness passed on 2026-05-10 |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |
| Requirement | FR-FLOW-003 | depends_on | Existing release readiness summary is expanded into a core contract. |
| Requirement | FR-FLOW-008 | depends_on | Evidence, AC coverage, trace, and command evidence policy violations feed readiness. |
| Requirement | FR-PARSE-014 | related_to | Index consistency diagnostics feed release readiness. |
| Requirement | OPS-NODE-002 | related_to | CI must execute release readiness gates. |

#### Research / Analysis

- [SpecKiwi Hardening and Governance Research](../research/06.hardening-governance-research.md) sections 5.1 and 7.2.

#### Implementation Notes

- Implementation sequencing places this after diagnostic registry/parser fixtures and table mutation safety.

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-05-10 | 최초 작성 | v1.2.0 release governance hardening target 반영 |



### FR-FLOW-008 — evidence and AC coverage release gate

| Field | Value |
| --- | --- |
| Type | functional |
| Target | v1.2.0 |
| Status | verified |
| Priority | critical |
| Tags | release, evidence, traceability |
| Risk | high |
| Stability | evolving |
| Verification Method | test, inspection |
| GitHub Issue | - |
| Related Docs | [docs/research/06.hardening-governance-research.md](../research/06.hardening-governance-research.md) |

#### Requirement

SpecKiwi release readiness must verify evidence existence and Acceptance Criteria coverage for every verified requirement across `docs/spec`.

#### Rationale

The `verified` status should mean the tool can prove AC-level coverage and evidence reference validity, not merely that a requirement has at least one evidence row.

#### Acceptance Criteria

- [x] AC-1: Every `verified` requirement has all Acceptance Criteria checked.
- [x] AC-2: Every checked AC is covered by evidence with `Covers = all` or an explicit matching AC identifier.
- [x] AC-3: Local evidence references resolve under the project root or are reported as missing evidence references.
- [x] AC-4: URL evidence references are syntactically valid or reported as invalid evidence references.
- [x] AC-5: Command evidence references are classified as command evidence and checked against the release policy before a requirement can contribute to release readiness.
- [x] AC-6: Requirement trace links resolve to existing requirement IDs or are reported as broken trace links.
- [x] AC-7: Release readiness reports AC coverage gaps, missing evidence references, broken trace links, and command evidence policy violations separately from generic validation diagnostics.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |
| VE-1 | command | npm run build; npm run typecheck; npm run lint; npm test; npm run test:integration; npm run release:acceptance; npm run perf:srs | all | P9 bash verification passed on 2026-05-10 |
| VE-2 | command | npm run release:check | all | P9 release readiness passed on 2026-05-10 |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |
| Requirement | FR-FLOW-003 | depends_on | Release readiness summary must include traceability gaps. |
| Requirement | FR-FLOW-007 | depends_on | Release readiness result must expose the detailed gate outputs. |
| Requirement | FR-PARSE-006 | depends_on | Evidence parser provides `Covers` values. |
| Requirement | FR-PARSE-007 | depends_on | Trace link parser provides requirement references. |

#### Research / Analysis

- [SpecKiwi Hardening and Governance Research](../research/06.hardening-governance-research.md) sections 5.2 and 7.2.

#### Implementation Notes

- Command evidence classification and policy violations are part of the v1.2.0 P0 release gate. A command evidence row must either satisfy the documented policy or appear in `commandEvidencePolicyViolations`.
- The release gate fixture set should include stale evidence, broken local path evidence, invalid URL evidence, command evidence policy violation, AC coverage gap, and broken trace link cases.

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-05-10 | 최초 작성 | v1.2.0 evidence and AC release gate target 반영 |



### FR-FLOW-009 — canonical root agent instruction files

| Field | Value |
| --- | --- |
| Type | functional |
| Target | v1.2.0 |
| Status | verified |
| Priority | critical |
| Tags | agents, init, governance |
| Risk | high |
| Stability | evolving |
| Verification Method | test, inspection |
| GitHub Issue | - |
| Related Docs | [docs/research/06.hardening-governance-research.md](../research/06.hardening-governance-research.md), [docs/rule/SRS-MD-Rules-v1.0.0.md](../rule/SRS-MD-Rules-v1.0.0.md) |

#### Requirement

SpecKiwi repositories must provide canonical root `AGENTS.md` and `CLAUDE.md` managed instruction blocks, and init must upsert the current managed block deterministically.

#### Rationale

Many coding agents discover uppercase root instruction files; relying only on lowercase or stale agent files weakens SRS workflow enforcement.

#### Acceptance Criteria

- [x] AC-1: Repository hardening ensures root `AGENTS.md` and `CLAUDE.md` exist with the current SpecKiwi managed block.
- [x] AC-2: `speckiwi init` and MCP startup init upsert the managed block by versioned heading and suffix marker, not by skipping when a rules link exists.
- [x] AC-3: Existing legacy unversioned or older managed blocks are replaced with the current managed block without removing unrelated user content.
- [x] AC-4: Lowercase `agents.md` is either mirrored intentionally or documented as non-canonical.
- [x] AC-5: Tests or inspection verify managed block presence and replacement behavior.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |
| VE-1 | command | npm run build; npm run typecheck; npm run lint; npm test; npm run test:integration; npm run release:acceptance; npm run perf:srs | all | P9 bash verification passed on 2026-05-10 |
| VE-2 | command | npm run release:check | all | P9 release readiness passed on 2026-05-10 |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |
| Requirement | FR-FLOW-005 | depends_on | Existing agent instruction generation is hardened. |
| Requirement | FR-FLOW-006 | depends_on | Current work status workflow remains in the managed block. |
| Requirement | FR-NODE-009 | depends_on | Bootstrap service implements managed block upsert. |
| Requirement | FR-NODE-012 | related_to | Package init must reliably deploy rules and agent instructions. |

#### Research / Analysis

- [SpecKiwi Hardening and Governance Research](../research/06.hardening-governance-research.md) sections 3.4 and 7.2.

#### Implementation Notes

- This planned requirement formalizes canonical root agent files; it does not create those files in this SRS update.

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-05-10 | 최초 작성 | v1.2.0 canonical agent instruction target 반영 |



### FR-FLOW-010 — stability-aware agent and release workflow

| Field | Value |
| --- | --- |
| Type | functional |
| Target | v1.3.0 |
| Status | verified |
| Priority | high |
| Tags | workflow, agent, release, stability |
| Risk | high |
| Stability | evolving |
| Verification Method | test, inspection |
| GitHub Issue | - |
| Related Docs | [docs/research/07.requirement-stability-lifecycle-research.md](../research/07.requirement-stability-lifecycle-research.md) |

#### Requirement

SpecKiwi must expose requirement `Stability` in agent current-work workflows, target summaries, and release readiness so humans and agents can distinguish draft requirements from implementation-ready, frozen, and deprecated requirements.

#### Rationale

Coding agents need more than implementation `Status` to decide whether work can proceed. A `planned` requirement can still be too unstable to implement, and a `verified` requirement should not remain `draft`.

#### Acceptance Criteria

- [x] AC-1: Current work status workflow reports active target plus `Status` and `Stability` counts for target-scoped requirements.
- [x] AC-2: Agents must stop before implementing non-discarded requirements with `Stability=draft` or `Stability=deprecated` unless the user explicitly overrides the workflow.
- [x] AC-3: Target summary and release readiness expose stability blockers or warnings separately from status blockers.
- [x] AC-4: Release readiness blocks release of a target that still contains non-discarded `draft` requirements.
- [x] AC-5: Managed `AGENTS.md` and `CLAUDE.md` instructions explain `Status` versus `Stability` and the required behavior for `draft` requirements.
- [x] AC-6: CLI and MCP JSON outputs for active target, target summary, and current work status include enough stability data for agents to answer status questions without scanning raw Markdown.
- [x] AC-7: Completed Work Log remains a summary only; `Stability` does not replace Requirement Block `Status`, Acceptance Criteria, Verification Evidence, or Change Notes as the source of truth.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |
| VE-1 | command | npm run test:integration | AC-1, AC-2, AC-3, AC-5, AC-6, AC-7 | 2026-05-11 integration suite passed: 10 test files and 32 tests. |
| VE-2 | command | npm run release:acceptance | AC-3, AC-4, AC-7 | 2026-05-11 release acceptance suite passed: release readiness stability behavior covered. |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |
| Requirement | FR-FLOW-006 | depends_on | Current work status workflow must include stability. |
| Requirement | FR-FLOW-007 | depends_on | Release readiness must expose stability blockers. |
| Requirement | FR-PARSE-015 | depends_on | Parser and validator define the stability taxonomy. |

#### Research / Analysis

- [Requirement Stability Lifecycle Research](../research/07.requirement-stability-lifecycle-research.md)

#### Implementation Notes

- The managed agent instruction block version must be bumped when stability guidance is added.
- JSON payloads should preserve backward compatibility by adding fields rather than replacing existing status fields.

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-05-10 | 최초 작성 | agent와 release workflow에서 `Stability=draft` 처리 규칙 반영 |


### FR-FLOW-011 — next target authoring workflow

| Field | Value |
| --- | --- |
| Type | functional |
| Target | v2.1.0 |
| Status | implemented |
| Priority | high |
| Tags | agents, workflow, target, active-target, authoring |
| Risk | medium |
| Stability | evolving |
| Verification Method | test, inspection |
| GitHub Issue | - |
| Related Docs | [AGENTS.md](../../AGENTS.md), [CLAUDE.md](../../CLAUDE.md) |

#### Requirement

SpecKiwi agent instructions must define how agents handle a user request such as "set the next target to 2.1.0" and ensure subsequent SRS authoring uses that Active Target by default.

#### Rationale

Active Target is useful only if agents consistently consult it before target-scoped SRS changes. The workflow must prevent agents from writing new requirements to stale targets after the user selects a new goal version.

#### Acceptance Criteria

- [x] AC-1: Agent workflow reads Active Target before creating or updating target-scoped requirements.
- [x] AC-2: When the user asks to set a new target that is not registered, the workflow uses MCP `set_active_target` with creation enabled or CLI `speckiwi set-active-target <target> --create`.
- [x] AC-3: After target assignment, agents confirm the resolved Active Target through MCP `get_active_target` or CLI `speckiwi active-target --json`.
- [x] AC-4: Subsequent SRS creation uses the resolved Active Target unless the user explicitly supplies a different target.
- [x] AC-5: If Active Target is empty and no explicit target exists, agents stop and ask for the target before making target-scoped SRS changes.
- [x] AC-6: Managed `AGENTS.md` and `CLAUDE.md` instructions document the next-target workflow and the difference between explicit target and Active Target defaulting.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |
| VE-1 | test | AGENTS.md; CLAUDE.md; test/release/tool-signature-parity.test.ts | all | Managed next-target workflow text and runtime parity for create/default target instructions. |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |
| Requirement | IR-CLI-023 | supports | CLI fallback implements target creation and Active Target defaulting. |
| Requirement | FR-MCP-015 | supports | MCP-first workflow implements target creation and Active Target defaulting. |
| Requirement | FR-NODE-014 | depends_on | Core services provide deterministic target lifecycle behavior. |
| Requirement | FR-FLOW-006 | depends_on | Current work status workflow already starts from Active Target. |

#### Research / Analysis

- Current managed instructions require reading Active Target, but they do not yet define target creation or defaulted authoring after a "next target" request.

#### Implementation Notes

- The managed agent instruction block version must be bumped when this workflow text is added.
- The workflow should prefer MCP and use CLI only when MCP is unavailable.
- The phrase "next target" should map to Active Target, not to Product Version.

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-05-11 | 최초 작성 | 사용자가 다음 목표 버전을 지정한 뒤 SRS 작성 target이 일관되게 유지되도록 함 |
| 2026-06-29 | Status -> implemented | v2.3.0 tool lifecycle, doctor, and signature parity implementation evidence attached |



### REL-FLOW-001 — baseline tag 운영

| Field | Value |
| --- | --- |
| Type | reliability |
| Target | v1.0.0 |
| Status | verified |
| Priority | medium |
| Tags | baseline, git, release |
| Risk | medium |
| Stability | stable |
| Verification Method | inspection |
| GitHub Issue | - |
| Related Docs | - |

#### Requirement

SpecKiwi는 SRS baseline을 Git tag 또는 release branch로 관리하는 방식을 문서화해야 한다.

#### Rationale

SRS는 Git-tracked artifact이므로 baseline도 Git 수준에서 관리해야 한다.

#### Acceptance Criteria

- [x] AC-1: README 또는 appendix에 `git tag srs-v1.0.0-baseline` 예시가 포함된다.
- [x] AC-2: baseline은 도구 내부 저장소가 아니라 Git tag로 관리한다.
- [x] AC-3: target freeze 시 Change Notes 작성 기준을 설명한다.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |
| VE-1 | test | test/release/srs-traceability.test.ts; npm test; npm run test:integration; npm run release:acceptance; npm run perf:srs | all | SpecKiwi v1.0.0 implementation verification on 2026-05-08 |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |

#### Research / Analysis

- -

#### Implementation Notes

- Git 명령 실행은 v1.0.0 도구 기능에 포함하지 않는다.

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-05-07 | 최초 작성 | SpecKiwi v1.0.0 제품 SRS baseline 정의 |



### OBS-FLOW-001 — diagnostics summary 제공

| Field | Value |
| --- | --- |
| Type | observability |
| Target | v1.0.0 |
| Status | verified |
| Priority | medium |
| Tags | diagnostics, reporting, validation |
| Risk | medium |
| Stability | stable |
| Verification Method | test |
| GitHub Issue | - |
| Related Docs | - |

#### Requirement

SpecKiwi는 validation과 summary 결과에 diagnostics count와 rule id를 포함해야 한다.

#### Rationale

문서 품질 문제를 빠르게 파악하려면 오류 종류별 집계가 필요하다.

#### Acceptance Criteria

- [x] AC-1: validation 출력은 error count와 warning count를 포함한다.
- [x] AC-2: JSON 출력은 rule id별 diagnostics를 포함한다.
- [x] AC-3: MCP validation output도 같은 diagnostics shape를 사용한다.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |
| VE-1 | test | test/release/srs-traceability.test.ts; npm test; npm run test:integration; npm run release:acceptance; npm run perf:srs | all | SpecKiwi v1.0.0 implementation verification on 2026-05-08 |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |

#### Research / Analysis

- -

#### Implementation Notes

- diagnostics 구조는 appendix에 정의한다.

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-05-07 | 최초 작성 | SpecKiwi v1.0.0 제품 SRS baseline 정의 |



### MIG-FLOW-001 — Claude Kiwi skills to Codex skills migration

| Field | Value |
| --- | --- |
| Type | migration |
| Target | v2.2.0 |
| Status | verified |
| Priority | high |
| Tags | skills, claude, codex, migration |
| Risk | medium |
| Stability | evolving |
| Verification Method | inspection, validation |
| GitHub Issue | - |
| Related Docs | [skills/claude](../../skills/claude), [skills/codex](../../skills/codex) |

#### Requirement

The skills workspace must migrate every Claude Kiwi skill and shared Kiwi resource under `skills/claude/` into Codex-compatible skill assets under `skills/codex/`, while preserving the Claude source tree and producing a migration plan before rewriting copied skill instructions.

#### Rationale

The repository now contains Claude-oriented Kiwi skills, but Codex skill discovery requires Codex-compatible `SKILL.md` anatomy, metadata, path conventions, and tool terminology. A governed migration requirement prevents ad hoc copying and keeps the Claude source assets intact as the migration baseline.

#### Acceptance Criteria

- [x] AC-1: The migration inventory enumerates every source skill folder and shared Kiwi resource under `skills/claude/` before copying.
- [x] AC-2: The `skills/codex/` tree contains copied counterparts for all source skill folders and shared Kiwi resources, and the original `skills/claude/` files remain unchanged unless separately requested.
- [x] AC-3: Each migrated Codex skill folder contains a required `SKILL.md` whose YAML frontmatter contains only `name` and `description`, and the folder name matches the skill name.
- [x] AC-4: Claude-specific commands, absolute Claude skill paths, tool names, model-role references, and invocation syntax are rewritten or isolated so the Codex-facing instructions use Codex-compatible terminology and available tools.
- [x] AC-5: Shared references, scripts, and relative links used by migrated skills resolve within `skills/codex/` after migration.
- [x] AC-6: A migration plan records the per-skill transformation approach, validation commands, risks, and deferred manual decisions before bulk rewriting copied skill bodies.
- [x] AC-7: Verification includes an inventory diff, UTF-8 read/write handling, Codex skill validation, and sub-agent reviewer validation before the migration is declared complete.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |
| VE-1 | inspection | skills/codex/MIGRATION_PLAN.md | AC-1, AC-6 | - |
| VE-2 | command | PowerShell inventory diff from skills/: Compare-Object relative rg --files output for claude and codex returned no differences after excluding codex/MIGRATION_PLAN.md | AC-2 | - |
| VE-3 | command | PowerShell SHA-256 source-preservation check from skills/: every rg-listed claude file had a codex counterpart with an identical SHA-256 hash; current claude file lengths and LastWriteTime values remained at the initial 2026-05-19 16:25:06 inventory snapshot. | AC-2 | - |
| VE-4 | command | PowerShell UTF-8 validation from skills/: all 10 codex/kiwi-* folders expose SKILL.md; no case-sensitive codex/**/skill.md paths remain; each SKILL.md frontmatter contains only name and description; every name matches its folder; quick_validate.py passed for all 10 skill folders with PYTHONUTF8=1. | AC-3 | - |
| VE-5 | command | skills/codex/MIGRATION_PLAN.md; commands from skills/: quick_validate.py for all 10 codex/kiwi-* folders with UTF-8 mode; rg Claude-residue scans; all-file backticked skill resource path resolution scan; disposition inventory check | AC-4, AC-5, AC-7 | - |
| VE-6 | review | Sub-agent validation: Hegel 019e3fb7-dcfe-7a33-b772-8fc8e1b2d128; Kant 019e3fb7-de6f-72f3-8421-1b2af1e18882 | AC-4, AC-5, AC-7 | - |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |

#### Research / Analysis

- -

#### Implementation Notes

- Initial source inventory observed on 2026-05-19: kiwi-srs, kiwi-pm, kiwi-srs-sync, kiwi-srs-research, kiwi-planner, kiwi-coder, kiwi-commit-auto-push, kiwi-pipeline, kiwi-srs-from-code, kiwi-srs-feasibility, and _shared/kiwi resources. Use MCP/SRS workflow for status and evidence updates after implementation.

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-05-19 | Created | add-requirement |

### FR-FLOW-012 — Kiwi skills require SpecKiwi MCP for normal operation

| Field | Value |
| --- | --- |
| Type | functional |
| Target | v2.2.3 |
| Status | verified |
| Priority | critical |
| Tags | skills, kiwi, mcp, workflow, local-llm |
| Risk | high |
| Stability | evolving |
| Verification Method | inspection, validation |
| GitHub Issue | - |
| Related Docs | - |

#### Requirement

SpecKiwi must require every repository-distributed Kiwi skill variant, including Codex skills and OpenCode/Hermes local-LLM `skills/etc` variants, to treat a working `speckiwi mcp` connection as the required normal-operation interface for SRS reads, mutations, validation, status and stability changes, acceptance-criteria updates, evidence, trace links, and completed-work logging.

#### Rationale

Kiwi skills depend on SpecKiwi SRS governance. Bypassing MCP for raw Markdown edits would weaken ID generation, validation, target summaries, status and stability lifecycle checks, evidence linkage, and completed-work consistency. Local LLM variants are especially likely to drift without a structured mutation interface, so MCP must be a hard operational dependency rather than an optional convenience.

#### Acceptance Criteria

- [x] AC-1: Every Kiwi skill variant documents that normal target-scoped SRS or implementation workflows require available SpecKiwi MCP tools before proceeding.
- [x] AC-2: If SpecKiwi MCP is unavailable, a Kiwi skill must not perform SRS mutations and must halt with remediation guidance or restrict itself to MCP setup diagnostics.
- [x] AC-3: CLI fallback is allowed only for preflight diagnostics, bootstrap assistance, or MCP remediation guidance, not as the normal SRS mutation path for Kiwi skills.
- [x] AC-4: Kiwi skill instructions must not direct agents to manually edit `docs/spec/**` as the normal path for requirement, acceptance-criteria, evidence, trace, status, stability, or completed-work changes.
- [x] AC-5: OpenCode/Hermes local-LLM `skills/etc` variants inherit the same MCP requirement even when their execution profile defaults to `--max` and single-subagent operation.
- [x] AC-6: Migration or install validation must include inspection or scanner evidence that migrated Kiwi skills carry the MCP dependency rule and do not contain normal-operation raw Markdown mutation guidance.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |
| VE-1 | command | MCP dependency scan over skills/codex, skills/claude, skills/etc entrypoints: Total=30 MissingMcp=0 | FR-FLOW-012 AC-1 AC-5 AC-6 | - |
| VE-2 | command | raw Markdown mutation scan over skills/codex skills/claude skills/etc entrypoints on 2026-05-20 | FR-FLOW-012 AC-4 AC-6 | - |
| VE-3 | inspection | skills/codex/kiwi-*/SKILL.md; skills/claude/kiwi-*/skill.md; skills/etc/kiwi-*/SKILL.md | FR-FLOW-012 AC-2 AC-3 AC-6 | - |
| VE-4 | command | npx vitest run test/cli/skills-cli.test.ts; npx vitest run test/smoke/package.test.ts | FR-FLOW-012 AC-3 AC-6 | - |
| VE-5 | subagent-review | Locke subagent review 019e40a3-c101-7b12-a10e-3739af9385e7 on 2026-05-20 | FR-FLOW-012 AC-1 AC-2 AC-3 AC-4 AC-5 AC-6 | - |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |

#### Research / Analysis

- The existing Codex migration requirement `MIG-FLOW-001` validated skill anatomy and terminology, but it did not elevate `speckiwi mcp` from preferred interface to mandatory runtime dependency. The v2.2.3 target goal and subsequent user clarification make MCP availability a product-level skill workflow constraint.

#### Implementation Notes

- Skill bodies and shared Kiwi references should centralize MCP preflight wording. Installer or migration validation should scan every generated `SKILL.md` and relevant shared reference for the MCP requirement. CLI-only fallback may help users start or diagnose `speckiwi mcp`, but should not be documented as a normal replacement for MCP mutations.

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-05-19 | Created | add-requirement |

### MIG-FLOW-002 — Kiwi etc skills migration for OpenCode and Hermes local LLMs

| Field | Value |
| --- | --- |
| Type | migration |
| Target | v2.2.3 |
| Status | verified |
| Priority | high |
| Tags | skills, migration, etc, opencode, hermes, local-llm, kiwi |
| Risk | high |
| Stability | evolving |
| Verification Method | inspection, validation |
| GitHub Issue | - |
| Related Docs | - |

#### Requirement

The skills workspace must migrate the Kiwi skill set and shared Kiwi resources into a repository-local `skills/etc` tree for OpenCode and Hermes local-LLM usage, preserving existing Codex and Claude source trees while producing a migration plan before rewriting copied skill instructions.

#### Rationale

OpenCode and Hermes can consume Agent Skills, but their runtime assumptions differ from Codex. Local LLM usage also changes the cost and quality tradeoff: default Max-style evaluation remains useful, but multi-subagent fanout is inappropriate. A dedicated `skills/etc` migration keeps Codex and Claude assets stable while creating a local-LLM-oriented source tree with explicit MCP dependency and validation gates.

#### Acceptance Criteria

- [x] AC-1: The migration inventory enumerates every Kiwi source skill folder and shared Kiwi resource considered for `skills/etc`, and records whether each item is copied from `skills/claude`, `skills/codex`, or another documented baseline.
- [x] AC-2: The `skills/etc` tree contains migrated counterparts for every selected Kiwi skill folder and shared Kiwi resource, and existing `skills/claude` and `skills/codex` files remain unchanged unless a separate requirement explicitly owns that change.
- [x] AC-3: Each migrated etc skill folder contains a required `SKILL.md` whose frontmatter is compatible with the open Agent Skills format, includes at least `name` and `description`, and whose `name` matches the containing folder.
- [x] AC-4: OpenCode/Hermes/local-LLM variants remove or isolate Codex-only and Claude-only tool syntax, absolute paths, provider names, and invocation wording so the instructions are suitable for OpenCode or Hermes execution contexts.
- [x] AC-5: Every migrated etc skill documents that normal operation requires `speckiwi mcp`; if MCP is unavailable, the skill must halt or provide remediation guidance rather than performing normal SRS mutations.
- [x] AC-6: Every migrated etc skill uses the local-LLM execution profile: `--max` is the default, multi-subagent fanout is disabled, and only one subagent or one evaluator worker is used at a time when delegation is available.
- [x] AC-7: The evaluation and improvement loop for etc skills terminates only after the applicable evaluator set reports no improvements for three consecutive rounds, with the single-worker local-LLM profile treating that as three consecutive clean evaluations from the same evaluator role.
- [x] AC-8: Validation includes inventory comparison, UTF-8 checks, `SKILL.md` frontmatter checks, path-reference resolution, OpenCode/Hermes compatibility scans, MCP dependency scans, and confirmation that no `skills/llm` source tree is created.
- [x] AC-9: A migration plan records per-skill transformation approach, source baseline, install-target assumptions, validation commands, risks, and deferred manual decisions before bulk rewriting copied skill bodies.
- [x] AC-10: The migration result distinguishes repository-local `skills/etc` as the canonical source tree from agent runtime destinations such as OpenCode global or project skill directories and Hermes user skill directories.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |
| VE-1 | document | skills/etc/MIGRATION_PLAN.md | MIG-FLOW-002 AC-1 AC-9 AC-10 | - |
| VE-2 | inspection | skills/etc/_shared/kiwi/local-llm-profile.md; skills/etc/kiwi-*/SKILL.md | MIG-FLOW-002 AC-2 AC-4 AC-5 AC-6 AC-7 AC-10 | - |
| VE-3 | command | python C:/Users/beom/.codex/skills/.system/skill-creator/scripts/quick_validate.py skills/etc/kiwi-* | MIG-FLOW-002 AC-3 AC-8 | - |
| VE-4 | command | rg residue scans and structural checks for skills/etc on 2026-05-20 | MIG-FLOW-002 AC-4 AC-5 AC-6 AC-8 | - |
| VE-5 | subagent-review | single evaluator 019e40a3-c101-7b12-a10e-3739af9385e7 rounds 2026-05-20 | MIG-FLOW-002 AC-6 AC-7 AC-8 | - |
| VE-6 | inspection | skills/etc/_shared/kiwi/local-llm-profile.md; skills/etc/kiwi-coder/SKILL.md; skills/etc/kiwi-commit-auto-push/SKILL.md; skills/etc/kiwi-srs*/**/*.md; skills/etc/kiwi-planner/SKILL.md | MIG-FLOW-002 AC-6 AC-7 AC-8 | - |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |
| Requirement | MIG-FLOW-001 | related_to | Builds on the completed Claude-to-Codex migration inventory and validation pattern. |
| Requirement | FR-FLOW-012 | depends_on | All etc skills must carry the mandatory SpecKiwi MCP normal-operation dependency. |
| Requirement | IR-CLI-027 | supports | The etc source tree is the default source for OpenCode and Hermes local-LLM install targets. |
| Requirement | FR-NODE-016 | supports | The installer core service will validate and copy migrated etc skills. |

#### Research / Analysis

- OpenCode supports `SKILL.md` discovery through `.opencode/skills`, `~/.config/opencode/skills`, `.claude/skills`, and `.agents/skills`. Hermes uses `~/.hermes/skills` as its primary skill directory and supports external directories for shared skills. These facts support maintaining `skills/etc` as a repository-local canonical source tree while leaving installation into runtime discovery paths to the CLI installer requirement.

#### Implementation Notes

- Use `skills/etc` as the target tree name, not `skills/llm`. The migration may use the already-cleaned Codex tree as a practical transformation baseline, but the inventory must state the chosen baseline per item. Shared Kiwi references should centralize local-LLM profile rules when possible to avoid divergent copies across skills.

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-05-19 | Created | add-requirement |

### OPS-FLOW-001 — 10-agent MCP and CLI tool abandonment research

| Field | Value |
| --- | --- |
| Type | operational |
| Target | v2.3.0 |
| Status | verified |
| Priority | critical |
| Tags | v2.3.0, research, mcp, cli, tools |
| Risk | high |
| Stability | stable |
| Verification Method | inspection, validation |
| GitHub Issue | - |
| Related Docs | [docs/research/09.mcp-cli-tool-abandonment-research.md](../research/09.mcp-cli-tool-abandonment-research.md) |

#### Requirement

SpecKiwi v2.3.0 must include a docs/research investigation that uses ten independent sub-agent research axes to explain why coding agents abandon MCP/CLI tools for SRS read, change, and add workflows, and to recommend concrete tool improvements.

#### Rationale

The active v2.3.0 goal is tool improvement, and real project use already showed agents falling back to raw Markdown because documented MCP/CLI workflows were incomplete or misleading. The research deliverable needs its own traceable SRS anchor before changing docs/research.

#### Acceptance Criteria

- [x] AC-1: The research uses ten distinct investigation axes covering CLI reads, CLI mutations, MCP reads, MCP mutations, parser/validation diagnostics, agent workflow docs/templates, tests/fixtures, core architecture/API reliability, dogfood workflow simulation, and packaging/install/MCP registration environment.
- [x] AC-2: The report identifies tool gaps with evidence from current source, SRS, tests, command output, or sub-agent findings, and distinguishes confirmed defects from design improvements.
- [x] AC-3: The report recommends prioritized v2.3.0 improvements for CLI, MCP, diagnostics, workflow docs, test coverage, and packaging/install reliability.
- [x] AC-4: The report is saved as a Markdown file under docs/research and is referenced from this requirement.
- [x] AC-5: The report proposes follow-up v2.3.0 SRS requirement seeds or implementation work packages.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |
| VE-1 | research | docs/research/09.mcp-cli-tool-abandonment-research.md | AC-1, AC-2, AC-3, AC-4, AC-5 | - |
| VE-2 | research | docs/research/10.kiwi-planner-jsonl-tooling-research.md | AC-2, AC-3, AC-5 | - |
| VE-3 | research | docs/research/11.v2.3.0-tooling-code-improvement-plan.md | AC-3, AC-5 | - |
| VE-4 | plan | docs/plans/2026-06-29.specwkiki.v230-plan.plan.md#T-PH001-02 | all | - |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |
| Requirement | IR-CLI-023 | related_to | Target registration and Active Target defaults are one confirmed abandonment driver. |
| Requirement | FR-MCP-015 | related_to | MCP target registration/defaulting is a confirmed tool gap. |
| Requirement | FR-NODE-014 | related_to | Core target lifecycle/default resolution is required for CLI/MCP parity. |
| Requirement | FR-FLOW-011 | related_to | Agent next-target workflow depends on these tool capabilities. |
| Task | T-PH001-02 | implements | - |

#### Research / Analysis

- Initial evidence: active target v2.3.0 has no requirements; existing planned requirements IR-CLI-023, FR-MCP-015, FR-NODE-014, and FR-FLOW-011 cover target/defaulting gaps but not the requested 10-axis research report itself.

#### Implementation Notes

- Use explicit target v2.3.0 because add_requirement Active Target defaulting is itself one of the investigated tool gaps. Complete the report before checking AC and adding verification evidence.

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-06-25 | Created | add-requirement |
| 2026-06-29 | Stability -> stable | Feasibility committee approved stable promotion. OPS-FLOW-001 is verified, has 5/5 AC checked and VE-1..VE-3 evidence; dry-run passed with no warnings. Run: 2026-06-29.specwkiki.v230.01 |

### REL-FLOW-002 — Tool signature parity across runtime and docs

| Field | Value |
| --- | --- |
| Type | reliability |
| Target | v2.3.0 |
| Status | verified |
| Priority | high |
| Tags | v2.3.0, workflow, docs, tools, signature |
| Risk | high |
| Stability | evolving |
| Verification Method | test, inspection |
| GitHub Issue | - |
| Related Docs | [docs/research/09.mcp-cli-tool-abandonment-research.md](../research/09.mcp-cli-tool-abandonment-research.md) |

#### Requirement

SpecKiwi must keep CLI help, MCP schemas, appendix command examples, managed agent instructions, and Kiwi skill snippets aligned with the runtime tool registry through generated output or automated signature parity tests.

#### Rationale

The research found that docs and skills mention unsupported options or stale parameter shapes. Signature drift causes agents to call tools that do not exist or pass unsupported arguments, then abandon official tools.

#### Acceptance Criteria

- [x] AC-1: A signature parity check compares Commander CLI commands and options against documented appendix examples for supported tool surfaces.
- [x] AC-2: A signature parity check compares MCP tool schemas against documented MCP examples and managed agent snippets for supported parameters.
- [x] AC-3: Kiwi skill snippets and managed instruction templates fail tests when they reference unsupported SpecKiwi CLI flags, MCP fields, or response shapes.
- [x] AC-4: Runtime help, MCP schema metadata, appendix examples, and skill snippets are generated from a shared registry or validated against a shared extracted registry.
- [x] AC-5: Tests include known drift cases from the v2.3.0 research, including unsupported target creation flags, optional target defaulting claims, include-content style drift, and mutation parameter aliases.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |
| VE-1 | plan | docs/plans/2026-06-29.specwkiki.v230-plan.plan.md#T-PH005-02 | all | - |
| VE-2 | test | test/release/tool-signature-parity.test.ts; skills/codex/kiwi-pm/references/extended-workflow.md; skills/claude/kiwi-pm/SKILL.md | all | Runtime CLI/MCP signature parity and unsupported includeContent skill drift guard. |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |
| Requirement | OPS-FLOW-001 | related_to | Research anchor for docs and runtime signature drift. |
| Requirement | FR-FLOW-005 | extends | Managed agent instruction generation must stay signature-accurate. |
| Requirement | FR-FLOW-009 | extends | Canonical root instruction files consume these signatures. |
| Requirement | FR-ARCH-005 | related_to | Mutation governance and tool kind classification are related registry inputs. |
| Code | src/cli/commands/read.ts:66-135 | related_to | CLI read command signature extraction source. |
| Code | src/mcp/server.ts:36-113 | related_to | MCP schema signature extraction source. |
| Code | src/core/bootstrap/templates.ts:213-234 | related_to | Managed instruction template signature consumer. |
| Requirement | FR-NODE-017 | depends_on | - |
| Requirement | IR-CLI-029 | depends_on | - |
| Requirement | FR-MCP-021 | depends_on | - |
| Requirement | IR-CLI-030 | depends_on | - |
| Requirement | FR-MCP-022 | depends_on | - |
| Task | T-PH005-02 | implements | - |

#### Research / Analysis

- Derived from docs/research/09.mcp-cli-tool-abandonment-research.md sections 4.3, 6, 7, and 9. This requirement prevents future drift between tool runtime, appendix, managed instructions, and skills.

#### Implementation Notes

- Start with snapshot or scanner tests before generating any docs. Keep unsupported future proposals clearly marked as proposed in research until implementation lands.

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-06-26 | Created | add-requirement |
| 2026-06-29 | Status -> implemented | v2.3.0 tool lifecycle, doctor, and signature parity implementation evidence attached |
| 2026-06-30 | Status -> verified | v2.3.0 closeout: acceptance criteria and verification evidence reviewed; final validation gates pending. |

### MIG-FLOW-003 — Kiwi skills migration to official workflow tools

| Field | Value |
| --- | --- |
| Type | migration |
| Target | v2.3.0 |
| Status | verified |
| Priority | high |
| Tags | v2.3.0, kiwi, skills, workflow, migration |
| Risk | medium |
| Stability | evolving |
| Verification Method | test, inspection |
| GitHub Issue | - |
| Related Docs | [docs/research/10.kiwi-planner-jsonl-tooling-research.md](../research/10.kiwi-planner-jsonl-tooling-research.md), [docs/research/09.mcp-cli-tool-abandonment-research.md](../research/09.mcp-cli-tool-abandonment-research.md) |

#### Requirement

Kiwi skill instructions must migrate from bespoke workflow artifact discovery, raw JSONL parsing, and shell append snippets to official SpecKiwi workflow resolvers, readers, validators, work-order tools, and guarded mutations when those tools are available.

#### Rationale

The Kiwi planner research found that skills currently encode path and JSONL conventions directly. Once official tools exist, skill text should prefer them to reduce token use, prevent field hallucination, and avoid unsafe manual writes.

#### Acceptance Criteria

- [x] AC-1: kiwi-planner, kiwi-pm, kiwi-coder, kiwi-pipeline, kiwi-srs, and related Kiwi skills instruct agents to use official workflow artifact readers and work-order tools before raw file reads for covered flows.
- [x] AC-2: Skill text uses official guarded workflow mutation tools before shell JSONL append snippets or direct plan checkbox edits for covered flows.
- [x] AC-3: Fallback to raw files is documented as degraded mode and requires captured tool diagnostics, affected artifact paths, active target, and follow-up requirement or candidate ID.
- [x] AC-4: Skill snapshot or scanner tests fail when covered flows reference unsupported SpecKiwi CLI flags, MCP fields, legacy path assumptions, or bespoke JSONL append snippets after official tooling exists.
- [x] AC-5: Migration notes preserve the existing MCP-required SRS workflow rule and do not reintroduce CLI as a normal replacement for MCP SRS mutations.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |
| VE-1 | plan | docs/plans/2026-06-29.specwkiki.v230-plan.plan.md#T-PH018-02 | all | - |
| VE-2 | test | skills/codex/kiwi-planner/SKILL.md; skills/codex/kiwi-pm/SKILL.md; skills/codex/kiwi-coder/SKILL.md; test/core/skills/install-skill.test.ts; test/release/tool-signature-parity.test.ts | all | Canonical Kiwi skills prefer official workflow tools, guarded mutations, degraded raw-file fallback diagnostics, and signature scanner coverage. |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |
| Requirement | OPS-FLOW-001 | related_to | Research anchor for Kiwi skill workflow gaps. |
| Requirement | FR-FLOW-012 | extends | Preserves the rule that Kiwi skills require SpecKiwi MCP for normal SRS workflows. |
| Requirement | MIG-FLOW-001 | related_to | Codex skill migration baseline. |
| Requirement | MIG-FLOW-002 | related_to | OpenCode and Hermes etc skill migration baseline. |
| Requirement | REL-FLOW-002 | depends_on | Skill migration must stay aligned with runtime tool signatures. |
| Requirement | IR-CLI-032 | depends_on | Skills can use CLI work-order only as appropriate fallback or CLI surface. |
| Requirement | FR-MCP-024 | depends_on | Skills should prefer MCP next work-order when available. |
| Requirement | FR-NODE-030 | depends_on | Skills should use canonical guarded workflow mutations before raw writes. |
| Code | skills/codex/kiwi-pipeline/SKILL.md | related_to | Pipeline skill workflow migration target. |
| Code | skills/codex/kiwi-pm/SKILL.md | related_to | PM skill workflow migration target. |
| Code | skills/codex/kiwi-coder/SKILL.md | related_to | Coder skill workflow migration target. |
| Task | T-PH018-02 | implements | - |

#### Research / Analysis

- Derived from docs/research/10.kiwi-planner-jsonl-tooling-research.md sections 5, 6, 10, 12, and 13. It also depends on Doc09 signature parity and MCP doctor work.

#### Implementation Notes

- Do this after official workflow read and mutation tools are implemented. Avoid editing skills to call proposed tools before the runtime contracts exist unless the text clearly marks degraded fallback.

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-06-26 | Created | add-requirement |
| 2026-06-29 | Replaced discarded workflow mutation dependency with FR-NODE-030 | Post-fix max evaluation found MIG-FLOW-003 still depended on discarded FR-NODE-022. |
| 2026-06-29 | Status -> implemented | Migrated Kiwi skill instructions to official workflow tools and guarded mutations. |
| 2026-06-30 | Status -> verified | v2.3.0 closeout: acceptance criteria and verification evidence reviewed; final validation gates pending. |

### FR-FLOW-013 — v2.3.0 target lifecycle completion wrapper

| Field | Value |
| --- | --- |
| Type | functional |
| Target | v2.3.0 |
| Status | verified |
| Priority | high |
| Tags | v2.3.0, target, active-target, tooling, workflow |
| Risk | medium |
| Stability | evolving |
| Verification Method | test, inspection |
| GitHub Issue | - |
| Related Docs | [docs/research/09.mcp-cli-tool-abandonment-research.md](../research/09.mcp-cli-tool-abandonment-research.md), [AGENTS.md](../../AGENTS.md), [CLAUDE.md](../../CLAUDE.md) |

#### Requirement

SpecKiwi v2.3.0 must explicitly track completion of target registration, Active Target defaulting, and targetSource reporting across CLI, MCP, core services, and agent workflow so the Doc09 P1 target-lifecycle work appears in the v2.3.0 active-target work list without retargeting historical v2.1.0 requirements.

#### Rationale

Doc09 identifies target lifecycle and Active Target defaulting as P1 tool-improvement work. The detailed canonical requirements already exist under v2.1.0, but v2.3.0 needs an active-target wrapper so agents do not lose this work when selecting v2.3.0 new work candidates.

#### Acceptance Criteria

- [x] AC-1: v2.3.0 summary and work selection include a planned requirement that traces to IR-CLI-023, FR-MCP-015, FR-NODE-014, and FR-FLOW-011 or their explicit successors.
- [x] AC-2: Release verification for this wrapper requires CLI set-active-target --create, MCP set_active_target creation support, add_requirement Active Target defaulting, safe failure when create is omitted for missing targets, and result targetSource reporting.
- [x] AC-3: The wrapper can be marked implemented only when the linked CLI, MCP, core, and workflow requirements have implementation evidence or successor evidence that covers the same behavior.
- [x] AC-4: The wrapper can be marked verified only when validation, CLI tests, MCP tests, and workflow/agent-instruction inspection evidence are attached to this requirement or to each linked requirement with explicit coverage notes.
- [x] AC-5: Research and planning documents must reference this wrapper instead of treating OPS-FLOW-001 alone or the historical v2.1.0 requirements alone as the v2.3.0 implementation contract.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |
| VE-1 | plan | docs/plans/2026-06-29.specwkiki.v230-plan.plan.md#T-PH002-02 | all | - |
| VE-2 | test | test/core/mutation/set-active-target.test.ts; test/core/mutation/add-requirement.test.ts; test/cli/mutation-commands.test.ts; test/mcp/mutation-tools-errors.test.ts | all | v2.3 target lifecycle wrapper coverage for create, default target, safe failure, and targetSource reporting. |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |
| Requirement | OPS-FLOW-001 | related_to | Doc09 research anchor for P1 target lifecycle work. |
| Requirement | IR-CLI-023 | depends_on | CLI target registration and Active Target defaulting contract. |
| Requirement | FR-MCP-015 | depends_on | MCP target registration and Active Target defaulting contract. |
| Requirement | FR-NODE-014 | depends_on | Core target lifecycle mutation and default target resolution contract. |
| Requirement | FR-FLOW-011 | depends_on | Agent workflow contract for next target authoring. |
| Task | T-PH002-02 | implements | - |

#### Research / Analysis

- Derived from docs/research/09.mcp-cli-tool-abandonment-research.md section "P1. Complete target lifecycle and Active Target defaulting". This wrapper resolves the target mismatch by keeping v2.3.0 planning tied to the existing detailed contracts IR-CLI-023, FR-MCP-015, FR-NODE-014, and FR-FLOW-011 without changing their historical v2.1.0 target metadata.

#### Implementation Notes

- Do not retarget the historical v2.1.0 requirements. Use this v2.3.0 wrapper to keep the P1 target-lifecycle work visible in current tool-improvement planning while preserving the original detailed requirement IDs.

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-06-26 | Created | add-requirement |
| 2026-06-29 | Status -> implemented | v2.3.0 tool lifecycle, doctor, and signature parity implementation evidence attached |
| 2026-06-30 | Status -> verified | v2.3.0 closeout: acceptance criteria and verification evidence reviewed; final validation gates pending. |

### OPS-FLOW-002 — Merge-time SRS duplicate ID repair workflow

| Field | Value |
| --- | --- |
| Type | operational |
| Target | v2.3.0 |
| Status | verified |
| Priority | high |
| Tags | v2.3.0, workflow, merge, requirement-id, collision-repair |
| Risk | medium |
| Stability | evolving |
| Verification Method | test, inspection |
| GitHub Issue | - |
| Related Docs | [docs/research/230.md](../research/230.md), [AGENTS.md](../../AGENTS.md), [CLAUDE.md](../../CLAUDE.md) |

#### Requirement

SpecKiwi agent and release workflows must define a merge-time SRS duplicate Requirement ID repair runbook that uses validation and guarded repair tools after branch, rebase, or worktree merges, and forbids manual or aesthetic renumbering when official repair tooling is available.

#### Rationale

Doc230 identified the practical user workflow: parallel branches or worktrees add requirements, merge later, and trigger SRS-E002. Agent instructions must prevent unsafe manual renumbering and teach the supported diagnose, plan, apply, validate sequence.

#### Acceptance Criteria

- [x] AC-1: Agent instructions and workflow documentation require running validate before repair and using the repair workflow only when SRS-E002 duplicate Requirement ID diagnostics exist or a named duplicate ID is confirmed in parsed diagnostics.
- [x] AC-2: The runbook requires normal Git conflict markers to be resolved first, then diagnose and dry-run repair, explicit keep and rename occurrence selection, apply from a plan or explicit mapping, and final validate, target summary, and link-check evidence.
- [x] AC-3: The workflow explicitly distinguishes merge-time collision repair from general renumbering, gap filling, ID beautification, bulk archive, bulk finalize, or status and stability transition workflows.
- [x] AC-4: The workflow prohibits hand-editing Requirement IDs when an implemented and validated runtime-callable CLI or MCP repair surface from IR-CLI-044 or FR-MCP-039 exists in the active checkout; planned requirements or core-only APIs do not block a degraded minimal SRS-MD patch path, but that path requires explicit user authorization, must be limited to the selected duplicate occurrence and explicitly mapped references, and must be followed by validate, target summary, and links checks.
- [x] AC-5: The runbook states that duplicate logical requirements must first receive unique IDs, and any discard, logical deletion, supersedes, status, stability, AC, or evidence changes must happen through separate guarded SRS mutations.
- [x] AC-6: Release or workflow verification for this feature requires evidence that duplicate IDs are zero after repair, ambiguous references were reported or explicitly mapped, and validation, target summary, and links checks pass.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |
| VE-1 | plan | docs/plans/2026-06-29.specwkiki.v230-plan.plan.md#T-PH017-02 | all | - |
| VE-2 | test | AGENTS.md; CLAUDE.md; agents.md; docs/next/2026-06-30-merge-time-duplicate-id-repair.md; test/release/merge-repair-runbook.test.ts | all | Runbook inspection covers validate-first, conflict-marker, diagnose-plan-apply, no-renumbering, degraded manual exception, and final evidence gates. |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |
| Requirement | OPS-FLOW-001 | depends_on | v2.3.0 tool improvement research anchor. |
| Requirement | REL-PARSE-003 | depends_on | Workflow is triggered by grouped SRS-E002 diagnostics. |
| Requirement | FR-NODE-032 | depends_on | Core repair service provides guarded diagnose, plan, and apply behavior. |
| Requirement | IR-CLI-044 | depends_on | CLI repair commands support fallback and human workflows. |
| Requirement | FR-MCP-039 | depends_on | MCP repair tools support MCP-first agent workflows. |
| Requirement | FR-FLOW-005 | extends | Extends managed agent instruction expectations with merge-time SRS duplicate-ID repair rules. |
| Requirement | REL-FLOW-002 | depends_on | Runtime signatures and docs must stay aligned before skills reference the new tools. |
| Code | src/core/bootstrap/templates.ts:208-225 | related_to | Managed AGENTS.md instruction template surface that will need update after repair tooling exists. |
| Code | test/release/release-readiness.test.ts:1-80 | related_to | Release workflow tests can host inspection coverage for runbook and validation gates. |
| Task | T-PH017-02 | implements | - |

#### Research / Analysis

- Derived from docs/research/230.md sections 16.1, 16.2, 16.4, 16.6, 16.10, 16.11, and the registered merge-time collision repair bridge in section 18.

#### Implementation Notes

- Update managed instruction templates and workflow docs only after the runtime repair tools exist or clearly mark the process as future guarded tooling. Keep manual Markdown repair as an explicit degraded exception, not normal operation.

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-06-29 | Created | add-requirement |
| 2026-06-29 | Clarified runtime-callable repair surface gate | Max evaluation found the previous availability wording could block degraded manual repair before an official CLI or MCP repair tool exists. |
| 2026-06-29 | Restored explicit authorization for degraded manual repair | Post-fix max evaluation found the degraded minimal SRS-MD patch path needed explicit user authorization and validation gates. |
| 2026-06-29 | Status -> implemented | Documented merge-time duplicate Requirement ID repair workflow and added runbook regression tests. |
| 2026-06-30 | Status -> verified | v2.3.0 closeout: acceptance criteria and verification evidence reviewed; final validation gates pending. |

### MIG-FLOW-004 — Runtime-installed Kiwi skill mirror coverage

| Field | Value |
| --- | --- |
| Type | migration |
| Target | v2.3.0 |
| Status | verified |
| Priority | high |
| Tags | v2.3.0, kiwi, skills, runtime-mirror, agents |
| Risk | high |
| Stability | evolving |
| Verification Method | test, inspection |
| GitHub Issue | - |
| Related Docs | [docs/research/11.v2.3.0-tooling-code-improvement-plan.md](../research/11.v2.3.0-tooling-code-improvement-plan.md), [docs/research/230plus.md](../research/230plus.md) |

#### Requirement

SpecKiwi v2.3.0 skill migration work must cover both canonical repository skill sources and runtime-installed Kiwi skill mirrors discovered in the active checkout, or explicitly classify those mirrors as generated install outputs with a validated refresh path.

#### Rationale

The 10-axis SRS usefulness review found that current runtime skills are loaded from .agents/skills, while MIG-FLOW-003 and the implementation plan focused on canonical skills/codex sources and left installed mirrors out of scope.

#### Acceptance Criteria

- [x] AC-1: Migration inventory identifies canonical skill source trees and runtime-installed mirrors such as .agents/skills when they exist in the active checkout.
- [x] AC-2: Each runtime mirror is either included in migration validation or classified as generated output with a documented refresh command, source manifest, and no-manual-edit rule.
- [x] AC-3: Shared resource references from runtime mirrors, including ../_shared/kiwi paths, are validated or explicitly blocked before claiming migration readiness.
- [x] AC-4: Scanner tests or inspection compare canonical source files, install metadata, runtime mirror paths, and shared-resource availability so stale or broken installed skills cannot pass unnoticed.
- [x] AC-5: Skill migration remains gated on official workflow tools being implemented and validated; until then, runtime mirrors may document degraded raw-file fallback only with captured diagnostics and follow-up requirement IDs.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |
| VE-1 | plan | docs/plans/2026-06-29.specwkiki.v230-plan.plan.md#T-PH018-02 | all | - |
| VE-2 | test | .agents/skills/kiwi-planner/SKILL.md; .agents/skills/kiwi-pm/SKILL.md; .agents/skills/kiwi-coder/SKILL.md; src/core/skills/install-skill.ts; test/core/skills/install-skill.test.ts | all | Runtime-installed mirrors include official-tool guidance, no-manual-edit provenance, shared-resource validation metadata, and install scanner coverage. |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |
| Requirement | MIG-FLOW-003 | extends | Adds runtime-installed mirror coverage to the official workflow tool migration. |
| Requirement | FR-NODE-016 | depends_on | Skill install/update core service owns mirror refresh semantics. |
| Requirement | IR-CLI-027 | depends_on | CLI skill install/update is the expected refresh surface for generated mirrors. |
| Requirement | FR-FLOW-012 | extends | Preserves MCP-required normal SRS workflow guidance in skill text. |
| Requirement | REL-FLOW-002 | depends_on | Runtime mirror text must stay aligned with implemented tool signatures. |
| Code | .agents/skills/kiwi-pipeline/SKILL.md:1-40 | related_to | Observed runtime-installed skill mirror and shared-resource reference pattern. |
| Code | .agents/skills/kiwi-planner/SKILL.md:1-45 | related_to | Observed runtime-installed planner mirror and shared-resource reference pattern. |
| Task | T-PH018-02 | implements | - |

#### Research / Analysis

- Current Codex skill discovery in this checkout loads Kiwi skills from `.agents/skills/**`, not only from the canonical `skills/codex/**` source tree.
- Earlier runtime mirrors contained `.speckiwi-skill-install.json` files with only `name`, `agent`, and `installedAt`, which was not enough by itself to prove source path, source revision, checksum, refresh command, or no-manual-edit policy. Current mirror manifests and the installer contract must preserve those provenance fields so a documented refresh path can reproduce the same metadata.
- Some runtime skill instructions reference shared resources through `../_shared/kiwi/...`; migration readiness therefore requires validating whether the installed mirror includes those shared resources, whether they resolve back to the canonical source tree, or whether the mirror must be refreshed.
- The implementation plan must treat runtime mirrors as first-class migration inputs or explicitly classify them as generated install outputs before declaring skill migration complete.

#### Implementation Notes

- Do not migrate runtime mirrors to proposed workflow tool calls before those tools exist. First make mirror/source identity and shared-resource resolution visible and testable.

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-06-29 | Created | add-requirement |
| 2026-06-29 | Status -> implemented | Covered runtime-installed Kiwi skill mirrors and generated-output metadata in migration validation. |
| 2026-06-30 | Status -> verified | v2.3.0 closeout: acceptance criteria and verification evidence reviewed; final validation gates pending. |

### FR-FLOW-014 — kiwi-step lightweight step-local authoring skill

| Field | Value |
| --- | --- |
| Type | functional |
| Target | v2.3.0.1 |
| Status | verified |
| Priority | high |
| Tags | - |
| Risk | medium |
| Stability | evolving |
| Verification Method | test |
| GitHub Issue | - |
| Related Docs | - |

#### Requirement

The system SHALL provide a lightweight kiwi-step authoring skill for step-local requirement authoring under docs/spec/steps/<name>/. Before authoring, the skill SHALL claim the target step through the claim_step mutation. It SHALL confine all requirement writes to the claimed step directory and never write directly into body-scope SRS files, and it SHALL run step-local validation (validate_step) as part of its authoring workflow.

#### Rationale

-

#### Acceptance Criteria

- [x] AC-1: The kiwi-step skill claims a step via the claim_step mutation before authoring any step-local requirement.
- [x] AC-2: The kiwi-step skill authors requirements only under docs/spec/steps/<name>/ and never writes directly into body-scope SRS files.
- [x] AC-3: The kiwi-step skill runs step-local validation via validate_step as part of its authoring workflow.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |
| VE-1 | plan | docs/plans/2026-07-08.speckiwi.v2301-kiwi-step.plan.md#T-PH001-02 | all | - |
| VE-2 | test | test/skills/kiwi-step-content.test.ts | AC-1, AC-2, AC-3 | - |
| VE-3 | test | test/doctor/package-doctor.test.ts | AC-2 | - |
| VE-4 | test | test/skills/kiwi-step-content.test.ts | all | - |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |
| Task | T-PH001-01 | depends_on | - |
| Task | T-PH001-02 | depends_on | - |

#### Research / Analysis

- -

#### Implementation Notes

- -

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-07-08 | Created | add-requirement |

### ~~FR-FLOW-015 — kiwi-spec-merge per-REQ sequential merge orchestrator skill~~ [DISCARDED]

| Field | Value |
| --- | --- |
| Type | functional |
| Target | v2.3.0.1 |
| Status | discarded |
| Priority | high |
| Tags | - |
| Risk | medium |
| Stability | evolving |
| Verification Method | test |
| GitHub Issue | - |
| Related Docs | - |

#### Requirement

The kiwi-spec-merge skill SHALL orchestrate an SRS merge as a sequence of per-REQ, single-REQ, req-scoped operations — dry-run, explicit user approval, a single one-REQ mutation call, journal append, and resume — and SHALL NOT invoke any N-REQ batch finalize tool. For each requirement it SHALL route the diff_steps classification to the matching guarded mutation: NEW to promote_step_requirement, UPDATE to an in-place body update, and CONFLICT to supersede_requirement. On completion it SHALL migrate loser-side verification evidence and emit a post-merge verification-decrease report.

#### Rationale

-

#### Acceptance Criteria

- [ ] AC-1: The skill issues exactly one requirement per mutation call and never invokes an N-REQ batch finalize tool.
- [ ] AC-2: The skill runs a dry-run and obtains explicit user approval before each applied per-REQ mutation call.
- [ ] AC-3: The skill routes a diff_steps NEW class to promote_step_requirement, an UPDATE class to an in-place body update, and a CONFLICT class to supersede_requirement.
- [ ] AC-4: The skill resumes an interrupted merge from the merge-journal without duplicating already-applied per-REQ calls.
- [ ] AC-5: The skill produces a post-merge verification-decrease report and migrates loser-side (superseded) evidence.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |

#### Research / Analysis

- -

#### Implementation Notes

- -
- [2026-07-08] BLOCKER (feasibility 2026-07-08.speckiwi.v2301.01): diff_steps is core-only (src/core/query/diff-steps.ts), unregistered as any MCP/CLI tool (schemas.ts registers workflow_diff, not diff_steps); merge-journal (src/core/patch/merge-journal.ts) is unwired. AC-3/AC-4 unachievable until diff_steps is exposed and merge-journal wired. Deferred from the v2.3.0.1 skill pipeline. Governance defect: FR-MCP-044 (diff_steps tool) is Status=implemented but phantom (no registration).
- [2026-07-08] [2026-07-09] DISCARDED per user directive: the kiwi-spec-merge skill is removed from scope. step -> body promotion/merge continues via the existing kiwi-srs-sync skill and the promote_step_requirement MCP tool (no new skill needed). The blocking runtime gaps (diff_steps MCP exposure, merge-journal wiring) are therefore not pursued for v2.3.0.1.

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-07-08 | Created | add-requirement |

### FR-FLOW-016 — Versioned agent-instruction governance snippet with idempotent AGENT_INSTRUCTION_VERSION upsert

| Field | Value |
| --- | --- |
| Type | functional |
| Target | v2.3.0.1 |
| Status | implemented |
| Priority | high |
| Tags | - |
| Risk | medium |
| Stability | evolving |
| Verification Method | test |
| GitHub Issue | - |
| Related Docs | - |

#### Requirement

The system SHALL render a single managed SpecKiwi SRS-workflow governance snippet into both CLAUDE.md and AGENTS.md during project init, bounded by a versioned top-level heading (`# SpecKiwi SRS 워크플로 v<AGENT_INSTRUCTION_VERSION>`) and a fixed end marker (`<!-- /SpecKiwi SRS 워크플로 -->`). The snippet body SHALL carry the SRS MUST rules (spec source of truth, Requirement-ID lookup, Status/Stability lifecycle, TDD, current-work and next-target workflows). When AGENT_INSTRUCTION_VERSION is bumped, the versioned-marker upsert SHALL idempotently replace the prior managed block in place so that re-running init leaves exactly one up-to-date block per file, while unmanaged local sections and mere prose mentions of the heading are preserved.

#### Rationale

-

#### Acceptance Criteria

- [ ] AC-1: Running init on a repository writes the governance snippet into both CLAUDE.md and AGENTS.md, each containing the current versioned heading (`# SpecKiwi SRS 워크플로 v1.4`), the SRS MUST-rule / Status-Stability / TDD / workflow prose, and the closing end marker `<!-- /SpecKiwi SRS 워크플로 -->`.
- [ ] AC-2: The rendered heading version is driven by the AGENT_INSTRUCTION_VERSION constant; a block whose version equals the current AGENT_INSTRUCTION_VERSION and which has the end marker is treated as up to date and skipped, whereas an older versioned block is superseded — so bumping AGENT_INSTRUCTION_VERSION causes the next init to replace the prior block.
- [ ] AC-3: Re-running init is idempotent: each of CLAUDE.md and AGENTS.md ends with exactly one versioned heading and exactly one end marker (no duplicated snippet).
- [ ] AC-4: A stale managed block — either an older versioned heading paired with the end marker, or a legacy unversioned `# SpecKiwi SRS workflow` heading — is replaced in place by the current block, while unmanaged sibling sections (e.g. a local heading) are preserved.
- [ ] AC-5: A versioned heading that lacks the end marker, a bare prose mention of the heading, or a malformed heading that would otherwise pair with a later unrelated block's marker is NOT treated as a managed block; such content and any intervening local sections are preserved and the current block is added without corrupting surrounding content.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |

#### Research / Analysis

- -

#### Implementation Notes

- -

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-07-08 | Created | add-requirement |

### FR-FLOW-017 — Work-Modes-Rules v1.0.0 bundled rule document and CLAUDE.md reference

| Field | Value |
| --- | --- |
| Type | functional |
| Target | v2.3.0.1 |
| Status | implemented |
| Priority | high |
| Tags | - |
| Risk | medium |
| Stability | evolving |
| Verification Method | test |
| GitHub Issue | - |
| Related Docs | - |

#### Requirement

The system SHALL ship a bundled rule document `docs/rule/Work-Modes-Rules-v1.0.0.md` that defines the three work modes (sdd, vibe, wait), the vibe intent-first obligation, the task-name question with auto-derivation, the per-step task-name code-comment convention, the trace contract, synthesis idempotency, and the commit gate. Project initialization SHALL write this document via the bundled-rules pattern (packaged copy when present, rendered fallback otherwise) only when the file is absent, and SHALL add a reference line to CLAUDE.md pointing to the rule document.

#### Rationale

-

#### Acceptance Criteria

- [ ] AC-1: `docs/rule/Work-Modes-Rules-v1.0.0.md` defines the three work modes sdd, vibe, and wait, including the vibe intent-first obligation.
- [ ] AC-2: The rule document defines the task-name question with auto-derivation and the per-step task-name code-comment convention.
- [ ] AC-3: `init` writes `docs/rule/Work-Modes-Rules-v1.0.0.md` via the bundled-rules pattern only when the file is absent (write-if-missing, idempotent on re-run).
- [ ] AC-4: CLAUDE.md gains a reference line pointing to the Work-Modes-Rules rule document.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |

#### Research / Analysis

- -

#### Implementation Notes

- -

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-07-08 | Created | add-requirement |

### ~~FR-FLOW-018 — kiwi-vibe skill for vibe entry~~ [DISCARDED]

| Field | Value |
| --- | --- |
| Type | functional |
| Target | v2.3.0.1 |
| Status | discarded |
| Priority | high |
| Tags | - |
| Risk | medium |
| Stability | evolving |
| Verification Method | test |
| GitHub Issue | - |
| Related Docs | - |

#### Requirement

The system SHALL provide a `kiwi-vibe` skill that drives entry into vibe work mode. The skill SHALL elicit the implementation intent from the user and either accept an explicit task name or auto-derive the task name from the intent. It SHALL invoke the `start_vibe_task` operation with intent-first ordering so that the intent record is persisted before the Active Task state is set. It SHALL then guide the coding agent to insert step task-name trace comments into code during implementation, so that later synthesis can attribute code regions to the vibe task.

#### Rationale

-

#### Acceptance Criteria

- [ ] AC-1: The kiwi-vibe skill asks for the implementation intent and either accepts an explicit task name or auto-derives the task name from the stated intent.
- [ ] AC-2: The kiwi-vibe skill calls start_vibe_task with intent-first ordering, so the intent record (intent.md) is written before the Active Task is set.
- [ ] AC-3: The kiwi-vibe skill guides the coding agent to insert step task-name comments into code during implementation.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |

#### Research / Analysis

- -

#### Implementation Notes

- -
- [2026-07-08] BLOCKER (feasibility 2026-07-08.speckiwi.v2301.01): start_vibe_task has no implementing code anywhere in src/ (no core function, MCP tool, or CLI command). AC-1/AC-2/AC-3 are unachievable until start_vibe_task is implemented. Deferred from the v2.3.0.1 skill-implementation pipeline pending that runtime work. Governance defect: FR-MCP-046 (start_vibe_task tool) is Status=implemented but phantom (no code, empty evidence and trace).
- [2026-07-08] [2026-07-09] DISCARDED per user directive: the kiwi-vibe skill is removed from scope. Vibe / quick implementation-entry is covered by the existing kiwi-hot-fix skill (no new skill needed). The blocking runtime gap (start_vibe_task has no implementing code) is therefore not pursued for v2.3.0.1.

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-07-08 | Created | add-requirement |

### ~~FR-FLOW-019 — kiwi-vibe-commit skill for trace-to-SRS synthesis~~ [DISCARDED]

| Field | Value |
| --- | --- |
| Type | functional |
| Target | v2.3.0.1 |
| Status | discarded |
| Priority | high |
| Tags | - |
| Risk | medium |
| Stability | evolving |
| Verification Method | test |
| GitHub Issue | - |
| Related Docs | - |

#### Requirement

The kiwi-vibe-commit skill SHALL orchestrate synthesis of the active vibe task into a step-scoped SRS by invoking the vibe-to-SRS synthesis engine (FR-NODE-041). It SHALL be idempotent, performing no state-changing action when the active task's step SRS already exists, and after synthesis it SHALL hand the resulting step off to the kiwi-spec-merge skill (FR-FLOW-015) for per-REQ promotion into the SRS body.

#### Rationale

-

#### Acceptance Criteria

- [ ] AC-1: Invoking kiwi-vibe-commit synthesizes the active vibe task into a step-scoped SRS by calling the synthesis engine (FR-NODE-041, src/core/mutation/synthesis.ts).
- [ ] AC-2: When the active vibe task has already been synthesized, kiwi-vibe-commit is an idempotent no-op and makes no further changes to the step SRS.
- [ ] AC-3: After synthesis, kiwi-vibe-commit hands the synthesized step to the kiwi-spec-merge skill (FR-FLOW-015) for per-REQ promotion into the SRS body.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |

#### Research / Analysis

- -

#### Implementation Notes

- -
- [2026-07-08] [2026-07-09] DISCARDED per user directive (committee 2/3, high confidence). Its vibe entry (kiwi-vibe / start_vibe_task) was removed, orphaning it, and AC-3 + the statement handed off to the removed kiwi-spec-merge (FR-FLOW-015). No kiwi-vibe-commit skill file exists, so Status=implemented was phantom. FR-NODE-041 synthesis remains a runtime primitive; step->body promotion is covered by the retained kiwi-srs-sync skill. Minority dissent: keep and repoint AC-3 to kiwi-srs-sync instead.
- [2026-07-09] [2026-07-10] Record correction (adversarial review): the discard statement and AC-1 above call FR-NODE-041 the vibe-to-SRS synthesis engine, but FR-NODE-041 is edit_acceptance_criteria; the actual synthesis engine is FR-NODE-056 (docs/spec/50.nodejs-implementation.srs.md). synthesis.ts exists. The discard decision is unaffected by this cross-reference error.

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-07-08 | Created | add-requirement |

### FR-FLOW-020 — Code comment trace-tag convention for sdd and vibe modes

| Field | Value |
| --- | --- |
| Type | functional |
| Target | v2.3.0.1 |
| Status | implemented |
| Priority | medium |
| Tags | - |
| Risk | low |
| Stability | evolving |
| Verification Method | test |
| GitHub Issue | - |
| Related Docs | - |

#### Requirement

The system SHALL define a code-comment trace-tag convention so that synthesis and plain grep can attribute a code region to its originating work item. In sdd mode, a change to a newly defined class, method, or function SHALL carry the governing SRS Requirement ID in a single-line comment tag of the form `@req <REQ-ID>` (e.g. `// @req FR-NODE-039`, `# @req FR-X-001`), one Requirement ID per line, attached immediately before the definition or in its docstring. In vibe mode, where no SRS requirement exists yet, the change SHALL instead carry the active step task-name as the trace tag. The convention SHALL be inserted and applied by the coding skills and documented as its source of truth, while remaining a purely informational breadcrumb that gates nothing (a missing or stale tag never blocks a build, test, review, or commit).

#### Rationale

-

#### Acceptance Criteria

- [ ] AC-1: An sdd-mode change to a newly defined named class/method/function carries the governing SRS Requirement ID in a `@req <REQ-ID>` code comment (single Requirement ID per line, placed on the line before the definition or in its docstring, using the language's comment style: `//` for TS/JS/Java/Go/Rust/C-family, `#` for Python/Ruby/Shell, `* ` inside JSDoc/JavaDoc blocks).
- [ ] AC-2: A vibe-mode change, made while no SRS requirement exists for the work, carries the active step task-name as its trace tag in place of a REQ-ID, so the region can later be attributed to that task during synthesis.
- [ ] AC-3: The trace-tag convention is inserted and applied by the coding skill during its implementation phase and is documented as the convention's source of truth (scope of the attach obligation, closed-list exemptions, single-line tag format, and dedupe/placement rules), while being explicitly exempt from every verification, review, plan-code-match, and gate check so it can never block work.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |

#### Research / Analysis

- -

#### Implementation Notes

- -

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-07-08 | Created | add-requirement |

### FR-FLOW-021 — Init-injected agent instruction block declares a tool-first SRS policy

| Field | Value |
| --- | --- |
| Type | functional |
| Target | v2.3.0.1 |
| Status | implemented |
| Priority | medium |
| Tags | - |
| Risk | medium |
| Stability | evolving |
| Verification Method | test |
| GitHub Issue | - |
| Related Docs | - |

#### Requirement

The system SHALL, on project init, generate and idempotently maintain a versioned, canonical managed agent-instruction block that declares a tool-first policy for SRS work. The block SHALL state that when SpecKiwi MCP tools are available agents MUST use them for requirement lookup and safe SRS updates, and MUST fall back to the `speckiwi` CLI when MCP is unavailable, and it SHALL forbid creating an alternate requirements source outside `docs/spec/`. The same block SHALL be injected into both `AGENTS.md` and `CLAUDE.md` and be embedded in the bundled SRS-MD-Rules document, and it SHALL be versioned by `AGENT_INSTRUCTION_VERSION` so that re-running init upserts exactly one current block without duplicating or clobbering unrelated content.

#### Rationale

-

#### Acceptance Criteria

- [ ] AC-1: After init of an empty repo, both AGENTS.md and CLAUDE.md contain a managed agent-instruction block headed `# SpecKiwi SRS 워크플로 v<AGENT_INSTRUCTION_VERSION>` and terminated by the `<!-- /SpecKiwi SRS 워크플로 -->` end marker.
- [ ] AC-2: The injected block declares the tool-first policy: when SpecKiwi MCP tools are available agents MUST use them for requirement lookup and safe SRS updates, and MUST use the `speckiwi` CLI when MCP is unavailable.
- [ ] AC-3: The injected block forbids creating an alternate requirements source outside `docs/spec/`, keeping docs/spec as the single source of truth.
- [ ] AC-4: Re-running init is idempotent — exactly one managed block instance remains (a single versioned heading and a single end marker, no duplication).
- [ ] AC-5: Init deterministically replaces an outdated versioned block or an unversioned legacy block (heading paired with the end marker) with the current version, while preserving unrelated local sections; a heading with no paired end marker is left untouched.
- [ ] AC-6: The bundled SRS-MD-Rules document embeds the same managed agent-instruction block wording that renderAgentInstructionSnippet produces.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |

#### Research / Analysis

- -

#### Implementation Notes

- -

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-07-08 | Created | add-requirement |

### FR-FLOW-022 — Single current-model verification subagent across kiwi skills (remove --mini)

| Field | Value |
| --- | --- |
| Type | functional |
| Target | v2.3.0.1 |
| Status | implemented |
| Priority | high |
| Tags | - |
| Risk | medium |
| Stability | evolving |
| Verification Method | test |
| GitHub Issue | - |
| Related Docs | - |

#### Requirement

All kiwi-* skills SHALL remove the `--mini` cost-reduction option and its shared `_shared/kiwi/mini-option.md` SSOT. Skill verification and evaluation SHALL be performed by a single subagent that inherits the current session model, replacing the prior dual-model (Opus + Sonnet) evaluator panels. A `--model <name>` argument, or a model name the user names in the prompt, SHALL override the model used for that verification subagent.

#### Rationale

v2.3.0.1 tool-improvement: simplify the kiwi verification model to the current session model with explicit --model control, replacing cross-model Opus+Sonnet panels and the --mini option. User-directed 2026-07-09.

#### Acceptance Criteria

- [ ] AC-1: No kiwi-* skill body or shared SSOT references `--mini`; the option and `_shared/kiwi/mini-option.md` are removed repo-wide.
- [ ] AC-2: Skill verification/evaluation spawns a single subagent that runs on the current session model by default, in place of the Opus×1+Sonnet×1 (and Max Opus×2+Sonnet×1) dual-model evaluator panels.
- [ ] AC-3: A `--model <name>` argument, or a model name the user names in the prompt, selects the model for the verification subagent instead of the current session model.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |
| VE-1 | plan | docs/plans/2026-07-10.speckiwi.v2301-flow.plan.md#T-PH001-02 | all | - |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |
| Task | T-PH001-01 | depends_on | - |
| Task | T-PH001-02 | depends_on | - |
| Task | T-PH006-01 | depends_on | - |

#### Research / Analysis

- -

#### Implementation Notes

- -

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-07-08 | Created | add-requirement |

### FR-FLOW-023 — kiwi-srs research-document-driven SRS verify/improve (A/B) loop

| Field | Value |
| --- | --- |
| Type | functional |
| Target | v2.3.0.1 |
| Status | implemented |
| Priority | high |
| Tags | - |
| Risk | medium |
| Stability | evolving |
| Verification Method | test |
| GitHub Issue | - |
| Related Docs | - |

#### Requirement

kiwi-srs SHALL accept research document(s) as an argument or via a user prompt reference. After SRS authoring it SHALL run an A/B loop. Process A spawns verification subagent(s) that read the research document(s) and compare them against the newly authored SRS to detect missing requirements, incorrectly authored requirements, and implementability in the current architecture — reusing the existing kiwi-srs-feasibility implementability evaluation and additionally checking that existing product features are not broken — and write an improvements document to a run-scoped temp directory; if there are no improvements, Process A terminates. Process B spawns a separate subagent that reads the improvements document and improves the SRS, after which control returns to Process A. The loop SHALL be bounded by a divergence guard (maximum iterations).

#### Rationale

v2.3.0.1 tool-improvement: close the loop between research documents and authored SRS so missing/incorrect requirements and architectural infeasibility are caught and improved before work proceeds. User-directed 2026-07-09.

#### Acceptance Criteria

- [ ] AC-1: kiwi-srs accepts research document(s) as an explicit argument or resolves them from a user prompt reference.
- [ ] AC-2: Process A: a verification subagent compares the research document(s) against the newly added SRS and reports (i) missing requirements, (ii) incorrectly authored requirements, and (iii) implementability in the current architecture — reusing the existing feasibility evaluation AND flagging any break to existing product features — writing an improvements document to a run-scoped temp directory; when no improvements are found, Process A terminates.
- [ ] AC-3: Process B: a separate subagent reads the improvements document and applies improvements to the SRS, then returns control to Process A, bounded by a maximum-iteration divergence guard.
- [ ] AC-4: In `--max` mode, Process A uses 3 verification subagents.
- [ ] AC-5: With multiple research documents, non-max mode spawns verification subagents sequentially per document; `--max` mode spawns (document count × 3) verification subagents.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |
| VE-1 | plan | docs/plans/2026-07-10.speckiwi.v2301-flow.plan.md#T-PH002-02 | all | - |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |
| Task | T-PH002-01 | depends_on | - |
| Task | T-PH002-02 | depends_on | - |

#### Research / Analysis

- -

#### Implementation Notes

- -

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-07-08 | Created | add-requirement |

### FR-FLOW-024 — kiwi-srs no-research-document ambiguity resolution, qna loop, and requirement-gathering research

| Field | Value |
| --- | --- |
| Type | functional |
| Target | v2.3.0.1 |
| Status | implemented |
| Priority | high |
| Tags | - |
| Risk | medium |
| Stability | evolving |
| Verification Method | test |
| GitHub Issue | - |
| Related Docs | - |

#### Requirement

When no research document is provided, kiwi-srs SHALL investigate the requirements for ambiguity and question the user only about genuinely non-standard ambiguities that have no reasonable default; a user-supplied decision SHALL itself be re-examined for ambiguity and re-questioned when needed. A sufficiently vague request SHALL auto-activate an unbounded qna loop (also forced by `--qna-force`), replacing the prior bounded QnA — except under `--auto`, where the interactive qna loop and `--qna-force` are suppressed and remaining ambiguities are resolved directly by the FR-FLOW-025 committee. The `--qna-force` flag SHALL be parsed fail-closed: it matches the exact token `--qna-force` only (never `--qna`, whose prefix it contains), and any unrecognized or single-dash flag variant SHALL raise a hard error rather than a silent no-op or an inverted action. When `--auto` is absent the loop SHALL run until the user signals end, at which point kiwi-srs SHALL enter auto-decision mode for the remaining ambiguities. It SHALL then spawn 3 subagents that perform requirement gathering and research into how to cleanly implement the requirements in the current architecture and project structure (architecture, algorithms, and a brief implementation plan), saving the result under `docs/research/`, and SHALL then proceed to the research-document verify/improve loop (FR-FLOW-023).

#### Rationale

v2.3.0.1 tool-improvement: give kiwi-srs a principled path for under-specified requests — ask only genuinely non-standard questions, otherwise research and decide — feeding the research output into the verify/improve loop. User-directed 2026-07-09.

#### Acceptance Criteria

- [ ] AC-1: With no research document, kiwi-srs detects ambiguity in the requirements and asks the user only about genuinely non-standard ambiguities that have no reasonable default; a user-supplied decision is re-checked for ambiguity and re-questioned when needed.
- [ ] AC-2: A sufficiently vague request (e.g. "make an SRS for a game") auto-activates the unbounded qna loop, and `--qna-force` forces it — except under `--auto`, where the interactive qna loop and `--qna-force` are suppressed and remaining ambiguities are resolved by the FR-FLOW-025 committee (no interactive qna). `--qna-force` is parsed fail-closed as an exact token (never the `--qna` alias it prefixes; unrecognized or single-dash variants hard-error). This replaces the prior bounded QnA. When `--auto` is absent, the qna loop runs until the user signals end.
- [ ] AC-3: On end, kiwi-srs enters auto-decision mode for the remaining ambiguities.
- [ ] AC-4: kiwi-srs spawns 3 subagents that gather requirements and research a clean implementation approach in the current architecture/project structure (architecture, algorithms, a brief implementation plan), save it under `docs/research/`, then proceed to the research-document verify/improve loop (FR-FLOW-023).

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |
| VE-1 | plan | docs/plans/2026-07-10.speckiwi.v2301-flow.plan.md#T-PH003-02 | all | - |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |
| Requirement | FR-FLOW-023 | depends_on | - |
| Requirement | FR-FLOW-025 | depends_on | - |
| Task | T-PH003-01 | depends_on | - |
| Task | T-PH003-02 | depends_on | - |

#### Research / Analysis

- -

#### Implementation Notes

- -

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-07-08 | Created | add-requirement |
| 2026-07-12 | Renamed forced qna flag `-qna` to `--qna-force` and codified fail-closed exact-token parsing (unrecognized or single-dash variants hard-error). | committee-decision-D2: remove convention-inverted single-dash forced-flag footgun |

### FR-FLOW-025 — --auto decision committee sizing and unanimity escalation

| Field | Value |
| --- | --- |
| Type | functional |
| Target | v2.3.0.1 |
| Status | implemented |
| Priority | high |
| Tags | - |
| Risk | medium |
| Stability | evolving |
| Verification Method | test |
| GitHub Issue | - |
| Related Docs | - |

#### Requirement

The `--auto` auto-decision mode SHALL convene a decision committee of research-performing subagents that decides the most reasonable option, replacing the prior `--auto` "block instead of ask" behavior. The committee SHALL have 3 members under `--auto`, or 5 members under `--max`. A non-unanimous 3-member committee SHALL escalate to 5 members and re-decide; a non-unanimous 5-member committee in non-max mode SHALL decide by majority. Under `--max`, a non-unanimous 5-member committee SHALL escalate to 7 members, which SHALL decide by majority without requiring unanimity.

#### Rationale

v2.3.0.1 tool-improvement: make --auto auto-decide via a research-performing committee with a defined unanimity-escalation ladder (3->5 majority; --max 5->7 majority), superseding the prior --auto "block" semantic. User-directed 2026-07-09.

#### Acceptance Criteria

- [ ] AC-1: `--auto` convenes a 3-member decision committee whose members perform research investigation and select the most reasonable option.
- [ ] AC-2: `--max` raises the decision committee to 5 members.
- [ ] AC-3: A non-unanimous 3-member (non-max) committee escalates to 5 members and re-decides; a non-unanimous 5-member (non-max) committee decides by plurality (the option with the most votes), and any tie is broken deterministically by the lead committee member ranking.
- [ ] AC-4: Under `--max`, a non-unanimous 5-member committee escalates to 7 members, which decide by plurality (most votes; a tie is broken deterministically by the lead committee member ranking), without requiring unanimity.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |
| VE-1 | plan | docs/plans/2026-07-10.speckiwi.v2301-flow.plan.md#T-PH001-04 | all | - |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |
| Task | T-PH001-03 | depends_on | - |
| Task | T-PH001-04 | depends_on | - |

#### Research / Analysis

- -

#### Implementation Notes

- -
- [2026-07-08] [2026-07-09] The statement/rationale phrase "replacing the prior --auto block-instead-of-ask behavior" is imprecise: prior --auto behavior varies by skill (kiwi-srs blocks at certain critical gates instead of AskUserQuestion; other kiwi skills already auto-decide via committee). At implementation, describe only the new committee behavior and verify the prior semantic per skill before asserting what it replaces.
- [2026-07-09] [2026-07-10] Guardrail (review completeness-critic): the decision committee auto-decides only auto-decidable gates. Critical / business-decision gates defined in _shared/kiwi/auto-option.md critical_gates (e.g. external-module impact, scope-boundary, rollback-confirmation) STILL HALT for the user even under --auto; the committee does not auto-resolve a decision the prior --auto guardrail required to halt.

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-07-08 | Created | add-requirement |

### FR-FLOW-026 — kiwi-pipeline end-to-end research-to-implementation cycle orchestration

| Field | Value |
| --- | --- |
| Type | functional |
| Target | v2.3.0.1 |
| Status | implemented |
| Priority | high |
| Tags | - |
| Risk | medium |
| Stability | evolving |
| Verification Method | test |
| GitHub Issue | - |
| Related Docs | - |

#### Requirement

The kiwi-pipeline skill SHALL orchestrate the full research -> planning -> implementation cycle by chaining the kiwi-* sub-skills, rather than only recommending a single next step. Given a work input it SHALL run `/kiwi-srs`, then judge whether `/kiwi-srs-feasibility` is needed and run it only when required, then run `/kiwi-planner`, `/kiwi-pm`, and `/kiwi-review-fix-loop` in sequence. When invoked without a work input it SHALL advance the cycle from the last pipeline.jsonl event via Table T1. Between stages it SHALL apply a user gate unless `--auto` is set; under `--auto` it SHALL decide every gate autonomously via the FR-FLOW-025 decision committee and run to the end, while still halting on a sub-skill NEEDS_USER/FAILED and the existing critical gates. Under `--max` it SHALL propagate `--max` to every spawned sub-skill. If the user supplies a research document it SHALL pass that document through to `/kiwi-srs`.

#### Rationale

v2.3.0.1 tool-improvement: elevate kiwi-pipeline from a single-step next-action advisor to an end-to-end research->plan->implement cycle orchestrator that composes the kiwi-* skills. Designed to compose with goal mode for large autonomous runs. User-directed 2026-07-09.

#### Acceptance Criteria

- [ ] AC-1: With a work input, kiwi-pipeline runs the sequence /kiwi-srs -> (conditional /kiwi-srs-feasibility) -> /kiwi-planner -> /kiwi-pm -> /kiwi-review-fix-loop, chaining stages rather than stopping after a single next-step recommendation.
- [ ] AC-2: kiwi-pipeline runs /kiwi-srs-feasibility only when it judges it is needed (e.g. the just-authored requirements carry draft stability or unverified implementability); otherwise it skips feasibility.
- [ ] AC-3: Between stages a user gate is applied unless `--auto` is set or the user asks to run automatically; under `--auto` every gate is auto-decided via the FR-FLOW-025 committee and the cycle runs to the end, while a sub-skill NEEDS_USER/FAILED or an existing critical gate still halts.
- [ ] AC-4: `--max` is propagated to every spawned sub-skill.
- [ ] AC-5: A user-supplied research document is passed through to `/kiwi-srs` (per FR-FLOW-023).

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |
| VE-1 | plan | docs/plans/2026-07-10.speckiwi.v2301-flow.plan.md#T-PH003-04 | all | - |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |
| Requirement | FR-FLOW-025 | depends_on | - |
| Requirement | FR-FLOW-023 | depends_on | - |
| Task | T-PH003-03 | depends_on | - |
| Task | T-PH003-04 | depends_on | - |

#### Research / Analysis

- -

#### Implementation Notes

- -

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-07-08 | Created | add-requirement |

### FR-FLOW-027 — kiwi-pipeline worktree isolation with merge-or-PR completion gate

| Field | Value |
| --- | --- |
| Type | functional |
| Target | v2.3.0.1 |
| Status | implemented |
| Priority | high |
| Tags | - |
| Risk | medium |
| Stability | evolving |
| Verification Method | test |
| GitHub Issue | - |
| Related Docs | - |

#### Requirement

When the user requests worktree isolation or passes a `--wt` argument, the kiwi-pipeline skill SHALL create a dedicated git worktree and run the cycle inside it. On successful completion it SHALL resolve the worktree: in interactive (non-auto) mode it SHALL ask the user whether to merge the branch or open a PR; under `--auto` it SHALL open a PR (via kiwi-commit-auto-pr) and SHALL NOT direct-merge the base branch. This extends kiwi-pipeline beyond its prior read-only nature (whose only side effect was appending to pipeline.jsonl) to a defined git worktree mutation.

#### Rationale

v2.3.0.1 tool-improvement: let a full pipeline cycle run in an isolated worktree and resolve cleanly (ask merge-or-PR interactively; auto-PR under --auto), keeping the base branch safe. User-directed 2026-07-09.

#### Acceptance Criteria

- [ ] AC-1: A `--wt` argument, or a user request for worktree isolation, makes kiwi-pipeline create a dedicated git worktree and run the cycle inside it.
- [ ] AC-2: On successful completion in interactive (non-auto) mode, kiwi-pipeline asks the user whether to merge the worktree branch or open a PR.
- [ ] AC-3: Under `--auto`, kiwi-pipeline opens a PR (kiwi-commit-auto-pr) on completion and does not direct-merge the base branch.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |
| VE-1 | plan | docs/plans/2026-07-10.speckiwi.v2301-flow.plan.md#T-PH004-02 | all | - |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |
| Requirement | FR-FLOW-026 | depends_on | - |
| Task | T-PH004-01 | depends_on | - |
| Task | T-PH004-02 | depends_on | - |

#### Research / Analysis

- -

#### Implementation Notes

- -

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-07-08 | Created | add-requirement |

### FR-FLOW-028 — kiwi-pipeline GitHub issue entry mode with research-first flow

| Field | Value |
| --- | --- |
| Type | functional |
| Target | v2.3.0.1 |
| Status | implemented |
| Priority | high |
| Tags | - |
| Risk | medium |
| Stability | evolving |
| Verification Method | test |
| GitHub Issue | - |
| Related Docs | - |

#### Requirement

When the user supplies a GitHub issue number, the kiwi-pipeline skill SHALL first run `/kiwi-srs-research` to research how to resolve the issue and perform additional research on the implementation approach, then start `/kiwi-srs`. When the research alone does not sufficiently resolve the requirement's ambiguities it SHALL invoke `/kiwi-srs` with the `--qna-force` option — except under `--auto`, where it SHALL suppress `--qna-force` and resolve remaining ambiguities via the FR-FLOW-025 decision committee. The cycle SHALL then continue through the standard planning and implementation stages (/kiwi-planner, /kiwi-pm, /kiwi-review-fix-loop).

#### Rationale

v2.3.0.1 tool-improvement: give kiwi-pipeline a GitHub-issue entry point that researches the resolution and implementation approach first, then authors SRS (escalating to --qna-force only when research is insufficient and not under --auto). User-directed 2026-07-09.

#### Acceptance Criteria

- [ ] AC-1: A supplied GitHub issue number makes kiwi-pipeline run `/kiwi-srs-research` to research the resolution plus additional implementation-approach research before starting `/kiwi-srs`.
- [ ] AC-2: When research alone leaves the requirement ambiguous, kiwi-pipeline starts `/kiwi-srs` with `--qna-force`; under `--auto` it instead suppresses `--qna-force` and resolves ambiguities via the FR-FLOW-025 committee.
- [ ] AC-3: After the issue-driven research and authoring, the cycle continues through /kiwi-planner, /kiwi-pm, and /kiwi-review-fix-loop.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |
| VE-1 | plan | docs/plans/2026-07-10.speckiwi.v2301-flow.plan.md#T-PH004-04 | all | - |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |
| Requirement | FR-FLOW-026 | depends_on | - |
| Task | T-PH004-03 | depends_on | - |
| Task | T-PH004-04 | depends_on | - |

#### Research / Analysis

- -

#### Implementation Notes

- -

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-07-08 | Created | add-requirement |
| 2026-07-12 | Renamed forced qna flag `-qna` to `--qna-force` for consistency with FR-FLOW-024. | committee-decision-D2 |

### FR-FLOW-029 — kiwi-wave-master multi-wave orchestrator with per-wave targets and resumable progress

| Field | Value |
| --- | --- |
| Type | functional |
| Target | v2.3.0.1 |
| Status | implemented |
| Priority | high |
| Tags | - |
| Risk | medium |
| Stability | evolving |
| Verification Method | test |
| GitHub Issue | - |
| Related Docs | - |

#### Requirement

The system SHALL provide a `kiwi-wave-master` skill that orchestrates a large research/plan effort as an ordered sequence of waves above kiwi-pipeline. Given the user's existing research and plan documents, it SHALL decompose them into ordered waves — using explicit wave structure in the document when present, otherwise a subagent SHALL analyze the document's overall flow to split the waves. For each wave it SHALL register a dedicated target (e.g. `wave-{n}`) through `/kiwi-srs` with an explicitly specified work scope, so that downstream feasibility, planning, and review are bounded to that wave and never look beyond it. It SHALL persist wave progress to a JSONL file (e.g. `./kiwi/waves.jsonl`) and invoke `/kiwi-pipeline` per wave in registration order to implement each wave, marking a wave complete in the JSONL only after its kiwi-pipeline run finishes successfully, so that a cleared or restarted session resumes from the first incomplete wave. The up-front `/kiwi-srs` pass SHALL author each wave's requirements; the per-wave `/kiwi-pipeline` SHALL skip re-authoring and run from feasibility/planning through implementation. Under `--auto` it SHALL run all waves autonomously to the end (per-wave kiwi-pipeline safety gates still applying); under `--max` it SHALL propagate `--max` to every wave's kiwi-pipeline and sub-skills.

#### Rationale

v2.3.0.1 tool-improvement: add a wave-level orchestrator above kiwi-pipeline so a large research/plan effort is decomposed into scoped, independently-targeted waves executed in order with JSONL-backed resumability across cleared sessions. Composes strongly with goal mode. Name and division auto-decided 2026-07-09 in the user's absence (provisional). User-directed 2026-07-09.

#### Acceptance Criteria

- [ ] AC-1: kiwi-wave-master decomposes the user's research/plan documents into ordered waves — using explicit wave structure in the document when present, otherwise a subagent analyzes the document's overall flow to split waves.
- [ ] AC-2: For each wave, kiwi-wave-master registers a dedicated target (e.g. `wave-{n}`) via `/kiwi-srs` with an explicitly specified work scope, so downstream feasibility/planning/review are bounded to that wave and do not look beyond it.
- [ ] AC-3: kiwi-wave-master persists wave progress to a JSONL file (e.g. `./kiwi/waves.jsonl`); a wave is marked complete only after its execution finishes, so a cleared/restarted session resumes from the first incomplete wave.
- [ ] AC-4: kiwi-wave-master invokes `/kiwi-pipeline` per wave in order to implement each wave; the up-front `/kiwi-srs` authors each wave's requirements and the per-wave `/kiwi-pipeline` skips re-authoring, running from feasibility/planning through implementation.
- [ ] AC-5: Under `--auto`, kiwi-wave-master runs all waves autonomously to the end (per-wave kiwi-pipeline safety gates still apply); under `--max`, `--max` is propagated to every wave's kiwi-pipeline and its sub-skills.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |
| VE-1 | plan | docs/plans/2026-07-10.speckiwi.v2301-flow.plan.md#T-PH005-02 | all | - |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |
| Requirement | FR-FLOW-026 | depends_on | - |
| Requirement | FR-FLOW-025 | depends_on | - |
| Task | T-PH005-01 | depends_on | - |
| Task | T-PH005-02 | depends_on | - |

#### Research / Analysis

- -

#### Implementation Notes

[2026-07-09] Skill name confirmed: kiwi-wave-master (working name was kiwi-wave). AC-5 --auto (all-wave autonomy) and --max (propagation) are confirmed by the user and are no longer provisional.


#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-07-08 | Created | add-requirement |

### FR-FLOW-030 — kiwi-wave-master epic-issue entry mode

| Field | Value |
| --- | --- |
| Type | functional |
| Target | v2.3.0.1 |
| Status | implemented |
| Priority | high |
| Tags | - |
| Risk | medium |
| Stability | evolving |
| Verification Method | test |
| GitHub Issue | - |
| Related Docs | - |

#### Requirement

When the user supplies an epic issue, the `kiwi-wave-master` skill SHALL extract the ordered waves from that epic (its structure, task list, or linked sub-issues) instead of analyzing a research/plan document. After wave extraction it SHALL proceed identically to the research/plan flow (FR-FLOW-029): scoped per-wave target registration, waves.jsonl progress tracking, and per-wave `/kiwi-pipeline` execution in order. Only the up-front wave-splitting research analysis is skipped, because the epic already provides the wave structure; each wave's `/kiwi-pipeline` SHALL still perform its own per-wave research as needed.

#### Rationale

v2.3.0.1 tool-improvement: let kiwi-wave-master start from an epic issue by deriving waves from the epic itself, reusing the same scoped-target/resumable-execution machinery as the research/plan entry. Epic-research-skip semantics auto-decided 2026-07-09 (provisional, pending user confirmation). User-directed 2026-07-09.

#### Acceptance Criteria

- [ ] AC-1: Given an epic issue, kiwi-wave-master extracts the ordered waves from the epic (its structure, task list, or linked sub-issues) rather than analyzing a research/plan document.
- [ ] AC-2: After wave extraction, kiwi-wave-master proceeds identically to the FR-FLOW-029 flow (scoped per-wave target registration, waves.jsonl, per-wave /kiwi-pipeline in order).
- [ ] AC-3: Only the up-front wave-splitting research analysis is skipped; each wave's /kiwi-pipeline still performs its own per-wave research as needed.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |
| VE-1 | plan | docs/plans/2026-07-10.speckiwi.v2301-flow.plan.md#T-PH005-04 | all | - |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |
| Requirement | FR-FLOW-029 | depends_on | - |
| Task | T-PH005-03 | depends_on | - |
| Task | T-PH005-04 | depends_on | - |

#### Research / Analysis

- -

#### Implementation Notes

- -
- [2026-07-10] OQ-030 RESOLVED 2026-07-10 (5-member research committee, 3A/1B/1conditional, high-conf): CONFIRM epic-research-skip (AC-3) when the epic has explicit structure (task-list groups / >=2 linked sub-issues). GUARD: if no extractable structure (free-form prose, <2 sub-issues, no partitionable task list), fall back to FR-FLOW-029's wave-split subagent (no new component). Provisional flag cleared.

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-07-08 | Created | add-requirement |

### FR-FLOW-031 — kiwi-planner planner-document management and validation via v2.3.0 workflow MCP tools

| Field | Value |
| --- | --- |
| Type | functional |
| Target | v2.3.0.1 |
| Status | implemented |
| Priority | high |
| Tags | - |
| Risk | medium |
| Stability | evolving |
| Verification Method | test |
| GitHub Issue | - |
| Related Docs | - |

#### Requirement

The kiwi-planner skill (all packaged variants) SHALL use the v2.3.0 SpecKiwi workflow_* MCP tool family as the primary path for reading, validating, and mutating its planner document (plan.md + sidecar), superseding bespoke file handling and the bundled validator.mjs. Reads SHALL go through workflow_plan_status / workflow_plan_task / workflow_next_plan_task; validation and diagnostics through workflow_doctor / workflow_schema_check / workflow_diff; checkbox mutations through workflow_task_check / workflow_task_uncheck / workflow_checklist_set; and pipeline/worklog events through the guarded workflow emit tools. Validation coverage SHALL NOT regress: any plan-contract check formerly done by validator.mjs (C01-C25) that the workflow MCP family does not yet provide SHALL be preserved during implementation. Raw docs/plans file access SHALL remain only a documented degraded fallback per MIG-FLOW-003 AC-3.

#### Rationale

v2.3.0.1 tool-improvement: kiwi-planner uses the v2.3.0 workflow_* MCP tool family as the primary path to read, validate, and mutate its planner document, superseding bespoke file handling and the bundled validator.mjs, while ensuring plan-contract validation coverage does not regress. Extends MIG-FLOW-003 (depends_on) to concrete planner-document management and closes the unmigrated claude variant. User-directed 2026-07-09.

#### Acceptance Criteria

- [ ] AC-1: kiwi-planner uses the workflow_* MCP tool family as the primary path for reading (workflow_plan_status / workflow_plan_task / workflow_next_plan_task), validating (workflow_doctor / workflow_schema_check / workflow_diff), and mutating (workflow_task_check / workflow_task_uncheck / workflow_checklist_set) its planner document, superseding raw file handling and the bundled validator.mjs.
- [ ] AC-2: All packaged kiwi-planner variants (claude, codex, etc) route planner-document operations through the workflow_* MCP tools; the claude variant, which currently invokes none, is brought into compliance (also satisfying MIG-FLOW-003).
- [ ] AC-3: Validation coverage does not regress: any plan-contract check formerly in validator.mjs (C01-C25) not yet provided by the workflow MCP family is preserved during implementation, and raw docs/plans file access remains only a documented degraded fallback.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |
| VE-1 | plan | docs/plans/2026-07-10.speckiwi.v2301-flow.plan.md#T-PH002-04 | all | - |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |
| Requirement | MIG-FLOW-003 | depends_on | - |
| Task | T-PH002-03 | depends_on | - |
| Task | T-PH002-04 | depends_on | - |

#### Research / Analysis

- -

#### Implementation Notes

- -

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-07-08 | Created | add-requirement |

### FR-FLOW-032 — kiwi-planner plan-vs-SRS coverage verification loop

| Field | Value |
| --- | --- |
| Type | functional |
| Target | v2.3.0.1 |
| Status | implemented |
| Priority | high |
| Tags | - |
| Risk | medium |
| Stability | evolving |
| Verification Method | test |
| GitHub Issue | - |
| Related Docs | - |

#### Requirement

After kiwi-planner produces a plan, it SHALL run a coverage verification loop that thoroughly checks the plan against the SRS and the intended implementation scope. The loop SHALL start by reconciling the count of target SRS requirements against the plan coverage entries, then cross-check each requirement ID one-by-one against the plan to confirm every requirement and acceptance criterion is satisfied. The plan coverage for a requirement SHALL be verified by 2 subagents applied in sequence (running on the current session model per FR-FLOW-022): the first subagent confirms it, and only after that does a second subagent independently re-confirm it; the requirement SHALL be marked verification-complete (implementation-ready) only after both subagents confirm, and that marking SHALL be persistent so a marked requirement is not re-analyzed or changed in later iterations. Under --max the verification SHALL be strengthened: coverage SHALL be checked at acceptance-criterion granularity (each AC, and where applicable its test/TDD task, mapped to a plan task); after the two sequential confirmations an independent third subagent SHALL attempt to refute full coverage and the requirement is marked verification-complete only if that refutation fails; and the loop SHALL terminate only after two consecutive rounds with zero new omissions. When an omission is found — a requirement or acceptance criterion not covered by any plan task, or a plan task outside the SRS scope — kiwi-planner SHALL improve the plan and re-verify only the not-yet-marked items, iterating until every requirement is marked verification-complete, bounded by a divergence guard.

#### Rationale

v2.3.0.1 tool-improvement: require kiwi-planner to formally verify, in a loop, that its plan covers every target requirement and AC before completing, catching plan/SRS omissions early. Formalizes kiwi-planner's internal SRS-satisfaction evaluation as an explicit requirement; uses 2 subagents by default under FR-FLOW-022's current-model policy. User-directed 2026-07-10.

#### Acceptance Criteria

- [ ] AC-1: The verification loop starts by reconciling the count of target SRS requirements against the plan coverage entries, then cross-checks each requirement ID one-by-one against the plan.
- [ ] AC-2: Verification uses 2 subagents applied in sequence (on the current session model per FR-FLOW-022): the first subagent confirms a requirement plan coverage, and only after it passes does the second subagent independently re-confirm; the requirement is marked verification-complete (implementation-ready) only after both confirm, and that marking is persistent so a marked requirement is not re-analyzed or changed in later iterations. Under --max, coverage is checked at acceptance-criterion granularity and an independent third subagent must fail to refute full coverage before the requirement is marked.
- [ ] AC-3: When an omission is found (a requirement or AC uncovered by any plan task, or a plan task outside the SRS scope), kiwi-planner improves the plan and re-verifies only the not-yet-marked items, iterating until every requirement is marked verification-complete, bounded by a divergence guard; under --max, the loop terminates only after two consecutive rounds with zero new omissions.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |
| VE-1 | plan | docs/plans/2026-07-10.speckiwi.v2301-flow.plan.md#T-PH002-06 | all | - |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |
| Requirement | FR-FLOW-022 | depends_on | - |
| Task | T-PH002-05 | depends_on | - |
| Task | T-PH002-06 | depends_on | - |

#### Research / Analysis

- -

#### Implementation Notes

- -

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-07-09 | Created | add-requirement |

## 5. Cross-scope Dependencies

| From | To | Relation | Notes |
|---|---|---|---|
| FLOW | CLI | depends_on | CLI 명령으로 workflow를 수행한다. |
| FLOW | MCP | depends_on | agent는 MCP tool로 workflow를 수행할 수 있다. |
| FLOW | PARSE | depends_on | release readiness는 validation과 summary를 사용한다. |

## 6. Open Questions

| ID | Question | Impact | Status |
|---|---|---|---|
| - | - | - | - |

## 7. Change Notes

| Date | Change | Reason |
|---|---|---|
| 2026-05-07 | 최초 작성 | SpecKiwi v1.0.0 scope 요구사항 정의 |
| 2026-05-10 | active target workflow 요구사항 추가 | agent가 현재 작업 목표 버전을 기준으로 진행/잔여 작업을 파악하도록 명시 |
| 2026-05-10 | v1.2.0 release/agent hardening 요구사항 추가 | release readiness, evidence coverage, canonical agent file 권고 반영 |
| 2026-05-10 | v1.3.0 stability-aware workflow 요구사항 추가 | `Stability=draft`가 현재 작업과 release 판단에 미치는 영향 반영 |
| 2026-05-11 | v2.1.0 next target workflow 요구사항 추가 | 다음 목표 버전 설정 후 SRS 작성 target을 Active Target으로 일관되게 사용하도록 명시 |
