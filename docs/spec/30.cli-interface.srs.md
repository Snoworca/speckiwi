# SpecKiwi CLI Interface SRS

| Field | Value |
|---|---|
| Document Type | scope_srs |
| Product | SpecKiwi |
| Product Version | 1.0.0 |
| Scope | CLI |
| Scope Name | CLI Interface |
| Version | 1.0.0 |
| Last Updated | 2026-05-13 |
| Rules | [SRS-MD Authoring Rules v1.0.0](../rule/SRS-MD-Rules-v1.0.0.md) |

## 1. Scope Overview

이 문서는 `speckiwi` Node.js CLI의 명령, 옵션, 출력, mutation behavior, exit code 정책을 정의한다.

## 2. Scope Boundaries

### In Scope

- CLI entrypoint
- init, validate, extract, list, show, targets, scopes
- status, AC, evidence, trace mutation 명령
- summary, links check, mcp launcher
- exit code와 output format

### Out of Scope

- 웹 UI
- 대화형 TUI
- 원격 서버 API
- YAML export/import

## 3. Assumptions and Constraints

- CLI는 Node.js package bin으로 제공된다.
- CLI는 core service를 호출한다.
- 쓰기 명령은 최소 변경 원칙을 따른다.

## 4. Requirements

### IR-CLI-001 — CLI entrypoint 제공

| Field | Value |
| --- | --- |
| Type | interface |
| Target | v1.0.0 |
| Status | verified |
| Priority | critical |
| Tags | cli, bin, nodejs |
| Risk | medium |
| Stability | stable |
| Verification Method | test |
| GitHub Issue | - |
| Related Docs | - |

#### Requirement

SpecKiwi는 `speckiwi`라는 Node.js CLI entrypoint를 제공해야 한다.

#### Rationale

사용자는 저장소 로컬 또는 npx 실행 방식으로 SRS 문서를 조회하고 검증할 수 있어야 한다.

#### Acceptance Criteria

- [x] AC-1: 패키지 `bin`은 `speckiwi` 명령을 제공한다.
- [x] AC-2: `speckiwi --version`은 패키지 버전을 출력한다.
- [x] AC-3: `speckiwi --help`는 최상위 명령 목록을 출력한다.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |
| VE-1 | test | test/release/srs-traceability.test.ts; npm test; npm run test:integration; npm run release:acceptance; npm run perf:srs | all | SpecKiwi v1.0.0 implementation verification on 2026-05-08 |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |

#### Research / Analysis

- -

#### Implementation Notes

- CLI 구현은 `src/cli` adapter에 둔다.
- 비즈니스 로직은 `src/core`를 호출한다.

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-05-07 | 최초 작성 | SpecKiwi v1.0.0 제품 SRS baseline 정의 |



### IR-CLI-002 — 공통 옵션 정의

| Field | Value |
| --- | --- |
| Type | interface |
| Target | v1.0.0 |
| Status | verified |
| Priority | critical |
| Tags | cli, options, root |
| Risk | medium |
| Stability | stable |
| Verification Method | test |
| GitHub Issue | - |
| Related Docs | - |

#### Requirement

SpecKiwi CLI는 모든 주요 명령에서 공통 옵션 `--root`, `--json`, `--no-color`, `--quiet`를 일관되게 지원해야 한다.

#### Rationale

일관된 옵션은 자동화와 agent 사용성을 높인다.

#### Acceptance Criteria

- [x] AC-1: `--root <path>`는 프로젝트 루트를 지정한다.
- [x] AC-2: `--json`은 stdout에 valid JSON만 출력한다.
- [x] AC-3: `--no-color`는 ANSI color를 비활성화한다.
- [x] AC-4: `--quiet`는 성공 메시지를 줄인다.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |
| VE-1 | test | test/release/srs-traceability.test.ts; npm test; npm run test:integration; npm run release:acceptance; npm run perf:srs | all | SpecKiwi v1.0.0 implementation verification on 2026-05-08 |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |

#### Research / Analysis

- -

#### Implementation Notes

- 쓰기 명령에서 `--json`은 변경 결과와 diagnostics를 JSON으로 출력한다.
- [2026-07-06] 2026-07-06 예외: mcp 서브커맨드는 --root 공통 옵션을 지원하지 않는다(IR-CLI-045로 대체). 그 외 명령의 --root 지원은 유지.

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-05-07 | 최초 작성 | SpecKiwi v1.0.0 제품 SRS baseline 정의 |



### IR-CLI-003 — init 명령

| Field | Value |
| --- | --- |
| Type | interface |
| Target | v1.0.0 |
| Status | verified |
| Priority | high |
| Tags | cli, init, bootstrap |
| Risk | medium |
| Stability | stable |
| Verification Method | test |
| GitHub Issue | - |
| Related Docs | - |

#### Requirement

SpecKiwi는 `speckiwi init` 명령으로 새 저장소에 SRS와 rule 기본 구조를 생성해야 한다.

#### Rationale

새 프로젝트 적용 시 사용자는 수동으로 모든 문서와 agent 규칙을 만들지 않아야 한다.

#### Acceptance Criteria

- [x] AC-1: `speckiwi init`은 project root의 `docs/`, `docs/spec/`, `docs/rule/` 구조를 확인하고 없으면 생성한다.
- [x] AC-2: `docs/spec/00.index.md`가 없으면 요구사항이 없는 기본 SRS index 문서를 생성한다.
- [x] AC-3: `docs/spec/90.appendix.md`가 없으면 기본 appendix 문서를 생성한다.
- [x] AC-4: `docs/rule/SRS-MD-Rules-v1.0.0.md`가 없으면 canonical SRS-MD rules 문서를 생성한다.
- [x] AC-5: `speckiwi init`은 대문자 `AGENTS.md`를 항상 생성 또는 갱신한다.
- [x] AC-6: `speckiwi init`은 대문자 `CLAUDE.md`를 항상 생성 또는 갱신한다.
- [x] AC-7: agent 파일에 추가되는 managed block은 `# SpecKiwi SRS 워크플로 v1.1` heading, `<!-- /SpecKiwi SRS 워크플로 -->` suffix marker, SRS source-of-truth 강제 문장, current work status workflow를 포함한다.
- [x] AC-8: 동일한 현재 버전 managed block이 이미 있으면 중복 삽입하지 않고, 다른 버전 또는 legacy unversioned block이 있으면 현재 버전 block으로 교체한다.
- [x] AC-9: 기존 파일이 있으면 기본적으로 덮어쓰지 않는다.
- [x] AC-10: `init --target`은 Target Map row를 `planned`로 만들 수 있지만 `Active Target` metadata value는 비워 둔다.
- [x] AC-11: init으로 생성된 index는 빈 Completed Work Log table을 포함한다.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |
| VE-1 | test | test/release/srs-traceability.test.ts; npm test; npm run test:integration; npm run release:acceptance; npm run perf:srs | all | SpecKiwi v1.0.0 implementation verification on 2026-05-08 |
| VE-2 | test | test/core/bootstrap/init-project.test.ts; test/cli/mutation-commands.test.ts | AC-7, AC-8, AC-10, AC-11 | v1.1 init template and agent instruction coverage on 2026-05-10 |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |
| Requirement | FR-NODE-009 | depends_on | CLI init은 공통 bootstrap service를 사용한다. |
| Requirement | FR-MCP-011 | related_to | MCP `init_project` tool과 같은 초기화 정책을 공유한다. |

#### Research / Analysis

- -

#### Implementation Notes

- 지원 옵션: `--force`, `--target <target>`, `--scope <scope:prefix>`
- `--agent-file` 입력은 제공하지 않는다. `init`은 항상 `AGENTS.md`와 `CLAUDE.md`를 모두 생성 또는 갱신한다.
- rules 문서의 canonical 위치는 `docs/rule/SRS-MD-Rules-v1.0.0.md`다.
- agent instruction의 현재 managed block version은 `v1.1`이다.
- `--target`은 active target 선택이 아니라 Target Map seed 입력이다.

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-05-07 | 최초 작성 | SpecKiwi v1.0.0 제품 SRS baseline 정의 |
| 2026-05-08 | init 생성 구조와 rules canonical path 갱신 | rules 문서를 `docs/rule`로 분리하고 agent 파일 중복 삽입 방지 요구 반영 |
| 2026-05-09 | `--agent-file` 입력 제거 및 항상 양쪽 agent 파일 갱신 | init 정책을 단순화하고 AGENTS.md/CLAUDE.md 누락을 방지 |
| 2026-05-09 | versioned agent instruction update 정책 추가 | SRS 강제 instruction 변경 시 중복 append 대신 deterministic replace 수행 |
| 2026-05-10 | empty Active Target init 및 v1.1 block 반영 | 새 workspace가 임의 target을 active로 선택하지 않도록 함 |



### IR-CLI-004 — validate 명령

| Field | Value |
| --- | --- |
| Type | interface |
| Target | v1.0.0 |
| Status | verified |
| Priority | critical |
| Tags | cli, validation, diagnostics |
| Risk | medium |
| Stability | stable |
| Verification Method | test |
| GitHub Issue | - |
| Related Docs | - |

#### Requirement

SpecKiwi는 `speckiwi validate` 명령으로 SRS 문서 전체를 검증해야 한다.

#### Rationale

검증 명령은 CI와 개발자 수동 확인의 기준이다.

#### Acceptance Criteria

- [x] AC-1: validation error가 없으면 exit code `0`을 반환한다.
- [x] AC-2: validation error가 있으면 exit code `1`을 반환한다.
- [x] AC-3: `--fail-on-warning`이 지정되고 warning이 있으면 exit code `1`을 반환한다.
- [x] AC-4: `--json`은 diagnostics array를 출력한다.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |
| VE-1 | test | test/release/srs-traceability.test.ts; npm test; npm run test:integration; npm run release:acceptance; npm run perf:srs | all | SpecKiwi v1.0.0 implementation verification on 2026-05-08 |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |

#### Research / Analysis

- -

#### Implementation Notes

- 지원 옵션: `--strict`, `--fail-on-warning`, `--scope <scope>`, `--target <target>`, `--json`

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-05-07 | 최초 작성 | SpecKiwi v1.0.0 제품 SRS baseline 정의 |



### IR-CLI-005 — extract 명령

| Field | Value |
| --- | --- |
| Type | interface |
| Target | v1.0.0 |
| Status | verified |
| Priority | critical |
| Tags | cli, json, extraction |
| Risk | medium |
| Stability | stable |
| Verification Method | test |
| GitHub Issue | - |
| Related Docs | - |

#### Requirement

SpecKiwi는 `speckiwi extract` 명령으로 requirement record를 JSON 또는 Markdown summary로 출력해야 한다.

#### Rationale

외부 스크립트와 MCP 테스트는 요구사항 목록을 기계적으로 읽을 수 있어야 한다.

#### Acceptance Criteria

- [x] AC-1: 기본 출력은 사람이 읽는 compact list다.
- [x] AC-2: `--json`은 normalized requirement record array를 출력한다.
- [x] AC-3: `--include-markdown`은 raw block을 포함한다.
- [x] AC-4: `--target`, `--status`, `--scope`, `--type`, `--tag` filter를 지원한다.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |
| VE-1 | test | test/release/srs-traceability.test.ts; npm test; npm run test:integration; npm run release:acceptance; npm run perf:srs | all | SpecKiwi v1.0.0 implementation verification on 2026-05-08 |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |

#### Research / Analysis

- -

#### Implementation Notes

- JSON 출력은 원본이 아닌 파생 산출물이다.

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-05-07 | 최초 작성 | SpecKiwi v1.0.0 제품 SRS baseline 정의 |



### IR-CLI-006 — list 명령

| Field | Value |
| --- | --- |
| Type | interface |
| Target | v1.0.0 |
| Status | verified |
| Priority | critical |
| Tags | cli, list, filter |
| Risk | medium |
| Stability | stable |
| Verification Method | test |
| GitHub Issue | - |
| Related Docs | - |

#### Requirement

SpecKiwi는 `speckiwi list` 명령으로 요구사항을 target, status, scope, type, tag 기준으로 조회해야 한다.

#### Rationale

개발자는 특정 목표 버전이나 상태의 작업 목록을 빠르게 확인해야 한다.

#### Acceptance Criteria

- [x] AC-1: `speckiwi list --target v1.0.0`은 해당 target 요구사항 목록을 출력한다.
- [x] AC-2: `speckiwi list --status in_progress`는 해당 상태 요구사항 목록을 출력한다.
- [x] AC-3: 복수 filter는 AND 조건으로 적용한다.
- [x] AC-4: `--format table|plain|json`을 지원한다.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |
| VE-1 | test | test/release/srs-traceability.test.ts; npm test; npm run test:integration; npm run release:acceptance; npm run perf:srs | all | SpecKiwi v1.0.0 implementation verification on 2026-05-08 |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |

#### Research / Analysis

- -

#### Implementation Notes

- 기본 정렬은 target, scope, ID 순이다.

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-05-07 | 최초 작성 | SpecKiwi v1.0.0 제품 SRS baseline 정의 |



### IR-CLI-007 — show 명령

| Field | Value |
| --- | --- |
| Type | interface |
| Target | v1.0.0 |
| Status | verified |
| Priority | critical |
| Tags | cli, show, requirement |
| Risk | medium |
| Stability | stable |
| Verification Method | test |
| GitHub Issue | - |
| Related Docs | - |

#### Requirement

SpecKiwi는 `speckiwi show <RequirementID>` 명령으로 단일 Requirement Block의 상세 정보를 출력해야 한다.

#### Rationale

구현자는 작업 전 단일 요구사항의 statement, AC, evidence를 빠르게 확인해야 한다.

#### Acceptance Criteria

- [x] AC-1: 존재하는 ID는 title, metadata, requirement, AC, evidence, trace links를 출력한다.
- [x] AC-2: 존재하지 않는 ID는 exit code `2`와 오류 메시지를 출력한다.
- [x] AC-3: `--json`은 normalized record를 출력한다.
- [x] AC-4: `--markdown`은 원문 block을 출력한다.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |
| VE-1 | test | test/release/srs-traceability.test.ts; npm test; npm run test:integration; npm run release:acceptance; npm run perf:srs | all | SpecKiwi v1.0.0 implementation verification on 2026-05-08 |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |

#### Research / Analysis

- -

#### Implementation Notes

- MCP `get_requirement` tool과 동일한 core 함수를 사용한다.

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-05-07 | 최초 작성 | SpecKiwi v1.0.0 제품 SRS baseline 정의 |



### IR-CLI-008 — targets 및 active-target 명령

| Field | Value |
| --- | --- |
| Type | interface |
| Target | v1.0.0 |
| Status | verified |
| Priority | medium |
| Tags | cli, targets, index |
| Risk | medium |
| Stability | stable |
| Verification Method | test |
| GitHub Issue | - |
| Related Docs | - |

#### Requirement

SpecKiwi는 `speckiwi targets` 명령으로 index의 Target Map과 target별 요구사항 수를 출력하고, active target을 조회/갱신하는 CLI 명령을 제공해야 한다.

#### Rationale

사용자는 어떤 버전 또는 milestone이 정의되어 있는지와 지금 진행해야 할 active target이 무엇인지 알아야 한다.

#### Acceptance Criteria

- [x] AC-1: Target Map의 target, type, status, description을 출력한다.
- [x] AC-2: 각 target의 requirement count를 포함한다.
- [x] AC-3: `--json`을 지원한다.
- [x] AC-4: `speckiwi active-target`은 `00.index.md`의 `Active Target` 값을 그대로 출력하며, 빈 값이면 fallback target을 만들지 않는다.
- [x] AC-5: `speckiwi set-active-target <target>`은 Target Map에 등록된 target만 active target으로 갱신한다.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |
| VE-1 | test | test/release/srs-traceability.test.ts; npm test; npm run test:integration; npm run release:acceptance; npm run perf:srs | all | SpecKiwi v1.0.0 implementation verification on 2026-05-08 |
| VE-2 | test | test/cli/read-commands.test.ts; test/cli/mutation-commands.test.ts | AC-4, AC-5 | Active Target empty read and mutation regression coverage on 2026-05-10 |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |

#### Research / Analysis

- -

#### Implementation Notes

- Target summary는 release readiness와 공유된다.
- active target read path는 index metadata의 `Active Target` row 값을 그대로 보고한다.
- active target mutation은 index metadata의 `Active Target` row와 Target Map의 `active` status를 함께 갱신한다.

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-05-07 | 최초 작성 | SpecKiwi v1.0.0 제품 SRS baseline 정의 |
| 2026-05-10 | active target CLI 추가 | 현재 작업 목표 버전을 CLI로 조회/갱신하기 위함 |
| 2026-05-10 | empty Active Target read 정책 반영 | target 미선택 상태를 CLI가 숨기지 않도록 함 |



### IR-CLI-009 — scopes 명령

| Field | Value |
| --- | --- |
| Type | interface |
| Target | v1.0.0 |
| Status | verified |
| Priority | medium |
| Tags | cli, scopes, index |
| Risk | medium |
| Stability | stable |
| Verification Method | test |
| GitHub Issue | - |
| Related Docs | - |

#### Requirement

SpecKiwi는 `speckiwi scopes` 명령으로 index의 Scope Map과 scope별 요구사항 수를 출력해야 한다.

#### Rationale

사용자는 기능 스코프별 문서와 요구사항 prefix를 빠르게 파악해야 한다.

#### Acceptance Criteria

- [x] AC-1: Scope Map의 scope, prefix, primary document를 출력한다.
- [x] AC-2: 각 scope의 requirement count를 포함한다.
- [x] AC-3: `--json`을 지원한다.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |
| VE-1 | test | test/release/srs-traceability.test.ts; npm test; npm run test:integration; npm run release:acceptance; npm run perf:srs | all | SpecKiwi v1.0.0 implementation verification on 2026-05-08 |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |

#### Research / Analysis

- -

#### Implementation Notes

- Scope Map 누락은 validation warning 또는 error와 연결된다.

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-05-07 | 최초 작성 | SpecKiwi v1.0.0 제품 SRS baseline 정의 |



### IR-CLI-010 — update-status 명령

| Field | Value |
| --- | --- |
| Type | interface |
| Target | v1.0.0 |
| Status | verified |
| Priority | critical |
| Tags | cli, status, mutation |
| Risk | high |
| Stability | stable |
| Verification Method | test |
| GitHub Issue | - |
| Related Docs | - |

#### Requirement

SpecKiwi는 `speckiwi update-status <RequirementID> <Status>` 명령으로 요구사항 status를 안전하게 갱신해야 한다.

#### Rationale

구현 완료와 검증 완료 상태를 문서에 최소 변경으로 반영해야 한다.

#### Acceptance Criteria

- [x] AC-1: 허용 status 외 값은 거부한다.
- [x] AC-2: 허용되지 않은 상태 전이는 거부하거나 warning을 출력한다.
- [x] AC-3: `verified` 전환 시 모든 AC 체크와 evidence 존재를 검증한다.
- [x] AC-4: 성공 시 `Status` row만 수정한다.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |
| VE-1 | test | test/release/srs-traceability.test.ts; npm test; npm run test:integration; npm run release:acceptance; npm run perf:srs | all | SpecKiwi v1.0.0 implementation verification on 2026-05-08 |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |

#### Research / Analysis

- -

#### Implementation Notes

- 지원 옵션: `--dry-run`, `--reason <text>`, `--json`

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-05-07 | 최초 작성 | SpecKiwi v1.0.0 제품 SRS baseline 정의 |



### IR-CLI-011 — check-ac 명령

| Field | Value |
| --- | --- |
| Type | interface |
| Target | v1.0.0 |
| Status | verified |
| Priority | critical |
| Tags | cli, acceptance-criteria, mutation |
| Risk | medium |
| Stability | stable |
| Verification Method | test |
| GitHub Issue | - |
| Related Docs | - |

#### Requirement

SpecKiwi는 `speckiwi check-ac <RequirementID> <AC-ID...>` 명령으로 지정 AC를 체크 상태로 변경해야 한다.

#### Rationale

검증이 완료된 수용 기준을 개별적으로 반영할 수 있어야 한다.

#### Acceptance Criteria

- [x] AC-1: 존재하는 AC ID만 변경한다.
- [x] AC-2: 이미 체크된 AC는 변경하지 않고 success로 처리한다.
- [x] AC-3: 지정되지 않은 AC는 변경하지 않는다.
- [x] AC-4: `--all`은 모든 AC를 체크한다.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |
| VE-1 | test | test/release/srs-traceability.test.ts; npm test; npm run test:integration; npm run release:acceptance; npm run perf:srs | all | SpecKiwi v1.0.0 implementation verification on 2026-05-08 |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |

#### Research / Analysis

- -

#### Implementation Notes

- 지원 옵션: `--all`, `--dry-run`, `--json`

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-05-07 | 최초 작성 | SpecKiwi v1.0.0 제품 SRS baseline 정의 |



### IR-CLI-012 — uncheck-ac 명령

| Field | Value |
| --- | --- |
| Type | interface |
| Target | v1.0.0 |
| Status | verified |
| Priority | medium |
| Tags | cli, acceptance-criteria, mutation |
| Risk | medium |
| Stability | stable |
| Verification Method | test |
| GitHub Issue | - |
| Related Docs | - |

#### Requirement

SpecKiwi는 `speckiwi uncheck-ac <RequirementID> <AC-ID...>` 명령으로 지정 AC를 미체크 상태로 변경해야 한다.

#### Rationale

검증 결과가 취소되거나 요구사항이 변경되면 AC 상태를 되돌릴 수 있어야 한다.

#### Acceptance Criteria

- [x] AC-1: 존재하는 AC ID만 변경한다.
- [x] AC-2: 미체크 처리 후 status가 `verified`이면 `implemented` 또는 `in_progress`로 낮추도록 경고한다.
- [x] AC-3: `--all`은 모든 AC를 미체크한다.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |
| VE-1 | test | test/release/srs-traceability.test.ts; npm test; npm run test:integration; npm run release:acceptance; npm run perf:srs | all | SpecKiwi v1.0.0 implementation verification on 2026-05-08 |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |

#### Research / Analysis

- -

#### Implementation Notes

- status downgrade는 자동 적용하지 않고 warning을 우선한다.

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-05-07 | 최초 작성 | SpecKiwi v1.0.0 제품 SRS baseline 정의 |



### IR-CLI-013 — add-evidence 명령

| Field | Value |
| --- | --- |
| Type | interface |
| Target | v1.0.0 |
| Status | verified |
| Priority | critical |
| Tags | cli, evidence, mutation |
| Risk | medium |
| Stability | stable |
| Verification Method | test |
| GitHub Issue | - |
| Related Docs | - |

#### Requirement

SpecKiwi는 `speckiwi add-evidence <RequirementID>` 명령으로 Verification Evidence row를 추가해야 한다.

#### Rationale

구현 및 검증 증거를 Requirement Block 안에 일관된 table 형식으로 남겨야 한다.

#### Acceptance Criteria

- [x] AC-1: `--type`, `--ref`, `--covers`는 필수다.
- [x] AC-2: `--notes`는 선택이다.
- [x] AC-3: Evidence ID는 기존 row를 기준으로 다음 `VE-N`으로 자동 생성한다.
- [x] AC-4: row는 Verification Evidence table 끝에 추가된다.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |
| VE-1 | test | test/release/srs-traceability.test.ts; npm test; npm run test:integration; npm run release:acceptance; npm run perf:srs | all | SpecKiwi v1.0.0 implementation verification on 2026-05-08 |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |

#### Research / Analysis

- -

#### Implementation Notes

- 지원 옵션: `--type <type>`, `--ref <reference>`, `--covers <AC-ID|all>`, `--notes <text>`, `--dry-run`, `--json`

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-05-07 | 최초 작성 | SpecKiwi v1.0.0 제품 SRS baseline 정의 |



### IR-CLI-014 — add-trace 명령

| Field | Value |
| --- | --- |
| Type | interface |
| Target | v1.0.0 |
| Status | verified |
| Priority | medium |
| Tags | cli, trace-links, mutation |
| Risk | medium |
| Stability | stable |
| Verification Method | test |
| GitHub Issue | - |
| Related Docs | - |

#### Requirement

SpecKiwi는 `speckiwi add-trace <RequirementID>` 명령으로 Trace Links row를 추가해야 한다.

#### Rationale

요구사항 간 의존성, issue, PR, docs 관계를 명시적으로 남길 수 있어야 한다.

#### Acceptance Criteria

- [x] AC-1: `--type`, `--ref`, `--relation`은 필수다.
- [x] AC-2: Requirement reference는 존재 여부를 검증한다.
- [x] AC-3: row는 Trace Links table 끝에 추가된다.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |
| VE-1 | test | test/release/srs-traceability.test.ts; npm test; npm run test:integration; npm run release:acceptance; npm run perf:srs | all | SpecKiwi v1.0.0 implementation verification on 2026-05-08 |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |

#### Research / Analysis

- -

#### Implementation Notes

- 지원 옵션: `--type <type>`, `--ref <reference>`, `--relation <relation>`, `--notes <text>`, `--dry-run`, `--json`

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-05-07 | 최초 작성 | SpecKiwi v1.0.0 제품 SRS baseline 정의 |



### IR-CLI-015 — summary 명령

| Field | Value |
| --- | --- |
| Type | interface |
| Target | v1.0.0 |
| Status | verified |
| Priority | high |
| Tags | cli, summary, target |
| Risk | medium |
| Stability | stable |
| Verification Method | test |
| GitHub Issue | - |
| Related Docs | - |

#### Requirement

SpecKiwi는 `speckiwi summary --target <Target>` 명령으로 target별 진행 요약을 출력해야 한다.

#### Rationale

release 후보 판단과 구현 진행률 확인에 target summary가 필요하다.

#### Acceptance Criteria

- [x] AC-1: status별 count를 출력한다.
- [x] AC-2: type별 count를 출력한다.
- [x] AC-3: blocked 목록을 출력한다.
- [x] AC-4: implemented but not verified 목록을 출력한다.
- [x] AC-5: missing evidence 목록을 출력한다.
- [x] AC-6: `--json`과 `--markdown`을 지원한다.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |
| VE-1 | test | test/release/srs-traceability.test.ts; npm test; npm run test:integration; npm run release:acceptance; npm run perf:srs | all | SpecKiwi v1.0.0 implementation verification on 2026-05-08 |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |

#### Research / Analysis

- -

#### Implementation Notes

- 기본 target이 지정되지 않으면 active target을 사용한다.

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-05-07 | 최초 작성 | SpecKiwi v1.0.0 제품 SRS baseline 정의 |



### IR-CLI-016 — links check 명령

| Field | Value |
| --- | --- |
| Type | interface |
| Target | v1.0.0 |
| Status | verified |
| Priority | medium |
| Tags | cli, links, validation |
| Risk | medium |
| Stability | stable |
| Verification Method | test |
| GitHub Issue | - |
| Related Docs | - |

#### Requirement

SpecKiwi는 `speckiwi links check` 명령으로 SRS 문서의 local links와 Requirement references를 검증해야 한다.

#### Rationale

관련 문서와 요구사항 링크가 깨지면 추적성이 약해진다.

#### Acceptance Criteria

- [x] AC-1: local Markdown link 대상 파일 존재 여부를 확인한다.
- [x] AC-2: Trace Links의 Requirement reference 존재 여부를 확인한다.
- [x] AC-3: GitHub URL은 형식만 확인하고 네트워크 요청은 기본적으로 수행하지 않는다.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |
| VE-1 | test | test/release/srs-traceability.test.ts; npm test; npm run test:integration; npm run release:acceptance; npm run perf:srs | all | SpecKiwi v1.0.0 implementation verification on 2026-05-08 |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |

#### Research / Analysis

- -

#### Implementation Notes

- 지원 옵션: `--json`, `--include-external`

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-05-07 | 최초 작성 | SpecKiwi v1.0.0 제품 SRS baseline 정의 |



### ~~IR-CLI-017 — mcp 명령~~ [DISCARDED → see IR-CLI-045]

| Field | Value |
| --- | --- |
| Type | interface |
| Target | v1.0.0 |
| Status | discarded |
| Priority | critical |
| Tags | cli, mcp, stdio |
| Risk | high |
| Stability | stable |
| Verification Method | test, inspection |
| GitHub Issue | - |
| Related Docs | - |

#### Requirement

SpecKiwi는 `speckiwi mcp` 명령으로 stdio MCP 서버를 시작해야 한다.

#### Rationale

AI coding agent는 CLI 명령을 직접 실행하지 않고 MCP tool을 통해 SRS를 조회하고 갱신할 수 있어야 한다.

#### Acceptance Criteria

- [x] AC-1: `speckiwi mcp`는 기본 transport로 stdio를 사용한다.
- [x] AC-2: `speckiwi mcp --transport stdio`는 명시적으로 stdio transport를 사용한다.
- [x] AC-3: MCP mode에서 stdout에는 MCP JSON-RPC 메시지만 출력한다.
- [x] AC-4: 로그는 stderr에 출력한다.
- [x] AC-5: `--root` 없이 `speckiwi mcp`를 실행하면 현재 작업 디렉터리를 기준으로 project root를 탐색한다.
- [x] AC-6: MCP server startup 시 `docs/spec/00.index.md`가 없으면 tool 호출 처리 전에 `init`과 같은 SRS 기본 구조를 자동 생성한다.
- [x] AC-7: `speckiwi mcp --root <path>`의 `<path>`가 존재하지 않는 디렉터리이면 새 디렉터리를 만들지 않고 오류로 종료한다.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |
| VE-1 | test | test/release/srs-traceability.test.ts; npm test; npm run test:integration; npm run release:acceptance; npm run perf:srs | all | SpecKiwi v1.0.0 implementation verification on 2026-05-08 |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |

#### Research / Analysis

- -

#### Implementation Notes

- MCP 상세 요구사항은 MCP scope 문서에서 정의한다.
- MCP client 등록에는 `--root` 인자가 필수가 아니다. 생략 시 서버 process의 current working directory에서 `.git` 또는 `docs/spec/00.index.md`를 상위 탐색한다.
- project root를 찾지 못한 완전 빈 작업 디렉터리에서는 현재 작업 디렉터리를 root로 삼아 자동 init한다.
- 명시적 `--root` 오타는 신규 workspace 신호로 보지 않는다. 명시 root는 존재하는 directory여야 한다.

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-05-07 | 최초 작성 | SpecKiwi v1.0.0 제품 SRS baseline 정의 |
| 2026-05-09 | root 없는 MCP 등록 동작 명시 | MCP client 등록 설정을 단순화하고 cwd 기반 실행을 지원 |
| 2026-05-09 | MCP startup 자동 init 요구 추가 | `docs/spec/00.index.md` 누락 상태에서도 첫 MCP 호출이 실패하지 않도록 함 |
| 2026-07-06 | Status -> discarded | 사용자 지시(2026-07-06)로 mcp 명령의 --root 지원을 제거. IR-CLI-045가 stdio AC를 계승하고 root 옵션 거부로 대체 |



### IR-CLI-018 — exit code 정책

| Field | Value |
| --- | --- |
| Type | interface |
| Target | v1.0.0 |
| Status | verified |
| Priority | high |
| Tags | cli, exit-code, automation |
| Risk | medium |
| Stability | stable |
| Verification Method | test |
| GitHub Issue | - |
| Related Docs | - |

#### Requirement

SpecKiwi CLI는 자동화를 위해 명확한 exit code 정책을 제공해야 한다.

#### Rationale

CI와 agent는 exit code를 기준으로 후속 작업을 결정한다.

#### Acceptance Criteria

- [x] AC-1: 성공은 `0`이다.
- [x] AC-2: validation error는 `1`이다.
- [x] AC-3: 사용법 또는 인자 오류는 `2`다.
- [x] AC-4: 파일 I/O 오류는 `3`이다.
- [x] AC-5: parse fatal error는 `4`다.
- [x] AC-6: 상태 전이 또는 verified guard 실패는 `5`다.
- [x] AC-7: MCP server fatal error는 `6`이다.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |
| VE-1 | test | test/release/srs-traceability.test.ts; npm test; npm run test:integration; npm run release:acceptance; npm run perf:srs | all | SpecKiwi v1.0.0 implementation verification on 2026-05-08 |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |

#### Research / Analysis

- -

#### Implementation Notes

- stderr 메시지는 code와 원인을 포함한다.

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-05-07 | 최초 작성 | SpecKiwi v1.0.0 제품 SRS baseline 정의 |



### OPS-CLI-001 — 명령 도움말과 예제 제공

| Field | Value |
| --- | --- |
| Type | operational |
| Target | v1.0.0 |
| Status | verified |
| Priority | medium |
| Tags | cli, help, docs |
| Risk | medium |
| Stability | stable |
| Verification Method | inspection |
| GitHub Issue | - |
| Related Docs | - |

#### Requirement

SpecKiwi CLI는 각 명령의 도움말과 대표 예제를 제공해야 한다.

#### Rationale

새 사용자와 AI agent가 명령을 오용하지 않으려면 interface 자체가 명확해야 한다.

#### Acceptance Criteria

- [x] AC-1: `speckiwi <command> --help`는 필수 인자, 옵션, 예제를 출력한다.
- [x] AC-2: README에 주요 명령 예제가 포함된다.
- [x] AC-3: AGENTS.md에는 agent가 사용할 명령 우선순위가 포함된다.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |
| VE-1 | test | test/release/srs-traceability.test.ts; npm test; npm run test:integration; npm run release:acceptance; npm run perf:srs | all | SpecKiwi v1.0.0 implementation verification on 2026-05-08 |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |

#### Research / Analysis

- -

#### Implementation Notes

- CLI framework는 help output을 일관되게 생성해야 한다.

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-05-07 | 최초 작성 | SpecKiwi v1.0.0 제품 SRS baseline 정의 |



### IR-CLI-019 — add-requirement 명령

| Field | Value |
| --- | --- |
| Type | interface |
| Target | v1.0.0 |
| Status | verified |
| Priority | high |
| Tags | cli, requirement, mutation |
| Risk | high |
| Stability | stable |
| Verification Method | test |
| GitHub Issue | - |
| Related Docs | - |

#### Requirement

SpecKiwi는 `speckiwi add-requirement` 명령으로 scope SRS 문서의 Requirements section 끝에 새 Requirement Block을 생성해야 한다.

#### Rationale

사용자와 AI agent는 SRS-MD 구조를 수동으로 복사하지 않고도 검증 가능한 요구사항을 일관된 형식으로 추가할 수 있어야 한다.

#### Acceptance Criteria

- [x] AC-1: 명령은 `--type`, `--scope`, `--target`, `--title`, `--requirement`와 최소 1개의 acceptance criteria 입력을 필수로 요구한다.
- [x] AC-2: 명령은 `--status`, `--priority`, `--tags`, `--risk`, `--stability`, `--verification-method`, `--rationale`, `--implementation-notes`, `--related-docs`, `--github-issue`, evidence, trace link, research, change note 입력을 선택적으로 받는다.
- [x] AC-3: Requirement ID는 type prefix와 scope prefix를 기준으로 자동 생성하며, 기존 ID와 discarded ID를 재사용하지 않는다.
- [x] AC-4: 새 Requirement Block은 해당 scope 문서의 `## 4. Requirements` section 끝에 추가된다.
- [x] AC-5: `--status verified` 입력은 모든 AC가 checked이고 Verification Evidence가 1개 이상이며 evidence reference가 비어 있지 않을 때만 허용된다.
- [x] AC-6: 실패 시 문서를 변경하지 않고 exit code `5` 또는 사용법 오류에 맞는 exit code를 반환한다.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |
| VE-1 | test | test/release/srs-traceability.test.ts; npm test; npm run test:integration; npm run release:acceptance; npm run perf:srs | all | SpecKiwi v1.0.0 implementation verification on 2026-05-08 |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |
| Requirement | FR-NODE-008 | depends_on | CLI 명령은 공통 Requirement Block 생성 mutation service를 사용한다. |
| Requirement | FR-MCP-010 | related_to | MCP `add_requirement` tool과 같은 core mutation을 공유한다. |

#### Research / Analysis

- -

#### Implementation Notes

- acceptance criteria 입력은 반복 가능한 option 또는 structured option으로 받을 수 있다.
- `--scope`는 Scope Map의 `Prefix` 값을 우선 사용한다.
- 기본 `Status`는 입력이 없으면 `planned`다.
- `--dry-run`은 patch summary를 출력하고 파일을 변경하지 않아야 한다.

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-05-08 | 최초 작성 | Requirement Block 추가 자동화 요구사항 반영 |



### IR-CLI-020 — completed-work와 add-completed-work 명령

| Field | Value |
| --- | --- |
| Type | interface |
| Target | v1.1.0 |
| Status | verified |
| Priority | high |
| Tags | cli, completed-work, index |
| Risk | medium |
| Stability | stable |
| Verification Method | test |
| GitHub Issue | - |
| Related Docs | - |

#### Requirement

SpecKiwi는 `speckiwi completed-work`로 index-level Completed Work Log를 조회하고, `speckiwi add-completed-work`로 완료 작업 요약 row를 추가해야 한다.

#### Rationale

사람과 coding agent는 현재 진행 중인 요구사항 상태와 최근 완료 작업 요약을 같은 SRS index에서 확인할 수 있어야 한다.

#### Acceptance Criteria

- [x] AC-1: `speckiwi completed-work`는 `--target`, `--scope`, `--since`, `--limit`, `--json` option을 지원한다.
- [x] AC-2: target filter는 같은 target row와 Target cell이 비어 있는 cross-target row를 함께 반환한다.
- [x] AC-3: `--since`는 `YYYY-MM-DD` 형식만 허용하고 `--limit`은 양의 정수만 허용한다.
- [x] AC-4: `speckiwi add-completed-work`는 `--date`와 `--summary`를 필수로 요구하고, `--target`, `--scope`, `--requirements`, `--dry-run`, `--json`을 지원한다.
- [x] AC-5: `add-completed-work --dry-run`은 patch preview를 반환하고 파일을 변경하지 않는다.
- [x] AC-6: pipe 문자를 포함한 Completed Work Log cell 입력은 파일 변경 없이 mutation failure를 반환한다.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |
| VE-1 | test | test/cli/read-commands.test.ts; test/cli/mutation-commands.test.ts | all | CLI Completed Work Log read and mutation coverage on 2026-05-10 |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |
| Requirement | FR-NODE-010 | depends_on | CLI 명령은 공통 Completed Work Log core service를 사용한다. |
| Requirement | FR-MCP-012 | related_to | MCP tool과 같은 core query/mutation을 공유한다. |

#### Research / Analysis

- Completed Work Log는 완료 상태의 source of truth가 아니므로 CLI는 summary row를 조회/추가하는 역할만 맡는다.

#### Implementation Notes

- `--requirements`는 comma-separated ID 목록이며 빈 값은 empty array로 정규화한다.
- human output은 기존 JSON fallback formatter를 사용할 수 있지만 `--json` 출력은 machine-readable 객체여야 한다.

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-05-10 | 최초 작성 | Completed Work Log CLI 조회/추가 요구사항 반영 |



### IR-CLI-021 — CLI read diagnostics and summary parity

| Field | Value |
| --- | --- |
| Type | interface |
| Target | v1.2.0 |
| Status | verified |
| Priority | medium |
| Tags | cli, read, diagnostics, summary |
| Risk | medium |
| Stability | evolving |
| Verification Method | test |
| GitHub Issue | - |
| Related Docs | [docs/research/06.hardening-governance-research.md](../research/06.hardening-governance-research.md) |

#### Requirement

SpecKiwi CLI read commands must expose diagnostics summaries and expanded target summaries consistently across `validate`, `targets`, `summary`, `active-target`, and related JSON outputs.

#### Rationale

Users and agents should not need separate validation calls to know whether a read payload was produced from a clean workspace, and CLI summary shape should match MCP where the same core data is exposed.

#### Acceptance Criteria

- [x] AC-1: JSON output for read-oriented commands includes a standard diagnostics summary when parse or validation diagnostics are available.
- [x] AC-2: `speckiwi targets --json` includes target metadata and requirement counts using the shared target summary contract.
- [x] AC-3: `speckiwi summary --json` includes status counts, type counts, blocked requirements, implemented-not-verified requirements, missing evidence, and diagnostics summary.
- [x] AC-4: `speckiwi active-target --json` returns the active target value plus the expanded summary for that target when available.
- [x] AC-5: CLI JSON field names are documented and contract-tested against MCP read payloads where behavior overlaps.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |
| VE-1 | command | npm run build; npm run typecheck; npm run lint; npm test; npm run test:integration; npm run release:acceptance; npm run perf:srs | all | P9 bash verification passed on 2026-05-10 |
| VE-2 | command | npm run release:check | all | P9 release readiness passed on 2026-05-10 |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |
| Requirement | IR-CLI-008 | depends_on | Target and active-target read payloads are expanded. |
| Requirement | IR-CLI-015 | depends_on | Summary output becomes the shared read contract. |
| Requirement | FR-MCP-013 | related_to | MCP read parity follows the same core shape. |

#### Research / Analysis

- [SpecKiwi Hardening and Governance Research](../research/06.hardening-governance-research.md) sections 6.3 and 7.2.

#### Implementation Notes

- This is a v1.2.0 planned hardening requirement and does not mark the current CLI payloads as already compliant.

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-05-10 | 최초 작성 | v1.2.0 CLI read parity hardening target 반영 |



### IR-CLI-022 — Completed Work Log latest and prevalidation

| Field | Value |
| --- | --- |
| Type | interface |
| Target | v1.2.0 |
| Status | verified |
| Priority | high |
| Tags | cli, completed-work, validation |
| Risk | medium |
| Stability | evolving |
| Verification Method | test |
| GitHub Issue | - |
| Related Docs | [docs/research/06.hardening-governance-research.md](../research/06.hardening-governance-research.md) |

#### Requirement

SpecKiwi CLI must define latest ordering for Completed Work Log reads and prevalidate new Completed Work Log rows before writing them.

#### Rationale

Completed Work Log is useful as an index-level summary only if reads have predictable recency semantics and new rows do not silently point to unknown or incomplete work.

#### Acceptance Criteria

- [x] AC-1: `speckiwi completed-work --limit <n>` returns the latest matching rows by date descending and source line descending unless an explicit alternate order is documented.
- [x] AC-2: `speckiwi add-completed-work` prevalidates target, scope, and requirement references before write mode mutates the index.
- [x] AC-3: New completed-work rows referencing requirements that are not `implemented` or `verified` are rejected by default.
- [x] AC-4: Intentional historical or incomplete rows require an explicit documented override.
- [x] AC-5: Historical inconsistencies may remain validator warnings and are not automatically rewritten by the CLI.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |
| VE-1 | command | npm run build; npm run typecheck; npm run lint; npm test; npm run test:integration; npm run release:acceptance; npm run perf:srs | all | P9 bash verification passed on 2026-05-10 |
| VE-2 | command | npm run release:check | all | P9 release readiness passed on 2026-05-10 |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |
| Requirement | IR-CLI-020 | depends_on | Existing completed-work CLI behavior is hardened. |
| Requirement | FR-NODE-010 | depends_on | CLI delegates query and mutation policy to core service. |
| Requirement | FR-MCP-013 | related_to | MCP completed-work behavior must remain aligned. |

#### Research / Analysis

- [SpecKiwi Hardening and Governance Research](../research/06.hardening-governance-research.md) sections 6.2 and 7.2.

#### Implementation Notes

- Completed Work Log remains a summary; Requirement Block status and evidence remain the completion source of truth.

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-05-10 | 최초 작성 | v1.2.0 completed-work CLI hardening target 반영 |


### IR-CLI-023 — target registration and Active Target defaults

| Field | Value |
| --- | --- |
| Type | interface |
| Target | v2.1.0 |
| Status | implemented |
| Priority | high |
| Tags | cli, target, active-target, mutation |
| Risk | medium |
| Stability | evolving |
| Verification Method | test, inspection |
| GitHub Issue | - |
| Related Docs | [docs/spec/90.appendix.md](./90.appendix.md) |

#### Requirement

SpecKiwi CLI must let users register a new target and make it the Active Target in one safe command, and target-scoped mutation commands must be able to default to the Active Target when an explicit target is omitted.

#### Rationale

사용자가 "다음 목표를 2.1.0으로 설정해줘"라고 요청하면 agent가 `00.index.md`를 직접 편집하지 않고도 Target Map과 Active Target을 일관되게 갱신해야 한다. 이후 새 SRS 요구사항 작성은 같은 target을 자동으로 사용해야 한다.

#### Acceptance Criteria

- [x] AC-1: `speckiwi set-active-target <target> --create`는 Target Map에 없는 target을 새 row로 등록하고 `Active Target` metadata row를 같은 값으로 갱신한다.
- [x] AC-2: `--create` 없이 미등록 target을 지정하면 기존처럼 실패하며 문서를 변경하지 않는다.
- [x] AC-3: target 생성 시 type과 description을 명시할 수 있고, 생략 시 deterministic default를 사용한다.
- [x] AC-4: 새 active target 지정은 기존 `active` Target Map row를 non-active 상태로 낮추고 active row를 최대 하나로 유지한다.
- [x] AC-5: `speckiwi add-requirement`는 `--target`을 선택 option으로 허용하고, 생략 시 `00.index.md`의 Active Target을 사용한다.
- [x] AC-6: `--target` 생략 상태에서 Active Target이 비어 있으면 명령은 실패하고 target을 먼저 설정하라는 structured error를 반환한다.
- [x] AC-7: JSON 출력은 target이 explicit 입력인지 Active Target default인지 구분할 수 있는 값을 포함한다.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |
| VE-1 | test | test/cli/mutation-commands.test.ts; test/release/tool-signature-parity.test.ts | all | CLI target creation, Active Target defaulting, empty-target failure, and targetSource output. |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |
| Requirement | FR-NODE-014 | depends_on | CLI는 Target Map mutation과 Active Target default resolution을 core service에 위임한다. |
| Requirement | FR-MCP-015 | related_to | MCP도 같은 target registration/defaulting semantics를 제공해야 한다. |
| Requirement | FR-FLOW-011 | supports | Agent workflow가 CLI fallback으로 이 명령을 사용한다. |

#### Research / Analysis

- Current implementation rejects unknown targets in `set-active-target` and requires explicit `--target` for `add-requirement`.

#### Implementation Notes

- `--create`는 오타로 인한 target row 생성을 막기 위한 명시적 opt-in이다.
- `add-requirement`의 explicit `--target`은 Active Target보다 우선한다.
- 기존 scripts나 agent가 target을 명시적으로 주는 방식은 계속 동작해야 한다.

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-05-11 | 최초 작성 | 새 목표 버전 설정 후 작성되는 SRS가 Active Target을 기본 target으로 사용하도록 하기 위함 |
| 2026-06-29 | Status -> implemented | v2.3.0 tool lifecycle, doctor, and signature parity implementation evidence attached |



### IR-CLI-024 — add-completed-work report path option

| Field | Value |
| --- | --- |
| Type | interface |
| Target | v2.1.1 |
| Status | verified |
| Priority | high |
| Tags | cli, completed-work, report-paths |
| Risk | medium |
| Stability | evolving |
| Verification Method | test |
| GitHub Issue | - |
| Related Docs | [README.md](../../README.md), [docs/spec/90.appendix.md](./90.appendix.md) |

#### Requirement

SpecKiwi CLI must expose an optional repeatable report path option for add-completed-work and return the persisted report path list in completed-work outputs.

#### Rationale

Users need to attach one or more completion report document paths at the same time they append a completed work summary.

#### Acceptance Criteria

- [x] AC-1: speckiwi add-completed-work accepts repeatable --report <path> options without requiring the option for existing workflows.
- [x] AC-2: CLI rejects blank --report values and values that violate the FR-PARSE-016 report path grammar before invoking the shared mutation.
- [x] AC-3: completed-work --json and add-completed-work --json include reportPaths as a string array for each affected completed work entry or mutation result.
- [x] AC-4: CLI forwards all provided report paths to the shared Completed Work Log core mutation in both write and dry-run modes while preserving caller order and duplicates.
- [x] AC-5: Human-readable output remains backward compatible when no report paths are present and does not print empty reportPaths noise.
- [x] AC-6: Human-readable output shows non-empty report path lists in a stable, readable form.
- [x] AC-7: CLI help, README, and appendix command reference describe repeatable --report usage, comma-separated storage, and forbidden characters.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |
| VE-1 | test | test/cli/mutation-commands.test.ts; test/cli/read-commands.test.ts | all | - |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |
| Requirement | IR-CLI-020 | depends_on | Extends existing completed-work CLI mutation. |
| Requirement | IR-CLI-022 | depends_on | Preserves completed-work prevalidation and latest read semantics. |
| Requirement | FR-NODE-015 | depends_on | Delegates persistence to shared core support. |

#### Research / Analysis

- 2026-05-13 feasibility review found repeatable `--report <path>` is the lowest-risk CLI shape because shell quoting is simpler than a comma-separated user option.
- The review also found non-JSON output compatibility requires suppressing empty reportPaths fields.

#### Implementation Notes

- Prefer repeatable --report over a comma-separated --reports option because shell quoting is simpler and existing CLI patterns already use repeatable options.
- Do not add a separate --reports option in v2.1.1.

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-05-13 | Refined CLI validation and output compatibility | feasibility review found delimiter handling and human output behavior were underspecified |
| 2026-05-13 | Created | add-requirement |

### REL-CLI-001 — CLI --version package binding 회귀 방지

| Field | Value |
| --- | --- |
| Type | reliability |
| Target | v2.1.2 |
| Status | verified |
| Priority | high |
| Tags | cli, version, regression-guard, package-binding |
| Risk | medium |
| Stability | stable |
| Verification Method | test |
| GitHub Issue | - |
| Related Docs | - |

#### Requirement

SpecKiwi CLI `--version` 출력은 항상 런타임에 `package.json`의 `version` 필드를 동적으로 binding 하여 결정해야 한다.

#### Rationale

CLI 진입 모듈에 버전 문자열을 하드코딩하면 publish 된 npm 패키지의 `package.json.version`과 출력이 분리되어 사용자가 잘못된 버전을 보고받는 회귀가 발생한다 (2.1.1 → 1.0.0 출력 사례).

#### Acceptance Criteria

- [x] AC-1: `speckiwi --version` 실행 시 stdout 첫 줄은 `package.json`의 `version` 값과 정확히 일치한다.
- [x] AC-2: CLI 소스 (`src/cli/command.ts` 및 동등 모듈) 에는 버전 문자열 리터럴이 하드코딩되지 않는다.
- [x] AC-3: 빌드/번들 산출물 (`dist/cli/command.js`) 도 동일 보장을 유지하며, `package.json.version` 변경 후 재빌드 시 출력이 자동 반영된다.
- [x] AC-4: 회귀 방지 자동 테스트가 `package.json`의 `version`을 동적으로 읽어 비교한다.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |
| VE-1 | test | test/cli/common-options-exit.test.ts | AC-1, AC-4 | 2026-05-14 --version 출력이 package.json.version 과 일치하는지 동적 검증 (commit b74631c) |
| VE-2 | inspection | src/cli/command.ts | AC-2, AC-3 | createRequire(import.meta.url) 기반 동적 binding 적용 확인 |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |
| Requirement | IR-CLI-001 | refines | IR-CLI-001 AC-2 의 회귀 방지 보강 |

#### Research / Analysis

- -

#### Implementation Notes

- `src/cli/command.ts` 에서 `createRequire(import.meta.url)` 로 `../../package.json` 의 `version` 필드를 import 시점에 한 번 로드하여 `command.version(PACKAGE_VERSION)` 에 전달한다. ESM 환경에서 JSON import assertion 비호환을 피하기 위해 `createRequire` 를 사용한다.

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-05-15 | 최초 작성 | v2.1.2 patch 의 `--version` 회귀 방지 책임을 명시적 REQ 로 등록 |

### IR-CLI-025 — 서브커맨드 --help 에 공통 옵션 가시성 노출

| Field | Value |
| --- | --- |
| Type | interface |
| Target | v2.1.3 |
| Status | verified |
| Priority | medium |
| Tags | cli, help, discoverability, root, common-options |
| Risk | low |
| Stability | stable |
| Verification Method | test |
| GitHub Issue | - |
| Related Docs | - |

#### Requirement

SpecKiwi CLI 의 모든 액션 가능한 서브커맨드는 `--help` 출력 시 부모로부터 상속되는 공통 옵션 (`--root`, `--json`, `--no-color`, `--quiet`) 을 사용자에게 명시적으로 보여주어야 한다.

#### Rationale

현재 `--root` 등 공통 옵션은 commander 부모 명령 레벨에만 정의되어 `speckiwi --help` 에서만 표시되고, `speckiwi mcp --help`, `speckiwi init --help`, `speckiwi list --help` 등 서브커맨드 help 에는 나타나지 않는다. parser 는 자식 위치 옵션도 자동 위임으로 받아들이지만, 사용자가 help 만 보고는 `--root` 가 해당 서브커맨드에서도 사용 가능한지 알 수 없다. IR-CLI-002 의 "모든 주요 명령에서 일관되게 지원" 요구사항을 실제 help 출력 가시성까지 일치시키기 위함이다.

#### Acceptance Criteria

- [x] AC-1: SpecKiwi CLI 의 모든 액션 서브커맨드 의 `--help` 출력은 `--root <path>` 옵션과 설명을 포함한다.
- [x] AC-2: 공통 옵션을 표시하는 섹션은 구분 가능한 표제 (예: `Global options:`) 아래에 나열되어 자식 명령 옵션과 구분된다.
- [x] AC-3: 옵션 표시 추가가 commander parser 동작을 변경하지 않으며 기존 IR-CLI-002 의 처리 동작을 그대로 유지한다.
- [x] AC-4: 자동 테스트가 주요 서브커맨드 (`validate`, `extract`, `list`, `show`, `targets`, `active-target`, `completed-work`, `scopes`, `summary`, `mcp`, `init`, `update-status`, `set-active-target`, `add-completed-work`) 의 help 출력이 `--root <path>` 와 `--json` 을 모두 포함하는지 검증한다.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |
| VE-1 | test | test/cli/global-options-help.test.ts | AC-1, AC-2, AC-3, AC-4 | - |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |
| Requirement | IR-CLI-002 | refines | IR-CLI-002 공통 옵션 일관성 요구를 help 입출력 가시성까지 확장 |

#### Research / Analysis

- -

#### Implementation Notes

- commander v14 에는 `showGlobalOptions` 메서드가 없으므로, `buildCommand` 마지막 단계에서 등록된 모든 자식 명령을 재귀 순회하며 `addHelpText("after", ...)` 로 inherited options 섹션을 부착한다. 옵션 정의 자체는 부모 명령에만 두고 자식에 명시 등록하지 않아 parser 동작 변경 없이 가시성만 보강한다.
- [2026-07-06] 2026-07-06 예외: AC-4의 서브커맨드 목록 중 mcp는 --root help 노출 대상에서 제외된다(IR-CLI-045 AC-9로 대체). AC 텍스트 정밀 개정은 granular edit 도구(v2.3.0 릴리스) 가용 시 수행.

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-05-15 | 최초 작성 | 사용자가 서브커맨드 --help 에서 --root 등 공통 옵션을 발견할 수 없는 가시성 결함을 v2.1.3 patch target 으로 분리 |

### IR-CLI-026 — update-stability CLI 명령

| Field | Value |
| --- | --- |
| Type | interface |
| Target | v2.2.1 |
| Status | verified |
| Priority | high |
| Tags | cli, command, stability, mutation, feasibility:high |
| Risk | low |
| Stability | stable |
| Verification Method | test |
| GitHub Issue | - |
| Related Docs | - |

#### Requirement

SpecKiwi CLI 는 FR-PARSE-017 의 stability lifecycle mutation core 를 호출하는 `speckiwi update-stability <id> <stability>` 서브커맨드를 제공해야 한다.

#### Rationale

사용자와 ad-hoc 스크립트가 MCP server 없이도 stability 전이를 수행할 수 있어야 한다. IR-CLI-010 (`update-status`) 와 동일 명령 인터페이스 패턴을 stability 차원에 동형 노출.

#### Acceptance Criteria

- [x] AC-1: `speckiwi update-stability <id> <stability>` 형태로 REQ 의 stability 를 변경하며, `<stability>` 가 enum (`draft`/`evolving`/`stable`/`frozen`/`deprecated`) 이 아니면 exit code 2 로 종료한다.
- [x] AC-2: `--reason <text>` 옵션을 통해 Change Notes row 를 함께 append 하며, `--reason` 부재 시 새 stability 가 `frozen` 이면 mutation 거부 + stderr 안내 + exit code 1 로 종료한다.
- [x] AC-3: `--dry-run` 옵션 제공 시 실제 파일 변경 없이 patch operation 요약을 stdout 에 출력하고 exit code 0 으로 종료한다.
- [x] AC-4: `--json` 공통 옵션이 적용되면 결과를 JSON envelope 로 출력하며 IR-CLI-021 의 diagnostics 구조를 준수한다.
- [x] AC-5: IR-CLI-025 에 의거하여 `speckiwi update-stability --help` 출력에 `--root <path>`, `--json`, `--no-color`, `--quiet` 가 Global options 섹션으로 노출된다.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |
| VE-1 | test | test/cli/mutation-commands.test.ts; test/cli/global-options-help.test.ts | AC-1, AC-2, AC-3, AC-4, AC-5 | - |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |
| Code | src/cli/commands/mutations.ts | addition_site | trace_intent: addition_site; update-stability 명령 등록 |
| Requirement | FR-PARSE-017 | depends_on | core mutation 의 CLI 노출 |
| Requirement | IR-CLI-010 | related_to | update-status 명령 의 stability 차원 동형 패턴 |

#### Research / Analysis

- -

#### Implementation Notes

- `src/cli/commands/mutations.ts` 에 `update-stability <id> <stability>` 명령 추가. `--reason <text>` 와 `--dry-run` 옵션을 IR-CLI-010 `update-status` 와 동일하게 처리. JSON 출력 envelope 는 `mutationOk` / `mutationFail` 결과를 그대로 reuse.

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-05-15 | 최초 작성 | FR-PARSE-017 mutation core 의 CLI 노출 책임 분리 |

### IR-CLI-027 — Skill add/install CLI for coding-agent targets

| Field | Value |
| --- | --- |
| Type | interface |
| Target | v2.2.3 |
| Status | verified |
| Priority | high |
| Tags | cli, skills, install, codex, claude, opencode, hermes, global |
| Risk | high |
| Stability | evolving |
| Verification Method | test, inspection |
| GitHub Issue | - |
| Related Docs | - |

#### Requirement

SpecKiwi CLI must provide a skill add or install command that installs repository-managed Kiwi skills for an explicit coding-agent target, supporting Codex, Claude, OpenCode, and Hermes destinations, including a global installation option and a dry-run mode for automation.

#### Rationale

A repository-local skill tree is useful only when users can place skills into the directory layout expected by each coding agent without memorizing per-agent paths. The CLI should make target selection explicit, support global installation, and keep install behavior machine-readable for scripts and agents.

#### Acceptance Criteria

- [x] AC-1: The CLI exposes a documented skill installation command group or subcommand with a required skill selector and a required target agent selector for Codex, Claude, OpenCode, or Hermes.
- [x] AC-2: The CLI supports `--global` and `-g` for user-level installation, supports a mutually exclusive `--dest <dir>` custom destination root, and the default non-global mode installs into a deterministic project-local destination or returns a structured unsupported-mode error when a target has no safe project-local destination.
- [x] AC-3: For Codex targets the default canonical source is `skills/codex`; for Claude targets the default canonical source is `skills/claude`; for OpenCode and Hermes local-LLM targets the default canonical source is `skills/etc`.
- [x] AC-4: The CLI does not use `llm` as a canonical source directory name and must report a clear error or compatibility alias warning if a user tries to target a deprecated `skills/llm` location.
- [x] AC-5: The CLI supports `--dry-run` and `--json` together so automation can inspect planned source, destination, operation (`install`, `update`, `skip`, or `conflict`), file operations, conflicts, validation findings, and MCP preflight status without copying files.
- [x] AC-6: The CLI refuses unsupported agent targets, missing skill names, unsafe skill names, missing source roots, missing or unsupported skill entrypoints, invalid frontmatter, unsafe/non-skill destination conflicts, invalid `--global`/`--dest` combinations, and invalid Hermes category usage with structured errors and no partial installation; an existing valid same-identity destination skill is treated as `update`, and a byte-identical destination is treated as `skip`.
- [x] AC-7: The CLI surfaces the mandatory `speckiwi mcp` dependency and an MCP preflight status (`satisfied`, `missing`, or `not_checked`) in human and JSON output so users know installed Kiwi skills require MCP for normal operation.
- [x] AC-8: The CLI delegates copy, validation, path resolution, conflict policy, and dry-run planning to the shared core skill installation service.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |
| VE-1 | command | npx vitest run test/cli/skills-cli.test.ts --hookTimeout 30000 --testTimeout 30000 | IR-CLI-027 AC-1 AC-2 AC-3 AC-5 AC-6 AC-7 AC-8 | - |
| VE-2 | inspection | src/cli/commands/skills.ts; src/core/skills/install-skill.ts; rg llm source mapping scan 2026-05-20 | IR-CLI-027 AC-4 AC-6 AC-8 | - |
| VE-3 | subagent-review | Locke subagent review 019e40a3-c101-7b12-a10e-3739af9385e7 on 2026-05-20 | IR-CLI-027 AC-1 AC-2 AC-3 AC-4 AC-5 AC-6 AC-7 AC-8 | - |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |
| Requirement | FR-NODE-016 | depends_on | CLI delegates install behavior to the shared core service. |
| Requirement | FR-FLOW-012 | supports | CLI output and validation must expose the mandatory MCP dependency. |

#### Research / Analysis

- OpenCode supports project and global skills directories, including `.opencode/skills` and `~/.config/opencode/skills`. Hermes uses `~/.hermes/skills` as the primary user skill directory and can scan external directories. These destination differences make explicit CLI target selection and dry-run visibility necessary.

#### Implementation Notes

- Candidate syntax can be finalized during implementation, but the contract should preserve explicit target selection, `--global` or `-g`, `--dry-run`, and JSON output. The CLI should not silently install OpenCode or Hermes variants from `skills/codex`; those targets use `skills/etc` unless the user explicitly selects another source in a future extension.
- [2026-05-19] 2026-05-20 research: CLI should expose skill installation/update semantics for codex|claude|opencode|hermes. Re-running install for an existing skill reports operation=update in human/JSON/dry-run output. Avoid using --target as the positional/flag name because CLI already uses target for SRS versions.
- [2026-05-20] Phase 0 contract repair: command shape is `speckiwi skills install <agent> <skill|all>` with `skills add` as an alias. `--dest` is a destination root, not a final skill folder. Hermes `--category` applies only to global Hermes installs and defaults to `kiwi`.

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-05-19 | Created | add-requirement |

### IR-CLI-028 — CLI mutation parity for safe previews

| Field | Value |
| --- | --- |
| Type | interface |
| Target | v2.3.0 |
| Status | verified |
| Priority | critical |
| Tags | v2.3.0, cli, mutation, dry-run, CAND-CLI-MUTATION-PARITY |
| Risk | high |
| Stability | evolving |
| Verification Method | test |
| GitHub Issue | - |
| Related Docs | [docs/research/09.mcp-cli-tool-abandonment-research.md](../research/09.mcp-cli-tool-abandonment-research.md) |

#### Requirement

SpecKiwi CLI mutation commands must expose safe dry-run, alias, documented row grammar, JSON error, and mutation-envelope behavior that matches the shared core mutation services and MCP mutation semantics.

#### Rationale

CLI is the fallback path when MCP is unavailable. If CLI mutation flags or outputs differ from core and MCP behavior, agents cannot safely use it as a documented fallback and tend to patch SRS Markdown directly.

#### Acceptance Criteria

- [x] AC-1: CLI mutation commands for AC checks, evidence additions, trace additions, requirement creation, status changes, stability updates, target activation, target goal updates, and completed-work additions expose dry-run behavior when core supports it.
- [x] AC-2: CLI mutation JSON output includes the shared mutation envelope from FR-NODE-017, including mutation.kind, mutation.written, operations, preview, diagnostics, and diagnosticsSummary.
- [x] AC-3: Documented aliases such as trace reference aliases are either implemented in Commander or removed from docs and skills through signature parity checks.
- [x] AC-4: CLI mutation commands reject malformed table-cell input before writing and return structured JSON errors when JSON mode is active.
- [x] AC-5: CLI and MCP mutation parity tests prove equivalent fixture changes and equivalent no-write dry-run behavior for the same core mutation scenario.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |
| VE-1 | plan | docs/plans/2026-06-29.specwkiki.v230-plan.plan.md#T-PH006-02 | all | - |
| VE-2 | test | test/cli/mutation-commands.test.ts | AC-1, AC-2, AC-3, AC-4 | - |
| VE-3 | test | test/integration/cli-mcp-parity.test.ts | AC-5 | - |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |
| Requirement | OPS-FLOW-001 | related_to | Research anchor for CLI mutation abandonment findings. |
| Requirement | FR-NODE-017 | depends_on | CLI parity must expose the shared core mutation envelope. |
| Requirement | IR-CLI-011 | extends | Hardens AC check command behavior. |
| Requirement | IR-CLI-013 | extends | Hardens evidence command behavior. |
| Requirement | IR-CLI-014 | extends | Hardens trace command behavior. |
| Requirement | IR-CLI-019 | extends | Hardens requirement creation command behavior. |
| Code | src/cli/commands/mutations.ts:184-267 | related_to | CLI mutation command registration and output path. |
| Task | T-PH006-02 | implements | - |
| Code | src/cli/commands/mutations.ts | implements | - |

#### Research / Analysis

- Derived from docs/research/09.mcp-cli-tool-abandonment-research.md sections 4.2, 6, 7, and 9. The research calls out unsupported flags, row grammar drift, and missing envelope parity as agent trust blockers.

#### Implementation Notes

- Start with CLI command tests for currently unsupported dry-run and alias behavior. Reuse the core envelope instead of building CLI-specific result shapes.

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-06-26 | Created | add-requirement |
| 2026-06-30 | Status -> verified | v2.3.0 closeout: acceptance criteria and verification evidence reviewed; final validation gates pending. |

### IR-CLI-029 — Compact CLI requirement search

| Field | Value |
| --- | --- |
| Type | interface |
| Target | v2.3.0 |
| Status | verified |
| Priority | critical |
| Tags | v2.3.0, cli, search, discovery, CAND-CLI-REQ-SEARCH |
| Risk | high |
| Stability | evolving |
| Verification Method | test |
| GitHub Issue | - |
| Related Docs | [docs/research/09.mcp-cli-tool-abandonment-research.md](../research/09.mcp-cli-tool-abandonment-research.md) |

#### Requirement

SpecKiwi CLI must provide compact requirement discovery through list, extract, and search commands with field selection, snippets, filters, pagination, and explicit full-detail opt-in so agents can find relevant Requirement IDs without raw Markdown search.

#### Rationale

The research found that list and extract payloads are too large or under-filtered, and list --format is accepted without changing output. CLI discovery must become a practical replacement for rg in agent workflows.

#### Acceptance Criteria

- [x] AC-1: CLI list supports deterministic ids, compact, full, and selected-fields projections, and full Markdown is omitted unless explicitly requested.
- [x] AC-2: CLI extract or its replacement supports target, scope, status, stability, priority, missing evidence, related docs, evidence reference, trace reference, tag, type, and new-work-candidate filters.
- [x] AC-3: CLI search matches requirement IDs, titles, requirement statements, rationale, research text, acceptance criteria, evidence references, trace links, related docs, and change notes with bounded snippets.
- [x] AC-4: CLI discovery commands support pagination or limit parameters and report cursor or truncation information in JSON output.
- [x] AC-5: CLI tests compare ids, compact, full, selected fields, snippets, filters, and pagination outputs without requiring raw Markdown inspection.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |
| VE-1 | plan | docs/plans/2026-06-29.specwkiki.v230-plan.plan.md#T-PH004-02 | all | - |
| VE-2 | test | test/cli/read-commands.test.ts | AC-1, AC-2, AC-3, AC-4, AC-5 | - |
| VE-3 | test | test/integration/cli-mcp-parity.test.ts | AC-5 | - |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |
| Requirement | OPS-FLOW-001 | related_to | Research anchor for raw Markdown discovery gaps. |
| Requirement | FR-PARSE-019 | depends_on | Compact search filters require normalized query fields. |
| Requirement | REL-PARSE-002 | depends_on | Discovery output must carry canonical diagnostics counts. |
| Requirement | IR-CLI-005 | extends | Extends extract behavior beyond include-markdown only. |
| Requirement | IR-CLI-006 | extends | Extends list behavior with compact projections. |
| Code | src/cli/commands/read.ts:66-88 | related_to | Current extract and list command registration. |
| Code | src/core/query/lookup.ts:8-20 | related_to | Core list and lookup query extension point. |
| Requirement | IR-CLI-030 | depends_on | - |
| Task | T-PH004-02 | implements | - |
| Code | src/core/query/discovery.ts | implements | - |
| Code | src/cli/commands/read.ts | implements | - |

#### Research / Analysis

- Derived from docs/research/09.mcp-cli-tool-abandonment-research.md sections 3.3, 4.5, 6, 7, and 9. It is the CLI side of replacing raw Markdown requirement discovery.

#### Implementation Notes

- Fix list --format behavior and add search red tests before changing output defaults. Coordinate query fields with FR-PARSE-019.

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-06-26 | Created | add-requirement |
| 2026-06-30 | Status -> verified | v2.3.0 closeout: acceptance criteria and verification evidence reviewed; final validation gates pending. |

### IR-CLI-030 — Structured JSON errors for CLI reads

| Field | Value |
| --- | --- |
| Type | interface |
| Target | v2.3.0 |
| Status | verified |
| Priority | high |
| Tags | v2.3.0, cli, json, errors, diagnostics |
| Risk | medium |
| Stability | evolving |
| Verification Method | test |
| GitHub Issue | - |
| Related Docs | [docs/research/09.mcp-cli-tool-abandonment-research.md](../research/09.mcp-cli-tool-abandonment-research.md) |

#### Requirement

SpecKiwi CLI read commands must return structured JSON error envelopes when JSON mode is active, including error code, message, diagnostics, diagnosticsSummary, and recovery hints instead of plaintext-only stderr failures.

#### Rationale

The research found that commands such as show on a missing ID can emit plaintext under JSON mode. Agents need machine-readable failures to recover without hallucinating command state or parsing stderr text.

#### Acceptance Criteria

- [x] AC-1: CLI read command failures in JSON mode return valid JSON on stdout or the documented JSON output channel with ok false, error.code, error.message, diagnostics, and diagnosticsSummary.
- [x] AC-2: Missing requirement, invalid filter, unknown target, malformed option, and workspace parse failures have stable error codes and nonzero exit status.
- [x] AC-3: Human output includes compact diagnostics for nonzero diagnostics without replacing the JSON envelope in JSON mode.
- [x] AC-4: CLI tests cover show on a missing requirement, extract or search invalid filters, target lookup failures, and validation diagnostics under JSON mode.
- [x] AC-5: Structured CLI errors include recommended follow-up command names when recovery is deterministic, such as list, search, active-target, validate, or init.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |
| VE-1 | plan | docs/plans/2026-06-29.specwkiki.v230-plan.plan.md#T-PH004-02 | all | - |
| VE-2 | test | test/cli/read-commands.test.ts | AC-1, AC-2, AC-3, AC-4, AC-5 | - |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |
| Requirement | OPS-FLOW-001 | related_to | Research anchor for CLI JSON error findings. |
| Requirement | REL-PARSE-002 | depends_on | CLI errors must carry canonical diagnostics summaries. |
| Requirement | IR-CLI-018 | extends | Extends exit-code policy with structured JSON errors. |
| Requirement | IR-CLI-021 | extends | Extends CLI read diagnostics parity. |
| Code | src/cli/commands/read.ts:90-98 | related_to | Current show command missing requirement error path. |
| Code | src/cli/formatters.ts | related_to | CLI JSON and human output formatting path. |
| Task | T-PH004-02 | implements | - |
| Code | src/cli/errors.ts | implements | - |
| Code | src/cli/index.ts | implements | - |
| Code | src/cli/commands/read.ts | implements | - |

#### Research / Analysis

- Derived from docs/research/09.mcp-cli-tool-abandonment-research.md sections 3.4, 6, 7, and 9. The research includes show NO-SUCH-ID under JSON mode as a representative failure.

#### Implementation Notes

- Start with failing tests around src/cli/commands/read.ts and command error handling. Keep exit-code policy compatible with IR-CLI-018.

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-06-26 | Created | add-requirement |
| 2026-06-30 | Status -> verified | v2.3.0 closeout: acceptance criteria and verification evidence reviewed; final validation gates pending. |

### IR-CLI-031 — CLI workflow artifact read commands

| Field | Value |
| --- | --- |
| Type | interface |
| Target | v2.3.0 |
| Status | verified |
| Priority | critical |
| Tags | v2.3.0, cli, workflow, planner, jsonl |
| Risk | high |
| Stability | evolving |
| Verification Method | test |
| GitHub Issue | - |
| Related Docs | [docs/research/10.kiwi-planner-jsonl-tooling-research.md](../research/10.kiwi-planner-jsonl-tooling-research.md) |

#### Requirement

SpecKiwi CLI must provide compact read commands for workflow workspace info, artifact discovery, plan status, plan task lookup, next-task selection, pipeline status and tailing, session status and resume hints, and worklog tailing using one workflow-read envelope.

#### Rationale

The Kiwi planner research found that agents repeatedly read full plan, sidecar, session, pipeline, and worklog files to answer narrow questions. CLI read tools must expose bounded workflow state without raw artifact scans.

#### Acceptance Criteria

- [x] AC-1: CLI commands expose workspace info, artifacts list, latest artifact, artifact resolution, plan status, plan task lookup, next plan task, pipeline status, pipeline tail, pipeline next recommendation, session status, resume hint, and worklog tail.
- [x] AC-2: Every workflow read command returns a compact envelope with ok, value, meta, artifacts, cursor when applicable, diagnostics, and diagnosticsSummary in JSON mode.
- [x] AC-3: Full Markdown or full JSON artifact bodies are omitted by default and require an explicit include-body or equivalent option.
- [x] AC-4: Next-task selection follows sidecar depends_on_task DAG semantics and PM execution state precedence instead of task ID sorting.
- [x] AC-5: CLI workflow read tests cover current artifacts, legacy artifacts, target mismatch, ambiguous artifacts, dependency cycles, blocked dependencies, invalid JSONL, and bounded tail cursors.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |
| VE-1 | plan | docs/plans/2026-06-29.specwkiki.v230-plan.plan.md#T-PH012-02 | all | - |
| VE-2 | test | test/cli/workflow-read-commands.test.ts; test/fixtures/workflow-artifacts.ts | all | - |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |
| Requirement | OPS-FLOW-001 | related_to | Research anchor for workflow read tooling. |
| Requirement | FR-NODE-020 | depends_on | CLI reads use shared workflow artifact resolution. |
| Requirement | FR-NODE-021 | depends_on | Pipeline and worklog reads use shared JSONL utilities. |
| Requirement | IR-CLI-029 | related_to | Requirement compact search complements workflow compact reads. |
| Code | src/cli/commands/read.ts:1-135 | related_to | CLI read command registration extension point. |
| Code | skills/codex/kiwi-planner/references/extended-workflow.md:279-325 | related_to | Sidecar task schema used by plan reads. |
| Task | T-PH012-02 | implements | - |
| Code | src/core/workflow/read.ts | implements | - |
| Code | src/cli/commands/read.ts | implements | - |

#### Research / Analysis

- Derived from docs/research/10.kiwi-planner-jsonl-tooling-research.md sections 3.7.5, 6, 7.1, 7.2, 7.3, 7.6, 8, 10, 11, and 12.

#### Implementation Notes

Implement read-only commands before workflow mutation commands. Use FR-NODE-020 and FR-NODE-021 instead of skill-local path or JSONL parsing. Pipeline next recommendation is only a pipeline-state projection or resume hint; cross-artifact next-action selection belongs to IR-CLI-032 work-order.


#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-06-26 | Created | add-requirement |
| 2026-06-30 | Status -> verified | v2.3.0 closeout: acceptance criteria and verification evidence reviewed; final validation gates pending. |

### IR-CLI-032 — CLI next work-order command

| Field | Value |
| --- | --- |
| Type | interface |
| Target | v2.3.0 |
| Status | verified |
| Priority | critical |
| Tags | v2.3.0, cli, workflow, work-order, next |
| Risk | high |
| Stability | evolving |
| Verification Method | test |
| GitHub Issue | - |
| Related Docs | [docs/research/10.kiwi-planner-jsonl-tooling-research.md](../research/10.kiwi-planner-jsonl-tooling-research.md), [docs/research/09.mcp-cli-tool-abandonment-research.md](../research/09.mcp-cli-tool-abandonment-research.md) |

#### Requirement

SpecKiwi CLI must provide a compact work-order next command that combines active target, target summary, requirement search, latest workflow artifacts, pipeline state, PM session state, and worklog tail into the next safe action without raw artifact scans.

#### Rationale

The Kiwi planner research identified next-work-order as the main token-reduction and hallucination-reduction tool. Agents should not have to reread every SRS, plan, sidecar, pipeline, and worklog file to choose the next step.

#### Acceptance Criteria

- [x] AC-1: The CLI work-order next command returns action kinds including create-plan, execute-task, resume-session, ask-user, fix-artifact, blocked, complete, and no-action when those states are supported by available evidence.
- [x] AC-2: The command includes target, targetSource, requirement IDs, artifact references, hashes or mtimes, nextAction, reason, blockingDiagnostics, and diagnosticsSummary.
- [x] AC-3: The command fails closed when pipeline state is FAILED or NEEDS_USER, workflow validators report stale_artifact or invalid_artifact, or no active target or explicit target is available.
- [x] AC-4: The command omits raw Markdown and raw JSON bodies by default and records payload measurement fields when invoked in measurement mode.
- [x] AC-5: Integration tests cover execute-task, create-plan, resume-session, ask-user, fix-artifact, blocked, and complete fixtures with deterministic outputs.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |
| VE-1 | plan | docs/plans/2026-06-29.specwkiki.v230-plan.plan.md#T-PH013-02 | all | - |
| VE-2 | test | test/core/workflow/work-order.test.ts; test/cli/workflow-work-order.test.ts | all | - |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |
| Requirement | OPS-FLOW-001 | related_to | Research anchor for next-work-order tooling. |
| Requirement | IR-CLI-029 | depends_on | Work-order needs compact requirement search for SRS context. |
| Requirement | IR-CLI-031 | depends_on | Work-order composes CLI workflow reads. |
| Requirement | REL-NODE-003 | depends_on | Work-order must consume validator outcomes. |
| Requirement | REL-NODE-002 | depends_on | Pair-level payload measurement fixture proves utility. |
| Code | src/cli/commands/read.ts:1-135 | related_to | CLI command registration extension point. |
| Code | skills/codex/_shared/kiwi/pipeline-event.md:9-63 | related_to | Pipeline status input for next work order. |
| Task | T-PH013-02 | implements | - |

#### Research / Analysis

- Derived from docs/research/10.kiwi-planner-jsonl-tooling-research.md sections 7.7, 8, 9.4, 10, 11, 12, and 13. It also depends on the compact SRS discovery work from docs/research/09.

#### Implementation Notes

- Implement after compact requirement search and workflow read tools exist. Add the pair-level agent-next-work-order fixture before claiming token reduction.

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-06-26 | Created | add-requirement |
| 2026-06-30 | Status -> verified | v2.3.0 closeout: acceptance criteria and verification evidence reviewed; final validation gates pending. |

### ~~IR-CLI-033 — CLI workflow progress mutation commands~~ [DISCARDED → see IR-CLI-042]

| Field | Value |
| --- | --- |
| Type | interface |
| Target | v2.3.0 |
| Status | discarded |
| Priority | critical |
| Tags | v2.3.0, cli, workflow, mutation, checklist |
| Risk | high |
| Stability | evolving |
| Verification Method | test |
| GitHub Issue | - |
| Related Docs | [docs/research/10.kiwi-planner-jsonl-tooling-research.md](../research/10.kiwi-planner-jsonl-tooling-research.md), [docs/research/09.mcp-cli-tool-abandonment-research.md](../research/09.mcp-cli-tool-abandonment-research.md) |

#### Requirement

SpecKiwi CLI must provide guarded workflow progress mutation commands for plan checkbox and checklist display state, PM task status, pipeline and worklog JSONL append events, and workflow repair records using the shared mutation envelope and core workflow mutation services.

#### Rationale

The Kiwi planner research proposes CLI progress mutation commands alongside core workflow mutation services. Without a CLI surface, agents still have to edit plan Markdown, PM state, or JSONL files directly when MCP is unavailable.

#### Acceptance Criteria

- [ ] AC-1: CLI registers workflow mutation commands for plan task check and uncheck, checklist item checked-state updates, PM task status updates, pipeline JSONL append, worklog JSONL append, and workflow repair record creation.
- [ ] AC-2: Each CLI workflow mutation command supports dry-run and returns the shared mutation envelope, including mutation.kind, workflow.runId, workflow.taskId when task-scoped, workflow.owner, journalKey computed by FR-NODE-028, journalState, operations, stale guards, written, result, pendingRepair, diagnostics, and diagnosticsSummary.
- [ ] AC-3: Commands reject stale plan or sidecar hashes, stale PM state, forbidden child-coder ownership, invalid task IDs, invalid JSONL records, and dependency-blocked writes before mutating display state.
- [ ] AC-4: Commands treat already-applied checkbox, checklist, task-status, and append-idempotency states as successful no-op results with mutation.written false and use canonical journal states planned, applied, confirmed, failed, and skipped_dry_run.
- [ ] AC-5: CLI tests cover dry-run no-write, already checked, stale hash, forbidden owner, worklog append failure, guarded plan write failure, final PM confirmation failure, invalid JSONL append, and repair-record creation.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |
| Requirement | OPS-FLOW-001 | related_to | Research anchor for workflow mutation tooling. |
| Requirement | FR-NODE-022 | depends_on | CLI commands expose guarded core workflow progress mutations. |
| Requirement | FR-NODE-017 | depends_on | CLI outputs use the shared mutation envelope. |
| Requirement | IR-CLI-031 | depends_on | CLI mutation commands reuse workflow artifact resolution and read context from CLI readers. |
| Requirement | REL-NODE-003 | depends_on | Mutation commands rely on workflow artifact validation before writes. |
| Code | src/cli/commands/mutations.ts:1-220 | related_to | CLI mutation command registration surface for adding workflow progress mutation commands. |
| Code | skills/codex/kiwi-pm/SKILL.md:140-190 | related_to | PM ownership and plan checkbox mutation contract that CLI commands must replace. |

#### Research / Analysis

- Derived from docs/research/10.kiwi-planner-jsonl-tooling-research.md sections 3.7.6, 7.4, 7.5, 9.3, 10, 11, and 12. Covers CLI exposure of guarded workflow mutation kinds, journal key and state contract, stale-guard semantics, dry-run behavior, and partial-failure repair reporting.

#### Implementation Notes

- Historical draft only. Use IR-CLI-042 as the canonical CLI workflow mutation and pipeline emit contract, backed by FR-NODE-030 and FR-NODE-028.

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-06-26 | Created | add-requirement |

### ~~IR-CLI-034 — CLI logical workflow deletion commands~~ [DISCARDED → see IR-CLI-043]

| Field | Value |
| --- | --- |
| Type | interface |
| Target | v2.3.0 |
| Status | discarded |
| Priority | critical |
| Tags | v2.3.0, cli, workflow, jsonl, deletion |
| Risk | high |
| Stability | evolving |
| Verification Method | test |
| GitHub Issue | - |
| Related Docs | [docs/research/10.kiwi-planner-jsonl-tooling-research.md](../research/10.kiwi-planner-jsonl-tooling-research.md), [docs/research/11.v2.3.0-tooling-code-improvement-plan.md](../research/11.v2.3.0-tooling-code-improvement-plan.md), [docs/research/230.md](../research/230.md) |

#### Requirement

SpecKiwi CLI must provide guarded workflow logical-delete commands that mark supported plan, PM state, pipeline, worklog, and repair records as `deleted` through the shared workflow mutation envelope without physically deleting audit history.

#### Rationale

CLI fallback must be available when MCP is unavailable. A destructive delete command would make workflow recovery unsafe, so the CLI surface should expose logical deletion as a state transition with dry-run, stale guards, owner checks, and idempotency.

#### Acceptance Criteria

- [ ] AC-1: CLI registers workflow logical-delete commands or subcommands for supported record classes, including plan display/checklist records, PM task records, pipeline events, worklog events, and workflow repair records when those classes are implemented by core services.
- [ ] AC-2: Each CLI logical-delete command requires or records a deletion reason, supports --dry-run and --json, and returns the shared mutation envelope with mutation.kind, workflow identity, target record identity, desired effective state `deleted`, journalKey, journalState, operations, stale guards, written, result, pendingRepair, diagnostics, and diagnosticsSummary.
- [ ] AC-3: CLI logical-delete commands never physically remove JSONL lines or Markdown history during normal operation; they append or write guarded tombstone/repair state according to FR-NODE-023.
- [ ] AC-4: Commands treat already-deleted records as successful no-op results with mutation.written false unless the caller explicitly requests an additional repair note.
- [ ] AC-5: CLI read commands expose a way to include or inspect deleted records while default compact workflow reads treat deleted records as inactive.
- [ ] AC-6: CLI tests cover dry-run no-write, successful logical delete, already-deleted no-op, stale hash rejection, forbidden owner, invalid JSONL rejection, non-deletable record rejection, tombstone/correction-of-tombstone rejection, `status=deleted` rejection, includeDeleted read inspection, and parity with MCP logical-delete behavior.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |
| Requirement | OPS-FLOW-001 | related_to | Research anchor for workflow deletion tooling. |
| Requirement | FR-NODE-023 | depends_on | CLI commands expose core logical deletion semantics. |
| Requirement | IR-CLI-033 | extends | Logical deletion is a guarded workflow mutation command family. |
| Requirement | FR-NODE-017 | depends_on | CLI output uses the shared mutation envelope. |
| Requirement | REL-NODE-003 | depends_on | CLI deletion relies on workflow artifact validation. |
| Document | docs/research/230.md | derived_from | Follow-up decision review for CORRECTION, deleted effective state, includeDeleted read boundary, and restore exclusion. |
| Requirement | IR-CLI-042 | extends | - |

#### Research / Analysis

- Derived from planner JSONL tooling research, docs/research/230.md decision review, and the follow-up deletion-state requirement. This is the CLI exposure for FR-NODE-023.

#### Implementation Notes

- Implement after FR-NODE-023 and the existing workflow mutation command set in IR-CLI-033. Keep payloads equivalent to MCP logical-delete tools.
- [2026-06-28] Doc230 Plus refinement: implement against IR-CLI-042 for CLI mutation behavior; IR-CLI-033 is historical and discarded.

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-06-27 | Created | add-requirement |
| 2026-06-28 | Clarified logical-delete CLI contract | Five-axis validation found that deleted is an effective state, includeDeleted is a read boundary, and restore-style tombstone edits must be rejected. |

### ~~IR-CLI-035 — CLI external Completed Work Log commands~~ [DISCARDED → see IR-CLI-037]

| Field | Value |
| --- | --- |
| Type | interface |
| Target | v2.3.0 |
| Status | discarded |
| Priority | high |
| Tags | v2.3.0, completed-work, external-log, cli, CAND-NODE-EXTERNAL-COMPLETED-WORK-LOG |
| Risk | medium |
| Stability | evolving |
| Verification Method | test |
| GitHub Issue | - |
| Related Docs | [docs/research/230.md](../research/230.md), [docs/research/11.v2.3.0-tooling-code-improvement-plan.md](../research/11.v2.3.0-tooling-code-improvement-plan.md) |

#### Requirement

SpecKiwi CLI completed-work and add-completed-work commands must preserve their existing command names and JSON payload shapes while supporting the external docs/spec/05.completed-work.md Completed Work Log storage model and bounded read output.

#### Rationale

Doc230 requires official tools, not raw Markdown edits, to hide completed-work history from the index without losing agent usability.

#### Acceptance Criteria

- [ ] AC-1: speckiwi completed-work reads rows from the external log when present and from the legacy index table when the external log is absent.
- [ ] AC-2: speckiwi add-completed-work writes to the canonical target selected by FR-NODE-024 and reports the target file path in JSON output or diagnostics.
- [ ] AC-3: completed-work CLI JSON output preserves existing completedWork row fields while adding source metadata only in a backward-compatible optional field or envelope metadata.
- [ ] AC-4: completed-work read options support bounded output with limit and ordering and expose count or continuation metadata when rows are omitted.
- [ ] AC-5: CLI tests prove external-only, legacy-only, and dual-source diagnostic scenarios produce deterministic JSON output.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |
| Requirement | OPS-FLOW-001 | depends_on | v2.3.0 tool improvement research anchor. |
| Requirement | FR-PARSE-020 | depends_on | CLI reads external completed-work rows parsed by core. |
| Requirement | FR-NODE-024 | depends_on | CLI writes through the core canonical target resolver. |
| Requirement | IR-CLI-020 | extends | Extends baseline completed-work CLI commands. |
| Requirement | IR-CLI-024 | extends | Preserves report path CLI option behavior. |
| Code | src/cli/commands/read.ts:118-129 | related_to | Current completed-work read command. |
| Code | src/cli/commands/mutations.ts:158-182 | related_to | Current add-completed-work mutation command. |

#### Research / Analysis

- Derived from docs/research/230.md sections 14.2, 14.3, and 14.6. This is the CLI adapter contract for FR-PARSE-020 and FR-NODE-024.

#### Implementation Notes

- Historical draft only. Use IR-CLI-037 as the canonical CLI external Completed Work Log command contract.

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-06-28 | Created | add-requirement |

### ~~IR-CLI-036 — CLI SRS mutation lock controls~~ [DISCARDED → see IR-CLI-038]

| Field | Value |
| --- | --- |
| Type | interface |
| Target | v2.3.0 |
| Status | discarded |
| Priority | critical |
| Tags | v2.3.0, srs-lock, cli, ignore-lock, CAND-NODE-SRS-MUTATION-LOCK |
| Risk | high |
| Stability | evolving |
| Verification Method | test |
| GitHub Issue | - |
| Related Docs | [docs/research/230.md](../research/230.md), [docs/research/11.v2.3.0-tooling-code-improvement-plan.md](../research/11.v2.3.0-tooling-code-improvement-plan.md) |

#### Requirement

SpecKiwi CLI mutation commands that can write SRS files must honor the SRS mutation lock, return retry-later diagnostics when locked, and expose an explicit --ignore-lock escape hatch that bypasses only the SRS lock.

#### Rationale

The user requirement explicitly asks for a parameter to ignore locks, but only for SRS safety cases. CLI needs a consistent operator escape hatch without weakening other mutation guards.

#### Acceptance Criteria

- [ ] AC-1: All CLI commands that can write docs/spec, docs/rule, SRS index rollups, or SRS mutation cache files check the REL-NODE-004 lock before writing.
- [ ] AC-2: When a non-stale lock is active, CLI mutation commands fail without writing and JSON output includes code SRS_LOCKED, owner, operation, request ID, acquiredAt, expiresAt, and retry guidance.
- [ ] AC-3: The --ignore-lock option is available only on SRS mutation commands and is not accepted by read-only commands.
- [ ] AC-4: Using --ignore-lock records a diagnostic or response metadata entry indicating that the lock was ignored.
- [ ] AC-5: --ignore-lock does not bypass stale patch checks, generated-ID collision checks, table-cell safety, trace validation, or SRS validation.
- [ ] AC-6: CLI tests cover active lock denial, stale lock recovery, ignored-lock write path, read-only command behavior under lock, and unknown-option failure for --ignore-lock on read-only commands.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |
| Requirement | OPS-FLOW-001 | depends_on | v2.3.0 tool improvement research anchor. |
| Requirement | REL-NODE-004 | depends_on | CLI lock behavior delegates to core SRS lock service. |
| Requirement | FR-NODE-017 | depends_on | Lock diagnostics should use the shared mutation envelope. |
| Requirement | IR-CLI-028 | depends_on | CLI mutation parity must include lock-aware previews and diagnostics. |
| Code | src/cli/commands/mutations.ts:68-270 | related_to | Current CLI mutation command registration surface. |

#### Research / Analysis

- Derived from docs/research/230.md sections 9 through 11 and 17. This is the CLI adapter contract for REL-NODE-004.

#### Implementation Notes

- Historical draft only. Use IR-CLI-038 as the canonical CLI SRS mutation lock controls contract.

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-06-28 | Created | add-requirement |

### IR-CLI-037 — CLI external Completed Work Log commands

| Field | Value |
| --- | --- |
| Type | interface |
| Target | v2.3.0 |
| Status | verified |
| Priority | high |
| Tags | v2.3.0, completed-work, external-log, cli, CAND-NODE-EXTERNAL-COMPLETED-WORK-LOG |
| Risk | medium |
| Stability | evolving |
| Verification Method | test |
| GitHub Issue | - |
| Related Docs | [docs/research/230.md](../research/230.md), [docs/research/11.v2.3.0-tooling-code-improvement-plan.md](../research/11.v2.3.0-tooling-code-improvement-plan.md) |

#### Requirement

SpecKiwi CLI must read, validate, and append Completed Work Log entries through an external docs/spec/05.completed-work.md file when present, while preserving backward-compatible behavior for legacy index-embedded Completed Work Log rows.

#### Rationale

Doc230 found that keeping all work history in docs/spec/00.index.md increases token cost and encourages accidental index edits. CLI commands need explicit support for the external log after parser and core services define the source-selection behavior.

#### Acceptance Criteria

- [x] AC-1: completed-work CLI reads merge or select the authoritative Completed Work Log source according to the parser/core source-selection result and report each entry source file and line in JSON output.
- [x] AC-2: completed-work CLI appends new rows to docs/spec/05.completed-work.md when the external log exists and appends to docs/spec/00.index.md only for legacy workspaces without the external file.
- [x] AC-3: CLI mutation output reports which file was written, whether legacy index mode or external-log mode was used, and any duplicate-source or migration diagnostics.
- [x] AC-4: CLI supports a migration/preview flow that shows the proposed external completed-work file and index navigation link without deleting legacy rows automatically.
- [x] AC-5: CLI tests cover external-only, legacy-only, dual-source diagnostic, append-to-external, append-to-legacy, malformed report paths, and dry-run preview behavior.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |
| VE-1 | plan | docs/plans/2026-06-29.specwkiki.v230-plan.plan.md#T-PH008-02 | all | - |
| VE-2 | test | test/cli/read-commands.test.ts; test/cli/mutation-commands.test.ts; test/core/query/completed-work.test.ts | AC-1, AC-2, AC-3, AC-4, AC-5 | - |
| VE-3 | test | npx vitest run test/core/query/completed-work.test.ts test/cli/read-commands.test.ts test/cli/mutation-commands.test.ts test/mcp/read-tools-resources.test.ts; npx vitest run test/cli/mutation-commands.test.ts test/mcp/read-tools-resources.test.ts; npm run typecheck; npm test | AC-1, AC-2, AC-3, AC-4, AC-5 | - |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |
| Requirement | OPS-FLOW-001 | depends_on | v2.3.0 tool improvement research anchor. |
| Requirement | FR-PARSE-021 | depends_on | CLI reads rely on external Completed Work Log parsing. |
| Requirement | FR-NODE-026 | depends_on | CLI appends delegate to core completed-work source-selection services. |
| Requirement | IR-CLI-020 | extends | Extends baseline completed-work read behavior. |
| Requirement | IR-CLI-024 | extends | Extends report-path field support in completed-work CLI output. |
| Code | src/cli/commands/read.ts:118-129 | related_to | Current completed-work read command surface. |
| Code | src/cli/commands/mutations.ts:158-182 | related_to | Current add-completed-work mutation command surface. |
| Requirement | IR-CLI-035 | supersedes | - |
| Task | T-PH008-02 | implements | - |
| Code | src/cli/commands/read.ts:294 | implements | - |
| Code | src/cli/commands/read.ts:297 | implements | - |
| Code | src/core/mutation/add-completed-work.ts:313 | implements | - |
| Test | test/cli/read-commands.test.ts:411 | verifies | - |
| Test | test/cli/mutation-commands.test.ts:370 | verifies | - |

#### Research / Analysis

Derived from docs/research/230.md sections 14.1 through 14.7 and the docs/research/11 SRS Precondition work-package row for External completed-work log split. This is the CLI adapter contract for the external Completed Work Log split.

#### Implementation Notes

- Implement only after FR-PARSE-021 and FR-NODE-026 are testable. Keep migration preview non-destructive and keep add_completed_work semantics aligned with report-path grammar from FR-PARSE-016.

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-06-28 | Created | add-requirement |
| 2026-06-30 | Status -> verified | v2.3.0 closeout: acceptance criteria and verification evidence reviewed; final validation gates pending. |

### IR-CLI-038 — CLI SRS mutation lock controls

| Field | Value |
| --- | --- |
| Type | interface |
| Target | v2.3.0 |
| Status | verified |
| Priority | critical |
| Tags | v2.3.0, srs-lock, ignore-lock, cli, concurrency, CAND-NODE-SRS-MUTATION-LOCK |
| Risk | high |
| Stability | evolving |
| Verification Method | test |
| GitHub Issue | - |
| Related Docs | [docs/research/230.md](../research/230.md), [docs/research/11.v2.3.0-tooling-code-improvement-plan.md](../research/11.v2.3.0-tooling-code-improvement-plan.md) |

#### Requirement

SpecKiwi CLI SRS mutation commands must honor the SRS mutation lock, report active-lock diagnostics to users, and expose a narrow --ignore-lock escape hatch that bypasses only the SRS lock while preserving all other mutation safety checks.

#### Rationale

Doc230 found that CLI and stdio MCP can run concurrently against the same repository. CLI users need deterministic lock errors and a deliberate stale-lock override, not silent races or manual file edits.

#### Acceptance Criteria

- [x] AC-1: All CLI commands that can mutate docs/spec, docs/rule as part of SRS mutation contracts, SRS index rollups, or SRS mutation cache files acquire the shared SRS lock before writing.
- [x] AC-2: When the lock is active and non-stale, CLI mutations fail before editing files and print owner, operation, requestId, acquiredAt, expiresAt, and retry guidance in human output and JSON output.
- [x] AC-3: CLI exposes --ignore-lock only on SRS mutation commands, records diagnostics when it is used, and never lets the flag bypass stale patch guards, generated-ID collision checks, table-cell safety, trace validation, or SRS validation.
- [x] AC-4: Dry-run or preview modes report whether a lock would block the real mutation without modifying lock state or SRS files.
- [x] AC-5: CLI tests cover active lock denial, stale lock recovery, --ignore-lock diagnostics, dry-run lock preview, and preservation of existing mutation safety failures under --ignore-lock.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |
| VE-1 | plan | docs/plans/2026-06-29.specwkiki.v230-plan.plan.md#T-PH007-02 | all | - |
| VE-2 | test | test/cli/mutation-commands.test.ts; npm test | all | Covers CLI lock denial for update-status and init, dry-run lock blocking, human/JSON lock details, --ignore-lock diagnostics, and preservation of table-cell safety under --ignore-lock. |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |
| Requirement | REL-NODE-005 | depends_on | CLI lock controls expose the shared core SRS lock. |
| Requirement | FR-NODE-017 | depends_on | CLI output should use the shared mutation envelope and diagnostics. |
| Requirement | IR-CLI-028 | extends | Extends CLI mutation preview and dry-run option parity. |
| Code | src/cli/commands/mutations.ts:68-270 | related_to | Current CLI SRS mutation command registration and handlers. |
| Requirement | IR-CLI-036 | supersedes | - |
| Task | T-PH007-02 | implements | - |
| Code | src/cli/commands/mutations.ts | implements | CLI SRS mutation commands expose --ignore-lock and route writes through locked core services, including init and sync-index. |

#### Research / Analysis

Derived from docs/research/230.md sections 9 through 12 and the docs/research/11 SRS Precondition work-package row for Requirement ID status cache and SRS mutation lock. This is the CLI adapter contract for SRS mutation locking.

#### Implementation Notes

- Wire CLI options only after REL-NODE-005 provides shared lock acquisition and diagnostics. Keep --ignore-lock scoped to SRS mutation commands; do not add a global unsafe mode.

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-06-28 | Created | add-requirement |
| 2026-06-29 | Status -> implemented | Implemented SRS mutation lock/status cache controls and regression tests via kiwi-pm PH-007. |
| 2026-06-30 | Status -> verified | v2.3.0 closeout: acceptance criteria and verification evidence reviewed; final validation gates pending. |

### ~~IR-CLI-039 — CLI workflow projection commands and profiles~~ [DISCARDED → see IR-CLI-041 +1]

| Field | Value |
| --- | --- |
| Type | interface |
| Target | v2.3.0 |
| Status | discarded |
| Priority | critical |
| Tags | v2.3.0, cli, workflow, projection, work-order |
| Risk | high |
| Stability | evolving |
| Verification Method | test |
| GitHub Issue | - |
| Related Docs | [docs/research/230plus.md](../research/230plus.md), [docs/research/10.kiwi-planner-jsonl-tooling-research.md](../research/10.kiwi-planner-jsonl-tooling-research.md) |

#### Requirement

SpecKiwi CLI must expose public workflow projection command names and work-order output profiles that delegate to the shared workflow read, validator, work-order, JSONL, and mutation services instead of implementing duplicate selection or parsing logic.

#### Rationale

Doc230 Plus found that tool names such as workflow doctor, workflow diff, workflow schema-check, pipeline compact, workflow explain-next, workflow context-pack, and pipeline emit are useful only if they are thin CLI surfaces over canonical core behavior. Naming them in SRS prevents skills from inventing shell-based or divergent interfaces.

#### Acceptance Criteria

- [ ] AC-1: CLI read-only projection commands include workflow doctor, workflow diff, workflow schema-check or plan validate, pipeline status, pipeline tail, and optional pipeline compact alias, all returning the compact workflow-read envelope without raw bodies by default.
- [ ] AC-2: CLI work-order exposes explain and compact context profiles through the canonical work-order next command, including decisionTrace, rejected candidates, blockers, artifact references, and source hashes or mtimes generated by the work-order engine.
- [ ] AC-3: CLI pipeline emit is available only as a wrapper over the shared JSONL append and guarded workflow mutation services, validates schema, serializes one JSON object per line, enforces trailing LF, dedupes by parsed skill and run_id, supports dry-run, and returns the shared mutation envelope.
- [ ] AC-4: CLI projection and profile commands fail closed on invalid artifacts, unsupported schema versions, dependency blockers, active FAILED or NEEDS_USER pipeline state, and ambiguous workspace or target state.
- [ ] AC-5: CLI tests prove projection aliases produce decisions equivalent to canonical read or work-order commands, pipeline emit matches core append semantics, no duplicate selector is introduced, and raw artifact bodies require explicit include-body opt-in.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |
| Requirement | OPS-FLOW-001 | depends_on | v2.3.0 tool improvement research anchor. |
| Requirement | IR-CLI-031 | extends | Names public read-only projection commands over CLI workflow reads. |
| Requirement | IR-CLI-032 | extends | Adds explain and context profiles to the canonical work-order command. |
| Requirement | IR-CLI-033 | extends | Names pipeline emit as a guarded workflow mutation command surface. |
| Requirement | REL-NODE-006 | depends_on | CLI projections consume core diagnostic projection contracts. |
| Requirement | FR-NODE-028 | depends_on | CLI mutation outputs use canonical journal identity. |
| Code | src/cli/index.ts:14-20 | related_to | Current CLI registration has no workflow or pipeline command family. |
| Code | src/cli/commands/read.ts:53-147 | related_to | Current CLI read extension point. |
| Code | src/cli/commands/mutations.ts:68-270 | related_to | Current CLI mutation extension point. |

#### Research / Analysis

- Derived from docs/research/230plus.md sections 5.1 through 5.9 and 7. The research recommends these command names as read or mutation surfaces over existing core contracts, not as independent engines.

#### Implementation Notes

- Implement after IR-CLI-031, IR-CLI-032, IR-CLI-033, REL-NODE-006, and FR-NODE-028 are testable. Do not edit Kiwi skill text to rely on these names until runtime commands exist.

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-06-28 | Created | add-requirement |

### IR-CLI-040 — CLI read-only legacy workflow migration preview

| Field | Value |
| --- | --- |
| Type | interface |
| Target | v2.3.0 |
| Status | verified |
| Priority | high |
| Tags | v2.3.0, cli, workflow, migration-preview, legacy |
| Risk | high |
| Stability | evolving |
| Verification Method | test |
| GitHub Issue | - |
| Related Docs | [docs/research/230plus.md](../research/230plus.md) |

#### Requirement

SpecKiwi CLI must expose a read-only legacy workflow migration preview command that delegates to the core migration-preview service and never applies, writes, moves, deletes, or normalizes workflow artifacts.

#### Rationale

Doc230 Plus identifies workflow migrate-preview as the only proposed tool that needs a narrow new requirement. The CLI surface must make preview semantics unambiguous and prevent users or agents from mistaking the diagnostic for an apply operation.

#### Acceptance Criteria

- [x] AC-1: The CLI exposes speckiwi workflow migrate-preview --json or an equivalent documented command that returns the FR-NODE-029 preview envelope.
- [x] AC-2: The command reports legacy source paths, confidence, source hashes, proposed destinations, unsupported fields, data-loss risks, target drift, path drift, diagnostics, and diagnosticsSummary without raw bodies by default.
- [x] AC-3: The command always reports written false and does not create, update, move, delete, normalize, compact, or append any workflow, pipeline, plan, session, worklog, or SRS file.
- [x] AC-4: Any apply, write, fix, normalize, or migrate flag for this command fails with a structured unsupported_operation diagnostic unless a later migration-apply requirement exists.
- [x] AC-5: CLI tests cover legacy-only preview, mixed current and legacy artifacts, include-body opt-in, unsupported apply flags, malformed legacy artifacts, target drift, and no-write file hash preservation.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |
| VE-1 | plan | docs/plans/2026-06-29.specwkiki.v230-plan.plan.md#T-PH011-02 | all | - |
| VE-2 | test | test/cli/workflow-migration-preview.test.ts | all | Covers workflow migrate-preview CLI envelope, include-body opt-in, unsupported apply/write-style flags, malformed legacy diagnostics, target drift, and hash-preserving no-write behavior. |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |
| Requirement | OPS-FLOW-001 | depends_on | v2.3.0 tool improvement research anchor. |
| Requirement | FR-NODE-029 | depends_on | CLI command delegates to the core read-only preview service. |
| Requirement | IR-CLI-031 | extends | Migration preview is a compact workflow read projection. |
| Requirement | REL-NODE-006 | depends_on | CLI preview follows read-only diagnostic projection constraints. |
| Code | src/cli/index.ts:14-20 | related_to | Current CLI registration has no workflow command family. |
| Code | src/cli/commands/read.ts:53-147 | related_to | Current CLI read command registration extension point. |
| Task | T-PH011-02 | implements | - |

#### Research / Analysis

- Derived from docs/research/230plus.md section 5.10. Actual migration apply/write is explicitly out of scope for this requirement.

#### Implementation Notes

- Implement only after FR-NODE-029 exists. Keep the command read-only even in non-dry-run mode; --dry-run may be accepted but must be semantically redundant and still report written false.

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-06-28 | Created | add-requirement |
| 2026-06-29 | Status -> implemented | Implemented read-only workflow migrate-preview CLI command and verified no-write/unsupported-operation behavior. |
| 2026-06-30 | Status -> verified | v2.3.0 closeout: acceptance criteria and verification evidence reviewed; final validation gates pending. |

### IR-CLI-041 — CLI workflow read projections and work-order profiles

| Field | Value |
| --- | --- |
| Type | interface |
| Target | v2.3.0 |
| Status | verified |
| Priority | critical |
| Tags | v2.3.0, cli, workflow, projection, work-order |
| Risk | high |
| Stability | evolving |
| Verification Method | test |
| GitHub Issue | - |
| Related Docs | [docs/research/230plus.md](../research/230plus.md), [docs/research/10.kiwi-planner-jsonl-tooling-research.md](../research/10.kiwi-planner-jsonl-tooling-research.md) |

#### Requirement

SpecKiwi CLI must expose public read-only workflow projection command names and work-order output profiles that delegate to shared workflow read, validator, and work-order services without introducing duplicate selectors or mutation behavior.

#### Rationale

This requirement supersedes the read/profile portion of IR-CLI-039 and removes pipeline emit mutation naming from the read projection contract. Doc230 Plus recommends diagnostic projections and context packing as read-only or work-order profiles.

#### Acceptance Criteria

- [x] AC-1: CLI read-only projection commands include workflow doctor, workflow diff, workflow schema-check or plan validate, pipeline status, pipeline tail, and optional pipeline compact alias, all returning the compact workflow-read envelope without raw bodies by default.
- [x] AC-2: CLI work-order exposes explain and compact context profiles through the canonical work-order next command, including decisionTrace, rejected candidates, blockers, artifact references, and source hashes or mtimes generated by the work-order engine.
- [x] AC-3: CLI projection and profile commands fail closed on invalid artifacts, unsupported schema versions, dependency blockers, active FAILED or NEEDS_USER pipeline state, and ambiguous workspace or target state.
- [x] AC-4: CLI tests prove projection aliases produce decisions equivalent to canonical read or work-order commands, no duplicate selector is introduced, and raw artifact bodies require explicit include-body opt-in.
- [x] AC-5: This requirement does not define any mutating pipeline emit behavior; official JSONL append command naming belongs to the separate CLI mutation requirement that depends on FR-NODE-030.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |
| VE-1 | plan | docs/plans/2026-06-29.specwkiki.v230-plan.plan.md#T-PH013-02 | all | - |
| VE-2 | test | test/cli/workflow-read-commands.test.ts; test/cli/workflow-work-order.test.ts | all | Covers CLI workflow doctor/diff/schema-check, pipeline nested status/tail/compact aliases, compact raw-body omission, and work-order explain/compact/context profiles with decisionTrace, rejected candidates, blockers, artifact references, and source hashes. |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |
| Requirement | IR-CLI-039 | supersedes | Supersedes read/profile portions of IR-CLI-039; pipeline emit is split out. re_stated_from: IR-CLI-039#AC1, IR-CLI-039#AC2, IR-CLI-039#AC4, IR-CLI-039#AC5 |
| Requirement | OPS-FLOW-001 | depends_on | v2.3.0 tool improvement research anchor. |
| Requirement | IR-CLI-031 | extends | Names public read-only projection commands over CLI workflow reads. |
| Requirement | IR-CLI-032 | extends | Adds explain and context profiles to the canonical work-order command. |
| Requirement | REL-NODE-006 | depends_on | CLI projections consume core diagnostic projection contracts. |
| Code | src/cli/index.ts:14-20 | related_to | Current CLI registration has no workflow or pipeline command family. |
| Code | src/cli/commands/read.ts:53-147 | related_to | Current CLI read extension point. |
| Task | T-PH013-02 | implements | - |

#### Research / Analysis

- Derived from docs/research/230plus.md sections 5.1 through 5.4, 5.8, 5.9, and 7. It deliberately excludes pipeline emit because that is a mutation surface.

#### Implementation Notes

- Implement after IR-CLI-031, IR-CLI-032, and REL-NODE-006 are testable. Do not edit Kiwi skill text to rely on these names until runtime commands exist.

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-06-28 | Created | add-requirement |
| 2026-06-29 | Status -> implemented | Implemented CLI read-only workflow projection aliases and work-order profile options without adding mutation behavior. |
| 2026-06-30 | Status -> verified | v2.3.0 closeout: acceptance criteria and verification evidence reviewed; final validation gates pending. |

### IR-CLI-042 — CLI workflow mutation commands and pipeline emit

| Field | Value |
| --- | --- |
| Type | interface |
| Target | v2.3.0 |
| Status | verified |
| Priority | critical |
| Tags | v2.3.0, cli, workflow, mutation, checklist |
| Risk | high |
| Stability | evolving |
| Verification Method | test |
| GitHub Issue | - |
| Related Docs | [docs/research/10.kiwi-planner-jsonl-tooling-research.md](../research/10.kiwi-planner-jsonl-tooling-research.md), [docs/research/09.mcp-cli-tool-abandonment-research.md](../research/09.mcp-cli-tool-abandonment-research.md), [docs/research/230plus.md](../research/230plus.md) |

#### Requirement

SpecKiwi CLI must provide guarded workflow progress mutation commands and an official pipeline emit command using the shared mutation envelope, the FR-NODE-028 canonical journal identity, and the FR-NODE-030 guarded mutation core services.

#### Rationale

This requirement supersedes IR-CLI-033 to correct journalKey semantics and takes the mutating pipeline emit surface out of IR-CLI-039. CLI mutation naming must be separate from read-only projections.

#### Acceptance Criteria

- [x] AC-1: CLI registers workflow mutation commands for plan task check and uncheck, checklist item checked-state updates, PM task status updates, pipeline JSONL append, worklog JSONL append, and workflow repair record creation.
- [x] AC-2: Each CLI workflow mutation command supports dry-run and returns the shared mutation envelope, including mutation.kind, workflow.runId, workflow.taskId when task-scoped, workflow.owner, journalKey computed exactly by FR-NODE-028, journalState, operations, stale guards, written, result, pendingRepair, diagnostics, and diagnosticsSummary.
- [x] AC-3: Commands reject stale plan or sidecar hashes, stale PM state, forbidden child-coder ownership, invalid task IDs, invalid JSONL records, and dependency-blocked writes before mutating display state.
- [x] AC-4: Commands treat already-applied checkbox, checklist, task-status, and append-idempotency states as successful no-op results with mutation.written false and use canonical journal states planned, applied, confirmed, failed, and skipped_dry_run.
- [x] AC-5: CLI pipeline emit validates event schema, resolves the canonical pipeline path, serializes one JSON object per line, enforces trailing LF, dedupes by parsed skill and run_id, supports dry-run no-write behavior, and returns the shared mutation envelope.
- [x] AC-6: CLI tests cover dry-run no-write, already checked, stale hash, forbidden owner, worklog append failure, guarded plan write failure, final PM confirmation failure, invalid JSONL append, repair-record creation, and pipeline emit parsed-dedupe behavior.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |
| VE-1 | plan | docs/plans/2026-06-29.specwkiki.v230-plan.plan.md#T-PH014-02 | all | - |
| VE-2 | test | test/cli/workflow-mutation-commands.test.ts; test/core/workflow/mutation.test.ts | all | Covers CLI workflow mutation commands for task check/uncheck, checklist-set, PM task status update, pipeline/worklog append, repair-record creation, dry-run/no-op/write states, stale/dependency/owner guards, invalid JSONL failure, and parsed pipeline dedupe. |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |
| Requirement | IR-CLI-033 | supersedes | Supersedes IR-CLI-033 with FR-NODE-028 journal identity; re_stated_from: IR-CLI-033#AC1, IR-CLI-033#AC2, IR-CLI-033#AC3, IR-CLI-033#AC4, IR-CLI-033#AC5 |
| Requirement | IR-CLI-039 | supersedes | Supersedes the pipeline emit portion of IR-CLI-039; re_stated_from: IR-CLI-039#AC3 |
| Requirement | OPS-FLOW-001 | related_to | Research anchor for workflow mutation tooling. |
| Requirement | FR-NODE-030 | depends_on | CLI commands expose guarded core workflow progress mutations. |
| Requirement | FR-NODE-028 | depends_on | CLI journal identity must include run_id. |
| Requirement | FR-NODE-017 | depends_on | CLI outputs use the shared mutation envelope. |
| Requirement | IR-CLI-031 | depends_on | CLI mutation commands reuse workflow artifact resolution and read context from CLI readers. |
| Requirement | REL-NODE-003 | depends_on | Mutation commands rely on workflow artifact validation before writes. |
| Code | src/cli/commands/mutations.ts:1-270 | related_to | CLI mutation command registration surface for adding workflow progress and pipeline emit commands. |
| Code | skills/codex/_shared/kiwi/pipeline-event.md:9-63 | related_to | Existing skill-local pipeline event schema to replace with official CLI append surface. |
| Task | T-PH014-02 | implements | - |

#### Research / Analysis

- Derived from docs/research/10.kiwi-planner-jsonl-tooling-research.md and refined by docs/research/230plus.md sections 5.6 and 5.7. Pipeline emit is an official append surface, not a read projection.

#### Implementation Notes

- Implement after FR-NODE-030 exposes tested core mutation services. Keep JSON output and dry-run behavior equivalent to MCP workflow mutation tools.

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-06-28 | Created | add-requirement |
| 2026-06-29 | Status -> implemented | Implemented guarded CLI workflow mutation commands and pipeline/worklog append surfaces over the shared mutation core. |
| 2026-06-30 | Status -> verified | v2.3.0 closeout: acceptance criteria and verification evidence reviewed; final validation gates pending. |

### IR-CLI-043 — CLI logical workflow deletion with canonical mutation dependency

| Field | Value |
| --- | --- |
| Type | interface |
| Target | v2.3.0 |
| Status | verified |
| Priority | critical |
| Tags | v2.3.0, cli, workflow, jsonl, deletion |
| Risk | high |
| Stability | evolving |
| Verification Method | test |
| GitHub Issue | - |
| Related Docs | [docs/research/10.kiwi-planner-jsonl-tooling-research.md](../research/10.kiwi-planner-jsonl-tooling-research.md), [docs/research/11.v2.3.0-tooling-code-improvement-plan.md](../research/11.v2.3.0-tooling-code-improvement-plan.md), [docs/research/230.md](../research/230.md), [docs/research/230plus.md](../research/230plus.md) |

#### Requirement

SpecKiwi CLI must provide guarded workflow logical-delete commands that mark supported plan, PM state, pipeline, worklog, and repair records as `deleted` through the shared workflow mutation envelope without physically deleting audit history, using the canonical IR-CLI-042 mutation command contract.

#### Rationale

This requirement supersedes IR-CLI-034 so active CLI logical-delete work no longer extends the discarded IR-CLI-033 mutation contract. The behavior remains the same, but the dependency is now the canonical Doc230 Plus CLI mutation successor.

#### Acceptance Criteria

- [x] AC-1: CLI registers workflow logical-delete commands or subcommands for supported record classes, including plan display/checklist records, PM task records, pipeline events, worklog events, and workflow repair records when those classes are implemented by core services.
- [x] AC-2: Each CLI logical-delete command requires or records a deletion reason, supports --dry-run and --json, and returns the shared mutation envelope with mutation.kind, workflow identity, target record identity, desired effective state `deleted`, canonical journalKey, journalState, operations, stale guards, written, result, pendingRepair, diagnostics, and diagnosticsSummary.
- [x] AC-3: CLI logical-delete commands never physically remove JSONL lines or Markdown history during normal operation; they append or write guarded tombstone/repair state according to FR-NODE-031.
- [x] AC-4: Commands treat already-deleted records as successful no-op results with mutation.written false unless the caller explicitly requests an additional repair note.
- [x] AC-5: CLI read commands expose a way to include or inspect deleted records while default compact workflow reads treat deleted records as inactive.
- [x] AC-6: CLI tests cover dry-run no-write, successful logical delete, already-deleted no-op, stale hash rejection, forbidden owner, invalid JSONL rejection, non-deletable record rejection, tombstone/correction-of-tombstone rejection, status=deleted rejection, includeDeleted read inspection, and parity with MCP logical-delete behavior.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |
| VE-1 | plan | docs/plans/2026-06-29.specwkiki.v230-plan.plan.md#T-PH015-02 | all | - |
| VE-2 | test | test/cli/workflow-mutation-commands.test.ts; test/cli/workflow-read-commands.test.ts; test/core/workflow/jsonl.test.ts; test/core/workflow/mutation.test.ts | all | Covers CLI logical-delete write/no-op/no-physical-removal behavior, stale and forbidden-owner rejection, invalid JSONL and non-deletable/tombstone/status=DELETED rejection through core tests, and includeDeleted read inspection. |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |
| Requirement | IR-CLI-034 | supersedes | Supersedes IR-CLI-034 to replace dependency on discarded IR-CLI-033; re_stated_from: IR-CLI-034#AC1, IR-CLI-034#AC2, IR-CLI-034#AC3, IR-CLI-034#AC4, IR-CLI-034#AC5, IR-CLI-034#AC6 |
| Requirement | OPS-FLOW-001 | related_to | Research anchor for workflow deletion tooling. |
| Requirement | FR-NODE-031 | depends_on | CLI commands expose canonical core logical deletion semantics. |
| Requirement | IR-CLI-042 | extends | Logical deletion is a canonical guarded workflow mutation command family. |
| Requirement | FR-NODE-017 | depends_on | CLI output uses the shared mutation envelope. |
| Requirement | REL-NODE-003 | depends_on | CLI deletion relies on workflow artifact validation. |
| Document | docs/research/230.md | derived_from | Follow-up decision review for CORRECTION, deleted effective state, includeDeleted read boundary, and restore exclusion. |
| Document | docs/research/230plus.md | derived_from | Successor dependency cleanup after Doc230 Plus max evaluation. |
| Task | T-PH015-02 | implements | - |

#### Research / Analysis

- Derived from planner JSONL tooling research, docs/research/230.md decision review, and Doc230 Plus successor dependency cleanup. This replaces IR-CLI-034 only to remove active dependency on discarded IR-CLI-033.

#### Implementation Notes

- Implement after FR-NODE-031 and IR-CLI-042. Keep payloads equivalent to MCP logical-delete tools.

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-06-28 | Created | add-requirement |
| 2026-06-29 | Status -> implemented | Implemented CLI workflow logical-delete mutation and includeDeleted read inspection support. |
| 2026-06-30 | Status -> verified | v2.3.0 closeout: acceptance criteria and verification evidence reviewed; final validation gates pending. |

### IR-CLI-044 — CLI Requirement ID collision repair commands

| Field | Value |
| --- | --- |
| Type | interface |
| Target | v2.3.0 |
| Status | verified |
| Priority | critical |
| Tags | v2.3.0, cli, requirement-id, collision-repair, merge |
| Risk | high |
| Stability | evolving |
| Verification Method | test |
| GitHub Issue | - |
| Related Docs | [docs/research/230.md](../research/230.md) |

#### Requirement

SpecKiwi CLI must expose guarded Requirement ID collision repair commands that delegate to the FR-NODE-032 core service and provide diagnose, plan, and apply flows for merge-time duplicate Requirement IDs without supporting arbitrary renumbering.

#### Rationale

Doc230 recommends a CLI diagnose/plan/apply workflow because users most often encounter duplicate Requirement IDs while resolving branch or worktree merges. The CLI must make repair explicit and previewable instead of encouraging manual Markdown renumbering.

#### Acceptance Criteria

- [x] AC-1: CLI registers repair requirement-id-collisions diagnose, repair requirement-id-collisions plan, and repair requirement-id-collisions apply commands or documented equivalent names with JSON output support.
- [x] AC-2: Plan and apply require an explicit plan file or explicit occurrence mapping using duplicateId plus keep and rename occurrence identity; a duplicate ID alone is never enough to write.
- [x] AC-3: The command surface supports dry-run previews, replacement-id or allocate-next selection, write-plan output, explicit reference-edit entries, optional base/ours/theirs provenance hints, and --ignore-lock scoped only to SRS lock bypass.
- [x] AC-4: CLI JSON output includes the shared mutation envelope, grouped duplicate diagnostics, generated replacement ID, patch preview, ambiguity report, lock diagnostics, touched files, written state, and retry guidance.
- [x] AC-5: CLI refuses writes when validation has no selected SRS-E002 duplicate, Git conflict markers remain, occurrence identity is stale, inbound references are ambiguous without explicit edits, generated replacement IDs collide, or non-lock safety checks would be bypassed.
- [x] AC-6: CLI tests prove no-write dry-run behavior, same-file and cross-file repairs, explicit mapping requirements, ambiguous-reference failure, stale snapshot failure, active lock and ignore-lock behavior, idempotent rerun behavior, and fixture outputs needed for MCP parity comparisons.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |
| VE-1 | plan | docs/plans/2026-06-29.specwkiki.v230-plan.plan.md#T-PH017-02 | all | - |
| VE-2 | test | test/cli/requirement-id-repair-command.test.ts; test/cli/global-options-help.test.ts | all | CLI diagnose, plan, write-plan, apply, validate-clean, active lock, and ignore-lock apply-only coverage. |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |
| Requirement | FR-NODE-032 | depends_on | CLI commands delegate to the shared core collision repair service. |
| Requirement | REL-PARSE-003 | depends_on | CLI diagnose and validation output consume grouped SRS-E002 diagnostics. |
| Requirement | IR-CLI-028 | depends_on | CLI mutation JSON must preserve safe preview and mutation envelope semantics. |
| Requirement | IR-CLI-038 | depends_on | CLI repair apply exposes SRS lock controls with lock-only ignore behavior. |
| Requirement | FR-MCP-039 | related_to | MCP adapter owns CLI-equivalence parity over the same repair fixtures. |
| Requirement | OPS-FLOW-001 | depends_on | v2.3.0 tool improvement research anchor. |
| Code | src/cli/commands/mutations.ts:1-270 | related_to | Current CLI mutation command registration surface. |
| Code | src/cli/commands/read.ts:53-147 | related_to | Current CLI read and validation command surface for diagnostics parity. |
| Task | T-PH017-02 | implements | - |

#### Research / Analysis

- Derived from docs/research/230.md sections 16.4, 16.8, 16.10, and 16.11.

#### Implementation Notes

Register the CLI surface only after FR-NODE-032 is testable. --ignore-lock belongs only to apply/write repair commands; diagnose and plan stay read-only and must reject or report it ignored. Do not add --force or any bypass for stale, validation, trace, table-cell, or generated-ID checks.


#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-06-29 | Created | add-requirement |
| 2026-06-29 | Clarified CLI and MCP parity ownership | Max evaluation found CLI should emit parity-ready fixtures while FR-MCP-039 owns MCP equivalence checks without creating implementation ordering noise. |
| 2026-06-29 | Status -> implemented | Implemented CLI repair requirement-id-collisions diagnose plan apply commands. |
| 2026-06-30 | Status -> verified | v2.3.0 closeout: acceptance criteria and verification evidence reviewed; final validation gates pending. |

### IR-CLI-045 — mcp 명령 cwd 전용 root 해석과 root 옵션 거부

| Field | Value |
| --- | --- |
| Type | interface |
| Target | v2.3.0 |
| Status | implemented |
| Priority | high |
| Tags | v2.3.0, cli, mcp, root, safety, user-directive |
| Risk | high |
| Stability | evolving |
| Verification Method | test |
| GitHub Issue | - |
| Related Docs | - |

#### Requirement

SpecKiwi는 `speckiwi mcp` 명령으로 stdio MCP 서버를 시작하되, root 옵션(`--root`)을 지원하지 않아야 한다. project root는 서버 프로세스의 current working directory 기반 상위 탐색으로만 해석하며, root 옵션이 지정되면 서버를 시작하지 않고 usage 오류로 종료한다.

#### Rationale

사용자 지시(2026-07-06)로 MCP 표면에서 root 파라미터를 전면 제거한다. 명시 root로 서버를 띄우면 의도치 않은 workspace를 조용히 변이할 위험이 있고, per-call root fail-closed 거부(REL-MCP-003 계열)와 일관되게 기동 시에도 fail-loud가 되어야 한다. 3인 결정 위원회(거버넌스/구현 리스크/사용자 의도)가 범위 A(사용자 표면 제거, 내부 DI seam 유지)·error 동작·v2.3.0 귀속을 만장일치/다수결로 채택했다.

#### Acceptance Criteria

- [x] AC-1: `speckiwi mcp`는 기본 transport로 stdio를 사용한다.
- [x] AC-2: `speckiwi mcp --transport stdio`는 명시적으로 stdio transport를 사용한다.
- [x] AC-3: MCP mode에서 stdout에는 MCP JSON-RPC 메시지만 출력한다.
- [x] AC-4: 로그는 stderr에 출력한다.
- [x] AC-5: `speckiwi mcp`는 현재 작업 디렉터리 기준 상위 탐색(.git 또는 docs/spec/00.index.md)으로만 project root를 해석한다.
- [x] AC-6: MCP server startup 시 docs/spec/00.index.md가 없으면 tool 호출 처리 전에 init과 같은 SRS 기본 구조를 자동 생성한다.
- [x] AC-7: `speckiwi --root <path> mcp` 또는 `speckiwi mcp --root <path>`는 서버를 시작하지 않고 exit code 2의 usage 오류로 종료하며, cwd 기반 실행 안내 메시지를 stderr에 출력한다.
- [x] AC-8: `startMcpServer`의 옵션 타입(McpServerOptions)은 root 파라미터를 노출하지 않는다.
- [x] AC-9: `speckiwi mcp --help` 출력은 --root 옵션을 안내하지 않는다.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |
| VE-1 | test | test/cli/mcp-command.test.ts; test/cli/global-options-help.test.ts; test/mcp/startup-cwd.test.ts; test/mcp/stdio-purity.test.ts | all | - |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |
| Requirement | IR-CLI-017 | supersedes | - |
| Requirement | IR-CLI-002 | related_to | - |
| Requirement | IR-CLI-025 | related_to | - |
| Requirement | REL-MCP-004 | related_to | - |

#### Research / Analysis

- -

#### Implementation Notes

- IR-CLI-017을 supersede한다(AC-1~4·AC-6 계승, --root 관련 AC-5·AC-7 대체). IR-CLI-002·IR-CLI-025의 공통 옵션 상속 요구에서 mcp 서브커맨드는 예외가 된다. 내부 McpDependencies.root(테스트/doctor 전용 DI seam)는 사용자 표면이 아니므로 유지한다.

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-07-06 | Created | add-requirement |
| 2026-07-06 | Status -> implemented | mcp --root 제거 구현·TDD green(70/70)·독립 검증자 PASS. 전체 release-check는 별건 v3.0.0 WIP 빌드 오류로 미실행이라 verified 보류. |

## 5. Cross-scope Dependencies

| From | To | Relation | Notes |
|---|---|---|---|
| CLI | PARSE | depends_on | 조회와 검증은 parser와 validator를 사용한다. |
| CLI | NODE | depends_on | CLI adapter는 Node.js package 안에 구현된다. |
| FLOW | CLI | depends_on | workflow는 CLI 명령 예시를 사용한다. |

## 6. Open Questions

| ID | Question | Impact | Status |
|---|---|---|---|
| - | - | - | - |

## 7. Change Notes

| Date | Change | Reason |
|---|---|---|
| 2026-05-07 | 최초 작성 | SpecKiwi v1.0.0 scope 요구사항 정의 |
| 2026-05-08 | `add-requirement` 명령 요구사항 추가 | Markdown SRS에 새 Requirement Block을 안전하게 추가하는 CLI mutation 정의 |
| 2026-05-09 | root 없는 MCP 등록 요구사항 추가 | `speckiwi mcp`가 `--root` 없이 current working directory 기준으로 동작하도록 명시 |
| 2026-05-10 | active target CLI 요구사항 추가 | `00.index.md`의 현재 작업 목표 버전을 CLI로 관리하도록 명시 |
| 2026-05-10 | v1.2.0 CLI hardening 요구사항 추가 | CLI diagnostics/summary parity와 Completed Work Log latest/prevalidation 권고 반영 |
| 2026-05-11 | v2.1.0 target registration 요구사항 추가 | 새 목표 버전 등록과 Active Target 기반 SRS 작성 workflow를 CLI에 반영 |
| 2026-05-13 | v2.1.1 report path CLI 요구사항 추가 | add-completed-work 완료 보고서 경로 옵션 계약 정의 |
