> This file is an append-only Completed Work Log summary. It is NOT a source of truth for completion; see each Requirement Block (Status, Acceptance Criteria, Verification Evidence, Change Notes).

## 7. Completed Work Log

| Date | Target | Scope | Requirement IDs | Summary | Report Paths |
|---|---|---|---|---|---|
| 2026-07-16 | v2.4.0 | PARSE, CLI, NODE, FLOW | FR-PARSE-032, FR-PARSE-033, FR-NODE-071, FR-NODE-072, FR-NODE-073, FR-NODE-074, FR-NODE-075, IR-CLI-071, IR-CLI-072, FR-FLOW-036, FR-FLOW-037 | [tdd-first-mode] User-directed full pipeline (research docs/research/tddmode -> SRS -> TDD implementation -> verification). Added the tdd work-mode: StepStateMode/parser/work-mode/CLI mode accept tdd with vibe-parity Active Task (FR-PARSE-032, FR-NODE-071, IR-CLI-071); enforcement gates extended to tdd — vibe-gate checks synthesis + design.md, completion hard-gate enforced (IR-CLI-072, FR-NODE-072, fixing research finding F1 CRITICAL); SDS-MD Authoring Rules v1.0.0 with design.md template (FR-FLOW-036); synthesis merges design.md as a redacted Design section (FR-NODE-073); promote refuses evidence-less blocks in tdd mode with EVIDENCE_REQUIRED, advisory otherwise (FR-NODE-074); agent snippet v1.5 documents the tdd cycle, three gates, and the sdd boundary, atomically rolled out to repo CLAUDE.md/agents.md/appendix (FR-NODE-075); validate_step emits SDS-W050..W053 advisories in tdd mode on CLI+MCP (FR-PARSE-033); kiwi-tdd skill x3 variants orchestrates SDS->red->green->regression->promote and is package-doctor registered (FR-FLOW-037). Decisions by 5-lens committee (Q1 enum+G9 5:0, Q2 steps design.md 5:0, Q3 sdd boundary 5:0, Q4 v2.4.0 without Active Target flip 3:2, Q5 MVP+skill 4:1). Verification: TDD red->green for all 11 REQs (12 new test files / 53 cases); full suite 189 files/1208 pass 0 regressions; typecheck+lint+validate clean; 2-axis independent verification — SRS-intent ALL_MATCH (42/43, LOW fixed) + code quality PASS (MEDIUM pre-existing dormant gate noted in FR-NODE-072 implementation notes). Follow-ups: G5 diff_steps re-exposure, G10 SidecarTask tdd runtime enforcement, completion-gate mutation wiring. | docs/analysis/tdd-first-mode-2026-07-16/report.md, docs/research/tddmode/00.tdd-first-mode-research.md |
| 2026-07-16 | v2.4.0 | MCP | FR-MCP-052 | [tdd-first-mode/mcp] User-directed follow-up: the tdd work-mode toggle was CLI-only, so MCP-first agents could not turn TDD First mode on. Added get_work_mode (read-only, fail-open to wait, returns Active Task for vibe/tdd) and set_work_mode (workspace mutation, sdd/vibe/wait/tdd + optional activeTask/dryRun, INVALID_MODE guard mirroring IR-CLI-048/071) — registry rows (mode node hosts get, vibe-gate check leaf hosts set per MCP-only hosting precedent), zod toolSchemas, read-only classification, handlers forwarding to the FR-NODE-050/071 core, appendix tool-reference rows (REL-FLOW-002 parity), and kiwi-tdd skill x3 updated to prefer MCP with CLI fallback. TDD red->green (work-mode-tools.fr-mcp-052.test.ts, 4 ACs); full suite 190 files/1213 pass 0 fail; typecheck+lint clean; FR-MCP-052 verified with evidence. | docs/analysis/tdd-first-mode-2026-07-16/report.md |
| 2026-07-16 | v2.4.0 | NODE | FR-NODE-076, FR-NODE-077 | [tdd-first-mode/init] User-directed follow-up: the CLAUDE.md content init inserts predated the MCP work-mode tools and referenced SDS rules that init never shipped. FR-NODE-076: init now installs the bundled docs/rule/SDS-MD-Rules-v1.0.0.md (loadBundledSdsRulesDocument with ENOENT fallback, writeIfMissing + dryRun parity with the SRS-MD rules) and the npm files whitelist packages it. FR-NODE-077: agent snippet bumped to v1.6 — work-mode section is now MCP-first (get_work_mode read, set_work_mode switch, CLI speckiwi mode fallback), documents mode switching (any-to-any across sdd/vibe/wait/tdd, stale Active Task drop on switching to sdd/wait, INVALID_MODE rejection), and cites the installed SDS rules path; v1.5 tdd cycle/gates/boundary retained; idempotent v1.5-to-v1.6 upsert. Repo CLAUDE.md/agents.md/appendix synced; FR-NODE-075 suite refactored to the version constant (v1.5 literal superseded per FR-FLOW-016 AC-2); version-literal suites updated. TDD red->green (init-sds-rules 3 ACs + agent-snippet-v16 4 ACs); full suite 192 files/1220 pass 0 fail; typecheck+lint+validate clean. | docs/analysis/tdd-first-mode-2026-07-16/report.md |
| 2026-07-16 | v2.4.0 | CLI, MCP, NODE, FLOW | IR-CLI-073, IR-CLI-074, FR-MCP-053, FR-MCP-054, FR-NODE-078, FR-NODE-079, FR-NODE-080, FR-NODE-081, FR-NODE-082, FR-NODE-083, FR-FLOW-038 | [sds-tooling-gaps] User-directed: implement the G1-G10 gaps from docs/research/tddmode/05.sds-tooling-gaps.md (G4 orchestrator integration deferred pending the tdd_policy/work-mode unification design per 5-lens committee D2:A 4:1). G1 synthesis exposure: speckiwi step synthesize (IR-CLI-073) + synthesize_step_srs MCP tool (FR-MCP-053) surface the previously caller-less idempotent engine. G2 CLI parity: step claim/update-state/promote mirrors with registry mcpName relocation off the set-supersede/scaffold-scope/register-scopes host rows (IR-CLI-074, committee D1:A) - the tdd cycle no longer requires MCP. G3 completion gate wired: updateStepState merged transition now evaluates evaluateVibeCompletionGate over the step TouchesReq-closure dirty edges in vibe/tdd with COMPLETION_GATE_BLOCKED + acknowledged override, MCP exposes acknowledged (FR-NODE-078). G5 visibility: listSteps/list_steps emit sdsPresent/sdsStatus via parallel design.md loads (FR-NODE-079). G6: vibe-gate check extracted to core evaluateVibeGate shared by the CLI and the new read-only check_vibe_gate MCP tool (FR-MCP-054). G7 scaffold: step scaffold / scaffold_step creates design.md (template shares REQUIRED_SDS_HEADINGS with the validator - no drift) + intent.md stubs via writeIfMissing with INVALID_STEP_NAME guard (FR-NODE-080, committee D3:A stub-only framing). G8: step sds-status / set_sds_status enforces the forward-only draft-agreed-superseded lifecycle with single-cell patch (FR-NODE-081, committee D4:A). G9 doctor: existence-only SDS rules check, no index coupling (FR-NODE-082, committee D5:A 5:0). G10: source-derived codex mirror drift doctor check with EOL-normalized compare + .agents/skills regenerated via speckiwi skills install codex all (16 skills in sync, kiwi-step/kiwi-tdd/kiwi-wave-master restored; FR-NODE-083, committee D6:B). FR-FLOW-038 narrows the kiwi-step/kiwi-tdd x3 contracts: claim MCP-preferred + step CLI fallback, scaffold-as-stub with content still directly authored, synthesize/scaffold cited in the tdd cycle, merged gate documented. Verification: TDD red-green for all 11 REQs (10 new test files / 47 cases + FLOW content suite); 2-axis independent verification - SRS-intent ALL_MATCH (LOW-1 depends-on CLI coverage closed immediately) + code quality PASS with MEDIUM list_steps sequential I/O parallelized and LOW task-name guard + EOL compare fixed then re-verified green; final suite 203 files / 1271 pass / 0 fail; typecheck + lint + validate clean. | docs/research/tddmode/05.sds-tooling-gaps.md |
| 2026-07-17 | v2.4.0 | PARSE, MCP, NODE, FLOW | FR-FLOW-039, FR-FLOW-040, FR-PARSE-033, FR-NODE-081, FR-NODE-083, FR-MCP-054 | [tdd-followups] User-directed: resolve all followups from docs/next/2026-07-16.tdd-mode-followups.md and adopt v2.4.0. G4 orchestrator integration per committee model A (5:0, docs/research/tddmode/06.g4-orchestrator-integration.md): FR-FLOW-039 kiwi-pipeline §2.8 work-mode routing gate (get_work_mode MCP-first, `speckiwi mode` CLI fallback, fail-open wait; tdd+step-scoped routes to kiwi-tdd; §2.5 five-stage chain untouched) and FR-FLOW-040 tdd_policy default derivation via the new _shared/kiwi/workmode-policy.md SSOT x3 variants (tdd derives strict, sdd/wait/vibe relaxed, disabled never derived, explicit flag wins with WARN; kiwi-pm non-HALT mismatch warning, §7.1 disabled HALT unchanged) — 2 new content test files, 18 cases red-to-green. P3 minors: FR-MCP-054 AC-4 suite citation corrected to IR-CLI-049 (temporary verified downgrade, wording-only); FR-PARSE-033 AC-5 loadStepDesign unsafe-step-name guard via shared src/core/step-name.ts (avoids the validate-scoped/scaffold-step/templates import cycle); FR-NODE-081 AC-6 set-sds-status padding preservation (committee Q3 4:1 — value-token-only substitution); FR-NODE-083 AC-5/6 mirror-exclusions manifest (committee Q7:B 5:0 resolving the doctor-16-ok vs hermeticity-sentinel contradiction: .agents/skills/.speckiwi-mirror-exclusions.json excludes kiwi-step/kiwi-wave-master, manifest-sentinel consistency test, repo steady-state doctor ok durable — 14 skills; 2 excluded by manifest) plus a default-bundled-source integration test and mirror regeneration. Committee also decided: SDS-W051 partial heading match kept (Q4 4:1, noted in FR-PARSE-033); diff_steps stays discarded with the factually-wrong discard reason corrected in FR-MCP-044 research notes (Q5 5:0, workflow_diff is plan/worklog diff, not step-conflict diff); SidecarTask tdd stays skill-side — runtime mutation is a line-level markdown checkbox patch and sidecar JSON is never re-serialized, so no tdd-field data-loss path exists (Q6 5:0). Release (committee Q2): Active Target moved to v2.4.0 (the 85 implemented-not-verified requirements remain in v2.3.0.1 as an explicit verification backlog reachable via --target), package version bumped to 2.4.0; main fast-forward merge and tag 2.4.0 deferred to user return; npm publish prepared (pack dry-run) but delegated to the user as the sole irreversible outward step. Verification: TDD red-to-green per REQ; 2-axis independent verification SRS-intent ALL_MATCH (13/13 ACs) + quality PASS (0 CRITICAL/HIGH, lint+typecheck clean); full suite 206 files / 1295 pass / 1 skip / 0 fail. | docs/research/tddmode/06.g4-orchestrator-integration.md |
| 2026-07-18 | v2.4.0 | FLOW | FR-FLOW-041, FR-FLOW-020 | [tdd-traceability] User-directed follow-up (from the "does kiwi-tdd number code?" question): close the tdd path's code-traceability double-gap. Investigation (docs/research/tddmode/07.tdd-traceability-tags.md) found the tdd path writes production code at Phase 4 (green) but promotes requirements with NEITHER @req code comments NOR SRS Trace Links (Code anchor), while promote_step_requirement copies the block byte-for-byte so the body ID is confirmed only at Phase 6. Committee 2026-07-18 (5:0): Q1=A (Phase 6 post-promote back-annotation reusing FR-FLOW-020's vibe task-name-then-reconcile machinery, drift-free; cite kiwi-coder §0.17 for format/exemption only, no operational-hook import) + Q2=Yes (restore the SRS Code anchor via add_trace_link, the traceability SSOT) + Q3 (extend the FR-FLOW-020 convention SSOT to tdd scope + new FR-FLOW-041 for the skill behavior; do NOT reopen verified FR-FLOW-037). FR-FLOW-041: kiwi-tdd x3 variants gain a Phase 6 traceability step — add_trace_link(type=code, relation=implements) authoritative Code anchor + @req reconcile (task-name -> promoted REQ-ID) auxiliary breadcrumb, BOTH non-gating (never block promote, separate from FR-NODE-074 EVIDENCE_REQUIRED), applied only after promote confirms the ID; Phase 4 records touched production files. FR-FLOW-020 extended: title/Requirement/AC-4 now cover tdd mode (task-name during green, reconcile at synthesis/promote); stays implemented (AC-4 joins the v2.3.0.1 backlog). Verification: TDD red 12 -> green 12 (test/skills/kiwi-tdd-traceability-content.test.ts, 3 variants x 4 ACs); 2-axis independent verification SRS-intent ALL_MATCH (4/4 ACs, tdd-AC4<->vibe-AC2 consistent) + quality PASS (0 CRITICAL/HIGH; operational-hook import clean-checked, 3-variant semantic parity, mirror byte-identical); MEDIUM (FR-FLOW-020 Change Notes ledger row) + LOW-1 (add_trace_link row in the §4 dependency table x3) fixed and re-tested; mirror regenerated (sentinel-reserved kiwi-step/kiwi-wave-master kept out per Q7 manifest) with doctor ok (14 skills; 2 excluded); validate_spec clean. | docs/research/tddmode/07.tdd-traceability-tags.md |
| 2026-07-28 | v2.4.2 | FLOW, MCP | FR-FLOW-042, FR-FLOW-043, FR-MCP-055 | [run-root-integrity] kiwi-pipeline --cycle --auto from docs/research/12: wave/pipeline runs now fail closed on a run-root mismatch instead of silently writing into the wrong repository. FR-FLOW-042 adds a kiwi-wave-master preflight gate (normalized comparison of mcp_workspace_info.workspaceRoot against the run git root, critical halt --auto cannot bypass, both recovery paths plus the no-new-REQ-ID and host-docs/spec caveats, epic entry covered, workspaceRoot lookup failure treated as mismatch with no CLI fallback, --wt delegation refused) in all three shipped variants. FR-FLOW-043 repairs the unreachable POSIX fallback in all eight shipped event-contract copies (git failure now actually reaches the cwd/home fallbacks instead of targeting /kiwi outside the repository), states run-start root pinning, registers kiwi-wave-master in the skill enum of four pipeline-event copies so a halt event is not WARN+skipped, and pins the halt-emit contract (no waves.jsonl append, exactly one FAILED pipeline event resolved from the run's own cwd chain). FR-MCP-055 replaces the recovery hint that named the unregistered restart_mcp_server with actionable guidance while keeping REL-MCP-004 AC-3 fail-closed rejection byte-identical. Verification: TDD red-green per phase (28/21/1 failures before green); prickly-review two rounds surfaced 16 findings including the HIGH Windows path-separator defect that would have halted every legitimate run, all closed with 13 mutation-proofs; full suite 210 files / 1393 passed / 1 skipped / 0 failed; typecheck, lint, build exit 0; validate --fail-on-warning 0/0; package version 2.4.1 to 2.4.2. | docs/research/12.kiwi-wave-master-worktree-isolation-research.md, docs/plans/2026-07-28.speckiwi.v242.plan.md |
| 2026-07-30 | v2.5.0 | NODE, PARSE | FR-NODE-051, FR-NODE-085, FR-NODE-086, FR-NODE-087, FR-NODE-088, FR-NODE-089, FR-NODE-090, FR-PARSE-034 | Bundled authoring rules moved to 2.5.0 and now document every SRS-MD syntax the runtime writes, so a consumer following the installed rules never reverts tool-written content. Scope SRS documents are numbered one above the highest in use starting at 01, with SRS-W070 reporting a collision; the rule is stated identically in the tool, the rules document, the three skill variants and the injected agent block. Init repairs a drifted agent block by content rather than skipping on a version match, and skills install no longer deletes a shared contract a mirror-excluded skill depends on. All eight requirements promoted to verified after per-AC mutation audits. | docs/next/2026-07-30.numbering-and-2.5.0-residuals.md |
| 2026-07-30 | v2.5.1 | NODE, CLI | FR-NODE-091, IR-CLI-076, IR-CLI-077, IR-CLI-078 | `speckiwi upgrade` closes the two migration gaps a plain init leaves to the author: a reference to a rules document the release pruned, and an index metadata table with no Rules row. It plans by default and writes only with --apply, repairs both the path and prose spellings in the managed agent files, reports without rewriting a dangling mention elsewhere under docs/, and is CLI-only because it edits author-owned content. The author-owned repairs run before the refresh so a plan reports exactly what an apply performs, which matters because every project this targets already carries a managed agent block. `doctor` gained a Rules reference presence check whose remediation names init or upgrade depending on which document is missing, and `scaffold-scope` finally accepts --ignore-lock. Independent review and per-AC audits found 24 defects across four rounds, including three CRITICAL — the command scaffolded a new project when run from a subdirectory, a scope named Rules lost both its index rows, and the plan disagreed with the apply — plus two silent data-loss paths in init's own Rules-row refresh (an author's two-cell Rules row overwritten; a CRLF index left with one LF line). All fixed and red-checked. Four SRS statements that were false or self-contradictory were corrected. | docs/next/2026-07-31.speckiwi-upgrade-command.md, docs/next/2026-07-30.numbering-and-2.5.0-residuals.md |
| 2026-07-30 | v2.5.1 | PARSE, NODE, CLI, MCP, ARCH | FR-PARSE-036, FR-PARSE-037, FR-NODE-095, FR-NODE-096, FR-NODE-097, IR-CLI-080 | Closed the last residuals of the 2.5.0 run and the defects the audits of them found. Of fourteen dormant diagnostic codes, three are now emitted (`SRS-W001` by validation, `SRS-W003`/`SRS-W004` by the link check, which already ran those tests under free-text reasons) and eleven are retracted, each for a measured reason: `SRS-E009` because `SRS-E010` reports the same condition, the release codes because release readiness already returns them as typed fields, `SRS-W005` because the heading grammar makes a hyphen heading `SRS-E001`, `SRS-W006` because the ten expressions match eighteen occurrences here and every one is ordinary English, `SRS-W007` because a verified requirement's tags cannot be edited. The mirror-image defect was fixed too: step validation emits `SRS-W044`/`SRS-W045`, which were unregistered, so `explain` reported unknown for codes users can see. A numbered document colliding with a scope document is now reported as `SRS-W072`, and allocation skips an occupied number so the tool's own scaffold stops producing what its own validation warns about. Six rules-document statements were corrected to the runtime they describe. Independent per-AC mutation audits ran on all thirteen requirements and found defects behind a green suite in every group: a CRITICAL where the corrected §14.2 sentence was the exact inverse of the runtime, a HIGH where `init` hardcoded the scope number and was the one caller bypassing the allocator, two HIGHs where `FR-MCP-056` passed with its core call and its only dry-run line deleted, and a HIGH inside `IR-CLI-080` itself where the codex comparison ignored `CODEX_HOME`. All were fixed and re-probed red. `doctor` now reports drift in the skills an agent actually loads, which found the globally installed wave-master 404 lines behind the bundled one while every other check read healthy. | docs/next/2026-07-30.numbering-and-2.5.0-residuals.md, docs/next/2026-07-31.v2.5.1-residual-batch.md |
| 2026-08-01 | 2.5.2-phase1-target-lifecycle | NODE, CLI | FR-NODE-098, FR-NODE-099, FR-NODE-100, IR-CLI-081 | The tool now honours the target lifecycle its own shipped rules define. Its Target type table lists six values and three enforcement points accepted three, so phase, objective and experiment were refused while the rules document that ships inside the package promised them; nothing read the Type column at all, so a hand-written phase row already validated clean and the tool was refusing to write a value it accepted to read. The vocabulary is now stated once in src/core/target-types.ts and imported by the core, the MCP schema and the CLI, and the tests derive the expected set from the rules document rather than restating it, so widening that table fails a test instead of diverging silently. Separately, moving the Active Target demoted the departing target to planned, which the same document defines as not started yet; the departing status is now derived, becoming completed when every requirement is verified. Because that cannot repair a row already wrong and can never produce released, which records a human release decision, set-target-status was added as a CLI-only mutation on the precedent upgrade set. Eight Target Map rows were then rewritten with it. Five whose requirements are all verified became completed: v2.2.0, v2.2.2, v2.2.3, v2.4.2 and v2.5.0. v2.5.1 became released, which the rules reserve for a target that passed release readiness, and it is the only one of the tagged targets whose readiness gate actually passes. v2.3.0 and v2.4.0 were first written released on the strength of their git tags and then corrected to archived, because release readiness reports them not ready: a tag records that something shipped, which is a weaker claim than the one released makes. Four targets that read planned were left alone, and that is not a repair: v2.3.0.1 carries eighty-four implemented requirements and v2.4.4 carries twenty, and planned still says not started yet about them. Closing that gap needs its own requirement. Per-AC mutation probes were run against every criterion; each went red with its own test, and two weak assertions were found and replaced, one of which passed while its defect was present because commander wraps help text at eighty columns. | docs/research/kiwi-orchestrator/00.charter.md |
| 2026-08-07 | 2.7.1 | NODE | FR-NODE-178, FR-NODE-179, FR-NODE-180, IR-CLI-090 | Run-root invariant made honest: the preflight gate now corroborates the passed git root against the repository that path names, so it can no longer be satisfied by passing one value twice; doctor reports a project root that is not the git top level; resume verifies the repository it is resuming in against the pin the card has always carried and never compared. Three refusal conditions are told apart by a discriminator inside one unchanged gate id. Two review rounds reproduced two bypasses of the new gates themselves — a skippable pin and a Windows environment-variable case gap — both closed and pinned. | docs/research/15.orchestrator-run-root-multi-module-research.md |
| 2026-08-13 | 2.9.0-pipeline-cycle-default | FLOW | FR-FLOW-124, FR-FLOW-125, FR-FLOW-126, FR-FLOW-127, FR-FLOW-128, FR-FLOW-129, FR-FLOW-130 | [pipeline-cycle-default] The kiwi-pipeline default flipped from a single next-step recommendation to the full §2.5 research-to-implementation chain, with `--none-cycle` as the single opt-out and `--cycle` retained as a declared-inert token because installed skill copies lag the repository by charter and kiwi-wave-master keeps emitting it. The chain executes only when the invocation carries a work input — a closed enumeration of inline work text, a research document path, a GitHub issue number, `--from=`, `--req-filter` and `--plan-run-id`, with `--run` and `--target` alone excluded — so a status question does not become an SRS mutation, a plan file, code writes and implemented-to-verified transitions. FR-FLOW-026's verified text already split on work input and never named a `--cycle` flag, so the flip closes a divergence rather than widening one. Every gate, carve-out and refusal that had been keyed on the token moved to the condition it was written for: the work-mode router now keys on delegated wave entry with an explicit clause that running the default cycle alone does not exclude; `multi-candidate-ambiguous` decides by candidate count so a fork inside the chain still halts; `self-recursive-spawn` fires only on an automatic self re-entry so a cycle followed by a status check followed by a cycle does not error; the kiwi-commit-auto-push refusal no longer keys on a mode the flip made residual; and the kiwi-orchestrator refusal names the skill scoped to wave delegation, which keeps `wt-delegation-refused` a subject. Every effect claim the flip falsified was restated, from the body preamble and §0.1 through §0.2, §2.6, §8.2, §11 and §12, and the shared contract `_shared/kiwi/pipeline-v1.md` now states the default in all four renderings and gained its first test reader. Verification: TDD red 108/131 to green; two independent verifiers over five rounds, 2 CRITICAL and 5 HIGH in round 1 converging to 0 and 0 in round 5; fifteen mutation proofs with byte-identical restore; three assertions that pinned the pre-flip contract rewritten as red drivers rather than deleted, and the orchestrator mutation-ledger row re-pointed at its moved carrier; FR-FLOW-026, FR-FLOW-039, FR-FLOW-052, FR-FLOW-100 and FR-FLOW-117 amended with change notes; full suite 389 files / 5452 passed / 4 skipped / 0 failed; typecheck, lint and build exit 0; validate errors 0; links 958 checked / 0 broken; doctor 9/9 pass; package version 2.8.0 to 2.9.0. |  |
| 2026-08-14 | 2.10.0-terminal-review-loop | FLOW | FR-FLOW-131, FR-FLOW-132, FR-FLOW-133, FR-FLOW-134, FR-FLOW-135, FR-NODE-188 | [terminal-review-loop] The feedback loop became an obligation both orchestrating skills owe rather than a hop they inherit. kiwi-orchestrator declares one terminal-hop rule above its rung subsections binding R-STEP, R-PLAN and R-ORCH — exactly once per boundary, no rung exempt, not keyed on the child's return value because the §0.4 halt already makes the other two values unreachable, with `not-applicable-empty-window` for a boundary that judges no commits and `skipped-run-halted` for a run that stopped after commits landed, neither of which reports complete. R-STEP gained the hop it never had; R-PLAN's gained an explicit window because a scopeless hop after committed units falls back to five commits behind a prompt the committee answers under `--auto`; R-ORCH and kiwi-wave-master each own a run-scope hop over `run_diff_window` before their final verification. The obligation is refused by name rather than opted out of, and the stale neighbours moved with it. The half with a machine referent: a `terminal_review {skill, base, head, verdict}` record on the run-closing line, and a validator that refuses a close reporting completion without a discharging verdict. Both run-close shapes the rungs write are policed — the run-scope verification record and the `outcome: "delegated-complete"` dispatch result — and the window is checked by base equality, deliberately not head, because the review's own fix commit lands after the range it reviewed. Five defects this work shipped were found by four independent audits and one adversarial pass, then fixed: the gate could fire on ONE of the three rungs because it demanded a `status` the delegating close-outs never wrote and because R-PLAN wrote no outcome token at all; the contract's own emit example was a 1.5.0 close with no `terminal_review`, so an agent copying the shipped command wrote a journal the tool refuses; the shipped instruction demanded head equality the same document made impossible; five rule codes were declared in the union but not the rule table, exempting them from the per-rule fixture harness; and one code carried two distinct defects, which weakened the mutation evidence. Six acceptance criteria were checked with evidence that did not assert them, including one claiming the delegated-complete shape was policed when it was not; each was given the assertion it lacked or narrowed to what is true. FR-FLOW-134's requirement statement and the skill text both asserted the two review denominators are disjoint — false, the run window contains every wave window — and were corrected to the unit-of-judgement argument. FR-NODE-167 AC-3's claim that the recorded version-signal cost was paid down was corrected: it stopped growing, it was not paid. One gap is pinned open rather than claimed closed. The version gate is per line, so a resume under a newer release does not reinterpret a line written before the field existed; that leaves a close stamped below the gated version whose newer lines follow it outside both the gate and the forward-only downgrade guard. The remedy was implemented, went red against the retroactivity case, and was reverted, because it closes the gap by doing what the shipped additivity guarantee forbids; the alternative reverses IR-CLI-083 AC-3's asserted forward-only contract. A test asserts the shape produces nothing, so the day it starts refusing the replacement must be deliberate. Verification: TDD red-to-green throughout, with the red state recorded per criterion in VE-1..VE-9; mutation-proved on the caller, on the `event === "result"` clause and on the shipped examples; full suite 392 files / 5637 passed / 4 skipped / 0 failed; typecheck and build exit 0; validate errors 0; links 958 checked / 0 broken; release-check ready; release-acceptance 16/16; package version 2.9.0 to 2.10.0. |  |
| 2026-08-16 | 2.11.0-mcp-worktree-root | MCP | REL-MCP-005, FR-MCP-058, FR-MCP-059, IR-MCP-005 | [mcp-worktree-root] A session works in a worktree while its MCP server is pinned to the host checkout, so the tools addressing worktree-local run state could not reach it — passing an absolute worktree path was refused with "Path is outside project root", and every per-call root argument was refused outright. REL-MCP-004 bundled two prohibitions with different justifications: a root parameter on server startup, and one on a tool call. The first is sound — a wrongly-rooted server is silent — and is carried forward verbatim as AC-4. The second is not, because a per-call root is declared once per call and echoed back in the envelope, so it is less silent, not more; the user retracted it and REL-MCP-005 supersedes REL-MCP-004 to say so. The repository had already accepted the same argument once and contradicted itself: IR-CLI-082 AC-6 makes orchestrate preflight take both roots as arguments precisely because a server-fixed root makes an in-tool read vacuous. The rejection that sank multi-root five times cannot arise here by construction, because the tools that allocate Requirement IDs are exactly the ones that never gain the argument. Measured at runtime rather than assumed: 100 tools, 26 workflow_* and 27 orchestrate_* opened, 47 SRS-facing left closed. Safety is enforced twice over. By identity, at the registration seam every tool passes through, where the default is refusal — a tool registered with no metadata at all is refused, so a newly added SRS tool is safe without being listed. And by destination, because classifying by family alone would not have been enough: the checkbox tools take planPath straight from the caller, docs/spec is an ordinary subdirectory of any accepted root, and SRS files carry the same checkbox shape a plan does. A workspaceRoot must be absolute, existing, a git top level rather than a subdirectory, and share the git common directory of the startup root; all of it is decided before any handler runs, because resolveInsideRoot creates its root as its first statement and a typo'd path would otherwise be created rather than refused. The gate reuses resolveGitCommonDir instead of a new comparison, which avoids two measured traps: the raw --git-common-dir output is relative at a host and absolute in a worktree, and Windows git returns realpath casing. Separately, the topology gate meant to validate worktrees could not express a worktree over MCP at all — orchestrate_preflight's binding omitted --role, --lane-id and --lane-plan, so every MCP call was judged as a host and every linked worktree refused; IR-MCP-005 closes that. Four defects in the requirements themselves were found and corrected rather than implemented around: the seam map named one workflow injection point when there are two, and the second is shared with the SRS mutations so honouring workspaceRoot there would have opened the whole family; AC-3 said "reads or mutates docs/spec", which is too broad because tools read the index to fill their envelope, and was narrowed to authorship; FR-MCP-059's statement contradicted its own AC-4 over preflight; and IR-MCP-005 AC-2 named a refusal reason that pair cannot produce, since the legacy equality gate runs first for role host. Verification: TDD throughout, with one lapse caught and the implementation reverted and rewritten test-first rather than justified after the fact, and two criteria that passed trivially strengthened before implementing. Nine deliberate mutations each turned a test red, including moving validation after the handler, which let writes reach paths that should have been refused, one under docs/spec. An independent adversarial pass drove all 100 tools against a real linked worktree, a foreign repository and a nonexistent path: 49 refused, 51 accepted and honoured, zero silently ignored, 15 spellings of an SRS destination all refused, and no directory created. AC-7's second proof initially asserted neither of the verbs its criterion names and was rewritten to attempt four reads and four writes against a marker-seeded sibling with a whole-tree sha256 digest, then mutation-proved, where the broken build returned the sibling's plan body and accepted an append into it. Suite 399 files / 5670 passed / 4 skipped / 1 failed, the failure being the pre-existing load-dependent srs-lock-status-cache flake, which imports none of the changed modules and passes 3/3 alone. typecheck, lint and build clean. |  |
| 2026-08-17 | 2.12.0-verification-economy | FLOW, NODE, CLI | REL-FLOW-003, REL-NODE-007, FR-FLOW-136, IR-CLI-094 | [verification-economy] A census of every verification defect this repository has recorded found 38 of 56 were mechanically checkable, so the expensive rounds were mostly paying to find things a CPU could have found. This target moves those to the CPU. README's factual claims are now held by machine: every fenced command resolves against the live commander tree, every anchor against the explicit id tags the repository actually uses rather than GitHub slugs, every tool name against the runtime registry, and every load-bearing count against the enumerable set it describes. `@req` citations, used across 926 sites in 257 files with nothing checking them, now resolve against docs/spec, with a discarded citation reported rather than failed because a historical reference to a retracted requirement is legitimate. Prose re-review is scoped by a heading-keyed hash ledger so a round reads what changed, and the CLI verbs that make it possible exist because a skill cannot compute a hash and a ledger the tool did not write is an agent assertion rather than evidence. The research this came from got three of its own numbers wrong and made a wrong architectural claim, both found during implementation and corrected: it reported the handler map's 107 keys as 100 tools without saying seven are resource entries, missed the npx forms among the fenced commands, and concluded the existing signature-parity test already did command-existence checking when it drives a four-token blocklist. The document that named drift-from-a-single-source as the most frequent defect pattern had committed it. Its brittle counts are now deleted rather than corrected, because the numbers that matter belong in tests that go red. The checks caught defects in themselves, which is the strongest evidence they work. A strengthening exposed that the tool-token pattern never matched `workflow_*` or `orchestrate_*`, so the prefix-membership branch was covering nothing on the real README while every test stayed green — only a fixture failed, and without the real-document assertion an inert check would have shipped. Writing fabricated ids as literals in the @req fixtures planted four genuine unresolvable citations, which is exactly the failure the escape hatch answers; it was given same-line scope rather than a file header that silences text nobody looked at or a region form whose unterminated start swallows the rest of the file, and it was proved load-bearing by deleting one marker and watching three named failures appear. The full suite then found what neither implementation's own tests nor any review round had: registering the ledger's CLI verbs directly put command names in two places, and FR-ARCH-006's registry-versus-tree equality failed in both directions naming all three. It was fixed by registering them, not by editing the check. Four defects in FR-FLOW-136 were corrected rather than implemented around — append-only and prune contradicted literally, the pass named an orchestrator that is not the consumer, the audit was tied to a target close the consuming skill cannot perform, and the heading-path key's cost of dirtying every descendant on a parent rename was unstated. IR-CLI-094 was authored after its implementation and says so. An MCP counterpart was declined with its reasoning recorded: the only consumer forbids MCP mutations, so it would ship with no callers and require weakening that prohibition. The ledger's limitation is in the shipped skill text rather than omitted — a hash-clean section is not re-read, a section can be true alone and false in changed surroundings, and one full-document audit still gates the close. Verification: TDD throughout with red recorded per criterion; twelve criteria that passed trivially were strengthened before implementing and re-confirmed red; suite 404 files / 5759 passed / 4 skipped / 0 failed, with the load-dependent flake that appeared in three earlier concurrent runs absent from the sole-occupant run; typecheck on both tsconfigs, lint at zero warnings, and build all clean. |  |
| 2026-08-28 | 3.1.0 | FLOW | FR-FLOW-159, FR-FLOW-160 | Wired the promotion chain to the tool that checks acceptance criteria, and made every skill name a shipped body claims as an owner resolve in the tree that ships it. kiwi-review-fix-loop now calls check_acceptance_criteria between add_verification_evidence and update_status in all four renderings, its section-zero allowance admits that call under the same --close-reqs self-mode bounds as the other two, and the governing sentence demands a named test identifier per criterion while forbidding a tick without one; kiwi-coder's all-green transition rule now says implemented in every rendering, names kiwi-review-fix-loop --close-reqs as the successor and states why it stops short. The three fictional owners kiwi-reviewer, kiwi-prd and kiwi-srs-reviewer are gone from every rendering: five sites now point at a skill that exists and two say the responsibility has none. Held by three contract tests - the promotion sections compared byte-exact against a checked-in golden over a DERIVED set of seven sections, a per-rendering allowance-row check, and a pointer-pair check that requires every occurrence of an anchor to reach an owner that declares the responsibility within 120 characters. Verification produced CRITICAL 2, HIGH 5, MEDIUM 5, LOW 4 (L2, L3) and one surviving mutation (L1); all were addressed. Mirror --check divergence 0, validate_spec errors 0, full regression 6701 passing (the one failure is FR-NODE-177's cross-process lock contention test, flaky and green on both isolated runs, in code this change does not touch). |  |
| 2026-08-28 | 3.1.0 | FLOW | FR-FLOW-161 | The close-requirements skill now takes the set it is accountable for from list_requirements against the target resolved by get_active_target, instead of extracting that set itself; every requirement in the scoped subset carries exactly one disposition, the accounting identity of transitioned plus excluded equals scoped is stated with a run that fails it declared invalid, a run with at least one eligible requirement and zero transitions no longer reports TASK_DONE, and the two sentences in kiwi-orchestrator that described the retired behaviour are corrected while its compensating gate plan-coverage-unclosed stays. Verification ran in three rounds over 442 files and 6744 passing tests, and mutation testing supplied four of the corrections that pattern reading had missed. The read-allowance row naming get_active_target, summarize_target and list_requirements inside one bracket satisfied both a presence check and a proximity check even after the target-resolution instruction was deleted outright, so target resolution is now confirmed by the stopping rule instead. The tool-allowance check filtered for three fixed names and asserted the count was three, which is bounded above by three and therefore caught removal while admitting addition, so adding add_requirement beside the numeral four passed; set equality over the allowance clause replaced it. An edit that quoted the terminal-status rule and then cancelled it, placing a disclaimer before the clause and a repeal after it with the original left intact between them, kept both presence assertions green, so that line and the identity line are now held byte-exact against a checked-in golden confined to those two lines. And draft and deprecated appeared in no pattern at all, so erasing both values from the skill down to zero occurrences left every assertion green while half of the exclusion grounds sat outside verification; the stability axis is now asserted apart from the prose-evidence axis. |  |
| 2026-08-28 | 3.1.0 | FLOW | FR-FLOW-162 | Bound three claims the shipped skills make about the shipped code into one derived table: the argument names attributed to an MCP tool, the assertion that the deferred-mutation replay queue has no applying consumer, and the terminal-review verdict vocabulary with the subset that discharges completion. Each row reads its code-side fact by importing the shipped module and its skill-side claim from disk, so no value is restated in the test file. Verification changed the correction itself: the replacement sentence listed five arguments for add_trace_link and so shipped a fresh false statement about the schema, the very shape this requirement removes, and it was corrected to the seven the schema declares. Mutation testing then opened an axis on the table itself, where deleting a row, rewriting a judge into a tautology and freezing the corpus file list into a literal all survived; AC-9 and AC-10 close those with row-id assertions, per-row probes in both directions, corpus re-derivation and a byte golden over the verdict lines. |  |
| 2026-08-28 | 3.1.0 | FLOW | FR-FLOW-163 | Sidecar trace intent names the shipped argument that carries it. The plan pointed at three sites; three of them were already settled by 06 (FR-FLOW-162), and what remained was the one plan §7 named — the kiwi-planner boundary, where the intent a planner attached was dropped without a sound at flattening because the sidecar-to-flat table had no row for it and kiwi-coder, which owns the flattening, never mentioned it. Verification returned two findings worth recording. AC-5 had stated that an argument outside the schema is refused; measured, it is silently dropped instead, since objectFromShape wraps the shape with no .strict() under src/, and the AC as written said the opposite of the sentence this very change put into the skill. The mutation round found a third quote-cancellation variant, one that cancels the rule from the line beside it rather than on it, which neither the judge nor the byte golden could see; AC-9 closed it by scanning each site's enclosing section for a closed retraction vocabulary. The axis 06 had closed, a suite disabling itself, reopened in this new file and AC-10 closed it again, for the three edits measured to survive: a golden comparison turned into a self-comparison, a deleted golden block, and a lowered floor. Evidence VE-1 through VE-11, suite 13 passed, mirror --check 0 divergences, validate_spec 0 errors. |  |
| 2026-08-28 | 3.1.0 | MCP | FR-MCP-060 | Gave all 100 MCP tool registrations a description, projected from the ToolSpec registry through an mcp(name, description) builder in the spec layer rather than hardcoded beside the server, so a tool registered without a description throws instead of listing bare. Verification read all 98 shipped descriptions against the code and found nearly half of them at odds with it, and every one of those had passed the shape checks. Of the implementer's own seven self-comparisons one — add_completed_work — was corrected in the wrong direction, which is the risk self-verification carries. AC-8 moved part of the truthfulness axis onto the machine: each closed value list a description presents is compared in both directions against the runtime constant the tool's guard decides by. Mutation testing found the root cause that the values bounding the suite's reach were literals in the suite's own source; four of them moved into the requirement text, so narrowing the suite now leaves a requirement diff. The change collided with FR-FLOW-154, resolved by exempting the copy that is itself checked while checking that the exemption stays sound. The tools/list payload grew from 52,536 to 78,005 bytes (+48.5%), which is what the accuracy costs. |  |
| 2026-08-29 | 3.1.0 | FLOW | FR-FLOW-164 | The seven skills that implement, review and promote now declare validate-spec-error in their critical_gates[] table, in every rendering they ship, each with a prose line naming the call, the CLI fallback and the halt that survives --auto; before this exactly one skill declared it and that one sits outside the chain. Holding that with a test first required generalizing criticalGatesSection() from the literal heading two of the seven carry to the heading that names critical_gates, a change measured to be a no-op for the two and to move the other five from zero rows to rows. Seven verification rounds over four lenses produced 11, 13, 3, 3, 2, 4 and 3 new sites, with the last two at zero HIGH. Round two produced more than round one, and the reason is the method rather than the work: rounds one used mutation and truthfulness, round two was the first to run AC-contrast and instrumentation at all. New sites track lens coverage, not round count. Three defect shapes are worth carrying forward. A correction's denominator is set by whoever noticed the defect, so a claim fixed at the six sites a verifier named stayed false at two more in the same block and the requirement contradicted itself; this recurred across three rounds even with an exhaustive search written into the fix instructions. A closed axis reopens in a new check whose reach is narrower: the quote-cancellation FR-FLOW-163 closed came back three times as the polarity scan widened from one line to a block to quote markers, each fix exposing the next. And a scan that reads tokens rather than meaning admits an inversion, which AC-9 now closes in both directions with vocabularies observed in the shipped sentences rather than invented. What is not held is recorded rather than argued: a dedent between quote depths, unreachable in a tree whose seven quote lines never nest, and any paraphrase outside both vocabularies. Two gaps found on the way were registered rather than folded in: FR-NODE-201 for the acceptance-criterion length limit that only one of its three writers enforces, and the pre-existing racy pollution in test/core/orchestrator. Full npm test alone: 446 files, 6815 passing, 0 failures. |  |
| 2026-08-29 | 3.1.0 | FLOW | FR-FLOW-157 | kiwi-orchestrator's final-verify section carried two subjects while two requirements owned it, so no byte comparison could be drawn across it: freezing the whole would fail FR-FLOW-131's own edits under a message about a terminal-line contract, and freezing half left the other half to a closed vocabulary of eleven nouns. It is now two subsections, each declaring its owner in its own text, with the terminal-line half held byte-exact against a golden from its heading to its end in all four renderings, and the vocabulary removed in the same change. The seven legitimate review-hop edits that vocabulary refused are each measured green afterwards, planted on disk one at a time rather than judged in the test file; one of them adds a flag the same skill defines in its own options table, where the old failure message forbade both available repairs at once. Three verification rounds over four lenses produced 22, 5 and 2 new sites. Running all four lenses from round one is why this converged in three rounds where FR-FLOW-164 took seven: that one met AC-contrast and instrumentation only in round two and paid for it there. The split opened a hole while closing one, and the first round caught it: the section preamble belonged to neither half, so a retraction placed there was green until the preamble came under the first heading, and the repair cost nothing because it moved no bytes. Two defect shapes recurred. A correction's denominator is set by whoever noticed the defect, so a co-edit cost corrected at the shape a verifier named was found wrong at a second shape nobody had questioned, and a figure re-measured in one round went stale in the same round as the suite grew under it; measurements now carry the suite size they were taken at. A tool gap surfaced and was registered rather than worked around: no path edits or deletes one narrative note, and the only rewrite route caps text below the length of what it would replace, so five notes duplicated by a double-run needed an authorized deletion-only patch. That is FR-NODE-202. Full npm test alone: 447 files, 6888 passing, 0 failed. An earlier attempt reported two hermeticity failures that were a second concurrent run observing the first's writes. |  |
| 2026-08-29 | 3.1.0 | FLOW | FR-FLOW-158 | The five names by which a sentence can rule on the terminal line's validation are now held at zero across every shared file the orchestrator defers to, not the one its section-zero happened to point at. The corpus went from one file to 49, read from each rendering's directory rather than a written list, so a shared file added later is covered without editing the suite. Before this, a sentence in run-ledger.md saying the terminal line is not put through the validator and that a repeated diagnostic is answered by overwriting the line left three suites at 102 passing; nine of the ten shared files section-zero defers to were uncovered. Six verification rounds over four lenses produced 17, 6, 2, 6, 3 and 1 new sites. Three of them opened one axis: a closed vocabulary that catches spellings rather than the subject. Round one closed one missing conjugation, round two found twenty-one more, round three found six inside the very slots the requirement named as filled. Adding spellings was the wrong repair each time and the fourth attempt would have been too. It closed two ways instead. The three slots of every lemma are now built from the stem by Hangul syllable arithmetic and asserted as a product, so a lemma added without its slots fails naming the cell, and two exception tables carry the copula forms the rule would otherwise misspell. And the spelling variants collapsed once the ban treated a space the list writes as optional, the way its anchor already did; the suite then identified two lemmas as duplicates of each other. Four bypass classes are closed by normalising before the scan rather than by widening the patterns: full-width, markdown escape, paragraph fold and the unspaced anchor. What is not held is measured into AC-6 rather than argued: five residual classes, and a false-refusal class the fold widened, which is now pinned as a known cost so that widening or narrowing it both show. Two figures were withdrawn for being unreproducible by an auditor, and the round that withdrew them shipped a third of the same kind, corrected here. FR-FLOW-164, verified and committed earlier, shares the block scanner this change extended; its six closed axes were re-planted at every round and stayed red. Full npm test alone: 448 files, 6916 passing, 0 failed. |  |
| 2026-08-29 | 3.1.0 | FLOW | FR-FLOW-155 | The journal write gate now resolves the engine of the line it is about to write, so a kiwi-wave-master close that omits terminal_review is refused at append with no bytes reaching the file, and refused identically under dry-run, which is the only judgement an agent sees before writing. Before this the prospective parse pinned the engine to a literal, so the wave-master rung's candidate set was empty and a run could close on nothing while reporting clean. The resolution goes through engineOf, the resolver the parser itself filters with, and both parses in that function receive the one result; that second half is asserted over the function's source, because a restatement correct today is behaviourally identical to what it copies and no run can tell them apart. All four renderings of kiwi-orchestrator name the validator, its arguments, the CLI fallback, which caller can judge a wave-master journal at all, and the halt on refusal. This requirement was blocked once, at CRITICAL 0 and HIGH 3, and the three went out rather than being argued down: a closed vocabulary that caught spellings instead of the subject and refused seven legitimate edits to a neighbouring requirement's own text became FR-FLOW-157, and a coverage boundary drawn at one shared file when the skill defers to ten became FR-FLOW-158. Both are now verified. What is left is named rather than left to be discovered: FR-FLOW-156 owns the wave-master emit path that never reaches this guard, and FR-NODE-199 the MCP validator that takes no engine argument and the run-id axis that empties the same candidate set. The independent promotion check found seven of the eight criteria met and one clause of AC-7 false in the safe direction: it called a line invisible when its phase is not final-verify, but the predicate is closesTheRun, which admits a delegated-complete dispatch result too, because R-STEP and R-PLAN terminate there and never write a final-verify line. Measured, such a line is judged. Corrected from the code rather than from reading. Two suites, 65 tests; full npm test alone 448 files, 6916 passing, 0 failed. |  |
| 2026-08-30 | 3.1.0 | FLOW | FR-FLOW-165 | The R-ORCH rung stopped at a gate its own requirements caused and had no hop to clear it: a wave's requirements land at draft stability by default, the readiness derivation refuses draft, and the gate that fires is in critical_gates so an unattended run halts. The reason it reported named unmet hard dependencies, evidence drift and unverified ownership, so an agent went looking for a dependency that does not exist. There is now a promotion hop between SRS registration and readiness derivation, calling the skill that owns the promotion judgement rather than restating that judgement in orchestrator prose, and its position is asserted rather than only its presence. And the derivation carries lifecycleReady beside the existing booleans, so the gate names the cause by a field rather than by a message a translation would break; the pull-out the plan asked for was measured incompatible with preserving what the existing boolean means, and preservation won. The investigation the plan put first is what decided everything else, and the plan was wrong about it twice: its method could not be run, because the skill it names has no code implementation at all, and its citation of the filter line was stale, because an earlier item had already split that line and settled the axis. Measured against the shipped build instead, the axis is stability, and without correcting the filter the hop would evaluate 0 of 6 requirements and report a clean run — the exact shape of a check that is present and green and does nothing. Two verification rounds over four lenses produced 14 sites then 3, and the lenses split cleanly: AC-contrast and truthfulness found almost all of it while mutation confirmed the mechanism was sound. The one HIGH was an evidence figure that could not come from the plant it was filed under: a move was recorded with the signature of a deletion, so the ordering assertion the plan asks for had never been observed red. Re-planting gave the real figure, and every other figure was re-measured too, because round one had grown the suite. A citation of a section number that no rendering has was closed by reading the citation out of the shipped paragraph and requiring it to resolve, rather than by correcting the one instance. Two things are recorded rather than closed: a CRLF claim was retracted on a mis-measurement and the retraction itself retracted, with the block scanner fragility it pointed at registered as FR-FLOW-166. Full npm test alone: 450 files, 6939 passing, 0 failed. |  |
| 2026-08-30 | 3.1.0 | FLOW | FR-FLOW-167 | Every orchestrate_* tool the skill does not name now carries one disposition, derived from ORCHESTRATE_TOOL_BINDINGS rather than from a list written into the test: 10 to wire, 1 already performed by code, 4 owned by another tool, 3 deferred, none undecided. Three of the ten (route_probe, route_freeze, readiness_check) are wired into all four kiwi-orchestrator renderings; the remaining seven were split by landing file into plan items 19 and 20 so the deferral has a destination, which is the state that produced this item in the first place. Gate attribution is decided mechanically: the binding path cuts out the .action callback and reads the refuse names it can actually raise, so a gate the tool cannot emit fails rather than reads plausibly. Four verification rounds caught three HIGH defects - an instruction to pass an option the tool does not accept, a gate attributed to a function with zero src callers, and an acceptance criterion that named a mutation combination as red when it was green. The last was closed by making the reader refuse: the bytes it is about to return are compared against the bytes read independently from the block title now running, and a mismatch throws, so weakening the recorder no longer buys silence. | docs/plan/3.0/17.orchestrate-tool-triage.md |
| 2026-08-31 | 3.1.0 | FLOW | FR-FLOW-168 | The five orchestrate_* tools that land in the kiwi-orchestrator body are now named as calls in all four renderings, at six places: run_lock in a new 3.1, freeze in a new 3.2, handoff_validate in 9.3, run_abort in both 15 and V.abort-run, and issue_open in 13.2. The two spellings that 3.k activity (0) had been calling without a prefix were normalised to the leaf spelling in the same round, so the next count does not split again. The disposition table came from FR-FLOW-167 and was not re-derived; the (a0) column was re-measured at the start and matched that table down to the line number. Three defects were found by measuring rather than by reading. The per-copy gate assertion for 9.3 was scoped to the section, so deleting the gate from this requirement's own sentence left it green while a neighbouring paragraph owned by FR-FLOW-077 held it up; the assertion was narrowed to the block the instruction sits in, which is the unit the V sections require because they put the gate label on a line below the instruction. Two spellings this requirement claimed to introduce measured 0 to 0 against HEAD, and two it did introduce were missing from the list. And the shipped instruction generalised a refusal across lock, unlock and status when only lock raises the gate - unlock deletes a live lease held by another run without refusing, which the same section tells the agent to call on normal exit. | docs/plan/3.0/19.orchestrator-body-tool-wiring.md |
| 2026-08-31 | 3.1.0 | FLOW | FR-FLOW-169 | The two orchestrate_* tools that land in shared contract files are now named as calls at three places across all eight copies: round_record in the termination-condition section of verify-loop.md, and auto_gate in both the decision-rule and the severity-branch sections of auto-option.md. The denominator is the shared corpus FR-FLOW-158 already measures, re-counted at the start: 49 markdown files across the four renderings, eight of them in scope. The wiring calls a judgement the code cannot reach rather than duplicating one it already makes - waves-validate.ts excludes fix-in-clean-round as a code the journal cannot produce, and that exclusion still held when re-measured. Running the instructions rather than reading them corrected two: the handler requires --run-id and --payload although the binding does not mark them required, so the required set is now derived from both sides, and a --proof whose kind is not recomputed outside the journal ends at run-invariant-drift instead of a verdict. Red found two defects of its own. The gate reader that FR-FLOW-167 built missed a camelCase namespace, returned an empty body, and an empty body made 'this tool raises no gate' pass for that very reason; the emptiness check now runs before the membership test. And a negative assertion this requirement had written was false, because the round record handler does raise run-invariant-drift. Three verification rounds then caught a claim that was true in value but false in place, a gap note that described as missing a rule two shared files already carry in full, and a cardinality that a correction note had left stale because the note named only the two entries it replaced. | docs/plan/3.0/20.shared-contract-tool-wiring.md |
| 2026-08-31 | 3.1.0 | NODE | FR-NODE-203 | A lock released between its unlink and its fence close leaves the sentinel delete-pending, and Windows answers a read of that file with EPERM rather than ENOENT. readHolderAt absorbed only ENOENT, so the raised fault escaped past a thirty-second contention budget that retries on held alone, and the caller was refused in milliseconds where it was designed to wait. Not a test-only fault: the same reader serves the run lock, and any concurrent workflow-artifact write met the same odds of a refusal instead of a wait. The reader now answers every unreadable sentinel as contention, and the budget absorbs a raised fault rather than letting it past. Because the race reproduces at roughly half a percent per contending pair, a green suite proves nothing here and the requirement says so in its own acceptance criteria - the evidence is an injected EPERM boundary that is deterministic, and a paired before-and-after probe over twenty-four thousand acquisitions that went from fifteen escapes to none. One verified neighbour's test had encoded the defect as contract: it injected a single acquisition failure and asserted an immediate refusal. FR-NODE-177 AC-7 says nothing about when an acquisition fault is reported, so the injection became persistent, the two diagnostic assertions were left untouched, and one was added that the budget must be spent before refusing. Verification then caught the requirement asserting a property the system does not have - an unreadable sentinel past the torn-grace window really is reclaimed, on HEAD as well - and the claim was narrowed to what was measured. Three rounds also found a count of call sites that filtered its own denominator by the predicate being asserted, which is the same shape a prior requirement had already recorded. | docs/plan/3.0/21.exclusive-lock-transient-error.md |
| 2026-08-31 | 3.1.0 | NODE | FR-NODE-204, FR-FLOW-170 | A guard added earlier told the orchestrator to release only the lease this run acquired, and no run could tell. The owner flag defaults to the same string for every run and the skills never pass it, so a status response could not answer whose lease it was. Acquisition now returns the record it published, which gives the comparison one half that costs no extra call and cannot fail between the two. The other half had to survive compaction: a resumed session runs no Preflight, so the pin is written into the run contract the resume procedure already reads first, and both shared renderings carry it because the closed list lives in two documents and fixing only the skill would have shipped an instruction that breaks its own contract. Three verification rounds. The first found that the pin could not exist on the resume path at all - the case this item was created for - and the three alternatives were rejected by running them: re-acquiring refuses a compacted session against its own lease, putting the run id in the owner flag breaks a zero count another requirement rests on, and skipping release leaves the next run refused. The second found the release-site census filtered by a Korean verb, so the same instruction written another way escaped it silently; the denominator moved from the verb to the lock's own name, with the six lines that are not release sites excluded by a table that goes red when an entry matches zero lines or two. The third found that name is still a vocabulary - a line that names no lock at all still escapes - and that a correction had replaced one irreproducible timing figure with another. Both are now recorded as measured residue rather than asserted away. | docs/plan/3.0/22.run-lock-owner-discrimination.md |
| 2026-08-31 | 3.1.0 | FLOW | FR-FLOW-171 | The review loop had a denominator when it chose what to read and none when it read it. Scope selection already reconciles every candidate file into one of four dispositions and checks the identity; what crossed to the reviewer was diff text, and the output schema had no field saying what had been opened. A reviewer that read three of twelve files and returned no findings produced the same PASS as one that read all twelve. The scope set is now fixed by the loop and passed in as the reviewer's denominator, every included file gets a row, and each row carries one anchor per hunk that the loop checks against the diff. Counting was rejected before it was built and the plan says why: the hunk total is a number the loop hands over, so a reviewer can satisfy a count comparison by writing back what it was given. Three rounds of verification then found the same gap one level up each time. First, the anchor definition was narrow but no comparison enforced it, and nine common single letters filled every row on four commit ranges. Second, once containment was tightened to whole lines and token-boundary symbol names, the residual was described as few-hunk whole-file additions, and measurement refuted that: per file, four fifths of code files still fill from tokens guessable off the extension alone, an eleven-hunk hand-edited source among them. That measurement retired the requirement's own claim that the output is derived from the whole input; what it guarantees now is that every file of the denominator is enumerated and every anchor filed really occurs in its hunk, and it says plainly that opening the diff is not established. Third, three figures in the prose were wider than the evidence beneath them. What survives all of it is measured: eleven uncooperative outputs stay invalid and two honest ones stay valid, so shrinking the denominator to a sample and answering the schema without the input are both refused - neither of which a count comparison refuses at all. | docs/plan/3.0/08.review-denominator.md |
| 2026-09-01 | 3.1.0 | FLOW | FR-FLOW-172 | The same skill was reviewed or not depending on which door the work came through. Reached from the orchestrator, a step got a review hop; called directly as its own entry point, kiwi-tdd ran red, green, regression, promotion and report with the words review, pipeline and emit appearing zero times in its body, while the shared pipeline-event contract already listed it in the skill enum and held a next_hint row waiting on a body that never emitted. The hop now sits ahead of promotion, a residual CRITICAL or HIGH forbids reporting done, and Phase 7 appends one event by citing the shared contract rather than restating it. Suppression is fixed at both ends in the same commit: the orchestrator passes an explicit flag and no-pipeline-emit to the child, and the child skips only on that token, because a flag the child waits for and no parent sends would run the loop twice and break the exactly-once rule the orchestrator already states. The start gate of the review hop in kiwi-pm and kiwi-coder was decided rather than left open - the one irreversible branch was already registered by kiwi-coder and missing from kiwi-pm, whose inheritance of child declarations covers gates a child bubbles rather than its own judgement, so that row was added and the reversible branches carry their reason beside the table. Seven verification rounds each found the guard readable in a way that meant the opposite: inverted, worded around, reordered, hidden where neither reader looked, exempted by a rule whose stated reason was narrower than its licence, and excused by a negation attached to something else. The check moved from prose patterns to a two-case table, then to classifying every line of the clause so nothing goes unread, then to removing three exemptions rather than narrowing them. What it guarantees is now written as shape and enumerated vocabulary rather than meaning, with the surfaces measured to pass listed as measurement rather than as a census, because a regular expression does not decide what a sentence means. | docs/plan/3.0/09.tdd-review-hop.md |
| 2026-09-01 | 3.1.0 | PARSE | FR-PARSE-040, FR-FLOW-173 | Skipping the SDS was judged by the party the skip saves work for, and nothing checked the judgement. The validator emitted one advisory when design.md was absent and advisories are warnings by construction, so the step gate stayed green whether the skip had a reason or not. A skip now counts only where the step records it: intent.md carries an SDS Skip section whose Decision, Reason and EARS line the gate reads, and an unrecorded skip raises SDS-E054 at error severity instead, which turns the exit code. One function owns every branch of that judgement and only its last branch passes, so an input that satisfies none of the named failures cannot slip through as unjudged. The skills say where the record lives and what its three cells hold, route the skip past step validate before tests are written, and state the two cases as a table keyed on recorded and unrecorded rather than as prose a rewrite could invert. Verification found the MCP surface unwired - removing the intent argument left every existing suite green - and found the requirement claiming that surface exits non-zero when its handler always returns ok, so the claim was replaced with what that surface actually shows and a suite now reddens when the argument goes. It also found the note about the neighbour naming the wrong criterion: what this change narrows is not the one about advisories never flipping the gate but the one promising a warning whenever design.md is absent, which is now false for an unrecorded skip. That neighbour was demoted, its criterion narrowed to the recorded branch, and re-promoted on the same evidence, which its own fixtures already measure. Left open and recorded: the bundled authoring rules still describe the skip without the record format, so an author who follows them exactly is stopped by the gate. | docs/plan/3.0/10.sds-skip-gate.md |
| 2026-09-01 | 3.1.0 | FLOW | FR-FLOW-174 | 이슈 진입 이음매 다섯 자리를 배포되는 스킬 텍스트에 명시하고, 이음매 표를 분모로 삼는 양방향 항등식으로 그 명시를 고정했다 | test/skills/issue-entry-seam.fr-flow-174.test.ts |
| 2026-09-02 | 3.1.0 | NODE | FR-NODE-205 | 문서에 찍히는 날짜 스탬프를 한 함수가 만들고 작성자의 로컬 달력을 따르게 했다. 열두 자리를 하나의 모양으로 재는 프로브 표를 세우고, 그 표의 행이 자기 판정 근거에 닿는 통로를 아홉 번에 걸쳐 닫았다 | test/core/date-stamp.fr-node-205.test.ts, docs/plan/3.0/23.note-date-stamp-utc.md |
| 2026-09-03 | 3.1.0 | FLOW | FR-FLOW-175 | 축 면제가 스키마의 철자 그대로 대조하게 해 대문자를 담은 인자 열다섯이 도달 불가이던 것을 열고, 면제를 호출 자신의 스키마 항목으로 좁혔다. 기준과 그것을 덮는 검사를 잇는 청구 검사를 두되 막지 못하는 것을 실측 열거로 적었다 | test/skills/status-enum-contract.fr-flow-154.test.ts, docs/plan/3.0/29.axis-exemption-case-fold.md |
