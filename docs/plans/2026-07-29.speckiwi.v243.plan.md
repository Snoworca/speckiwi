---
run_id: 2026-07-29.speckiwi.v243
target: v2.4.3
plan_contract: "1.2.0"
tdd_policy: strict
requirements: [FR-FLOW-044, FR-FLOW-045, FR-FLOW-046]
research: docs/research/13.kiwi-wave-master-wave-end-cross-verification-research.md
---

# v2.4.3 — kiwi-wave-master 웨이브 종료 상호검증 구현 계획

## §1 범위

연구 문서 §5 의 채택 설계를 `kiwi-wave-master` 3변종과 `waves-event.md` 4사본에 반영한다.
산출물은 전부 **에이전트 지침 Markdown** 이며 런타임 코드 변경은 없다.

**In scope**

| 파일 | 사본 | 근거 |
|---|---|---|
| `skills/{claude,codex,etc}/kiwi-wave-master/SKILL.md` | 3 | `.agents` 미러 **제외** 대상 |
| `skills/{claude,codex,etc}/_shared/kiwi/waves-event.md` + `.agents/skills/_shared/kiwi/waves-event.md` | 4 | 미러 **포함** |
| `test/skills/kiwi-wave-master-content.test.ts` | 1 | FR-FLOW-044/045, 046 AC-6~8 |
| `test/skills/kiwi-event-contract-content.test.ts` | 1 | FR-FLOW-046 AC-1~5 |

**Out of scope** (연구 §7 · 위원회 컷 목록)

- 전체 run 종료 후의 최종 일괄 감사 (사용자는 "각각의 wave" 를 명시)
- `_shared/kiwi/wave-verify.md` 같은 신규 공유 SSOT — 소비자 1개짜리 추상화
- `waves.jsonl` status enum 신규 멤버
- `--no-verify` / `--verify-depth` 등 신규 옵션 계열
- `kiwi-pipeline` 등 다른 오케스트레이터로의 일반화
- `auto-option.md` §5.1 카탈로그 전면 도입 (5건으로 한정)
- `kiwi-wave-master` 의 `.agents` 미러 제외 상태 변경

## §2 TDD 정책

`tdd_policy: strict`. 전 Task 는 **red 선행**이다. 산출물이 자연어 계약이므로
검증 수단은 3변종/4사본에 대한 **content assertion** 이며, 다음 두 조건을 만족해야 red 가 유효하다.

1. 구현 전 실행 시 AssertionError 로 실패할 것 (파일 부재 시 `""` 반환 헬퍼 사용 — 기존 관례)
2. 단언이 **tautology 가 아닐 것** — 문장 하나를 지웠을 때 red 로 돌아가는지 뮤테이션으로 실증

> **뮤테이션 검증 시 `git checkout --` 사용 금지.** 이 저장소는 미커밋 작업물이 크다.
> `cp` 백업/복원만 사용한다. (2026-07-28 에 실제 유실 1회 발생)

## §3 Phase / Task

### PH-001 — 계약 (waves-event 1.1.0) · FR-FLOW-046 AC-1~5

| Task | 내용 | red |
|---|---|---|
| T-PH001-01 | `kiwi-event-contract-content.test.ts` 에 waves 4사본 대상 단언 추가: `schema_version` 1.1.0, status enum 정확히 4멤버 유지, 전이도 불변, optional 필드(`phase`/`verification`) 존재, complete-without-verify 무효 조항, residual 전량 조항, 라운드 카운터 영속·verdict 비영속 조항 | ✅ 선행 |
| T-PH001-02 | `waves-event.md` 4사본 구현 (§2.2 선택 필드 확장 + §3 에 검증 게이트 문단 + §4 재개 규약 보강 + 헤더 1.1.0) | green |

**주의**: FR-FLOW-043 이 §5 emit 예시의 폴백 체인을 방금 verified 로 고정했다. 그 블록을 **건드리지 않는다** —
회귀 시 verified 요구가 되열린다. `schema_version` 예시 문자열만 갱신한다.

### PH-002 — 검증 단계 본문 · FR-FLOW-044 전 AC

| Task | 내용 | red |
|---|---|---|
| T-PH002-01 | `kiwi-wave-master-content.test.ts` 에 FR-FLOW-044 AC-1~8 단언 추가 (3변종 루프) | ✅ 선행 |
| T-PH002-02 | 3변종에 `## 5.5 웨이브 종료 상호검증` 신설 + Phase 흐름 블록에 `Phase 3.5` 삽입 | green |

배치 단언은 **문자 인덱스 순서**로 확인한다 — `§5.5` 헤딩이 `§5` 뒤, `§6` 앞이며
Phase 흐름 블록에서 `Phase 3` < `Phase 3.5` < `Phase 4` 일 것.

### PH-003 — 수렴·개선 위임 · FR-FLOW-045 전 AC

| Task | 내용 | red |
|---|---|---|
| T-PH003-01 | FR-FLOW-045 AC-1~8 단언 추가 | ✅ 선행 |
| T-PH003-02 | §5.5 에 종료 조건·cap·위임 라우팅·금지사항 소절 추가 + §7.3 전파 표에 본 루프 결선 | green |

`--mini`/`--loops` 전파 표(§7.3)가 현재 per-wave `kiwi-srs`/`kiwi-pipeline` 만 가리키므로
본 루프를 추가하지 않으면 `--mini` 가 이 루프를 **줄이지 못한다** (loop-option §6 결선 누락).

### PH-004 — critical_gates + 기록 · FR-FLOW-046 AC-6~8

| Task | 내용 | red |
|---|---|---|
| T-PH004-01 | FR-FLOW-046 AC-6~8 단언 추가 (3변종): `§0.G` gate 표 5행, `--auto` HALT 문장, `pipeline_run_id` 기록 의무 | ✅ 선행 |
| T-PH004-02 | 3변종에 `## 0.G critical_gates[]` 표 신설 + §7.1 에 HALT 문장 + §6 에 `pipeline_run_id` 의무 | green |

`--auto` 가 비활성→활성으로 바뀌는 **동작 변경**이므로 SRS Change Notes 에 명시한다.

### PH-005 — 통합 검증

1. `npm test` 전체 (회귀 0)
2. `npm run typecheck` / `lint` / `build`
3. `speckiwi validate --fail-on-warning`
4. **독립 서브에이전트 2기**로 교차검증 (본 기능이 규정하는 것과 동일한 축 구성):
   V1 = SRS 의도 실현 + 과정 적합, V2 = 산출물 품질 + 회귀 위험. 입력 격리 — 제 결론 미전달
5. finding 해소 → 재검증 → `update_status(verified)` + `add_verification_evidence`

## §4 변종별 적응 (필수)

| 변종 | 적응 |
|---|---|
| claude | 기준. `/kiwi-srs` 슬래시 표기, `~/.claude/skills/_shared/...` 경로 |
| codex | `$kiwi-srs` 표기, `../_shared/...` 상대 경로 |
| **etc** | **단일 워커 프로파일** — `local-llm-profile.md` 가 "N 리뷰어"를 순차 단일 평가자로 재해석한다. 따라서 **"두 서브에이전트를 병렬 spawn"이 아니라 "두 검증 축"**으로 서술해야 한다. `--max` 기본값 `on` |

etc 변종에 claude 문구를 그대로 복사하면 QB-209 회귀 계열을 재발시킨다.

## §5 완료 정의

- 3 REQ × 8 AC = **24 AC** 전부 체크 + 증거 등록
- 전체 스위트 회귀 0, validate 0/0
- 서브에이전트 2축 교차검증에서 substantive finding 0
- 커밋은 **사용자 결정** (본 계획 범위 외)

## §6 미해결 (사용자 결정 대기)

1. v2.4.2 작업물이 여전히 미커밋 상태이며 본 작업이 그 위에 쌓인다 — 분리 커밋 여부
2. Target Map 에서 v2.4.2 가 `planned` 로 표기됨 (set_active_target 의 자동 전이). 실제로는 3 REQ verified·미릴리스
