# v2.4.4 라운드-3 구현 계약 — R3-H1 … R3-L2 + R3-H11 … R3-M20

대상 findings: `docs/analysis/wave-fit-eval/round3-findings.md`
(3축 평가 HIGH 10 · MEDIUM 17 · LOW 2 = 29건) + **추가 finding 5건**(H11 · H12 · M18 · M19 · M20)
= **전 34건**. 추가 5건의 SRS 소재지는 **FR-FLOW-066** 단일이며, 대응 편집은 본 문서 **§12** 에 모았다.

대응 테스트:

- `test/skills/kiwi-wave-continuity-r3-content.test.ts` (신규, 스킬 3사본)
- `test/skills/kiwi-event-contract-content.test.ts` 의 신규 describe 2개
  (`R3 — waves-event …`, `R3-H4 — the re-entry emit key …`, 공유 계약 4사본)

선행 계약 2건:

- `docs/plans/2026-07-29.speckiwi.v244.implementation-contract.md` (FR-FLOW-047~055)
- `docs/plans/2026-07-29.speckiwi.v244-r2.implementation-contract.md` (R2-C1~R2-L4)

본 계약의 신규 식별자는 위 두 계약이 확정한 필드명·enum·gate_id·옵션명 전체와 대조했고 **충돌 0건**이다(§9).

**저작 시점 red 기준선** (구현 착수 전): `npx vitest run test/skills --no-file-parallelism` →
20 파일 / 1200 tests / **263 fail / 937 pass**. 기존 928 건은 전량 통과 상태로 유지되고,
신규 272 건 중 **263 red / 9 green** 이었다(green 9건의 사유는 §12.3 · §12.4).

구현이 병행 진행 중이라 실측치는 계속 줄어든다. 최종 목표는 §13 의 **0 fail** 이다.
§0.1 의 R2 상수 갱신은 **이미 적용했다**(테스트 파일이라 저작자 소관) — 구현자가 다시 할 필요 없다.

---

## 0. 먼저 읽을 것 — 기존 테스트 1건과의 의도적 충돌

### 0.1 R2-M9 canonical gate 집합 (**이미 적용됨** — 구현자 작업 아님)

`test/skills/kiwi-wave-continuity-r2-content.test.ts` 의 상수
`CANONICAL_GATE_IDS["kiwi-wave-master"]` 는 gate_id 집합의 **정확한 동등**을 요구한다(12건).
본 계약은 §3.1 에서 4건을 추가하므로, 그 상수를 고치지 않으면
`R2-M9 › kiwi-wave-master › matches the canonical set pinned by the contract` 1건이 red 로 남는다.

**단언 약화가 아니라 같은 강도로 새 집합에 재고정**한 것이며, 테스트 파일이므로 저작자가 이미 반영했다.
배열에 아래 4개가 추가되어 있다(순서 무관) — 구현자는 확인만 하면 된다:

```ts
    "wave-verify-fail-residual",
    "out-of-scope-user-consent",
    "wave-append-cap-exhausted",
    "invalid-loop-option"
```

`RETIRED_GATE_IDS` 는 손대지 않는다. 다른 스킬의 canonical 집합도 바뀌지 않는다 —
본 라운드는 kiwi-wave-master 외 어느 스킬에도 gate 를 추가하지 않는다.

r3 스위트에도 같은 4건을 독립적으로 고정한 describe
(`R3 — the new kiwi-wave-master gates …`)가 있으므로, 한쪽만 고치면 다른 쪽이 잡는다.

### 0.1b R3-M20 — 기존 단언 1건을 **강화**로 교체함 (이미 적용됨)

`test/skills/kiwi-wave-continuity-content.test.ts` 의
`FR-FLOW-054 › AC-6: requires a green full regression run …` 안의 마지막 단언을 교체했다.

교체 전 정규식 `/`exit_code`\s*=?\s*0|회귀 스위트 통과/` 은 R2-H5 가 Normal 행에 붙인 **fallback 절**
("기준선 부재 시 `exit_code`=0")에 매치해 통과하고 있었다 — 이름이 말하는 주 조건은 더 이상 고정되지
않는 상태였다. 두 조건의 **역할**을 각각 고정하도록 바꿨다.

1. 주 조건 = `failing_tests ⊆ baseline_failing_tests`
2. `exit_code` 는 그 행에 **정확히 1회**만 등장
3. 그 1회는 `기준선 부재 시 … exit_code=0` 형태여야 함

**약화가 아니라 강화**이므로 예외적으로 기존 단언을 고쳤다(팀 리드 승인 범위). 이 교체 후에도 해당 파일은
171/171 통과한다. **그 외 기존 단언은 여전히 수정 금지**이며, 본 계약은 §0.1 하나만 추가로 요구한다.

### 0.2 이 외에 기존 단언과 양립 불가한 항목은 **없다**

아래 세 곳은 기존 단언이 걸려 있으므로 **문장을 지우지 말고 덧붙여** 고친다.

| 기존 단언 | 지키는 방법 |
|---|---|
| `kiwi-event-contract` R2 `%s detects an arithmetically unreachable pass` — §4 의 `남은 라운드` 줄이 `스트릭` · `fail-cap` 을 담을 것 | R3-L1 은 **그 줄에 숫자를 덧붙인다**(§2.5). 새 문장을 만들지 않는다 — 만들면 `line()` 이 여전히 기존 줄을 먼저 잡아 red 가 남는다 |
| `kiwi-wave-continuity-r2` R2-M5 — §5.5.2 `frozen_denominator` 줄이 4키를 담을 것 | R3-H8 은 5번째 키 `constraints` 를 **추가**한다. 기존 4키 토큰은 그대로 둔다 |
| `kiwi-wave-continuity-r2` R2-H7 — §3.1 의 `이월` 줄이 `이월 finding 목록` 을 담을 것 | R3-M5 는 **같은 줄**에 변환 단위와 `excerpt_path` 를 덧붙인다 |

`waves-event.md` §5 emit 예시의 주석
`# … diff 창은 wave 단위이므로 싣지 않는다.` 는 R3-H9 가 **교체**한다.
이 문장을 고정하는 기존 단언은 없다(확인 완료).

### 0.3 계약 버전은 1.3.0 에 머문다

본 라운드도 선택 필드를 추가한다(`plan_run_id` · `run_diff_window` · `integration_items` ·
`exclusion_class`). 파일 서두의 SemVer 규칙만 보면 1.4.0 minor bump 지만, **1.3.0 을 유지한다**:

- 1.3.0 은 아직 릴리스되지 않았다. 라운드 2 가 방금 올린 버전이며 소비자가 없다. 미출고 버전에
  필드를 더 얹는 것은 "이미 기록된 이벤트의 해석을 바꾸는" 행위가 아니다.
- 기존 테스트가 1.3.0 리터럴을 **6곳**(FR-FLOW-046 블록 3건 + R2 블록 3건 × 4사본)에서 고정한다.
  네 번째 버전 재고정은 그 6곳을 다시 건드리게 만들 뿐 얻는 것이 없다.
- 새 필드의 표 셀에는 `(v1.3.0 신설)` 을 그대로 붙인다 — 라운드 2 필드와 같은 표기다.

---

## 1. 3사본 / 4사본 원칙과 유지해야 하는 의도적 차이

| 대상 | 사본 |
|---|---|
| `kiwi-wave-master/SKILL.md` | `skills/{claude,codex,etc}/` **3사본** (`.agents` 미러 제외) |
| `_shared/kiwi/waves-event.md` · `_shared/kiwi/pipeline-event.md` | `skills/{claude,codex,etc}/_shared/kiwi/` + `.agents/skills/_shared/kiwi/` **4사본** |
| `kiwi-srs` · `kiwi-srs-feasibility` · `kiwi-pipeline` · `kiwi-planner` · `kiwi-pm` · `kiwi-coder` · `kiwi-review-fix-loop` | `skills/{claude,codex,etc}/` **3사본** |

**유지해야 하는 기존 차이 — 새 차이를 만들지 말 것**:

| 축 | claude | codex | etc |
|---|---|---|---|
| 스킬 호출 접두 | `/kiwi-srs` | `$kiwi-srs` | `$kiwi-srs` |
| `_shared` 경로 | `~/.claude/skills/_shared/kiwi/...` | `../_shared/kiwi/...` | `../_shared/kiwi/...` |
| `--max` 기본값 | off | off | **on** (local-llm-profile) |
| `--max` 연속 스트릭 | 2라운드 | 2라운드 | **3라운드** |
| §5.5.4 Normal 행의 지위 | 단독 종료 조건 | 단독 종료 조건 | `--max` 행의 구성요소로만 (etc 전용 단락 유지) |
| 상단 안내 블록 | Kiwi MCP rule | Codex clarification gate 안내 | etc local-llm-profile override |
| **`kiwi-coder`·`kiwi-planner`·`kiwi-pm` 의 emit 절 위치** | `SKILL.md` §12 / §17 / §10 | `references/extended-workflow.md` 의 **같은 절 번호** | codex 와 동일 |
| 그 emit 절의 기술 | 직접 `pipeline.jsonl` append | MCP `workflow_pipeline_emit` + mutation envelope | codex 와 동일 |

마지막 두 행이 본 라운드에서 새로 확인된 축이다. R3-H2 의 `gate_id` 문장과 R3-H4 의 재진입
접미사 문장은 **각 변형이 emit 을 기술한 곳**에 쓴다 — claude 는 `SKILL.md`, codex/etc 는
`references/extended-workflow.md` 다. 테스트가 두 위치를 그 순서로 찾는다(`skillOrRefSection`).
codex/etc 에서 SKILL.md 로 emit 절을 되옮기지 말 것 — 그것이 새 차이다.

`--max` 스트릭 숫자(R3-L1)만이 본 라운드에서 사본별로 달라지는 값이며(claude/codex `2`, etc `3`),
그 외 본 계약의 모든 신규 문장은 **세 사본 동일**이다.

---

## 2. `_shared/kiwi/waves-event.md` (4사본)

### 2.1 §2.2 선택 필드 — 신규 2행 (R3-H5 · R3-H9)

표 끝에 아래 두 행을 추가한다.

```
| `plan_run_id` | string | 그 wave 의 **계획 run-id** (`docs/plans/{plan_run_id}.plan.md` · `.kiwi/sessions/{plan_run_id}/`). `pipeline_run_id` 와 **다른 값**이며, 재개가 `--plan-run-id` 로 되돌려 줄 값이다 (v1.3.0 신설) |
| `run_diff_window` | object | `{ base_sha, head_sha }` — run 전체의 diff 창. `phase="final-verify"` 이벤트에만 싣는다 (v1.3.0 신설) |
```

`plan_run_id` 셀은 `pipeline_run_id` 를 **반드시 호명**한다 — 두 값이 혼동되는 것이 이 필드가
없던 이유다.

### 2.2 §2.2 산문 — 생산 의무의 수신측 (R3-H3)

`pipeline_run_id` 가 "사실상 필수"임을 말하는 단락 **바로 뒤**에 1줄 추가한다.

```
`diff_window` 와 `pipeline_run_ids` 도 `phase=wave-verify` 와 `complete` 이벤트에서는 **사실상 필수**다 — 보존 계층의 분모가 이 두 값에서만 도출되므로, 없으면 그 라운드의 판정을 재개 후 재구성할 수 없다.
```

한 줄에 두 필드명이 **함께** 들어가야 한다(테스트가 그 줄을 앵커로 잡는다). 헤지 어휘 금지.

### 2.3 §2.3 `verification` object (R3-H8 · R3-M4)

`frozen_denominator` 행의 값 셀을 아래로 교체한다(기존 4키 유지 + `constraints` 추가).

```
| `frozen_denominator` | object | `{ round: n, req_ac: n, design_items: n, preservation: n, constraints: n }` — 그 라운드 진입 시 freeze 한 분모 개수 (v1.3.0 신설) |
```

불릿 목록에 아래 2줄을 추가한다. 첫 줄은 `design_layer.expected` 불릿 **바로 아래**에 둔다.

```
- `constraint_layer.expected` 는 최신 `constraints_path` 아티팩트의 항목 수로 외부에서 고정하며, 검증자가 스스로 산정하지 않는다.
- `wave="all"` 이벤트의 `design_layer.expected` 는 모든 wave `design_items` 의 **합집합** 크기에 `integration_items` 길이를 더한 값이다 — 어느 한 wave 의 분모도 아니다.
```

두 줄 모두 헤지 금지. 두 번째 줄은 `wave="all"` 과 `design_layer.expected` 를 **같은 줄**에 담는다.

### 2.4 §2.4 `design_baseline` object (R3-H6 · R3-M4)

`out_of_scope` 행의 값 셀에 `exclusion_class` 를 넣는다.

```
| `out_of_scope` | array | 어느 wave 에도 배정되지 않고 명시적으로 범위 밖으로 기록한 최상위 섹션 `{ heading, reason, exclusion_class }` **전량** |
```

표 끝에 1행 추가한다.

```
| `integration_items` | array | wave 경계를 가로지르는 **통합 항목** 전량 — 어느 한 wave 의 scope 에도 속하지 않는 설계 항목. 각 항목 `{ id, heading_path, line_start, line_end, statement }` (v1.3.0 신설) |
```

표 아래 산문에 1줄 추가한다.

```
`exclusion_class` ∈ `already-implemented` / `superseded` / `external-ownership` / `user-excluded` / `non-normative` — 배제 사유를 **자유 텍스트**가 아니라 닫힌 분류로 남긴다(`reason` 은 그 분류의 부연이다). 목록 밖의 값은 쓰지 않는다.
```

### 2.5 §4 재개 규약 — 스트릭 숫자 (R3-L1)

**새 문장을 만들지 않는다.** 기존 "도달 불가한 PASS 탐지 (v1.3.0)" 문장의 끝에 이어 붙인다.

```
**도달 불가한 PASS 탐지 (v1.3.0)**: 재개 시점에 **남은 라운드** 수(`cap - rounds`)가 그 모드의 연속 clean **스트릭** 요구치보다 작으면 PASS 가 산술적으로 불가능하므로, 라운드를 더 돌지 않고 `verdict` 를 `fail-cap` 으로 기록하고 사용자 결정을 받는다. 그 **스트릭 요구치**는 Normal 이 **1** 이고(스트릭이 없는 모드도 clean 라운드 1회는 필요하다), `--max` 의 값은 변형마다 다르므로 `kiwi-wave-master §5.5.4` 의 표를 SSOT 로 읽는다.
```

기존 `남은 라운드` · `스트릭` · `fail-cap` 토큰은 전부 살아 있어야 한다(R2 단언).

### 2.6 §5 Emit 패턴 (R3-H9)

3번 블록(run-scope 최종 검증)의 주석과 JSON 을 아래로 바꾼다.

- 주석: `# 3) 마지막 wave 의 complete 뒤 1회. run-scope 최종 검증 (§3): wave 는 "all", order 는 0. run 창은 `run_diff_window` 로 싣는다(wave 단위 `diff_window` 는 싣지 않는다).`
- JSON: `"phase":"final-verify"` 뒤에 `"run_diff_window":{"base_sha":"<sha>","head_sha":"<sha>"}` 를 추가.

`diff 창은 wave 단위이므로 싣지 않는다` 라는 **원문 문구는 남기지 않는다**(테스트가 부재를 요구).
1)·2) 블록은 그대로 둔다 — 그 두 줄의 `diff_window` 는 유지된다.

---

## 3. `kiwi-wave-master/SKILL.md` (3사본)

### 3.1 §0.G `critical_gates[]` — 신규 4행 + 기존 2행 보강 (R3-H6 · R3-H10 · R3-M10 · R3-L2)

표에 아래 4행을 추가한다.

```
| `wave-verify-fail-residual` | 웨이브 종료 상호검증이 상한 전에 미해소 finding 을 남기고 끝남(`verdict="fail-residual"`) — 그 wave 는 `complete` 를 append 할 수 없어 위원회에는 저널 규칙을 만족하는 선택지가 없다 | §5.5 |
| `out-of-scope-user-consent` | 설계 항목을 `out_of_scope` 로 배제 — 배제된 항목은 모든 계층의 분모에서 빠지므로 `--auto` 라도 사용자 확인을 받는다 | §3.2 |
| `wave-append-cap-exhausted` | run 당 wave 추가 상한 **3** 소진 — 무인 실행의 종료를 보장하는 유일한 경계 | §5.6 |
| `invalid-loop-option` | `--loops N` 이 정수 ≥1 이 아님 — `loop-option.md` §1 이 명령하는 HALT 를 게이트로 전사 | §0.7 / loop-option.md |
```

기존 2행의 **reason 셀**을 보강한다(첫 셀 gate_id 와 발생 위치는 불변).

- `wave-verify-residual-critical` reason 끝에 추가:
  `. `fail-residual` 은 이 행이 아니라 **별도 행** `wave-verify-fail-residual` 이다`
- `final-verify-residual-critical` reason 끝에 추가:
  `, `fail-residual``
  (즉 최종 패스의 네 번째 트리거로 `fail-residual` 을 호명한다.)

### 3.2 §0.1 이벤트 SSOT 버전 핀 (R3-M1)

`waves-event.md` 를 가리키는 §0.1 행의 `v1.2.0` 을 **`v1.3.0`** 으로 바꾼다.
그 행에 `v1.2.0` · `v1.1.0` · `v1.0.0` 중 어느 것도 남기지 않는다.

### 3.3 §1.2 선택 입력 — 신규 2행 (R3-H7 · R3-M16)

```
| "제약", "이건 지켜줘", "금지 사항" | `--constraint <text>` (반복 가능; 선언 제약을 §3.1 아티팩트에 수집) | (없음) |
| "락 강제 해제", "force" | `--force` (kiwi-pm stale `pm.lock` 해제까지 pass-through §7.4, **명시** 입력만) | off |
```

`--force` 행에 `명시` 라는 단어가 반드시 들어간다.
`--auto-integration` · `--auto-cost-warning` 두 행은 이미 존재하므로 **건드리지 않는다**(§11 참조).

### 3.4 §2 Phase 흐름 — 인터리브 명시 (R3-M9)

코드펜스 아래 "재개 시 Phase 0 …" 문장 **앞**에 1줄 추가한다.

```
Phase 2~Phase 3.5 는 **wave 마다 반복**된다 — 모든 wave 의 target 을 앞에서 **일괄 등록하지 않는다**. §4 가 wave 진입 시점의 `carried_into` 를 수집하고 §6 이 3단계 재개 단위를 쓰는 것이 일괄 등록과 양립하지 않기 때문이다.
```

### 3.5 §3.1 설계 기준선 물질화 (R3-H6 · R3-H7 · R3-M4 · R3-M5)

**(a) out-of-scope 분류** — `out_of_scope` 를 처음 기술하는 문단 뒤에 1줄:

```
`out_of_scope` 의 각 항목은 `{ heading, reason, exclusion_class }` 이며, `exclusion_class` 는 `already-implemented` / `superseded` / `external-ownership` / `user-excluded` / `non-normative` 다섯 값의 **closed** 목록이다 — 목록 밖의 값을 쓰지 않는다. 자유 텍스트 `reason` 한 줄로 분모를 줄이는 경로를 닫는다.
```

**(b) 통합 항목 물질화** — `design_items` 문단 뒤에 1줄:

```
어느 한 wave 의 scope 에도 속하지 않는 **통합 항목**은 같은 시점에 `integration_items` 로 전량 고정한다 — 각 항목은 `design_items` 와 같은 `id` · `heading_path` · `line_start` · `line_end` · `statement` 를 갖는다. 최종 패스(§5.6)의 분모가 여기서 나온다.
```

**(c) 사용자 제약 수집** — `constraints_path` 문단 **앞**에 2줄:

```
사용자 제약은 판정하기 전에 **수집한다** — wave 분해 입력(사용자 프롬프트 · 대화 로그 · `--constraint` 인자)에서 선언된 **제약을 추출**해 호명 단위로 아티팩트에 적는다. 수집 단계가 없으면 배열은 언제나 비고, 그때 `constraint_layer.expected=0` 이라 어떤 판정도 실패시키지 못한다.
추출 단위는 선언된 **제약 문장 1건 = 1 항목**이며, 각 항목은 `id` · `statement` · `source` 를 갖는다 — `source` 는 그 제약이 선언된 위치(프롬프트 / 대화 / `--constraint`)다.
```

**(d) 이월 wave 의 기준선** — 기존 `이월(carry-forward)로 신설된 wave …` 줄을 아래로 교체한다.
`이월 finding 목록` 이라는 문구는 **반드시 그대로 남긴다**(R2 단언).

```
이월(carry-forward)로 신설된 wave 는 입력 문서의 어느 구간에도 대응하지 않으므로, 설계 기준선 대신 **이월 finding 목록**을 그 wave 의 설계 계층 분모로 삼는다 — **이월 finding 1건 = `design_items` 1 항목**으로 변환하며(`id` 는 그 finding id, `statement` 는 그 finding 요약, `heading_path` 는 `carried-forward`, 줄 좌표는 `0`), 그 목록을 markdown 으로 물질화해 `excerpt_path` 에 기록한다. 변환하지 않으면 §5.5.2 의 `design_layer.expected` 규칙과 §4 의 발췌 전달 의무가 이 wave 에서 동시에 성립 불가능해진다.
```

### 3.6 §3.2 분해 커버리지 게이트 — 배제 동의 (R3-H6)

`out-of-scope 는 판단이 아니라 기록이다 …` 문단 뒤에 1줄:

```
기록만으로는 부족하다 — `out_of_scope` 에 항목이 하나라도 있으면 §0.G `out-of-scope-user-consent` 로 **`--auto` 라도 중단**하고 사용자 확인을 받는다. 배제는 그 항목을 REQ·설계·제약·보존 어느 분모에도 넣지 않으므로, run 전체에서 한 번도 검증되지 않는 유일한 경로다.
```

### 3.7 §4 target 등록 — 제약 인자 · 기존 모듈 전달 (R3-M2 · R3-M14)

`constraints_path` 를 전달한다는 기존 문장을 아래로 교체한다.

```
같은 호출에 `constraints_path` 아티팩트를 `--constraints-doc` 인자로 전달한다 — 저작 입력에 없는 제약은 검증에서 잡혀도 반영할 근거가 없고, 인라인 전달은 §4 가 바로 앞에서 부정한 경로다.
```

그 뒤에 1줄 추가한다.

```
그 wave 의 `existing_modules` 도 같은 호출의 **저작 입력**으로 전달한다 — 검증자만 읽는 목록은 무엇이 저작되는지를 바꾸지 못한다.
```

### 3.8 §5 per-wave pipeline — target 명시 전달 (R3-M6)

`target 을 wave-{n} 으로 지정하여 호출하면 …` 문장 뒤에 1줄:

```
target 은 `--target` 인자로 `wave-{n}` 을 **명시 전달**한다 — 활성 target 이 `/kiwi-srs` 의 `set_active_target` **부수효과**로만 정해지면, §4 를 건너뛰는 재개(§6)에서 그 호출 지점이 사라져 pipeline 이 이전 wave 의 target 위에서 돈다.
```

### 3.9 §5.5.2 두 검증자 (R3-H1 · R3-H8 · R3-M12)

**(a) 보존 분모에 약화 부류 추가** — `세 부류가 그 분모다 …` 문장을 아래로 교체한다.

```
검증자 2 의 **분모**는 그 wave 의 diff 에서 **기계적으로 도출한다** — 검증자가 고르지 않는다. **네 부류**가 그 분모다: 삭제·이동된 기존 파일, 삭제·변경된 기존 public 심볼, 삭제·수정된 기존 테스트 파일, 그리고 기존 테스트의 **단언 약화**(판정 기준은 `kiwi-coder §0.20.3` 의 closed list 를 그대로 따른다).
```

**(b) 약화의 verdict 고정** — `근거를 대지 못한 행은 …` 문단 뒤에 1줄:

```
단, **약화는** REQ 또는 Task 근거가 있어도 `intended-improvement` 로 **기록하지 않는다** — 언제나 `unapproved-damage` 다. 계획 Task 안에서 단언을 낮추는 것이 이 루프에서 가장 값싼 우회로이고, 하위 루프의 PASS 가 wave 게이트를 충족하지 않는다는 §5.5.5 의 규칙과도 어긋나기 때문이다.
```

**(c) 두 계층의 판정 규칙 공유** — `intended-improvement` 근거 규칙 문단 뒤에 1줄:

```
이 판정은 실행 계층과 **같은 규칙**이다 — `kiwi-coder §0.20.4` 가 같은 두 값 enum 과 같은 근거 요건을 쓴다. 두 계층이 서로 다른 규칙을 쓰면 계획으로 승인된 리팩터가 검증에서는 통과하고 실행에서는 HALT 한다.
```

**(d) 제약 분모의 외부 고정** — `constraint_layer` 문단 뒤에 1줄:

```
`constraint_layer.expected` 는 최신 `constraints_path` 아티팩트의 항목 수이며 **검증자가 스스로 산정하지 않는다** — 네 계층 중 이것만 외부 고정이 없으면 분모를 스스로 0 으로 만드는 경로가 남는다.
```

**(e) frozen_denominator 확장** — 기존 줄에 `constraints` 를 추가한다.

```
분모는 라운드 진입 시 freeze 하고 그 개수를 `frozen_denominator` 의 `round` · `req_ac` · `design_items` · `preservation` · `constraints` 에 기록한다.
```

### 3.10 §5.5.4 종료 조건 (R3-H10 / R3-M17 · R3-L1)

`cap 소진은 PASS 가 아니다` 문단 뒤에 2줄 추가한다.

```
**Normal 조기 종료**의 MEDIUM/LOW 잔여는 `pass` + `residual` 이며 **사용자 결정을 받지 않는다**. 그 밖의 미해소 finding 을 남긴 채 상한 전에 끝난 경우만 `fail-residual` 이고, 그때는 §0.G `wave-verify-fail-residual` 로 중단해 사용자 결정을 받는다. 두 경우를 한 문장으로 읽으면 조기 종료마다 무인 실행이 멈추거나, 반대로 `fail-residual` 이 조용히 통과한다.
**스트릭 요구치는** Normal **1** 라운드, `--max` **2** 라운드다 — 위 "남은 라운드" 판정이 비교하는 값이 이것이다.
```

**etc 사본만** 두 번째 줄의 `--max` 를 **3** 으로 쓴다(local-llm-profile 3연속 규칙과 일치).
첫 줄은 세 사본 동일하다.

두 번째 줄의 굵게 표기 위치에 주의한다 — `**스트릭 요구치**는` 이 아니라 `**스트릭 요구치는**` 이다.
테스트가 `스트릭 요구치는` 을 연속된 문자열로 찾고, 기존 `**스트릭 요구치**보다 작으면` 줄과
구분하기 위한 앵커가 그것이다.

### 3.11 §5.5.5 개선 위임 — feasibility 필터 (R3-M8)

라우팅 표의 draft REQ 행을 아래로 교체한다.

```
| feasibility · planning 단계가 draft 로 남은 REQ | 그 REQ 만 `--req-filter <REQ-ID[,…]>` 로 지정해 `/kiwi-srs-feasibility` 재실행 후 planning 재진입; 승급 실패 시 `reason_class="draft-stability-skip"` 로 residual |
```

### 3.12 §5.5.6 기록 — 생산 의무 (R3-H3 · R3-M1)

헤딩을 `### 5.5.6 기록 (waves-event 1.3.0)` 으로 바꾼다.

절 끝에 3줄 추가한다(각 줄은 한 필드의 **생산 시점**을 못 박는다).

```
`base_sha` 는 **wave 진입** 시점의 HEAD 를 캡처해 그 wave 의 이벤트에 싣는다 — 재개 세션에는 분해 대화가 없으므로, 캡처 시점을 규정하지 않으면 보존 계층의 분모가 통째로 사라진다.
`head_sha` 는 **wave-verify** 라운드 진입 시점의 HEAD 를 캡처하고, 두 값을 그 이벤트의 `diff_window` 에 싣는다.
pipeline 을 **spawn 할 때마다** 그 run_id 를 `pipeline_run_ids` 에 **append** 한다 — 재진입이 만든 run 이 빠지면 증거 창이 수정 전에서 멈춘다.
```

### 3.13 §5.6 전체 wave 최종 검증 (R3-H6 · R3-H9 · R3-M3 · R3-M4 · R3-M10)

**(a) 분모의 외부 고정** — 분모 문단을 아래로 교체한다.

```
최종 패스의 **분모**는 설계 기준선 **전체** — **모든 wave** 의 `design_items` 합집합 — 에 §3.1 이 고정한 `integration_items` 를 더한 것이며, **검증자가 스스로 산정하지 않는다**. 이것을 모든 wave 요구사항의 합집합에 대조한다.
```

**(b) 배제 항목 재검사** — 그 뒤에 1줄:

```
여기에 `out_of_scope` **전량**을 별도 계층으로 함께 싣고, 검증자 2 기가 "이 run 에서 구현되지 않음이 의도됨"을 항목마다 확인한다 — 배제는 run 전체에서 한 번도 검증되지 않는 유일한 경로이므로 최종 패스가 그 유일한 재검사 지점이다.
```

**(c) 루프 재사용 범위** — `루프 자체는 §5.5 를 그대로 재사용한다:` 문장의 열거에 §5.5.5 와
§5.5.7 을 추가한다.

```
루프 자체는 §5.5 를 그대로 재사용한다: **정확히 2기**의 검증자를 stance 로 분리하고, 교차반박은 `add-only` 이며, clean 라운드는 **그 라운드에서 수정이 적용되지 않았을 것**을 요구한다. 라운드 상한과 `--mini` / `--loops N` 도 §5.5.4 를 따르고, finding 의 수정 라우팅은 §5.5.5 를, wave 로 귀속되는 finding 의 이월은 §5.5.7 을 그대로 쓴다 — 재사용 목록에서 이 둘이 빠지면 최종 패스의 finding 은 전부 HALT 로만 끝난다.
```

**(d) 보존 분모의 창** — **절 안의 순서를 바꾼다.** 현재 §5.6 은
`최종 패스도 unapproved-damage 0 건을 …` → `§5.5.4 의 "wave head 회귀"는 run head 회귀로 읽는다`
순서인데, 창을 정하는 문장이 그 창에 의존하는 판정보다 **뒤**에 있다. 아래 순서로 바꾼다.

1. (기존) `§5.5.4 의 "wave head 회귀"는 최종 패스에서 **run head** 회귀로 읽는다.` — **문장 그대로**, 위치만 위로
2. (신규) `같은 방식으로 §5.5.2 의 **보존** 분모도 최종 패스에서는 **run 창** — `run_diff_window` 의 `base_sha` ~ run head — 으로 읽는다. 최종 이벤트는 `wave="all"` 이라 wave 단위 `diff_window` 가 원리적으로 없기 때문이다.`
3. (신규) `**마지막 wave** 의 창을 대신 고르지 않는다 — 그러면 run 전체의 파손 판정이 마지막 wave 의 재검사가 된다.`
4. (기존) `최종 패스도 `unapproved-damage` **0 건**을 통과 조건으로 요구한다.` — **문장 그대로**, 위치만 아래로

테스트가 `run_diff_window` 의 등장 위치가 `unapproved-damage` 보다 **앞**일 것을 요구한다(창을
먼저 세우지 않으면 그 창에 의존하는 통과 조건이 무엇을 읽는지가 정의되지 않는다). 두 기존 문장의
**본문은 한 글자도 바꾸지 않는다** — 기존 단언 2건이 그 문장들을 읽는다.

**(e) wave 추가 상한** — run-scope finding 라우팅 문단 뒤에 2줄:

```
wave 추가는 run 당 **3** 회를 상한으로 한다 — 상한에 닿으면 §0.G `wave-append-cap-exhausted` 로 중단하고 잔여를 전량 보고한다. 상한이 없으면 최종 패스가 wave 를 추가하고 그 wave 가 다시 최종 패스를 부르는 루프에 종료 보장이 없다.
최종 패스를 재실행할 때 **라운드 카운터**는 재실행을 가로질러 **누적**된다 — waves-event §4 의 재개 누적과 같은 규칙이며, 재실행마다 1 부터 다시 세면 cap 자체가 도달 불가능해진다.
```

### 3.14 §6 진행 추적 (R3-H5 · R3-M6)

재개 단위 3단계 문단 뒤에 3줄 추가한다.

```
**pipeline 단계 재개**는 §5.5.5 재진입과 동일하게 `--plan-run-id` 와 `--req-filter` 를 **반드시 함께** 전달하고, `kiwi-pm` 은 `--resume` 으로 진입한다 — 범위 없이 재개하면 feasibility 부터 통째로 다시 돌아 stability mutation 이 재적용되고 완료된 Task 가 중복 실행된다.
그 값은 `plan_run_id` 필드로 `waves.jsonl` 에 기록한다 — 재개 세션에는 대화가 없으므로 저널에서 해소하지 못하면 위 두 인자를 만들 수 없다.
§4 를 건너뛰는 재개에서는 활성 target 을 `set_active_target` 으로 **명시 고정**한 뒤 pipeline 에 진입한다 — 그 경로에는 `/kiwi-srs` 호출이 없어 target 을 세우던 부수효과가 사라진다.
```

### 3.15 §7.4 pass-through — 신규 1행 (R3-M13)

표에 1행 추가한다.

```
| `--regression-baseline` | kiwi-coder / kiwi-review-fix-loop 의 회귀 델타 판정 | kiwi-wave-master → kiwi-pipeline → kiwi-pm → kiwi-coder (§2.1 이 pin 한 값) |
```

### 3.16 §9 Pipeline emit — 재개 키 (R3-M11)

절 끝에 1줄 추가한다.

```
**재개**된 실행은 `{run_id}#r{n}` 를 emit 키로 쓴다(`pipeline-event.md` §5.4) — 멱등 skip 은 **같은 키**에만 적용된다. 중단 시 `FAILED` 를 남긴 run 이 재개로 완주하면, bare `run_id` 로는 최종 `TASK_DONE` 이 영구히 기록되지 않아 `kiwi-pipeline` 의 직전-이벤트 게이트가 계속 발동한다.
```

---

## 4. `_shared/kiwi/pipeline-event.md` (4사본) — R3-H4

`### 5.3 dry-run` **뒤**에 신설한다.

```
### 5.4 재진입 멱등 키

부모 오케스트레이터(`kiwi-wave-master`)의 개선 위임이 같은 run 을 다시 돌리는 **재진입** 실행은 emit 키에 회차 접미사를 붙인다: `{run_id}#r{n}` (`n` = 그 run 의 재진입 회차, 1-based).

§5.2 의 멱등 skip 은 **같은 키**에만 적용한다 — bare `run_id` 를 재사용하면 재진입이 skip 되어 체인이 볼 새 `TASK_DONE` 이 생기지 않는다.

본 규약은 `kiwi-pipeline` · `kiwi-planner` · `kiwi-pm` 이 함께 따른다 — 세 스킬 모두 run 을 재사용해 다시 실행될 수 있고, 한 곳만 접미사를 쓰면 나머지가 이벤트를 남기지 못한다.

emit 키는 계획 산출물의 `run_id` 와 **다른 id 공간**이다 — 사이드카 id 정규식 `[a-z0-9.-]{4,40}` 은 emit 키에 **적용하지 않는다**. 그 정규식은 계획 산출물의 식별자를 검사하는 규칙이고, emit 키는 저널의 중복 판정에만 쓰인다.
```

`§5.2 멱등성` 절 자체는 손대지 않는다 — 기존 단언이 그 블록을 읽는다.

---

## 5. `kiwi-pipeline/SKILL.md` (3사본)

### 5.1 §1.2 선택 입력 — 신규 1행 (R3-M6)

```
| "target X 로", "이 target 만" | `--target <target>` (그 사이클의 SRS target 명시 §7) | `get_active_target` |
```

### 5.2 §7.4 pass-through — 신규 2행 (R3-M7 · R3-M13)

```
| `--force` | `kiwi-pm` 의 stale `pm.lock` 해제 | kiwi-pipeline → `kiwi-pm` (**명시** 입력만) |
| `--regression-baseline` | `kiwi-coder` / `kiwi-review-fix-loop` 의 회귀 델타 기준선 | kiwi-pipeline → `kiwi-pm` → `kiwi-coder` |
```

### 5.3 §7.5 재진입 emit 키 — SSOT 이관 (R3-H4)

절 첫 문단 뒤(또는 접미사를 정의하는 문장 자리)에 1줄을 넣어 소유권을 넘긴다.

```
이 접미사 규약의 SSOT 는 `_shared/kiwi/pipeline-event.md` **§5.4** 다 — 본 절은 그 규약을 사이클 관점에서 인용할 뿐이며, 자식 스킬은 본 절이 아니라 그 공유 계약을 읽는다.
```

(claude 사본은 `~/.claude/skills/_shared/kiwi/pipeline-event.md`, codex/etc 는 `../_shared/…` 경로 표기를 쓴다.)
기존 `{run_id}#r{n}` 문장과 자식 적용 문장은 **그대로 둔다**(R2 단언).

---

## 6. `kiwi-planner/SKILL.md` (3사본)

### 6.1 §0 공통 규약 — 신규 §0.22 행 (R3-M14)

```
| §0.22 | **기존 구조 보존 입력**. 상위 오케스트레이터가 전달한 `existing_modules` 목록의 모듈을 **이동·삭제·시그니처 변경**하는 Task 는 그 사실을 Task `action` 에 **명시**한다 — `files[]` 등재만으로는 편집 허가일 뿐 제거·이동 허가가 아니며(`kiwi-coder §0.20.4`), action 에 없는 파손은 실행 계층에서 근거 없는 파손으로 판정되어 HALT 한다 |
```

### 6.2 emit 절 — 재진입 키 (R3-H4)

claude 는 `SKILL.md` §17, codex/etc 는 `references/extended-workflow.md` §17 에 1줄 추가한다.

```
- 멱등 키: 재진입 실행은 `{run_id}#r{n}` 를 쓴다(`pipeline-event.md` §5.4) — 같은 키가 아니면 skip 하지 않는다. `--plan-run-id` 로 계획 run 을 재사용한 재진입이 이벤트를 남기지 못하면 체인이 볼 새 `TASK_DONE` 이 없다.
```

claude 사본의 기존 `멱등성: 동일 `run_id` 의 이벤트가 이미 존재하면 skip.` 문장은 **지우지 않고**
위 줄을 그 예외로 덧붙인다.

---

## 7. `kiwi-pm/SKILL.md` (3사본)

### 7.1 §0.14 id 정규식 행 보강 (R3-H4)

기존 §0.14 행 끝에 추가한다(정규식 자체는 불변).

```
 이 정규식은 **사이드카 식별자** 전용이며 **이벤트 emit 키**에는 **적용하지 않는다** — 재진입 emit 키 `{run_id}#r{n}` 는 다른 id 공간이다(`pipeline-event.md` §5.4).
```

### 7.2 §1.3 CLI 인자 요약 — 신규 1행 (R3-M13)

```
         [--regression-baseline <path>]   # 부모가 pin 한 회귀 기준선을 kiwi-coder 로 pass-through
```

### 7.3 §3.4 메인까지 올라오는 NEEDS_USER — gate_id 매핑 (R3-H2)

목록 뒤에 1줄 추가한다.

```
자식이 §0.G6 게이트로 중단한 경우 그 payload 의 `gate_id` 를 그대로 읽어 **동명**의 §0.G7 게이트로 매핑한다 — severity 로 재분류하지 않는다. 재분류하면 always-HALT 로 선언한 두 보존 게이트가 `business-decision` 기본 분류로 되돌아간다.
```

### 7.4 emit 절 — 재진입 키 (R3-H4)

claude 는 `SKILL.md` §10, codex/etc 는 `references/extended-workflow.md` §10 에 §6.2 와
**같은 형식**의 1줄을 추가한다(문구 동일, `kiwi-planner` → `kiwi-pm` 만 다름).

---

## 8. `kiwi-coder` · `kiwi-review-fix-loop` · `kiwi-srs` · `kiwi-srs-feasibility` (각 3사본)

### 8.1 `kiwi-coder` §0 — 신규 §0.20.4 행 (R3-H1 · R3-M12)

§0.20.3 **바로 아래**에 넣는다.

```
| §0.20.4 | **증거 기반 해소**. 기존 파일의 삭제·이동(§0.G6 `existing-file-deleted-or-moved`) 과 기존 public 심볼의 삭제·시그니처 변경(동 `existing-public-contract-change`) 은, 그 변경을 요구하는 **REQ-ID** 또는 **Task-ID** 가 `sidecar` 에 있고 그 Task 의 `action` 이 이동·삭제·시그니처 변경을 **명시**할 때 `intended-improvement` 로 기록하고 진행한다. 근거를 대지 못한 변경은 `unapproved-damage` 이며 critical 이다 — `sidecar.files[]` 등재는 편집 허가일 뿐 제거 허가가 아니다. **단 §0.20.3 의 약화와 기존 테스트 삭제는 같은 근거로 해소되지 않는다** — 기준을 낮추는 것은 어떤 계획도 승인할 수 없다. 본 판정은 `kiwi-wave-master §5.5.2` 의 보존 계층과 **같은 두 값 enum · 같은 근거 요건**을 쓴다 |
```

### 8.2 `kiwi-coder` §0.G6 — 기존 2행의 reason 보강 (R3-H1)

- `existing-public-contract-change` reason 끝에 ` — 해소 경로는 §0.20.4` 를 추가.
- `existing-file-deleted-or-moved` reason 끝에 ` — 해소 경로는 §0.20.4` 를 추가.

(첫 셀 gate_id 와 발생 위치 셀은 불변. `existing-test-weakened-or-deleted` 는 **해소 경로가 없으므로
건드리지 않는다**.)

### 8.3 `kiwi-coder` §5.1 (d) — 자기 치유의 상한 (R3-H2)

`(d) 계획-코드 일치 게이트` 의 세 보존 검출 항목 아래(같은 트리의 마지막 자식으로) 1줄 추가한다.

```
  │       ├─ **동일 항목 2회째 검출** 시 (c) 재호출로 자기 치유하지 않고 §0.G6 의 대응 gate_id 로 부모에 **버블업**한다 — 1차 검출은 재구현으로 닫되, 같은 항목이 두 번 나오면 치유가 수렴하지 않는 것이므로 사용자 결정으로 올린다
```

### 8.4 `kiwi-coder` emit 절 — gate_id 채널 (R3-H2)

claude 는 `SKILL.md` §12, codex/etc 는 `references/extended-workflow.md` §12 의 `status` 불릿 아래
1줄 추가한다.

```
- `gate_id`: `NEEDS_USER` 로 종료한 경우 그 중단을 일으킨 §0.G6 게이트의 id. 부모(`kiwi-pm`)는 이 값을 동명 게이트로 매핑한다 — 이 채널이 없으면 어느 게이트가 멈췄는지가 부모에게 전달되지 않는다
```

### 8.5 `kiwi-coder` §1.2 · §3.5 — 기준선 수신 (R3-M13)

§1.2 표에 1행:

```
| "기준선 받아서", "부모 기준선" | `--regression-baseline <path>` | off (자기 시점 캡처) |
```

§3.5 절 끝에 1줄:

```
`--regression-baseline` 으로 부모가 pin 한 기준선을 받으면 그 값이 자기 시점 캡처보다 **우선한다** — 같은 run 의 두 수정 주체가 다른 기준선을 쓰면 앞 wave 가 만든 실패가 다음 계층에서 "원래 있던 실패"로 승격된다.
```

### 8.6 `kiwi-review-fix-loop` — 보존 스캔 절 신설 (R3-M15)

세 사본 모두에 **같은 제목의 절**을 만든다. claude 는 `### 6.2` 안의 기존 보존 스캔 문단을 아래
신설 절로 **옮기고**(같은 파일 안, Phase 4 다음), codex/etc 는 `## Workflow` 뒤·`### 회귀 테스트
기준선 캡처` 앞에 새로 쓴다.

```
### 보존 스캔 (fixer diff, §0.17)

fixer pass 가 적용한 **diff** 를 스캔한다 — **기존 테스트 파일 삭제 · 기존 테스트 케이스 제거 · 기존 단언 약화**, **기존 public 심볼의 삭제·시그니처 변경**, **비-테스트 기존 파일의 삭제·이동** 중 하나라도 검출되면 **CRITICAL** 로 올리고 §0.G8 의 대응 게이트(`existing-test-weakened-or-deleted` / `existing-public-contract-change` / `existing-file-deleted-or-moved`)로 중단한다. 판정 기준은 `kiwi-coder §0.20.1~§0.20.3`.

스캔은 까칠 리뷰어 **재검증(re-review)보다 먼저** 수행한다 — 뒤에 두면 약화된 테스트가 먼저 clean 판정을 받는다.
```

절 안에서 `fixer` 라는 토큰이 `재검증`/`re-review` 보다 **앞**에 나와야 한다(테스트가 위치를 비교한다).
codex/etc 의 `## Workflow` 번호 목록에서 fixer 단계 다음 줄에 `Run the preservation scan (보존 스캔)`
같은 참조를 넣어도 좋지만 필수는 아니다.

### 8.7 `kiwi-review-fix-loop` — 기준선 수신 (R3-M13)

입력 표(claude `### 1.2`, codex/etc `## Inputs`)에 1행:

```
| 부모 기준선 | `--regression-baseline <path>` | off (자기 시점 캡처) |
```

`회귀 테스트 기준선 캡처` 절 끝에 1줄:

```
- **부모 기준선 우선**: `--regression-baseline` 으로 상위 오케스트레이터가 pin 한 기준선을 받으면 그 값이 자기 시점 캡처보다 **우선한다** — 방금 만들어진 실패를 "기존 실패"로 분류해 `TASK_DONE` 을 반환하는 것이 wave 게이트와 정면으로 어긋나기 때문이다.
```

### 8.8 `kiwi-srs` §1.2 — 신규 1행 (R3-M2)

```
| "제약 문서 X", "--constraints-doc <path>", "선언된 제약 반영" | `--constraints-doc <path>` (선언된 사용자 제약 아티팩트; 저작 입력에 포함) | omit |
```

### 8.9 `kiwi-srs-feasibility` §1.2 — 신규 1행 (R3-M8)

```
| "이 REQ 만 재평가", "REQ 지정 feasibility" | `--req-filter <REQ-ID[,…]>` (그 REQ 만 평가·`update_stability`) | omit (전수) |
```

---

## 9. 확정된 식별자 목록 (요약)

**waves-event 신규 최상위 선택 필드 (2)**: `plan_run_id: string` · `run_diff_window { base_sha, head_sha }`

**`verification` 확장 (1)**: `frozen_denominator` 에 `constraints: n`

**`design_baseline` 신규/확장 (2)**: `integration_items: [{ id, heading_path, line_start, line_end, statement }]` ·
`out_of_scope[]` 에 `exclusion_class`

**신규 closed enum (1)**: `exclusion_class` ∈ `already-implemented` / `superseded` /
`external-ownership` / `user-excluded` / `non-normative`

**제약 항목 스키마**: `{ id, statement, source }` (아티팩트 내부, 이벤트 필드 아님)

**신규 옵션 (5)**: `--constraint`(kiwi-wave-master) · `--constraints-doc`(kiwi-srs) ·
`--req-filter`(kiwi-srs-feasibility — kiwi-pipeline 의 동명 옵션과 **같은 철자·같은 의미**) ·
`--target`(kiwi-pipeline) · `--regression-baseline`(kiwi-coder · kiwi-review-fix-loop, 그리고
kiwi-wave-master → kiwi-pipeline → kiwi-pm pass-through)

**§1.2 에만 추가되는 기존 옵션 (1)**: `--force`(kiwi-wave-master — 이미 §7.4 에 있음)

**신규 절 (2)**: `pipeline-event.md §5.4 재진입 멱등 키` ·
`kiwi-review-fix-loop ### 보존 스캔 (fixer diff, §0.17)`

**신규 규약 행 (2)**: `kiwi-coder §0.20.4` · `kiwi-planner §0.22`

**신규 emit 필드 (1)**: `gate_id` (kiwi-coder 의 `NEEDS_USER` payload)

**신규 상수 (2)**: wave 추가 상한 **3**/run · 스트릭 요구치 Normal **1** / `--max` **2**(etc **3**)

**신규 gate_id (4, 전부 kiwi-wave-master)**:

| gate_id | 발생 위치 |
|---|---|
| `wave-verify-fail-residual` | §5.5 |
| `out-of-scope-user-consent` | §3.2 |
| `wave-append-cap-exhausted` | §5.6 |
| `invalid-loop-option` | §0.7 / loop-option.md |

**추가 5건이 더하는 식별자 (§12)**:

- `kiwi-pm` 자식 반환 `severity` enum 에 **네 번째 값 `critical`** (기존 3종 유지). `auto-option.md §4`
  의 `critical` 행에 직접 매핑되며, 새 이름을 만들지 않았다.
- **신규 gate_id 0건** — H11 은 9종 전사 대신 catch-all 규칙 1줄로 닫는다.
- `auto-option.md §5.1` 카탈로그에서 `lifecycle-gate-draft` **제거**, claude 사본 예시의 그 자리를
  실재 id `lifecycle-gate-policy-stop`(kiwi-pm canonical) 으로 교체.
- 신규 필드·옵션·절 **0건**. H12 는 기존 `status` 매핑에 조건 1줄을 더한다.

**선행 계약과의 충돌 검사 결과**: 위 신규 식별자 전부를 선행 계약 §8(v244) 과 §9(v244-r2) 의
확정 목록과 대조했다. 겹치는 이름은 없다. 두 곳만 **의도적 재사용**이다.

1. `--req-filter` — kiwi-pipeline 이 이미 쓰는 철자를 kiwi-srs-feasibility 에 그대로 재사용한다.
   같은 의미(대상 REQ 한정)라 다른 철자를 만들면 체인 안에 두 이름이 생긴다.
2. `intended-improvement` / `unapproved-damage` — 선행 계약이 wave 계층에 확정한 enum 을
   kiwi-coder §0.20.4 가 **그대로** 쓴다. 이것이 H1 의 요구 자체다(두 계층이 한 문장을 공유).

---

## 10. 두 갈래 제안에서 고른 쪽과 사유

findings 가 명시적으로 두 갈래 이상을 제시한 것은 H4 · H6 두 건이다. 나머지는 제안이 단일했거나
(MEDIUM/LOW 표) 제안 열이 없어 본 계약이 방향을 확정했다. 확정이 필요했던 지점만 적는다.

| 항목 | 고른 쪽 | 사유 |
|---|---|---|
| **H4** (a) 접미사를 SSOT 로 올리고 id 공간 분리 / (b) `#` 대신 `.r1` 로 철자 변경 | **(a)** | (b) 는 emit 키를 **합법적인 사이드카 id 처럼 보이게** 만든다. 두 id 공간의 혼동이 결함의 원인인데 그것을 문자로 지워 더 찾기 어렵게 한다. 또 (b) 는 R2 가 확정하고 기존 단언이 고정한 `{run_id}#r{n}` 을 은퇴시켜, 단언 하나를 약화 방향으로 다시 쓰게 만든다. (a) 는 규약을 4사본 공유 파일로 올려 세 소비자가 **같은 문장**을 읽게 하므로 원인(생산자 1 : 소비자 3 비대칭)을 직접 닫는다 |
| **H6** (i) 사용자 확인 게이트 / (ii) 최종 분모에 별도 계층 / (iii) `reason` closed enum | **셋 다** | 세 제안은 서로 배타가 아니라 다른 시점을 막는다. (i) 은 배제가 **선언되는 순간**, (ii) 는 run 이 **끝나는 순간**, (iii) 은 배제의 **표현 가능 범위**를 제한한다. findings 스스로 (ii) 를 "최소한"으로 적었으므로 더 안전한 (i) 을 버릴 이유가 없고, (i) 만 채택하면 승인된 배제가 여전히 어느 분모에도 안 들어간다 — 동의는 배제 단위, 최종 확인은 run 단위라 대체 관계가 아니다 |
| **H1** 의 해소 범위 (findings 는 "세 보존 게이트"라고 적음) | **파일 삭제·이동 + public 심볼 변경 2종만**, 테스트 약화·삭제는 제외 | 세 번째 게이트까지 해소 가능하게 하면 "계획 Task 가 요구했다"는 근거로 단언을 낮추는 경로가 열린다. 이는 M12 가 지적한 wave 계층의 같은 구멍과 정확히 대칭이며, R2 계약이 kiwi-pm 에서 always-HALT 로 못박은 것을 한 계층 아래에서 되살린다. 실행/검증 두 계층은 **약화 제외**를 포함해 같은 규칙을 쓴다 |
| **M10** 상한 값 | **3 / run** | 무한이 아니면서, run-scope finding → wave 추가 → 재검증 이 정상적으로 수렴할 여지를 남기는 최소값. 상한 자체가 요점이므로 숫자는 게이트로 방어되고 초과는 사용자 결정으로 올라간다 |
| **M13** 전달 방식 | **명시 옵션 + 부모 우선 규칙** | 규칙만 쓰고 옵션을 만들지 않으면 M2·M8 과 같은 "전달 경로에 수신 인터페이스가 없다" 결함을 새로 만든다 |
| **L2** (a) 게이트 등재 / (b) 게이트 표 범위를 "실행 중 게이트"로 축소 | **(a)** | (b) 는 §0.G 스스로 세운 "선언되지 않은 중단은 위원회로 떨어진다"는 근거를 예외로 무너뜨린다. 한 행을 더 쓰는 비용이 그보다 싸다 |
| 계약 버전 | **1.3.0 유지** | §0.3 참조 |
| **H11** 9종 전사 / **일반 규칙 1줄** | **일반 규칙** | 전사는 자식이 게이트를 추가할 때마다 누락된다 — R2 가 `existing-test-weakened-or-deleted` 를 옮기면서 같은 라운드에 신설한 형제 `existing-file-deleted-or-moved` 를 놓친 것이 실증이다. 규칙은 다음 게이트가 생겨도 자동으로 덮는다 |
| **M18** (a) 자동 부여 / **(b) 명시 옵션만** | **(b) 유지 + AC 개정** (사용자 결정 반영) | 자동 부여는 사용자가 한 번도 동의하지 않은 통합 테스트 실행과 ≥10분 비용을 wave 수만큼 승인한다. 기존 "자동 부여 문구 부재" 단언과도 양립 불가다. "멈추지 않고 완주"는 옵션을 **도달 가능·문서화**하는 것으로 달성한다 |
| **M19** 제거 / 주석 | **둘 다** | 제거만 하면 옛 카탈로그를 본 독자가 무엇으로 대체됐는지 알 길이 없고, 주석만 남기면 목록에 그대로 있어 신규 스킬이 계속 채택한다. 카탈로그에서 빼고 **불릿이 아닌** 철회 주석을 남긴다 |
| **H12** no-op 반환값 | **`NEEDS_USER` + no-op 표식** | `TASK_DONE` 은 부모가 다음 라운드를 돌게 만들고 같은 finding 이 cap 까지 반복된다. `FAILED` 는 실패가 아닌 것을 실패로 만들어 §0.4 안전 게이트를 오발동시킨다 |

---

## 11. findings 근거 검증 결과

`round3-findings.md` 의 근거(file:line)를 **전부 직접 열어 확인**했다. 줄번호까지 포함해 대부분
일치하지만, **정확히 다른 것이 1건, 보충이 필요한 것이 2건** 있다.

### 11.1 실제와 다른 근거 — M16 (부분적으로 사실이 아님)

> M16: "`--auto` 완주에 필요한 **두 옵션**이 `WM §1.2` 자연어 매핑에 없어…"

`skills/claude/kiwi-wave-master/SKILL.md:78-79` 에 `--auto-integration` 과 `--auto-cost-warning`
**두 행이 이미 존재한다**(라운드 2 가 R2-H1 로 추가했고, 기존 테스트
`R2-H1 › maps both pass-through options in the natural-language table` 이 그것을 고정한다).

§7.4 pass-through 표의 세 옵션 중 §1.2 에 없는 것은 **`--force` 하나뿐**이다.
따라서 본 계약은 M16 을 **`--force` 1건으로 축소**해 반영했다(§3.3). 존재하는 두 행을 다시 추가하면
표에 중복 행이 생긴다 — 하지 말 것.

### 11.2 보충 1 — H2 · H4 의 대상 파일이 사본마다 다르다

findings 는 `CO:855`(kiwi-coder 상태 매핑) · `PL:939` · `PM:1206` 을 근거로 든다. 이 줄들은
**claude 사본에만** 있다. codex/etc 사본에서 kiwi-coder·kiwi-planner·kiwi-pm 의
`Pipeline event emit` 절은 `references/extended-workflow.md` 로 옮겨져 있고(절 번호 §12/§17/§10 은
동일), 본문 기술도 다르다 — 직접 append 가 아니라 MCP `workflow_pipeline_emit` + mutation envelope 다.

결함의 실체(자식에게 `gate_id` 채널이 없고 재진입 접미사를 모른다)는 **세 사본 모두 동일**하므로
findings 의 진단은 유효하다. 다만 편집 대상 파일이 사본마다 다르다 — §1 의 표와 §6.2 · §7.4 · §8.4 를
그대로 따를 것. 테스트도 두 위치를 순서대로 찾는다.

### 11.3 보충 2 — M15 의 범위가 findings 보다 넓다

findings 는 codex/etc 의 RFL 에 스캔 실행 지점이 없다고 적었고 그것은 사실이다. 다만 claude 사본의
스캔 문단도 `### 6.2 Phase 4 — 시니어 fixer 적용` 안의 **본문 한 문단**이라 세 사본을 같은 방식으로
검사할 앵커가 없다. 그래서 본 계약은 claude 사본에서도 그 문단을 **전용 절로 승격**시킨다(§8.6).
claude 의 규범 내용은 바뀌지 않는다 — 위치와 제목만 바뀌고, 재검증보다 앞선다는 순서 문장이 추가된다.

### 11.4 추가 5건의 근거 — 1건이 사본 한정, 1건은 파급이 더 넓다

**M19 의 `:232` 예시는 claude 사본에만 있다.** 네 사본을 모두 열어 확인한 결과:

| 사본 | 카탈로그 불릿 | 선언 예시 |
|---|---|---|
| `skills/claude` | 있음(gloss 포함, `:263`) | **있음** (`:232`) |
| `skills/codex` · `skills/etc` · `.agents/skills` | 있음(glossless) | **없음** |

codex/etc/.agents 사본의 `auto-option.md` 는 축약본이라 `### 5.1` 절 번호 자체가 없고
`## \`critical_gates[]\`` 아래에 같은 불릿 목록만 있다. 그래서 테스트의 카탈로그 스코프는 절 번호가
아니라 **인터페이스 헤딩**(`^#{2,3}\s.*critical_gates`)을 앵커로 쓴다. 카탈로그 불릿 제거와 철회 주석은
**네 사본 모두**, 예시 교체는 **claude 사본만** 해당한다.

**H11 의 파급은 findings 가 적은 것보다 넓다.** `kiwi-pm` 은 두 곳에서 severity enum 을 "3종"이라고
명시한다(§0 표 `§0.16` 행, §0.G7 표 아래 "기존 severity enum 유지" 문단). 네 번째 값을 추가하면서 그
두 문구를 고치지 않으면, 같은 파일 안에서 enum 크기가 서로 다르게 읽힌다 — 이번 라운드가 계속 잡아낸
바로 그 드리프트다. 테스트가 고정하지는 않지만 §12.1(d) 로 함께 요구한다.

나머지 3건(H12 `kiwi-pm:1211` · M18 `kiwi-wave-master:445` 와 `r2 테스트:441-444` · M20
`kiwi-wave-continuity-content.test.ts:1038`)의 근거는 줄번호까지 실제와 일치했다.

### 11.5 그 밖의 근거

그 밖의 근거는 실제와 일치했다. 특히 다음은 직접 확인했다:
`CO:119-121`·`CO:412-418`(같은 사건의 배타적 두 처리) · kiwi-coder 전문에 `버블업` **0건** ·
`PIPE §7.4` 에 `--force` **부재** · `kiwi-srs §1.2` 에 제약 인자 **부재** ·
`kiwi-srs-feasibility §1.2` 에 REQ 단위 인자 **부재** · `WM §1.2` 에 `--force` **부재** ·
`waves-event §2.2` 의 `diff_window`/`pipeline_run_ids` 가 선택 필드이며 생산 규정이 **§5 예시뿐**.

---

## 12. 추가 finding 5건 (FR-FLOW-066 단일 소재지)

H11 · H12 · M18 · M19 · M20 은 3축 평가가 아니라 R2 증거 등록 중 발견된 것이며, SRS 상 **FR-FLOW-066**
하나에만 편입되었다. 편집 대상도 위 §2~§8 과 겹치지 않게 여기 모았다 — 단 **kiwi-pm 은 §7 과 같은 파일**
이므로 두 절의 편집을 함께 적용한다.

### 12.1 R3-H11 — 자식 critical 게이트의 일반 규칙 (`kiwi-pm`, 3사본)

**(a) §0.G7 표 아래에 catch-all 규칙 1줄** — 표 바로 다음 문단에 넣는다.

```
자식 스킬이 **자기** `critical_gates[]` 로 선언한 gate_id 는 본 표에 동명 행이 **없더라도** `severity="critical"` 로 취급해 **무조건 HALT** 한다 — 게이트별 수동 전사는 자식이 게이트를 추가할 때마다 누락되고, 누락된 게이트는 `business-decision` 기본 분류로 떨어져 결정 서브에이전트가 승인한다.
```

이 한 줄이 H11 의 본체다. **9종을 표에 전사하지 않는다** — 전사는 다음 라운드에 다시 뚫린다(R2 가
`existing-test-weakened-or-deleted` 만 옮기고 형제 `existing-file-deleted-or-moved` 를 놓친 것이 그
증거다). §0.G7 표의 13행은 그대로 두고 canonical 집합도 바뀌지 않는다.

테스트는 §0.G7 절에서 **백틱 친 `critical_gates`** 를 담은 첫 줄을 앵커로 잡는다. 현재 그 절에는
백틱 친 표기가 없으므로(헤딩과 기존 산문 모두 무백틱) 위 문장이 첫 매치가 된다.

**(b) 자식 반환 계약의 severity enum 에 `critical` 추가** — §3.2 프롬프트 안 `## 반환 형식` 블록.

```
      "severity": "clarification | business-decision | rollback-confirmation | critical",
```

기존 3종은 **그대로 두고** 네 번째를 더한다. `critical` 이 없으면 자식은 "이건 HALT 다"를 표현할 수단이
아예 없고, `auto-option.md §4` 가 미선언 게이트를 `business-decision` 으로 기본 분류한다.

**(c) §3.5 severity 표에 `critical` 행 추가**

```
| `critical` | 자식이 자기 `critical_gates[]` 로 선언한 중단 | 기존 테스트 약화·삭제, 기존 파일 삭제·이동, TDD 우회, 계획-코드 CRITICAL 불일치 — `--auto` 무관 **항상 HALT** |
```

**(d) 정합 문구 2곳** — 테스트가 고정하지는 않지만 고치지 않으면 같은 계층에 모순이 남는다.

- §0 표 `§0.16` 행의 `본 스킬의 3종 severity enum` → `4종 severity enum`
- §0.G7 표 아래 `**기존 severity enum 유지**: … 3종 enum 은 …` → `4종 enum 은 …` 으로 바꾸고,
  네 번째 값 `critical` 이 `auto-option.md §4` 의 `critical` 행에 직접 매핑됨을 한 구절로 덧붙인다.

### 12.2 R3-H12 — no-op 재진입 가드 (`kiwi-pm`, 3사본)

emit 절(claude `SKILL.md` §10, codex/etc `references/extended-workflow.md` §10)의 `status` 불릿
아래에 1줄 추가한다.

```
- **no-op 가드**: 이번 실행에서 **상태가 바뀐 Task** 가 0 건이면 `TASK_DONE` 이 **아니라** `NEEDS_USER` 를 반환하고, `summary` 에 no-op 임을 명시한다 — 재진입이 아무 것도 실행하지 않고 성공을 보고하면 부모의 개선 루프가 같은 finding 을 cap 소진까지 되돌린다
```

"상태가 바뀐 Task" 는 이번 실행에서 `pending`/`failed` → `done` 등으로 **전이한** Task 를 뜻한다.
이미 `done` 인 Task 를 건너뛴 것은 변화가 아니다. 한 줄에 `TASK_DONE` 부정 · `NEEDS_USER` · `no-op` ·
`0 건` 이 모두 들어가야 한다(테스트가 그 줄 하나를 앵커로 잡는다).

### 12.3 R3-M18 — 분기 (b) 유지 + 도달 가능성 (`kiwi-wave-master`, 3사본)

**결정은 분기 (b)** — `--auto` 단독으로는 두 자식 동의 게이트를 통과하지 못하고, 옵션을 **명시**할 때만
우회된다. 따라서 "`--auto` 가 두 옵션을 자동 부여한다" 방향의 문장을 **쓰지 않는다** — 그런 문장은
`kiwi-wave-continuity-r2-content.test.ts` 의 "자동 부여 문구 부재" 단언과 정면 충돌한다.

편집은 **1건뿐**이다. §7.1 의 `--auto` 단독` 문장 끝에 경로를 덧붙인다(기존 문장 앞부분은 불변).

```
… 그 전까지 `--auto` 는 첫 코딩 wave 에서 멈춘다(§0.G). 두 옵션은 §7.4 pass-through 표의 경로로 자식 게이트까지 도달하므로, 사용자가 한 번 명시하면 그 다음부터는 체인이 값을 나른다.
```

한계만 말하고 경로를 말하지 않으면, 사용자는 "안 되는 것"만 듣고 "되는 방법"은 듣지 못한다.
개정 후의 `FR-FLOW-059 AC-1` 이 요구하는 것이 정확히 이 세 가지다 — pass-through 표 등재 ·
§1.2 자연어 매핑 등재 · §7.1 의 한계와 경로 명시.

**green 6건 (구현 불필요)**: `keeps both options declared at every hop` ×3 과
`still refuses to grant the consent on the user's behalf` ×3 은 **저작 시점에 이미 통과**한다.
체인 5홉(wave-master §1.2·§7.4 → pipeline §7.4 → pm §1.3 → coder §1.2)이 모두 갖춰져 있고 자동 부여
문구도 없기 때문이다. 결정된 분기가 조용히 뒤집히는 것을 막는 **회귀 가드**로 남긴다 — 지금 red 가
아니라는 이유로 지우면, 다음 개정에서 분기 (a) 로 되돌아가도 아무것도 잡지 못한다.

### 12.4 R3-M19 — 철회된 gate_id 제거 (`_shared/kiwi/auto-option.md`, 4사본)

**(a) 카탈로그 불릿 제거** — 네 사본 모두에서 아래 불릿을 지운다.

- claude: `- \`lifecycle-gate-draft\` — REQ stability=draft 인데 구현 진입`
- codex / etc / .agents: `- \`lifecycle-gate-draft\`` (glossless)

**(b) 철회 주석 1줄 추가** — 카탈로그 목록 **바로 아래**, **불릿이 아닌 문단**으로 쓴다.

```
`lifecycle-gate-draft` 는 **철회**됐다 — draft 차단은 per-REQ skip 으로 대체되었고(FR-FLOW-053) 어느 스킬의 canonical 집합에도 남아 있지 않다. 신규 스킬은 이 id 를 채택하지 않는다.
```

**`- ` 로 시작하는 불릿으로 쓰지 말 것.** 테스트는 카탈로그 절에서 `^- \`lifecycle-gate-draft\``
형태의 **부재**를 요구하고, 동시에 파일 어딘가에 `철회` + `FR-FLOW-053` 를 담은 언급이 **존재**하기를
요구한다. 불릿으로 쓰면 두 요구가 서로를 무너뜨린다.

**(c) 선언 예시의 id 교체 — claude 사본 전용** — `§5.0.1` 예시 블록의

```
          {gate_id: "lifecycle-gate-draft", reason: "REQ stability=draft 자동 진행 금지"},
```

를 실재하는 id 로 바꾼다.

```
          {gate_id: "lifecycle-gate-policy-stop", reason: "deprecated / frozen REQ 구현 정책 차단"},
```

**green 3건 (구현 불필요)**: `drops it from the declaration example too` 는 codex · etc · .agents
사본에서 **이미 통과**한다 — 그 세 사본에는 선언 예시 자체가 없다. findings 가 근거로 든 `:232` 예시는
claude 사본에만 존재한다(§11.4). 세 사본의 통과 단언은 예시가 나중에 역이식될 때를 위한 가드다.

### 12.5 R3-M20 — 테스트 전용 (구현 편집 없음)

§0.1b 참조. 스킬 문서는 바뀌지 않는다 — 이미 R2-H5 가 Normal 행에 델타 조건과 fallback 을 넣어 두었고,
M20 은 그 두 조건의 역할을 단언이 제대로 고정하게 만드는 작업이다. 이미 적용했다.

---

## 13. 검증

```
npx vitest run test/skills/kiwi-wave-continuity-r3-content.test.ts test/skills/kiwi-event-contract-content.test.ts --no-file-parallelism
```

green 조건: 두 파일 0 fail. §0.1 의 R2 상수 갱신은 이미 반영되어 있으므로
`R2-M9 › kiwi-wave-master › matches the canonical set pinned by the contract` 는 4 gate 를 3사본에 모두 넣는 순간 통과한다.

전체 회귀도 함께 돌린다 — `.agents` 미러 동기화 체크가 별도 스위트에 있고,
`waves-event.md` 와 `pipeline-event.md` 는 각각 4사본이므로 미러 사본을 빠뜨리면 그쪽에서 잡힌다.

```
npx vitest run test/skills --no-file-parallelism
npm test
```

목표: **20 파일 / 1200 tests / 0 fail**.
