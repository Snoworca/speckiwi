---
run_id: 2026-07-28.speckiwi.v242
target: v2.4.2
plan_version: 0.1.0
plan_contract: "1.2.0"
generated_at: 2026-07-28T00:00:00Z
tool_versions:
  speckiwi: 2.4.1
  kiwi_planner: 0.6.0
  validator: 0.6.0
stability_summary:
  frozen: 0
  stable: 0
  evolving: 3
  draft: 0
tdd_policy: relaxed
sidecar_path: ./2026-07-28.speckiwi.v242.sidecar.json
md_sha256: computed-over-body-excluding-this-line
---

## §1 개요

### 1.1 목표

v2.4.2 의 run-root 무결성 3개 요구를 구현한다. wave/pipeline 실행이 세션의 SpecKiwi MCP workspace root 와 실행 git root 가 어긋난 상태에서 조용히 잘못된 저장소에 쓰는 대신 fail-closed 로 멈추게 하고, 이벤트 저널 경로를 run 시작 시 확정한 root 로 고정하며, 존재하지 않는 도구를 가리키는 MCP 복구 안내를 실행 가능한 안내로 교체한다.

### 1.2 범위 (in_scope)

- `FR-FLOW-042` — kiwi-wave-master run-root preflight critical gate (AC-1..AC-7)
- `FR-FLOW-043` — run-root pinned wave/pipeline event journals + 죽은 폴백 수리 + skill enum 등재 (AC-1..AC-7)
- `FR-MCP-055` — 거부된 per-call workspace root 의 실행 가능한 복구 안내 (AC-1..AC-4)
- 사용자 지시에 따른 패키지 버전 범프 `2.4.1 → 2.4.2`

### 1.3 제외사항 (out_of_scope)

- 런타임 multi-root 호스트 도입(연구 §8 방안 D) — 결정 위원회가 별도 트랙으로 보류
- 미병합 브랜치 `work/kiwi-wave-master-multiroot-g0` 의 병합·폐기·워크트리 제거 — 사용자 결정 사항
- MCP per-call root override 허용(연구 §8 방안 E) — `REL-MCP-004` / `IR-CLI-045` 가 의도적으로 금지
- `.agents/skills/kiwi-wave-master/` 미러 생성 — `.agents/skills/.speckiwi-mirror-exclusions.json` 의 의도적 제외(hermeticity sentinel)
- `pipeline-event.md` §4 생산자 결정표 및 `kiwi-pipeline` §5.1 소비자 결정표에 skill 특화 행 추가 — `FR-FLOW-043` AC-6 이 명시적으로 금지
- `excluded_reqs`: 없음 (deprecated / draft REQ 0건)

### 1.4 전제조건 / 가정

- speckiwi MCP 가용, Active Target = `v2.4.2`, work-mode = `wait` → `tdd_policy = relaxed`(§3.4 파생)
- 3개 REQ 전부 `Status=planned` / `Stability=evolving` — 착수 게이트 통과
- `test/skills/kiwi-wave-master-content.test.ts` 는 `VARIANTS = ["claude","codex","etc"]` 3변종 raw-text 단언 하네스로 이미 존재(458행)
- 기존 MCP 테스트(`test/mcp/mutation-tools-errors.test.ts`, `test/mcp/read-tools-resources.test.ts`)는 `recovery` 필드를 단언하지 않으므로 `FR-MCP-055` 변경에 회귀면이 없다

## §2 Phase 목록

| phase_id | title | goal | depends_on | task_count |
| --- | --- | --- | --- | --- |
| PH-001 | 이벤트 계약 수리 | _shared 이벤트 계약 8개 사본의 죽은 폴백을 수리하고 run-start root pin 서술과 skill enum 등재를 반영한다 | - | 2 |
| PH-002 | wave-master preflight 게이트 | kiwi-wave-master 3변종에 run-root preflight critical gate 와 halt-emit 계약을 저작한다 | PH-001 | 2 |
| PH-003 | MCP 복구 안내 교정 | 존재하지 않는 restart_mcp_server 안내를 실행 가능한 안내로 교체하고 회귀 테스트로 고정한다 | - | 2 |
| PH-004 | 회귀 검증 및 릴리스 준비 | 전체 스위트·타입체크·린트·빌드로 회귀 0 을 확인하고 패키지 버전을 범프한다 | PH-001, PH-002, PH-003 | 2 |

## §3 Task 상세

### §3.PH-001

#### §3.PH-001.T-PH001-01

- id: T-PH001-01
- phase_id: PH-001
- title: 이벤트 계약 8개 사본에 대한 실패 콘텐츠 테스트 작성
- type: code
- req_ids: [FR-FLOW-043]
- files: [test/skills/kiwi-event-contract-content.test.ts]
- action: `test/skills/kiwi-event-contract-content.test.ts` 를 신규 작성한다. 8개 사본(`skills/{claude,codex,etc}/_shared/kiwi/{waves-event,pipeline-event}.md` + `.agents/skills/_shared/kiwi/` 2개)에 대해 (a) run-start root pin 서술 존재, (b) POSIX 폴백이 `git rev-parse --show-toplevel` 성공 후에만 `<root>/kiwi` 를 대입하는 형태, (c) 도달 불가 폴백 형태(`$(git rev-parse --show-toplevel 2>/dev/null)/kiwi`)의 부재를 단언한다. 추가로 `pipeline-event.md` 4개 사본의 §3 skill enum 에 `kiwi-wave-master` 가 포함됨을 단언하고, 생산자 결정표(`pipeline-event.md` §4)와 소비자 결정표(`skills/{claude,codex,etc}/kiwi-pipeline/SKILL.md` §5.1 및 `.agents/skills/kiwi-pipeline/SKILL.md`) 어디에도 `kiwi-wave-master` 특화 행이 추가되지 않았음을 단언한다. 이 시점에는 전부 실패해야 한다.
- acceptance_tests: [{"kind":"shell","cmd":"npx vitest run test/skills/kiwi-event-contract-content.test.ts","expected_exit":1,"stdout_regex":"FAIL|failed"}]
- verification_cmd: {posix: "npx vitest run test/skills/kiwi-event-contract-content.test.ts", windows: "npx vitest run test/skills/kiwi-event-contract-content.test.ts"}
- dod: 신규 테스트 파일이 존재하고, 8개 사본 전부를 순회하며, 실행 시 red 로 실패한다
- rollback: 신규 테스트 파일 삭제
- estimated_effort: M
- covers_ac: [AC-1, AC-2, AC-3, AC-4, AC-6]
- tdd: {applicable: true, phase: red, test_cases_count: 5}

#### §3.PH-001.T-PH001-02

- id: T-PH001-02
- phase_id: PH-001
- title: 8개 이벤트 계약 사본에 폴백 수리·root pin·enum 등재 적용
- type: code
- req_ids: [FR-FLOW-043]
- files: [.agents/skills/_shared/kiwi/pipeline-event.md:130-132, .agents/skills/_shared/kiwi/pipeline-event.md:66-84, .agents/skills/_shared/kiwi/waves-event.md:76-78, skills/claude/_shared/kiwi/pipeline-event.md:118-120, skills/claude/_shared/kiwi/pipeline-event.md:66-84, skills/claude/_shared/kiwi/waves-event.md:76-78, skills/codex/_shared/kiwi/pipeline-event.md:130-132, skills/codex/_shared/kiwi/pipeline-event.md:66-84, skills/codex/_shared/kiwi/waves-event.md:76-78, skills/etc/_shared/kiwi/pipeline-event.md:130-132, skills/etc/_shared/kiwi/pipeline-event.md:66-84, skills/etc/_shared/kiwi/waves-event.md:76-78]
- action: 8개 사본의 POSIX emit 예시를 `root=$(git rev-parse --show-toplevel 2>/dev/null); if [ -n "$root" ]; then DIR="$root/kiwi"; elif [ -d "./kiwi" ]; then DIR="./kiwi"; else DIR="$HOME/.kiwi"; fi` 형태로 교체하여 cwd·home 폴백이 도달 가능하게 만든다. 경로 해석 절(§1)에 run 시작 시 root 를 pin 하고 emit 마다 재해석하지 않는다는 서술을 추가한다. `pipeline-event.md` 4개 사본의 §3 skill enum 에 `kiwi-wave-master` 를 추가한다. PowerShell 예시(codex/etc/.agents 사본)는 이미 `$LASTEXITCODE` 가드가 있으므로 변경하지 않는다. 두 결정표에는 행을 추가하지 않는다.
- acceptance_tests: [{"kind":"shell","cmd":"npx vitest run test/skills/kiwi-event-contract-content.test.ts","expected_exit":0}]
- verification_cmd: {posix: "npx vitest run test/skills/kiwi-event-contract-content.test.ts", windows: "npx vitest run test/skills/kiwi-event-contract-content.test.ts"}
- dod: T-PH001-01 의 테스트가 green 이고, 8개 사본 어디에도 도달 불가 폴백이 남아 있지 않으며, PowerShell 예시와 두 결정표는 diff 상 무변경이다
- rollback: `git checkout -- skills/ .agents/skills/`
- estimated_effort: M
- depends_on_task: [T-PH001-01]
- covers_ac: [AC-1, AC-2, AC-3, AC-4, AC-6]
- tdd: {applicable: true, phase: green, test_cases_count: 5}

### §3.PH-002

#### §3.PH-002.T-PH002-01

- id: T-PH002-01
- phase_id: PH-002
- title: wave-master preflight 게이트와 halt-emit 계약에 대한 실패 콘텐츠 테스트 추가
- type: code
- req_ids: [FR-FLOW-042, FR-FLOW-043]
- files: [test/skills/kiwi-wave-master-content.test.ts:1-458]
- action: 기존 3변종 하네스에 FR-FLOW-042 AC-1..AC-7 및 FR-FLOW-043 AC-5/AC-7 단언 블록을 추가한다. run root 와 MCP workspaceRoot 비교의 선행성, `--auto` 로 우회 불가한 critical halt, 두 복구 경로와 신규 REQ ID 금지 및 호스트 docs/spec 편집 불가 경고, 에픽 진입 모드 포함, workspaceRoot 획득 실패의 동일 취급과 CLI 폴백 배제, halt 시점에 SRS mutation 0건이고 자식 스킬이 spawn 되지 않았다는 상태 서술, `--wt` 위임 거부와 그 근거(per-wave worktree 생성이 wave 누적을 깨뜨림) 및 동일한 두 복구 경로의 재제시, halt 시 waves.jsonl 무기록 및 pipeline.jsonl 을 FAILED/next_hint=null 로 1회 emit 하는 계약을 각각 단언한다. 이 시점에는 전부 실패해야 한다.
- acceptance_tests: [{"kind":"shell","cmd":"npx vitest run test/skills/kiwi-wave-master-content.test.ts","expected_exit":1,"stdout_regex":"FAIL|failed"}]
- verification_cmd: {posix: "npx vitest run test/skills/kiwi-wave-master-content.test.ts", windows: "npx vitest run test/skills/kiwi-wave-master-content.test.ts"}
- dod: 신규 단언이 3변종 전부에 대해 돌고 red 로 실패하며, 기존 단언은 그대로 통과한다
- rollback: `git checkout -- test/skills/kiwi-wave-master-content.test.ts`
- estimated_effort: M
- covers_ac: [AC-1, AC-2, AC-3, AC-4, AC-5, AC-6, AC-7]
- tdd: {applicable: true, phase: red, test_cases_count: 9}

#### §3.PH-002.T-PH002-02

- id: T-PH002-02
- phase_id: PH-002
- title: kiwi-wave-master 3변종에 preflight 게이트 절과 halt-emit 계약 저작
- type: code
- req_ids: [FR-FLOW-042, FR-FLOW-043]
- files: [skills/claude/kiwi-wave-master/SKILL.md:167-169, skills/claude/kiwi-wave-master/SKILL.md:56-67, skills/codex/kiwi-wave-master/SKILL.md:170-172, skills/codex/kiwi-wave-master/SKILL.md:59-70, skills/etc/kiwi-wave-master/SKILL.md:171-173, skills/etc/kiwi-wave-master/SKILL.md:60-71]
- action: 각 변종의 "Phase 흐름" 절 직후에 Phase 0 run-root preflight 전용 하위 절을 신설한다. `mcp_workspace_info.workspaceRoot` 와 `git rev-parse --show-toplevel` 비교, 불일치·조회 실패 시 `--auto` 로도 우회 불가한 critical halt, CLI 폴백 배제, 두 복구 경로와 두 경고, 에픽 진입 모드 포함, halt 시점의 무-mutation·무-spawn 상태, `--wt` 위임 거부와 그 근거 및 동일한 두 복구 경로 재제시를 규정한다. 각 변종의 pipeline emit 절에 halt 시 waves.jsonl 무기록 + pipeline.jsonl 을 FAILED/next_hint=null 로 run 자신의 git root 기준 1회 emit 하는 조항을 추가한다. `.agents` 미러는 의도적 제외이므로 생성하지 않는다.
- acceptance_tests: [{"kind":"shell","cmd":"npx vitest run test/skills/kiwi-wave-master-content.test.ts","expected_exit":0}]
- verification_cmd: {posix: "npx vitest run test/skills/kiwi-wave-master-content.test.ts", windows: "npx vitest run test/skills/kiwi-wave-master-content.test.ts"}
- dod: T-PH002-01 의 신규 단언이 3변종 전부에서 green 이고, `.agents/skills/kiwi-wave-master/` 가 생성되지 않았다
- rollback: `git checkout -- skills/`
- estimated_effort: L
- depends_on_task: [T-PH002-01]
- covers_ac: [AC-1, AC-2, AC-3, AC-4, AC-5, AC-6, AC-7]
- tdd: {applicable: true, phase: green, test_cases_count: 9}

### §3.PH-003

#### §3.PH-003.T-PH003-01

- id: T-PH003-01
- phase_id: PH-003
- title: MCP 복구 안내에 대한 실패 회귀 테스트 추가
- type: code
- req_ids: [FR-MCP-055]
- files: [test/mcp/mutation-tools-errors.test.ts]
- action: per-call `root` 인자 거부 응답에 대해 `recovery` 필드를 단언하는 회귀 테스트를 추가한다. `recovery.tool` 이 `restart_mcp_server` 가 아님, 안내 문구가 workspace root 가 MCP 서버 프로세스 작업 디렉터리에서 해석된다는 사실과 의도한 디렉터리에서 서버·세션을 기동하라는 조치를 담고 있음, 그리고 error code·`SRS-E075` 진단·diagnostics summary·`mcpWorkspace` 가 기존 값 그대로임을 단언한다. 이 시점에는 실패해야 한다.
- acceptance_tests: [{"kind":"shell","cmd":"npx vitest run test/mcp/mutation-tools-errors.test.ts","expected_exit":1,"stdout_regex":"FAIL|failed"}]
- verification_cmd: {posix: "npx vitest run test/mcp/mutation-tools-errors.test.ts", windows: "npx vitest run test/mcp/mutation-tools-errors.test.ts"}
- dod: 신규 단언이 red 로 실패하고 기존 단언은 통과한다
- rollback: `git checkout -- test/mcp/mutation-tools-errors.test.ts`
- estimated_effort: S
- covers_ac: [AC-1, AC-2, AC-3, AC-4]
- tdd: {applicable: true, phase: red, test_cases_count: 4}

#### §3.PH-003.T-PH003-02

- id: T-PH003-02
- phase_id: PH-003
- title: adapter 의 recovery 안내를 실행 가능한 안내로 교체
- type: code
- req_ids: [FR-MCP-055]
- files: [src/mcp/adapter.ts:45-64]
- action: `src/mcp/adapter.ts:63` 의 `recovery: { tool: "restart_mcp_server", ... }` 에서 존재하지 않는 도구 참조를 제거하고, workspace root 가 MCP 서버 프로세스 작업 디렉터리에서만 해석된다는 사실과 의도한 프로젝트 디렉터리에서 서버 또는 그 세션을 기동하라는 조치를 담은 안내로 교체한다. fail-closed 거부 동작·error code·`SRS-E075` 진단·`mcpWorkspace` 는 변경하지 않는다.
- acceptance_tests: [{"kind":"shell","cmd":"npx vitest run test/mcp/mutation-tools-errors.test.ts test/mcp/read-tools-resources.test.ts","expected_exit":0}]
- verification_cmd: {posix: "npx vitest run test/mcp/mutation-tools-errors.test.ts test/mcp/read-tools-resources.test.ts", windows: "npx vitest run test/mcp/mutation-tools-errors.test.ts test/mcp/read-tools-resources.test.ts"}
- dod: T-PH003-01 이 green 이고, `restart_mcp_server` 가 `src/` 에서 0건이며, 기존 두 MCP 테스트가 회귀 없이 통과한다
- rollback: `git checkout -- src/mcp/adapter.ts`
- estimated_effort: S
- depends_on_task: [T-PH003-01]
- covers_ac: [AC-1, AC-2, AC-3, AC-4]
- tdd: {applicable: true, phase: green, test_cases_count: 4}

### §3.PH-004

#### §3.PH-004.T-PH004-01

- id: T-PH004-01
- phase_id: PH-004
- title: 전체 회귀 스위트·타입체크·린트·빌드 검증
- type: review
- req_ids: [FR-FLOW-042, FR-FLOW-043, FR-MCP-055]
- files: [package.json]
- action: `npm test`, `npm run typecheck`, `npm run lint`, `npm run build` 를 순차 실행하여 신규 실패 0 을 확인한다. 사전 존재하던 flaky 실패가 있으면 격리 실행으로 사전 존재 여부를 판별해 기록한다. `speckiwi validate --fail-on-warning --json` 으로 SRS 무결성도 재확인한다.
- acceptance_tests: [{"kind":"checklist","items":["npm test 신규 실패 0","npm run typecheck exit 0","npm run lint exit 0","npm run build exit 0","speckiwi validate --fail-on-warning errors 0 warnings 0"]}]
- verification_cmd: {posix: "npm test && npm run typecheck && npm run lint && npm run build", windows: "npm test; if ($?) { npm run typecheck }; if ($?) { npm run lint }; if ($?) { npm run build }"}
- dod: 네 명령 모두 통과하고 신규 회귀 0 건이 기록된다
- rollback: 해당 없음 (검증 전용, 변경 없음)
- estimated_effort: M
- depends_on_task: [T-PH001-02, T-PH002-02, T-PH003-02]
- tdd: {applicable: false, phase: n/a, test_cases_count: 0, exempt_reason: "검증 전용 Task 로 제품 동작을 변경하지 않는다. 실행 대상이 이미 작성된 테스트 스위트 자체이므로 선행 실패 테스트를 따로 작성할 대상이 없다."}

#### §3.PH-004.T-PH004-02

- id: T-PH004-02
- phase_id: PH-004
- title: 패키지 버전을 2.4.2 로 범프
- type: file_op
- req_ids: []
- files: [package.json, package-lock.json]
- action: 사용자 지시("현재 버전에서 +0.0.1")에 따라 `package.json` 의 `version` 을 `2.4.1` 에서 `2.4.2` 로 올리고 `package-lock.json` 의 대응 필드를 동기화한다.
- acceptance_tests: [{"kind":"shell","cmd":"node -p \"require('./package.json').version\"","expected_exit":0,"stdout_regex":"^2\\.4\\.2$"}]
- verification_cmd: {posix: "node -p \"require('./package.json').version\"", windows: "node -p \"require('./package.json').version\""}
- dod: package.json 과 package-lock.json 의 version 이 모두 2.4.2 이다
- rollback: `git checkout -- package.json package-lock.json`
- estimated_effort: S
- depends_on_task: [T-PH004-01]
- tdd: {applicable: false, phase: n/a, test_cases_count: 0, exempt_reason: "버전 문자열 범프는 제품 동작 변경이 아닌 릴리스 메타데이터 작업이며, acceptance_tests 의 shell 단언이 결과를 직접 검증한다."}

## §4 REQ ↔ Task 역색인

| req_id | stability | task_ids[] | ac_covered/ac_total |
| --- | --- | --- | --- |
| FR-FLOW-042 | evolving | T-PH002-01, T-PH002-02, T-PH004-01 | 7/7 |
| FR-FLOW-043 | evolving | T-PH001-01, T-PH001-02, T-PH002-01, T-PH002-02, T-PH004-01 | 7/7 |
| FR-MCP-055 | evolving | T-PH003-01, T-PH003-02, T-PH004-01 | 4/4 |

> T-PH002-01/02 의 `covers_ac` 평면 목록은 두 REQ 를 합친 표기다. 실제 귀속은 사이드카 `test_cases[].req_id` / `ac_refs` 가 SSOT 이며, 해당 Task 가 덮는 것은 FR-FLOW-042 AC-1..AC-7 과 FR-FLOW-043 AC-5·AC-7 이다. FR-FLOW-043 AC-1/2/3/4/6 은 PH-001 소관이다.

## §5 위험 · 미해결

### 5.1 위험

| risk_id | severity | description | mitigation | affected_task_ids |
| --- | --- | --- | --- | --- |
| R-001 | med | `.agents/skills/_shared/**` 는 codex 미러 드리프트 검사 대상이 아니어서 미러 2개 사본이 조용히 드리프트할 수 있다 | T-PH001-01 의 콘텐츠 테스트가 미러 2개 사본을 명시적으로 순회하여 유일한 드리프트 가드가 되게 한다 | T-PH001-01, T-PH001-02 |
| R-002 | med | 저장소 테스트가 과거 speckiwi init/skill 산출물로 워킹트리를 오염시킨 이력이 있다 | T-PH004-01 실행 후 `git status --porcelain` 으로 비의도 산출물 유무를 확인하고, 발견 시 hermeticity 가드 보고에 기록한다 | T-PH004-01 |
| R-003 | low | 3변종 SKILL.md 는 플레이스홀더 표기가 서로 달라(claude `/kiwi-*`, codex/etc `$kiwi-*`) 동일 문구 복제 시 변종 규약을 깨뜨릴 수 있다 | T-PH002-02 에서 각 변종의 기존 표기 규약을 그대로 따르고, T-PH002-01 단언을 표기 비의존 형태로 작성한다 | T-PH002-01, T-PH002-02 |

### 5.2 Open Questions

없음.

### 5.3 unreferenced_reqs

없음 — target v2.4.2 의 비-deprecated REQ 3건이 모두 Task 에 매핑되었다.

### 5.4 deferred_ac

없음.

### 5.5 TDD 결정

`tdd_policy = relaxed`(work-mode `wait` 파생). 면제 2건은 §0.G7 의 `accept-as-exempt` 로 자동 결정되었다.

| task_id | decision | reason 요약 |
| --- | --- | --- |
| T-PH004-01 | accept-as-exempt | 검증 전용 Task, 실행 대상이 테스트 스위트 자체 |
| T-PH004-02 | accept-as-exempt | 릴리스 메타데이터 범프, shell 단언이 직접 검증 |

## §6 부록

### 6.1 사이드카 JSON 경로 / md_sha256

- 사이드카: `docs/plans/2026-07-28.speckiwi.v242.sidecar.json`
- `md_sha256` 는 본 파일에서 `md_sha256:` 라인을 제외한 내용에 대해 계산한다(자기참조 회피).

### 6.2 검증 스크립트 실행 방법

```
node ~/.claude/skills/kiwi-planner/validator.mjs \
  docs/plans/2026-07-28.speckiwi.v242.plan.md \
  docs/plans/2026-07-28.speckiwi.v242.sidecar.json \
  --target v2.4.2 \
  --inventory-file docs/analysis/kiwi-planner-2026-07-28.speckiwi.v242/inventory.json \
  --out docs/plans/2026-07-28.speckiwi.v242.validator.json
```

`--check-files` 는 사용하지 않는다 — T-PH001-01 의 `test/skills/kiwi-event-contract-content.test.ts` 는 아직 생성되지 않은 신규 파일이다.

### 6.3 mcp_call_log 요약

- `add_trace_link`: 11건 (Task → REQ, relation `depends_on`)
- `add_verification_evidence`: 3건 (coverage 의 REQ 별 1건, type `plan`)
