# TDD 모드 후속작업 목록 — 해소 완료 (2026-07-17)

| 항목 | 내용 |
|---|---|
| 작성일 | 2026-07-16 |
| 해소일 | 2026-07-17 — 사용자 지시 "v2.4.0으로 갑니다. 후속 문제를 모두 해결" + 5인 결정위원회(Q1~Q7) |
| 결과 REQ | FR-FLOW-039, FR-FLOW-040(신규 verified) + FR-PARSE-033 AC-5, FR-NODE-081 AC-6, FR-NODE-083 AC-5/6, FR-MCP-054 AC-4(개정 verified) |
| 상세 | docs/spec/91.completed-work-log.md 2026-07-17 행, docs/research/tddmode/06.g4-orchestrator-integration.md |

## 1. 해소 결과 (원 목록 대비)

| 원 항목 | 결정 | 처리 |
|---|---|---|
| P1 G4 오케스트레이터 통합 | **모델 A 채택 (Q1, 5:0)** | FR-FLOW-039(kiwi-pipeline §2.8 work-mode 라우팅 게이트) + FR-FLOW-040(workmode-policy.md SSOT 파생: tdd→strict, 명시 플래그 우선+WARN; pm non-HALT 모순 경고) — 3변형 저작 + 콘텐츠 테스트 18케이스, verified |
| P2 릴리스/버전 | **Q2 표결** | Active Target v2.4.0 전환(4:1), package 2.4.0 범프. main ff-머지·태그(3:2)와 npm publish(5:0)는 **사용자 복귀 후** — §2 참조 |
| P2 kiwi-tdd 런타임 설치 | 실행 | ~/.claude/skills에 kiwi-tdd + _shared/kiwi/workmode-policy.md 설치(신규 파일만; 기존 스킬 덮어쓰기는 §2 참조) |
| P3-1 FR-MCP-054 AC-4 인용 | 정정 | IR-CLI-031→IR-CLI-049 문안 정정(verified 일시 하향→복귀) |
| P3-2 미러 doctor 실경로 테스트 | 실행+확장 | 기본 분기 통합 테스트 추가. 조사 중 **실드리프트 + sentinel 모순 발견** → Q7:B(5:0)로 제외 manifest(.agents/skills/.speckiwi-mirror-exclusions.json) 1급화, repo doctor `ok(14 skills; 2 excluded by manifest)` durable |
| P3-3 sds-status 패딩 | **보존으로 변경 (Q3, 4:1)** | 값 토큰만 치환(FR-NODE-081 AC-6), red→green |
| P3-4 loadStepDesign 정규화 | 실행 | isSafeTaskName 가드(src/core/step-name.ts 공용 모듈, FR-PARSE-033 AC-5) |
| P3-5 SDS 헤딩 부분일치 | **현행 유지 (Q4, 4:1)** | 서수 프리픽스 허용은 의도된 설계 — FR-PARSE-033 구현 노트 기록. 실오탐 관측 시 재검토 |
| 잔여-1 diff_steps 재노출 | **discard 유지 (Q5, 5:0)** | 단 discard 사유의 사실오류("workflow_diff로 이미 노출")를 FR-MCP-044 research 노트로 정정. 재노출은 실소비자 등장 시 신규 ID로만 |
| 잔여-2 SidecarTask tdd 모델링 | **스킬-측 유지 (Q6, 5:0)** | 유실 경로 없음 확인: 런타임 mutation은 Markdown 라인 패치뿐, 사이드카 JSON은 재직렬화되지 않음 |

## 2. 남은 것 (사용자 결정 대기)

1. **릴리스 컷**: main으로 fast-forward 머지 + 태그 `2.4.0`(v 접두사 없음, 버전 범프 커밋에 — 2.3.1 선례). 위원회는 "사용자 복귀 후"로 결정(3:2).
2. **npm publish 2.4.0**: 준비(pack 검증)까지 완료, 실행은 사용자 전담(5:0 — 외부·비가역).
3. **사용자 런타임 스킬 갱신 여부**: 이번에 repo의 kiwi-pipeline/kiwi-planner/kiwi-pm이 개정됨. `~/.claude/skills`의 사용자 사본은 커스텀 보존 위험 때문에 **자동 덮어쓰지 않음** — 갱신 원하면 `speckiwi skills install claude --global`(단, _shared 미동기화 갭 주의) 또는 수동 복사.

## 3. 미래 개선 후보 (REQ 등록 전 아이디어)

- **git-status 인지형 hermeticity sentinel**: 현행 순수 존재 검사 대신 "존재 AND untracked"만 누출로 판정하면 제외 manifest/sentinel 이중 관리 자체가 소멸 (Q7 심의에서 위원 2인 권고; FR-NODE-083 구현 노트 기록).
- **테스트 파일명 구-넘버링 정리**: `vibe-gate.ir-cli-031.test.ts`(실제 IR-CLI-049), `diff-steps.fr-node-034.test.ts`(실제 FR-NODE-049) — 기능 무해, 개명은 churn이라 보류.
- **v2.3.0.1 검증 백로그**: implemented-not-verified 85건이 v2.3.0.1에 잔존(`speckiwi summary --target v2.3.0.1`로 조회). Active Target 전환으로 고아화되지 않도록 여기 명시 기록(Q2a 전환 조건).

## 4. 대응 불필요 (재제안 금지 포함 — 이전 목록 승계)

- **dogfood Rules version drift warn**: 의도된 자기-dogfood 상태. "init auto-normalize" 재제안 금지(2026-07-12 3인 만장일치 기각).
- **`_shared` 부재 의혹**: 실재함(과거 glob 오판).
- **kiwi-step의 add_requirement 금지 계약**: 유지(FND-001) — 완화 제안 금지.
- **diff_steps discard 사유를 영구 차단 근거로 인용 금지**(Q5): 사유는 정정됐고, 재노출은 실소비자+신규 ID 조건부로만.
