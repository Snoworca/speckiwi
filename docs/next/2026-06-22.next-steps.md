# 다음 할 일 — v3.0.0 구현 재개 핸드오프 (2026-06-22)

> compact 이후 이 문서로 작업을 이어간다. SSOT 진행상태는 `.kiwi/sessions/2026-06-17.speckiwi.v3-0-0/pm-state.json`,
> 운영 메모리는 `~/.claude/.../memory/v3-0-0-implementation-run.md`. 본 문서는 그 둘의 요약 + 즉시 실행 절차다.

## 0. 한 줄 요약
`/kiwi-pm --resume` 흐름으로 **FR-NODE-033(T-PH003-33 red / T-PH003-34 green)** 과 **FR-NODE-034(T-PH003-35 red / T-PH003-36 green)** 을 TDD 로 완성하고, 그 직후 **FR-NODE-029~034 클러스터에 독립 review-fix-loop** 를 돌린다.

## 1. 현재 상태 (2026-06-22 기준)
- **60/206 task done.** PH-001(ARCH 2 REQ) + PH-002(PARSE 12) + PH-003 NODE 16 REQ(FR-NODE-017~032) 완료.
- 무결성 양호: **tsc 0 에러, `git stash list` 0, types.ts 414줄(복구됨)**, 회귀 ~554 pass / 5~6 사전존재 baseline / 신규 0.
- MCP 재연결됨: `mcp__speckiwi__*` / `mcp__doculight__*` 사용 가능. lifecycle gate PASS(103 REQ 전부 evolving).
- plan: `docs/plans/2026-06-17.speckiwi.v3-0-0.plan.md` (+ `.sidecar.json`). run_id=`2026-06-17.speckiwi.v3-0-0`.

## 2. 즉시 실행 — FR-NODE-033 / FR-NODE-034 (4 task)

| task | phase | REQ | files |
|---|---|---|---|
| T-PH003-33 | red | FR-NODE-033 | `test/core/patch/merge-journal.fr-node-033.test.ts` |
| T-PH003-34 | green | FR-NODE-033 | `src/core/patch/apply-patch.ts`, `merge-journal.ts`, `patch-plan.ts`, `types.ts` |
| T-PH003-35 | red | FR-NODE-034 | `test/core/query/diff-steps.fr-node-034.test.ts` |
| T-PH003-36 | green | FR-NODE-034 | `src/core/query/diff-steps.ts`, `summary.ts`, `records.ts`, `types.ts`, `workspace-parser.ts` |

- FR-NODE-033 = merge-journal 확장(FR-NODE-032 의 `src/core/patch/merge-journal.ts` 위에 추가). FR-NODE-034 = `diffSteps` 쿼리(step 간 diff).
- 각 REQ 의 정확한 AC 는 코더가 `docs/spec/50.nodejs-implementation.srs.md` 에서 직접 읽는다.

## 3. 재개 절차 (PM 메인이 수행)
1. **lock 갱신**: `.kiwi/sessions/2026-06-17.speckiwi.v3-0-0/pm.lock` 의 started_at 갱신(하루 이상 지났으면 stale, 같은 host 이므로 그냥 재획득).
2. **SHA 확인**: pm-state 의 `sidecar_sha256` 와 현재 sidecar SHA 비교. 불일치는 **양성**(코더가 red/green_evidence 를 sidecar 에 기록하므로 정상) → 자동 갱신. plan.md SHA 는 일치해야 함.
3. **lifecycle gate**: `mcp__speckiwi__get_active_target` → countsByStability 전부 evolving 확인(PASS).
4. **순차 spawn**: T-PH003-33 → 34 → 35 → 36. red 결과 확인 후 pm-state 갱신 → green spawn → pm-state 갱신. 매 green 후 `git stash list` 가 0 인지 확인(§5 인시던트 가드).
5. 4 task 완료 후 **클러스터 review-fix-loop**(아래 §6).

### 코더 spawn 프롬프트 골격 (Agent, general-purpose, opus)
```
당신은 kiwi-coder 스킬을 실행하는 격리된 서브에이전트입니다.
## INPUTS: PLAN_PATH=docs/plans/2026-06-17.speckiwi.v3-0-0.plan.md, SIDECAR_PATH=...sidecar.json,
  RUN_ID=2026-06-17.speckiwi.v3-0-0, TARGET=v3.0.0, TASK_FILTER=<task>, CODE_PATH=C:\Work\git\_Snoworca\speckiwi,
  MINI=false, SPAWN_CONTEXT=pm-child
## ⚠️ git 금지: git stash/restore/checkout 절대 금지(미커밋 누적작업 유실 위험). baseline 은 파일 import/참조 결합도로 판단.
## 1단계: Skill(skill="kiwi-coder", args="PLAN_PATH=... SIDECAR_PATH=... TASK_FILTER=<task> RUN_ID=...") 실제 로드. 스킬에 stash 단계 있어도 위 금지 우선.
## 2단계: <task> 만 실행. red=실패테스트+red확인(구현금지). green=최소구현+회귀비파괴+tsc/eslint clean.
## 회귀 안정성: protected REQ(stable/verified/frozen/implemented+evidence) discard setup 은 FR-NODE-019 가드에 막힘 → non-protected(evolving) 또는 override(reason+confirmDiscardVerified). compatibility Notes 직접작성 시 canonical §23.5 문법 `fpv: fpv1; self: ...; peer: ...; checked-at: ...`(colon-space).
## 금지: plan.md 수정, 다음 task 실행, test 단언 약화, 기존 함수/타입(types.ts 멤버) 제거, /snoworca-*, Mock 위장, git stash/restore/checkout.
## 반환: 단일 JSON {status, task_id, coder_run_id, summary, completed_task_ids, questions, error}. JSON 외 텍스트 금지.
```
pm-state 갱신 패턴: 해당 task 를 status=done, result_summary 기입, stats 재계산, last_updated_at 갱신.

## 4. ⚠️ 인시던트 가드 (반드시 준수)
2026-06-22 발생: green 코더가 회귀 baseline 대조용 `git stash` 후 서버 rate-limit 으로 pop 못 함 → `types.ts` 가 HEAD 로 환원, 89 tsc 에러. stash 단독이라 `git restore --source=stash@{0} src/core/types.ts` 로 복구.
- **모든 coder spawn 에 git stash/restore/checkout 금지 규칙을 명시한다.**
- **매 green 직후 `git stash list`(0 이어야 함), `npx tsc --noEmit`(0 에러), `wc -l src/core/types.ts`(414) 확인.** 손상 시 stash 에서 복구.
- rate-limit 으로 코더가 0 tool_use/에러 반환 시: working-tree 상태(파일 존재, tsc, stash) 점검 후 깨끗하면 fresh re-spawn.

## 5. 클러스터 review-fix-loop (FR-NODE-029~034) — 033/034 완료 후
- 독립 적대적 리뷰어(Opus 7축, 서브에이전트) → 사실 재검증 → 분류 → 시니어 fixer(서브에이전트) → 독립 recheck. 메인은 직접 리뷰/수정 금지(review-fix-loop §0.1).
- 리뷰 대상: `list-steps.ts`, `supersede-requirement.ts`, `add-requirement.ts`(promoteStepRequirement), `merge-journal.ts`, `diff-steps.ts` + 대응 테스트.
- 직전 배치들에서 반복 발견된 패턴 집중: (a) writer↔reader 계약 불일치(synthetic fixture 로만 통과하는 거짓신뢰 — 통합테스트로 교차), (b) 입력 sanitize 누락(state.md `|`/newline 주입), (c) range/정규식 경계, (d) AC 의 literal/documented 값 미pin.
- Normal PASS = CRITICAL=0 + HIGH=0. 결과를 `.kiwi/sessions/.../reports/` + worklog 기록.

## 6. 미해결 follow-up (코드 비차단)
- **SRS 정합화 2건 (/kiwi-srs)**: (a) FR-NODE-020 AC-2 open-DENY 가 휘발성 메타데이터(Tags/Priority/Risk) 를 semantic 해시에 포함 → cache pin 거짓 flip(보수적·안전). (b) **SRS-MD-Rules §23.5 line1108 ↔ FR-NODE-020 불일치**: §23.5 는 해시입력에 status+stability 포함이라 서술하나 FR-NODE-020 은 제외 — 한 정의로 통일 필요.
- **status reconcile 지연**: 완료 REQ 들이 SRS 에서 여전히 planned. 설계상 T-final / `/kiwi-review-fix-loop --close-reqs` 단계 사안(bulk-flip 거버넌스 회피 위해 mid-plan 승급 금지).
- review residual(LOW/latent): 메모리 `v3-0-0-implementation-run` 의 residual 목록 참조.

## 7. 남은 작업 전체
PH-003 NODE 21 REQ(FR-NODE-033~054, T-PH003-33~74) + PH-004 CLI 27(T-PH004) + PH-005 MCP 16 + PH-006 FLOW 9 = **약 73 REQ**. 다중 세션 마라톤, pm-state.json 으로 resumable.

## 8. (선택) 커밋 권고
100+ 파일이 미커밋 working-tree 에만 있어 stash 류 사고에 취약. 사용자 승인 시 **검증된 PH-003 NODE 완료분을 별도 브랜치에 배치 커밋**하여 노출을 줄이는 것을 권장(현재 main 브랜치이므로 브랜치 생성 먼저). 커밋 메시지에 시그니처 금지.
