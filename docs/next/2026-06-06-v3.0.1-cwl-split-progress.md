# v3.0.1 Completed Work Log 분리 — 진행 인계 (2026-06-06)

> **목적**: target **v3.0.1**(Completed Work Log 를 `00.index.md` 밖 history 파일로 분리) 작업의 잔여(MIG-NODE-001 + 배치 verify)를 새 세션이 이어받기 위한 지시서. `docs/next` 규약(세션 인계 전용). 설계 SSOT 는 §1 참조.

---

## 0. 한 줄 현황 — ✅ 완료 (2026-06-06)

3인 위원회(만장일치 하이브리드)+타당성 검증→target v3.0.1 생성→9 REQ 등록→§5 3축 등록검증→**9 REQ 전부 TDD 구현**→§5 구현검증(REQ별 적대적)→gap 5건 보강→독립 재검증(all_closed)→**9 REQ 전부 verified**. `validate` clean, 전체 391 통과 신규 회귀 0.

- v3.0.1 status: **`verified 9`** (`speckiwi summary --target v3.0.1`), missing evidence 0, stability blockers 0.
- `speckiwi validate` clean (errors 0 / warnings 0 / diagnostics 0).
- 전체 `npx vitest run` = **391 통과**, 선재 실패 4건(release-readiness ×1, smoke/package ×3 — 본 작업 무관, 버전범프/README/skills 패키징)만. set-target-goal 동시성은 Windows EPERM flaky(단독 통과).
- **남은 것: 없음**(기능 완성). 미커밋 — 사용자 요청 시 커밋. (선택) kiwi-srs-from-code SKILL.md 의 설명용 "SRS-MD-Rules v1.0.0 §8.1" 잔재는 AC-4 범위 외로 미수정, 후속 정리 가능.

---

## 1. 권위 문서 (SSOT)

| 문서 | 역할 |
|---|---|
| `docs/research/2026-06-05-a1-a3-incremental-contradiction-cache-design.md` | 상위 설계(이 작업은 별개 패치) |
| doculight "위원회 보고: Completed Work Log 분리" | 위원회 결정·검증 요약(승인된 하이브리드 설계) |
| `docs/spec/00.index.md` §3 `### Target: v3.0.1` | v3.0.1 goal |
| `docs/rule/SRS-MD-Rules-v3.0.0.md` §7.4(11~16)·§7.1 | 개정된 Completed Work Log 규칙(in-place) |

**확정 설계(위원회 만장일치 + 사용자):** 하이브리드(index §7 heading 유지·기존 행 read-only·신규 행은 history 파일) · 기존 `add_completed_work` **retarget**(신규 도구 X) · history 파일 `docs/spec/91.completed-work-log.md`(non-`.srs.md`) · dual-read append-merge(정렬/dedup 은 query, 중복은 SRS-W025) · 무-마이그레이션 기본 + **opt-in dry-run 마이그레이션** · read-only 배너 · 전부 extend/refine(supersede 0) · 버전 = **v3.0.1 patch**, rules 는 **v3.0.0.md in-place** 개정(새 파일 X, FR-FLOW-013 AC-5 보존).

---

## 2. 완료된 8 REQ (DONE)

| REQ | 내용 | 코드 | 테스트 |
|---|---|---|---|
| FR-PARSE-029 | `discoverSrsFiles` 가 `91.completed-work-log.md` 를 optional `SrsFileSet.completedWorkLog` 로 read(appendix 동형), scope/step 제외 | `src/core/parser/discover.ts` | `test/core/parser/discover-completed-work-log.test.ts` |
| FR-PARSE-030 | `parseCompletedWork` export + workspace-parser 가 index§7+history 를 `index.completedWork` 로 append-concat, `CompletedWorkEntry.sourceFile` 신설, 중복행 **SRS-W025** dedup | `src/core/parser/{index-parser,workspace-parser}.ts`, `src/core/types.ts`, `src/core/diagnostic-registry.ts`, `docs/rule/SRS-MD-Rules-v1.0.0.md`(진단표) | `test/core/parser/completed-work-dual-read-merge.test.ts` |
| FR-PARSE-031 | validator §7 진단(W011~W015,W024)이 `entry.sourceFile` 를 location 으로 사용 | `src/core/validator/rules.ts:~240` | `test/core/validator/completed-work-source-location.test.ts` |
| FR-NODE-045 | `addCompletedWork` 가 history 파일에 기록 + 부재 시 bootstrap(배너), prevalidation·report-path 5→6 마이그레이션 유지 | `src/core/mutation/add-completed-work.ts` | `test/core/mutation/add-completed-work-history.test.ts` (+ 기존 `add-completed-work.test.ts` 를 history 계약으로 재작성) |
| FR-MCP-030 | MCP `add_completed_work` 가 retarget core 경유 history 기록, 스키마 불변 | (core 위임) | `test/mcp/mutation-tools-errors.test.ts` |
| IR-CLI-032 | CLI `add-completed-work` 가 retarget core 경유 history 기록, 옵션/출력 불변 | (core 위임) | `test/cli/mutation-commands.test.ts` |
| FR-FLOW-021 | SRS-MD-Rules §7.4(dual-source·history·배너·append-merge·SRS-W025)·§7.1(inline 행 optional) in-place 개정 + 포인터 v3.0.0 통일(00.index.md L16/L151 stale v1.0.0→v3.0.0) | `docs/rule/SRS-MD-Rules-v3.0.0.md`, `docs/spec/00.index.md`, `CLAUDE.md` | `test/release/cwl-history-split-rules.test.ts` |
| FR-FLOW-022 | repo CLAUDE.md 가 history 파일=read-only summary, REQ 블록=완료 SSOT 명시 | `CLAUDE.md:46` | `test/release/cwl-history-split-rules.test.ts` |

### 구현 중 알아둘 함정(재작업 방지)
- **배너는 heading 위**에 둔다 — `parseMarkdownTable` 은 heading 다음 빈 줄만 건너뛰고 즉시 table 을 요구하므로 배너를 heading↔table 사이에 두면 파싱 실패.
- 신규 진단 코드는 `diagnostic-registry.ts` + **`SRS-MD-Rules-v1.0.0.md` 진단표** 양쪽 등록 필수(동기화 테스트 `test/core/diagnostic-registry.test.ts` 가 v1.0.0.md 를 SSOT 로 검사).
- `patch.filePath` 는 절대경로(`applyPatchPlan` 이 `file.path` 반환). bootstrap 경로도 절대경로로 통일함 — 테스트는 `endsWith` 로 단언.
- valid-basic/mutation-target fixture 의 **FR-ARCH-001 은 planned** → 완료작업 입력 테스트는 `allowIncomplete: true` 필요(W015 회피).
- index §7 → history retarget 으로 기존 `add-completed-work.test.ts`·cli·mcp 테스트가 깨졌고 모두 history 계약으로 갱신함(신규 회귀 0 확인).

---

## 3. 남은 작업 (NEXT)

### ① MIG-NODE-001 — opt-in dry-run 마이그레이션 (마지막 REQ, planned)
- `speckiwi show MIG-NODE-001 --json` 으로 AC 확인. statement: index §7 inline 행을 `91.completed-work-log.md` 로 이동(status 불변, per-row prevalidation, dry-run 기본, 자동 호출 금지).
- trace = `Code|src/core/mutation/migrate-completed-work.ts:1|addition_site` (신규 파일).
- **AC**: (1) dry-run 기본 — 이동할 행을 보고만, 쓰기 X. (2) apply 시 index §7 에서 행 제거 + history 파일에 추가. (3) requirement Status/AC/Verification Evidence 불변. (4) init/parse/기타 명령이 자동 호출하지 않음.
- TDD: 실패 테스트 먼저(`test/core/mutation/migrate-completed-work.test.ts`) — fixture 의 index §7 inline 행 → dry-run 은 미변경+preview, apply 는 index 에서 사라지고 history 에 등장. 구현은 신규 module(index §7 행 parse → history append(부재 시 bootstrap) + index 행 deleteLine). 가능하면 `add-completed-work.ts` 의 patch 헬퍼 재사용.
- 완료 후: evidence + `update-status MIG-NODE-001 implemented`, 00.index §5 `planned 61→60 / implemented 12→13`, validate clean.

### ② 배치 §5 verified 전이 (구현→검증)
- v3.0.1 implemented 9개(위 8 + MIG)와 기존 implemented 4개(FR-MCP-020/029, FR-FLOW-013, FR-PARSE-019)를 **독립 서브에이전트 리뷰**(코드 vs REQ AC 일치, 편향 없는 입력)로 검증 후 `update-status <id> verified`.
- `verified` 게이트: AC 전부 checked + Verification Evidence 존재 필요. 각 REQ AC 박스를 `check-ac <id> <acIds...>` 로 체크 후 verified. (현재 AC 미체크 상태일 수 있음 — 확인 필요.)
- §5 권장: `Workflow` 로 REQ별/축별 병렬 리뷰 → must-fix 0 확인 후 일괄 verified.

---

## 4. 미커밋 / 커밋 정책
- 전부 **미커밋**. 사용자 요청 시에만 커밋(서명 금지, CLAUDE.md §6). 일회성 분석 스크립트(`docs/analysis/*.mjs`)는 커밋 전 삭제 검토.
- 이번 작업 주요 변경: `src/core/{parser/discover,parser/index-parser,parser/workspace-parser,types,diagnostic-registry,validator/rules,mutation/add-completed-work}.ts`, `docs/rule/SRS-MD-Rules-v{1.0.0,3.0.0}.md`, `docs/spec/00.index.md`(+20/30/40/50/60.srs.md REQ 등록), `CLAUDE.md`, 신규/수정 테스트 6종.

## 5. 새 세션 첫 메시지 권장
> "v3.0.1 이어서. `docs/next/2026-06-06-v3.0.1-cwl-split-progress.md` 읽고 MIG-NODE-001 TDD 부터 진행하고, 끝나면 v3.0.1 implemented 전체를 배치 §5 verified 전이해줘."
