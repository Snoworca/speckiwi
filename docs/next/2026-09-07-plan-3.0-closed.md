# 계획 3.0 종료와 릴리스 준비 — 세션 핸드오프

| Field | Value |
| --- | --- |
| 작성일 | 2026-09-07 |
| 저장소 / 브랜치 | `C:\Work\git\_Snoworca\speckiwi` / `feat/2.3.0.1` |
| 최종 작업 목표 | `docs/plan/3.0/` 의 33 항목을 순서대로 완료하고 저장소를 배포 가능 상태로 만든다 |
| 현재 상태 | **계획 33/33 완료. 코드·요구·회귀 전부 초록. 버전 범프와 색인 동기화 미실시** |
| SSOT | `C:\Work\git\_Snoworca\speckiwi\docs\plan\3.0\index.md` (항목의 진실), `C:\Work\git\_Snoworca\speckiwi\docs\plan\3.0\REMAINING.md` (남은 것과 작업 규율) |
| 다음 세션 첫 행동 | 버전 범프 방향을 사용자에게 확인하고 `/kiwi-srs-sync` 를 돌린다 |

> 이 문서는 다음 세션이 **이 문서와 SSOT 만 읽고** 자율적으로 작업을 이어갈 수 있도록 정리한 것이다. 대화 히스토리에 의존하지 말 것.

---

## 0. 다음 세션의 첫 행동

1. 이 문서를 끝까지 읽는다.
2. `C:\Work\git\_Snoworca\speckiwi\docs\plan\3.0\REMAINING.md` 를 정독한다 — 남은 것과 이 계획이 세운 작업 규율이 거기 있다.
3. `git status --porcelain` 으로 워킹트리가 아래 「현재 워킹트리·저장소 상태」와 일치하는지 확인한다.
4. **버전 범프 방향을 사용자에게 묻는다.** `package.json` 이 아직 `3.0.0` 인데 이 세션이 요구 13 건을 새로 verified 로 만들었다. 범프 없이는 배포할 수 없고, 어느 자리를 올릴지는 사용자 결정이다.
5. 사용자가 답하는 동안 `/kiwi-srs-sync` 를 돌려 색인을 동기화한다.

## 1. 최종 작업 목표

`docs/plan/3.0/` 에 등록된 33 개 항목을 순서대로 처리하고, 각 항목마다 SRS 요구를 등록해 TDD 로 구현한 뒤 독립 서브에이전트 검증을 거쳐 `verified` 로 승격하고 커밋한다. 최종 목표는 저장소를 배포 가능 상태로 만드는 것이다.

**완료 조건**: 계획 색인의 33 항목이 전부 `- [x]`, 전체 회귀 0 실패, `speckiwi validate` errors 0.

**세 조건 모두 2026-09-07 에 충족되었다.** 배포 자체(푸시·태그·`npm publish`)는 사용자 몫이다.

## 2. 현재까지 완료한 작업

- [x] 계획 33 항목 전부 완료 — `C:\Work\git\_Snoworca\speckiwi\docs\plan\3.0\index.md` 의 진행표가 A 12/12 · B 15/15 · C 5/5 · D 1/1 · 합계 33/33
- [x] 전체 회귀 — `NODE_ENV=test npm test` (2026-09-07 실행, 단독), **478 파일 / 7957 통과 / 4 skip / 0 실패**, 로그 `C:\Users\beom\AppData\Local\Temp\claude\C--Work-git--Snoworca-speckiwi\ea8e081f-d68f-4f86-8c55-516d857dcf8a\scratchpad\groupcd-regression.log`
- [x] 게이트 — 같은 실행 직전에 `npm run typecheck` · `npx tsc --noEmit -p tsconfig.test.json` · `npm run lint` 전부 EXIT 0, `node bin/speckiwi skills mirror --check` divergences 0, `node bin/speckiwi validate` errors 0 / warnings 3
- [x] 이 세션의 커밋 12 개 — `a7e87f8` 부터 `a617c52` 까지. `git log --oneline -12` 로 확인 가능

### 2.1 이 세션이 새로 등록해 verified 로 만든 요구 (13)

기준: `git show da434ab:docs/spec/*.srs.md` 에 부재하고 HEAD 에 `verified` 로 있는 요구.

| 요구 | 계획 항목 |
| --- | --- |
| `FR-NODE-205` | 23 |
| `FR-FLOW-175` | 29 |
| `FR-MCP-062` · `FR-FLOW-176` | 13 |
| `FR-MCP-063` | 31 |
| `FR-MCP-064` | 32 |
| `FR-NODE-206` | 24 |
| `FR-NODE-207` | 25 |
| `FR-FLOW-177` | 28 |
| `FR-FLOW-178` | 27 |
| `REL-NODE-008` | 33 |
| `FR-NODE-208` | 26 |
| `FR-FLOW-179` | 12 · 15 · 16 |

**계획 11번의 `FR-FLOW-174` 는 이 세션이 만든 것이 아니다** — 직전 커밋 `da434ab` 에 이미 `Status | verified` 로 존재한다. 이전 세션이 닫았다.

### 2.2 하향 → 수정 → 재승격한 기존 verified 요구 (6)

`REL-MCP-005` · `FR-NODE-131` · `FR-NODE-204` · `FR-FLOW-036` · `FR-MCP-018` · `FR-NODE-095`.

**여섯 다 이 작업이 만든 거짓이 아니라, 요구가 옛 사실을 계약으로 삼고 있던 자리다.** 문서나 코드를 사실대로 고치자 그 옛 사실에 기대던 문면이 붉었다.

`FR-MCP-063` 도 같은 하향 → 재승격을 거쳤으나(Change Notes: 2026-09-04 verified → implemented → 2026-09-05 verified) **이 세션이 신설한 요구**이므로 §2.1 에 속하고 이 목록에는 넣지 않는다.

## 3. 현재 워킹트리·저장소 상태

- 브랜치: `feat/2.3.0.1`, origin 대비 **ahead 76** (`git rev-list --count origin/feat/2.3.0.1..HEAD` = 76, 2026-09-07)
- 미커밋: `git status --porcelain` 이 **네 줄**을 낸다 — ` M AGENTS.md` · ` M docs/next/LATEST.md` · ` M docs/plan/3.0/REMAINING.md` · `?? docs/next/2026-09-07-plan-3.0-closed.md`. 가운데 셋 중 둘은 이 핸드오프 자신(문서와 그 포인터)이고, `REMAINING.md` 는 target `3.1.0` 계수 갱신과 철회 문구 한 줄이며, `AGENTS.md` 는 실제 변경이 아니라 대소문자 충돌로 생긴 유령 인덱스 엔트리다(아래 「거버넌스·게이트·함정」 참조)
- `package.json` 의 `version` 은 **`3.0.0`** (미범프)
- SpecKiwi target `3.1.0`: **verified 37 · planned 7 · 합계 44** (`node bin/speckiwi summary --target 3.1.0 --json`, 2026-09-07). `docs/spec/00.index.md` §5 의 **verified 473 · planned 11 · implemented 135 · discarded 27** 은 target 별 수가 아니라 **저장소 전체 롤업**이다 — 둘을 섞지 말 것

## 4. 관련 문서·코드 (절대경로)

| 문서 | 절대경로 | 역할 |
| --- | --- | --- |
| SSOT (항목) | `C:\Work\git\_Snoworca\speckiwi\docs\plan\3.0\index.md` | 33 항목의 완료 표시. **완료 표시는 여기에만 한다** |
| SSOT (남은 것) | `C:\Work\git\_Snoworca\speckiwi\docs\plan\3.0\REMAINING.md` | 남은 요구, 작업 규율, 알려진 함정, 별건 선재 결함 |
| 요구 색인 | `C:\Work\git\_Snoworca\speckiwi\docs\spec\00.index.md` | Active Target 과 롤업 |
| 완료 기록 | `C:\Work\git\_Snoworca\speckiwi\docs\spec\91.completed-work-log.md` | 이 세션의 완료 행 **8 개**가 여기 있다 |
| 저장소 규약 | `C:\Work\git\_Snoworca\speckiwi\CLAUDE.md` | SRS 워크플로와 검증 강도 정책 |
| 사용자 전역 규약 | `C:\Users\beom\.claude\CLAUDE.md` | 커밋 시그니처 금지·TDD·서브에이전트 검증 등 저장소 밖 규칙의 정본 |

완료 행이 8 개라는 것을 다시 재려면 (마크다운 표 밖에서 그대로 복붙):

```
git diff da434ab..HEAD -- docs/spec/91.completed-work-log.md | grep -c '^+| 2026-'
```

작은따옴표로 감싸는 것이 중요하다 — `grep -c "^+\|"` 처럼 백슬래시가 붙으면 BRE 에서 `\|` 가 교대 연산자가 되어 **전체 줄 수(16)** 를 센다.

**요구 문서**: `docs/spec/40.mcp-stdio-interface.srs.md`(MCP) · `50.nodejs-implementation.srs.md`(NODE) · `60.workflow-release.srs.md`(FLOW) — 전부 `C:\Work\git\_Snoworca\speckiwi\` 아래.

**이 세션의 조사·구현 보고서**: `C:\Users\beom\AppData\Local\Temp\claude\C--Work-git--Snoworca-speckiwi\ea8e081f-d68f-4f86-8c55-516d857dcf8a\scratchpad\` 아래에 `probe*.md` · `impl*-report.md` · `v*-*.md` 로 남아 있다. **스크래치패드는 세션별이라 다음 세션에서 사라질 수 있다** — 필요한 사실은 전부 요구의 증거 Notes 로 옮겨 두었다.

## 5. 확정된 결정 (변경 금지)

1. **계획 33 항목은 닫혔다** — **확정**. (근거: `docs/plan/3.0/index.md` 의 진행표 33/33, 미완료 체크박스 0 개)
2. **`FR-NODE-202` 는 부분 구현으로 `planned` 에 남긴다** — **확정**. 계획 30번이 AC-2·AC-3·AC-6·AC-7 을 닫았고 AC-1·AC-4·AC-5 가 남았다. (근거: 그 요구의 AC-4 가 "따로 닫지 말라"고 명시)
3. **파이프라인 이벤트를 MCP 전용 경로로 만들지 않는다** — **확정**. `FR-FLOW-043` AC-5 가 경로를 실행 디렉터리에 못박았고 홈 폴백은 그 도구로 표현이 불가능하다. (근거: `FR-FLOW-179` AC-4)
4. **`kiwi-orchestrator` 의 절을 참조로 옮기지 않는다** — **확정**. 선례로 든 codex 렌더링에도 `references/` 가 없고 두 렌더링이 139,660 바이트 중 174 만 다르다. (근거: `FR-FLOW-179` AC-7)
5. **스킬 description 에서 `--mini`·`--loops` 어휘를 걷어내지 않는다** — **확정**. 두 요구를 검사하는 `test/skills/kiwi-loop-option-content.test.ts:163-164` 가 옵션 이름 주위 고정 창 안에서 **전파 토큰**을 찾으며, 그 창을 벗어나면 `FR-FLOW-034`·`FR-FLOW-035` 스위트가 붉는다. (근거: `FR-FLOW-179` AC-7 이 같은 취지를 "a fixed distance" 로 기록)
6. **hermeticity 가드를 `%TEMP%` 로 넓히지 않는다** — **확정**. 그 가드가 자기 저널을 거기 일부러 쓰므로 넓히면 자기 쓰기에 걸린다. (근거: `FR-NODE-208` AC-3, 그리고 `test/support/hermeticity-guard.ts:57-58` 의 주석 — "It is written outside the repository so the guard cannot become a source of pollution itself" 와 `path.join(tmpdir(), "speckiwi-hermeticity")`. 저널 경로를 구성하는 다른 자리는 `test/support/hermeticity-global.ts:25` 이고, 그 위 22줄 주석이 같은 취지를 적고 있다. **`test/support/repo-hermeticity.ts` 가 아니다** — 그 파일은 `%TEMP%` 에 아무것도 쓰지 않는데 `FR-NODE-208` 의 Implementation Note 가 그리로 잘못 지목하고 있다)
7. **`%TEMP%` 의 선재 누적물을 지우지 않았다** — **확정**. `FR-NODE-208` 은 앞으로 안 쌓이게 하는 요구이고, 이미 쌓인 276,452 항목은 그대로다. 그 계수가 나중에 줄었는지 재는 기준선이다. (근거: `FR-NODE-208` Implementation Notes)

## 6. 미결정·유예 항목

- **버전 범프 자리** — 이 세션이 요구 13 건을 새로 verified 로 만들었으나 `package.json` 은 `3.0.0` 그대로다. 결정 방법: **사용자 확인**. 범프하지 않으면 배포할 수 없다.
- **푸시·태그·`npm publish`** — 사용자 몫으로 남겨 둔 것이다. 이 세션은 로컬 커밋만 했다.

## 7. 남은 작업 전체 목록

- [ ] **버전 범프** — `package.json` 의 `version` 이 `3.0.0` 이 아닌 값이 되고, 그 값이 릴리스 문서와 일치한다 (의존성: 사용자 결정)
- [ ] **`/kiwi-srs-sync`** — 색인이 요구 문서와 동기화되고 `speckiwi validate` errors 0 유지
- [ ] **계획 밖 미구현 요구 7 건** — `FR-NODE-199` · `FR-NODE-200` · `FR-NODE-201` · `FR-NODE-202` · `FR-FLOW-156` · `FR-FLOW-166` · `FR-MCP-061` 이 `planned` 을 벗어난다. 목록과 요지는 `REMAINING.md` 의 「계획 밖 — 미구현 요구」 절에 있다
- [ ] **별건 선재 결함** — `REMAINING.md` 의 「별건으로 남긴 선재 결함」 절에 다섯 건이 이름과 증상과 함께 적혀 있다. 각각 계획 항목으로 받을지 판단이 필요하다
- [ ] **푸시·태그·`npm publish`** — 사용자가 직접 수행

## 8. 다음 세션 지시서

1. **버전 범프 방향을 묻는다** → 검증: 사용자가 답한 값이 `package.json` 에 들어가고 `npm run build` EXIT 0
2. **`/kiwi-srs-sync` 실행** → 검증: `node bin/speckiwi validate --json` 이 errors 0 · warnings 3(선재 `SRS-W015`)
3. **선재 warnings 3 건의 처분을 정한다** → 검증: 남기기로 하면 그 이유가 어딘가에 적혀 있고, 고치기로 하면 warnings 가 0 이 된다. 세 건은 `REL-MCP-004` ×2 와 `IR-CLI-076` 의 "Completed Work Log requirement is not completed" 이며 이 세션 착수 전부터 있었다
4. **마지막 전체 회귀** → 검증: `NODE_ENV=test npm test` 를 **단독으로** 돌려 0 실패. 기준선은 478 파일 / 7957 통과 / 4 skip
5. **커밋** → 검증: `git log -1 --format="%B"` 에 시그니처 0 건

## 9. 거버넌스·게이트·함정

**저장소 규약** — 정본은 `C:\Work\git\_Snoworca\speckiwi\CLAUDE.md`

- `docs/spec/` 이 요구의 유일한 SSOT. 증거 없이 `verified` 로 올리지 않는다. MCP 도구 또는 `speckiwi` CLI 로만 수정한다
- **판단이 개입하는 검증은 서브에이전트에 위임한다** (같은 파일 19줄의 "subagent verification"). 자기검증 금지

**사용자 전역 규약** — 정본은 `C:\Users\beom\.claude\CLAUDE.md`. **저장소 `CLAUDE.md` 에는 이 두 항목이 없으므로 거기서 찾지 말 것**

- 커밋 메시지에 **어떤 시그니처도 넣지 않는다** — `Co-Authored-By` · `Generated with/by` · `🤖` 등 (§6). 커밋 후 `git log -1 --format="%B"` 로 확인
- 커밋 제목에 `Phase {n}` · `Step {n}` · `TASK-XXX` 를 넣지 않는다 (§6)

**절대 실행 금지**

- **`speckiwi init`(특히 `--force`) · `skills install` · `upgrade` · `remove`** — `docs/spec/00.index.md` 를 템플릿으로 덮어쓴다. `skills mirror --write` 와 `--check` 만 허용
- **`git checkout` · `git restore` · `git stash` · `git clean` · `git reset --hard`**
- **`taskkill /IM node.exe` 같은 일괄 종료** — ⚠️ 미검증 — "다른 세션이 여럿 돈다"는 실행 시점의 프로세스 상태에 대한 주장이라 저장소로 참·거짓을 가릴 수 없다. 다만 금지 자체는 사용자가 이 세션에 직접 지시한 것이므로 지킨다

**이번 세션에 실제로 밟은 함정**

| 함정 | 회피 방법 |
| --- | --- |
| **승격 게이트가 증거 `reference` 를 `;` 로만 쪼갠다** (`splitEvidenceReferences`, `src/core/workflow/release-readiness.ts:68-75`) — 쉼표·괄호·`+` 로 여러 파일을 담거나 앞에 경로 토큰을 두면 전체가 하나의 경로가 되어 `MUTATION_DENIED` 로 거절된다 | 여러 경로는 **`;` 로 구분**하거나 단일 경로 하나만. 나머지는 `notes` 로. **타입으로 면제되는 것은 `command`·`cmd`·`shell` 셋이고 `manual` 은 거기 없다**(`isCommandReference`, 81-85줄). `isPathLikeReference` 의 타입 화이트리스트도 `file`·`path`·`test`·`inspection`·`artifact` 뿐이다. 다만 `isCommandReference` 는 타입과 무관하게 **`$` 로 시작하거나 `npm`·`npx`·`node`·`git` 등 명령어 이름으로 시작하는 문자열**도 면제하므로, 안전한 쪽은 `manual` 참조를 **경로 모양이 아닌 서술**로 쓰는 것이다(`/`·`\` 없고 1–8자 확장자로 끝나지 않음) |
| **`git diff --numstat` 은 워킹트리가 깨끗하다는 증거가 못 된다** — 뮤테이션이 같은 길이로 치환하면 합계가 그대로다. 변종이면서 452/83 인 경우가 두 번 관측되었다 | 판별은 `git hash-object` 나 md5 로 |
| **소스를 고치는 검증 축과 읽는 축을 같은 워킹트리에서 병렬로 돌리면 오염된다** — 32번에서 뮤테이션 축의 in-flight 변종을 다른 세 축이 읽어 옳은 재서술 14 건이 전부 붉었다 | 소스를 고치는 축은 `git archive HEAD` + 미커밋 diff 로 만든 **격리 사본**에서만. `.gitattributes` 가 `eol=lf` 이므로 archive 의 CRLF 를 LF 로 되돌려야 blob 이 맞는다 |
| **`AGENTS.md` 와 `agents.md` 가 인덱스에 둘 다 등록되어 있다** — Windows 는 대소문자를 구분하지 않아 파일이 하나이므로 `git add AGENTS.md` 가 조용히 아무것도 하지 않는다. 대소문자를 구분하는 파일시스템에서 체크아웃하면 두 파일이 생기고 하나는 낡는다 | 건드리지 말 것. 실제 내용은 `agents.md` 엔트리로 커밋된다 |
| ⚠️ 미검증 — **heredoc 이 `<<'EOF'` 안에서도 백슬래시를 접는다.** 셸이 큰따옴표 안의 백틱을 명령 치환으로 읽어 문자열을 지운 사고도 있었다. 확인 불가 이유: 사고 흔적이 저장소에 남지 않았고, 앞 문장은 어떤 인용·이스케이프 조합인지 특정되지 않아 재현 형태로 좁힐 수 없다. 다음 세션이 확인하려면 백틱을 담은 `python -c "..."` 와 `<<'EOF'` 를 각각 최소 사례로 돌려 보면 된다 | 정규식이나 백틱이 들어가는 스크립트는 **파일로 쓰고 실행**. `String.raw` 사용 |
| **`npm run build` 통과를 타입 통과로 읽으면 안 된다** — `tsconfig.json` 의 `include` 에 테스트가 없다 | 테스트 타입은 `npx tsc --noEmit -p tsconfig.test.json`. 다만 그 `include` 는 `src` 와 `test/core/orchestrator` 와 `test/cli/orchestrate*` 뿐이라 대부분의 테스트 파일은 이 검사를 받지 않는다 |

**테스트 실행 명령** (복붙 가능)

```
NODE_ENV=test npm test
```

**반드시 단독으로 돌린다.** `--maxWorkers` 를 주지 않는다. ⚠️ 미검증 — "다른 폴링 루프와 동시에 돌리면 타임아웃 거짓 실패가 난다"는 두 조건으로 각각 전체 스위트를 돌려야 확인되는데 이 세션은 단독 실행 한 번의 로그만 남겼다. 사용자가 이 세션에 단독 실행을 직접 지시했으므로 규칙 자체는 지킨다. 약 15 분 걸린다.

## 10. 리스크·잔존 이슈

- **`REL-NODE-005` 취약 테스트는 이 세션에서 고쳐졌다** — 계획 33번(`REL-NODE-008`)이 `delay(30)` 두 곳을 신호 대기로 바꿨고, 부하 40 로더 아래 20/20 초록을 확인했다. **이전 핸드오프 문서가 "7회 중 4회 붉는다"고 적었다면 그것은 이제 낡은 정보다**
- **`FR-NODE-208` 이 임시 디렉터리 누수의 56%만 닫았다** — 나머지 44%(108,575 항목)는 `waves-fixtures.ts` 와 `mkdtemp` 를 직접 부르는 약 105 개 파일에 있다. 요구의 Implementation Notes 에 파일별로 적혀 있다
- **`FR-NODE-207` 의 뮤테이션 하나를 죽이지 못했다** — 회수 guard 가 lease 를 상속하는 변종인데 결정적 창이 없다. 소스 텍스트 스캔이면 죽지만 이 저장소가 기록한 교훈에 어긋나 쓰지 않았다. 요구의 증거에 그렇게 적혀 있다
- **`FR-FLOW-179` 가 `FR-FLOW-154` 의 tool-axis 바닥을 394 에서 324 로 낮췄다** — 떠난 82 자리를 전수로 재어 전부 옵션·하위명령·낱말 이름이고 status·stability 값은 하나도 없음을 확인한 뒤 그 이름들을 주석에 적었다. 하한을 낮춘 것이므로 **다음 사람이 검증할 수 있게 근거를 남긴 것이지 근거가 없는 것이 아니다**
- **선재 `SRS-W015` 경고 3 건이 남아 있다** — `REL-MCP-004` ×2, `IR-CLI-076`. 이 세션 착수 전부터 있었고 건드리지 않았다
