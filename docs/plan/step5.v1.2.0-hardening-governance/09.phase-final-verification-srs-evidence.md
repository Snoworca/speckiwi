# Phase P9: Final Verification and SRS Evidence

| Field | Value |
|---|---|
| Phase | P9 |
| Plan ID | plan-20260510-v1-2-hardening-governance |
| Status | approved |
| Research Step | 10. Re-run release readiness for v1.2.0, then add Completed Work Log row only after implementation is complete |
| Primary Output | Full regression, v1.2.0 SRS evidence, Completed Work Log update |

## 1. 목표

모든 Phase 구현 후 full verification을 실행하고, 통과한 명령만 v1.2.0 SRS evidence로 기록한다. 마지막에 v1.2.0 target 상태와 Completed Work Log를 갱신한다.

## 2. TASK 목록

### TASK-P9-001 — Full regression and release verification

| Field | Value |
|---|---|
| 관련 SRS 참조 | all v1.2.0 requirements |
| 파일 경로 | 수정 없음 unless failures require earlier Phase rework |
| 메서드/함수 시그니처 | N/A - verification task |
| 참고 패턴 | `package.json:45-53` command list and `scripts/release-check.mjs:5-10` release check entry point. |
| source_anchors | `["package.json:45-53", "scripts/release-check.mjs:5-10", "docs/plan/step5.v1.2.0-hardening-governance/00.index.md:157-205"]` |
| 예상 소요 | 4시간 |

#### 구현 가이드

1. Run every command in `acceptance_tests`.
2. If any command fails, return to the owning Phase and do not update SRS evidence.
3. Record command names and exit codes for later evidence rows.

#### Rationale

This target touches parser, validator, mutation, workflow, CLI, MCP, package, and CI. Evidence must be based on full regression, not isolated unit tests.

#### 함정 / 주의사항

- Do not record evidence for commands that failed.
- Do not weaken tests to make release readiness pass.
- Do not add v1.2.0 Completed Work Log row before verification passes.

#### 테스트 작성 지침

- 성공: all listed commands exit 0.
- 실패: first failing command is mapped back to the owning Phase.
- 경계: `node bin/speckiwi validate --json` output must parse as JSON and contain zero errors.

#### acceptance_tests

```yaml
acceptance_tests:
  - {shell: "pwsh", cmd: "npm run build", expected_exit: 0}
  - {shell: "pwsh", cmd: "npm run typecheck", expected_exit: 0}
  - {shell: "pwsh", cmd: "npm run lint", expected_exit: 0}
  - {shell: "pwsh", cmd: "npm test", expected_exit: 0}
  - {shell: "pwsh", cmd: "npm run test:integration", expected_exit: 0}
  - {shell: "pwsh", cmd: "npm run release:acceptance", expected_exit: 0}
  - {shell: "pwsh", cmd: "npm run release:check", expected_exit: 0}
  - {shell: "pwsh", cmd: "npm run perf:srs", expected_exit: 0}
  - {shell: "pwsh", cmd: "node bin/speckiwi validate --json", expected_exit: 0, stdout_regex: "\"errors\"\\s*:\\s*\\[\\]"}
  - {shell: "bash", cmd: "npm run build", expected_exit: 0}
  - {shell: "bash", cmd: "npm run typecheck", expected_exit: 0}
  - {shell: "bash", cmd: "npm run lint", expected_exit: 0}
  - {shell: "bash", cmd: "npm test", expected_exit: 0}
  - {shell: "bash", cmd: "npm run test:integration", expected_exit: 0}
  - {shell: "bash", cmd: "npm run release:acceptance", expected_exit: 0}
  - {shell: "bash", cmd: "npm run release:check", expected_exit: 0}
  - {shell: "bash", cmd: "npm run perf:srs", expected_exit: 0}
  - {shell: "bash", cmd: "node bin/speckiwi validate --json", expected_exit: 0, stdout_regex: "\"errors\"\\s*:\\s*\\[\\]"}
```

#### DoD

- All listed commands exit 0.
- Any failure is routed to its owning Phase before evidence work.

#### rollback

```yaml
rollback: {strategy: "none", command: "Verification task only. No file rollback is associated with this TASK."}
```

### TASK-P9-002 — Update SRS evidence, statuses, summaries, and Completed Work Log

| Field | Value |
|---|---|
| 관련 SRS 참조 | all v1.2.0 requirements |
| 파일 경로 | 수정: `docs/spec/00.index.md`, `docs/spec/20.parser-validation.srs.md`, `docs/spec/30.cli-interface.srs.md`, `docs/spec/40.mcp-stdio-interface.srs.md`, `docs/spec/50.nodejs-implementation.srs.md`, `docs/spec/60.workflow-release.srs.md`, `docs/spec/90.appendix.md`, `docs/rule/SRS-MD-Rules-v1.0.0.md` |
| 메서드/함수 시그니처 | N/A - SRS evidence task |
| 참고 패턴 | `docs/spec/00.index.md:57-89` summary rollups and Completed Work Log structure. |
| source_anchors | `["docs/spec/00.index.md:57-89", "docs/spec/20.parser-validation.srs.md:622-857", "docs/spec/30.cli-interface.srs.md:1284-1400", "docs/spec/40.mcp-stdio-interface.srs.md:1016-1132", "docs/spec/50.nodejs-implementation.srs.md:944-1180", "docs/spec/60.workflow-release.srs.md:406-589", "docs/spec/90.appendix.md:443-452"]` |
| 예상 소요 | 5시간 |

#### 구현 가이드

1. For each v1.2.0 requirement, add verification evidence rows referencing only commands that passed in TASK-P9-001.
2. Check all AC items covered by evidence `all` or explicit AC references.
3. Move implemented requirements to `verified` only after evidence is present.
4. Update Status Summary and Requirement Type Summary in `00.index.md`.
5. Change Target Map v1.2.0 from `active` to `released` only after release readiness passes.
6. Add one Completed Work Log row dated with the actual completion date and one-line summary.
7. Run validation and release acceptance after SRS edits.

#### Rationale

SpecKiwi's SRS is the source of truth. The implementation is not complete until SRS status and evidence reflect the verified behavior.

#### 함정 / 주의사항

- Completed Work Log is a summary, not the completion source of truth.
- Evidence rows must not include pipe characters.
- Do not mark v1.2.0 released until release readiness says ready.

#### 테스트 작성 지침

- 성공: `node bin/speckiwi validate --json` returns zero errors after SRS edits.
- 성공: `npm run release:acceptance` passes after evidence updates.
- 경계: Completed Work Log row has ISO date, target `v1.2.0`, and a one-line summary without pipe characters.

#### acceptance_tests

```yaml
acceptance_tests:
  - {shell: "pwsh", cmd: "node bin/speckiwi validate --json", expected_exit: 0, stdout_regex: "\"errors\"\\s*:\\s*\\[\\]"}
  - {shell: "pwsh", cmd: "npm run release:acceptance", expected_exit: 0}
  - {shell: "bash", cmd: "node bin/speckiwi validate --json", expected_exit: 0, stdout_regex: "\"errors\"\\s*:\\s*\\[\\]"}
  - {shell: "bash", cmd: "npm run release:acceptance", expected_exit: 0}
```

#### DoD

- All v1.2.0 requirements have passing evidence.
- `00.index.md` summaries match parsed records.
- Completed Work Log contains one v1.2.0 row after completion.
- Validation and release acceptance pass after SRS updates.

#### rollback

```yaml
rollback: {strategy: "manual", command: "1) Remove only evidence, status, summary, and Completed Work Log edits made by this TASK. 2) Re-run validate and release acceptance to confirm the rollback state."}
```
