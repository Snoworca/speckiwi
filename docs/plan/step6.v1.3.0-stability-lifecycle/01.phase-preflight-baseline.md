# Phase P0 — Preflight Baseline

## Goal

Repair the current release acceptance baseline mismatch before implementing v1.3.0 changes.

## TASK-P0-001 — README baseline tag acceptance repair

| Field | Value |
|---|---|
| Related REQ-ID | `REL-FLOW-001` |
| Files | `README.md`, `test/release/release-readiness.test.ts` |
| Source Anchors | `README.md:403-407`, `README.md:851-855`, `test/release/release-readiness.test.ts:66-72` |
| Feasibility | Low |
| Idempotent | true |

### Signature

No production signature change. Test assertion and documentation text only.

### Reference Pattern

`test/release/release-readiness.test.ts:66-72` already asserts release workflow documentation content. Keep this test as the documentation contract and align the README example with it, or intentionally update the assertion and README together if the baseline example should track the latest target.

### Implementation Guide

1. Decide the contract inside the task: the README should include a stable v1.0.0 baseline example because the test names the baseline workflow, not the current release target.
2. Update the README release baseline section so it contains `git tag srs-v1.0.0-baseline`.
3. Keep the Korean README section aligned with the English section.
4. Run the release readiness documentation test.

### Rationale

The planner observed `npm run release:acceptance` failing before v1.3.0 implementation. Leaving that failure in place would make later release gates noisy.

### Pitfalls

- Do not treat this as proof of v1.3.0 behavior.
- Do not add a git tag. The README example is documentation only.

### Test Guide

Test file: `test/release/release-readiness.test.ts`

Scenarios:

- Success: README contains the expected baseline tag example.
- Failure: README omits the baseline tag string.
- Boundary: release readiness still does not create git tags.

### Acceptance Tests

```yaml
acceptance_tests:
  - shell: "pwsh"
    cmd: "npx vitest run test/release/release-readiness.test.ts --hookTimeout 60000 --testTimeout 60000 --no-file-parallelism"
    expected_exit: 0
  - shell: "bash"
    cmd: "npx vitest run test/release/release-readiness.test.ts --hookTimeout 60000 --testTimeout 60000 --no-file-parallelism"
    expected_exit: 0
```

### DoD

- `npm run release:acceptance` no longer fails on the baseline documentation assertion.
- README English and Korean release sections contain equivalent baseline guidance.

### Rollback

```yaml
rollback:
  strategy: "manual"
  command: "1) Restore README.md and test/release/release-readiness.test.ts lines changed by this task. 2) Rerun the release readiness test to confirm the previous baseline state."
```
