# Phase 3: Patch, Mutations, Bootstrap

| Field | Value |
|---|---|
| Phase | P3 |
| Depends On | P2 |
| Primary Goal | implement minimal line patch engine and all shared write services used by CLI and MCP |
| Related SRS | `REL-ARCH-001`, `FR-NODE-002`, `FR-NODE-008`, `FR-NODE-009`, `IR-CLI-003`, `IR-CLI-010` to `IR-CLI-014`, `IR-CLI-019`, `FR-MCP-004` to `FR-MCP-007`, `FR-MCP-010`, `FR-MCP-011`, `FR-FLOW-002` |

## TASK-P3-001: Line-Based Patch Engine

- 목표: Markdown file edits use line-based patch primitives with dry-run, stale guard, newline preservation, and atomic write.
- 관련 SRS 요구사항 ID: `REL-ARCH-001`, `FR-NODE-002`, `REL-NODE-001`, `SEC-NODE-001`.
- 선행 조건: P2 parser provides line ranges.
- 파일 경로: add `src/core/patch/patch-plan.ts`, `src/core/patch/apply-patch.ts`, `src/core/patch/hunk-summary.ts`, `test/core/patch/line-patch.test.ts`.
- 메서드/함수 시그니처: `createPatchPlan(file: TextFile, operations: PatchOperation[]): PatchPlan`; `applyPatchPlan(plan: PatchPlan, options: { dryRun: boolean }): Promise<PatchResult>`; `summarizePatch(plan: PatchPlan): PatchSummary`.
- 참고 패턴: research patch strategy.
- source_anchors: `["docs/research/00.implementation-research-synthesis.md:162-176", "docs/spec/50.nodejs-implementation.srs.md:215-264"]`.
- 구현 가이드:
  1. Red tests cover replaceLine, insertLines, appendLines, replaceRange, dry-run, stale original line mismatch, CRLF preservation, and no-write-on-failure.
  2. Green implements pure patch planning first.
  3. Green adds filesystem apply through temp file and rename.
  4. Refactor operation validation before disk write.
- Rationale: all mutations rely on minimal and auditable Markdown changes.
- 함정 / 주의사항: patch engine must not know requirement semantics.
- 테스트 작성 지침: exact diff assertions for success and unchanged file assertions for failures.
- Red 단계: write `test/core/patch/line-patch.test.ts`; confirm failures.
- Green 단계: implement patch primitives and apply function.
- Refactor 단계: isolate filesystem apply from pure patch calculation.
- 검증 명령: 아래 `acceptance_tests`를 실행한다.
- acceptance_tests:
```json
[
  {"shell":"pwsh","cmd":"npm run test -- test/core/patch/line-patch.test.ts","expected_exit":0},
  {"shell":"bash","cmd":"npm run test -- test/core/patch/line-patch.test.ts","expected_exit":0}
]
```
- 완료 기준: exact diff tests pass and stale failure leaves bytes unchanged.
- rollback: `{strategy: "manual", command: "1. git diff -- src/core/patch test/core/patch/line-patch.test.ts 를 확인한다. 2. apply_patch로 이 TASK hunks를 제거한다."}`.

## TASK-P3-002: Status and AC Mutation Services

- 목표: update status, check AC, uncheck AC services implement status transition and verified guard.
- 관련 SRS 요구사항 ID: `IR-CLI-010`, `IR-CLI-011`, `IR-CLI-012`, `FR-MCP-004`, `FR-MCP-005`, `FR-PARSE-005`, `FR-PARSE-006`.
- 선행 조건: `TASK-P3-001` completed.
- 파일 경로: add `src/core/mutation/guards.ts`, `src/core/mutation/update-status.ts`, `src/core/mutation/check-ac.ts`, `test/core/mutation/status-ac.test.ts`.
- 메서드/함수 시그니처: `updateStatus(root: ProjectRoot, input: UpdateStatusInput): Promise<MutationResult>`; `setAcceptanceCriteriaChecked(root: ProjectRoot, input: SetAcCheckedInput): Promise<MutationResult>`; `assertVerifiedGuard(record: RequirementRecord): GuardResult`.
- 참고 패턴: verified status rules.
- source_anchors: `["docs/rule/SRS-MD-Rules-v1.0.0.md:640-690", "docs/spec/30.cli-interface.srs.md:555-720"]`.
- 구현 가이드:
  1. Red tests cover valid status update, invalid status, verified without all checked AC, verified without evidence, check AC, uncheck AC downgrade warning, all-AC selector behavior, and dry-run.
  2. Green locates metadata status row and AC lines using parser ranges.
  3. Green applies shared verified guard before write.
  4. Refactor shared mutation result builder.
- Rationale: status trust is core to SRS workflow.
- 함정 / 주의사항: AC deletion is never used to satisfy verified guard.
- 테스트 작성 지침: success, denied transition, unknown AC, all-AC selector for check and uncheck, dry-run, and no-write-on-failure cases.
- Red 단계: write `test/core/mutation/status-ac.test.ts`; confirm failures.
- Green 단계: implement services and guards.
- Refactor 단계: keep CLI/MCP-specific wording outside core.
- 검증 명령: 아래 `acceptance_tests`를 실행한다.
- acceptance_tests:
```json
[
  {"shell":"pwsh","cmd":"npm run test -- test/core/mutation/status-ac.test.ts","expected_exit":0},
  {"shell":"bash","cmd":"npm run test -- test/core/mutation/status-ac.test.ts","expected_exit":0}
]
```
- 완료 기준: verified guard is common and mutation denial leaves files unchanged.
- rollback: `{strategy: "manual", command: "1. git diff -- src/core/mutation test/core/mutation/status-ac.test.ts 를 확인한다. 2. apply_patch로 이 TASK hunks를 제거한다."}`.

## TASK-P3-003: Evidence and Trace Mutation Services

- 목표: add evidence and add trace services append rows in fixed tables and support implementation workflow evidence.
- 관련 SRS 요구사항 ID: `IR-CLI-013`, `IR-CLI-014`, `FR-MCP-006`, `FR-MCP-007`, `FR-FLOW-002`, `FR-PARSE-006`, `FR-PARSE-007`.
- 선행 조건: `TASK-P3-002` completed.
- 파일 경로: add `src/core/mutation/add-evidence.ts`, `src/core/mutation/add-trace.ts`, `test/core/mutation/evidence-trace.test.ts`.
- 메서드/함수 시그니처: `addVerificationEvidence(root: ProjectRoot, input: AddEvidenceInput): Promise<MutationResult>`; `addTraceLink(root: ProjectRoot, input: AddTraceInput): Promise<MutationResult>`.
- 참고 패턴: evidence and trace rules.
- source_anchors: `["docs/rule/SRS-MD-Rules-v1.0.0.md:941-1043", "docs/spec/30.cli-interface.srs.md:722-831"]`.
- 구현 가이드:
  1. Red tests append evidence with generated `VE-N` and trace with table columns.
  2. Red tests reject empty evidence reference when status would become verified.
  3. Green appends rows before placeholder row removal only when placeholder is empty.
  4. Green validates trace target requirement IDs against workspace records.
  5. Refactor table row rendering as a shared helper.
- Rationale: evidence and trace rows must be machine-readable and minimally changed.
- 함정 / 주의사항: never invent evidence references.
- 테스트 작성 지침: success, unknown requirement, broken trace target, dry-run, and no-write-on-failure.
- Red 단계: write `test/core/mutation/evidence-trace.test.ts`; confirm failures.
- Green 단계: implement evidence and trace services.
- Refactor 단계: share table insertion with add-requirement renderer where it reduces duplication.
- 검증 명령: 아래 `acceptance_tests`를 실행한다.
- acceptance_tests:
```json
[
  {"shell":"pwsh","cmd":"npm run test -- test/core/mutation/evidence-trace.test.ts","expected_exit":0},
  {"shell":"bash","cmd":"npm run test -- test/core/mutation/evidence-trace.test.ts","expected_exit":0}
]
```
- 완료 기준: new rows are deterministic and trace targets are validated before write.
- rollback: `{strategy: "manual", command: "1. git diff -- src/core/mutation test/core/mutation/evidence-trace.test.ts 를 확인한다. 2. apply_patch로 이 TASK hunks를 제거한다."}`.

## TASK-P3-004: Add Requirement Core Service

- 목표: shared service creates a complete Requirement Block with automatic ID generation and appends it to `## 4. Requirements`.
- 관련 SRS 요구사항 ID: `FR-NODE-008`, `IR-CLI-019`, `FR-MCP-010`, `CON-ARCH-001`, `REL-ARCH-001`.
- 선행 조건: `TASK-P3-003` completed.
- 파일 경로: add `src/core/mutation/add-requirement.ts`, `src/core/mutation/render-requirement.ts`, `test/core/mutation/add-requirement.test.ts`.
- 메서드/함수 시그니처: `addRequirement(root: ProjectRoot, input: AddRequirementInput): Promise<MutationResult<AddRequirementOutput>>`; `renderRequirementBlock(input: RenderRequirementInput): string[]`; `generateNextRequirementId(workspace: ParsedWorkspace, type: RequirementType, scopePrefix: string): string`.
- 참고 패턴: add-requirement research section and appendix command reference.
- source_anchors: `["docs/research/00.implementation-research-synthesis.md:178-224", "docs/spec/90.appendix.md:101-134"]`.
- 구현 가이드:
  1. Red tests require `type`, `scope`, `target`, title, statement, and AC inputs.
  2. Red tests reject unknown `scope` values not present in Scope Map and unknown `target` values not present in Target Map.
  3. Red tests preserve the full optional input set in the rendered block: `status`, `priority`, `tags`, `risk`, `stability`, `verificationMethod`, `githubIssue`, `relatedDocs`, `rationale`, `implementationNotes`, `research`, `changeNotes`, `evidence`, `trace`, repeated acceptance criteria, and checked acceptance criteria.
  4. Red tests verify discarded IDs are never reused.
  5. Red tests verify generated block contains metadata table and every fixed section.
  6. Red tests verify `status = verified` is denied unless all AC are checked and non-empty evidence exists.
  7. Green renders complete block and inserts before the next H2 after `## 4. Requirements`.
  8. Refactor ID generation and block rendering into pure helpers.
- Rationale: CLI and MCP add-requirement features must share one domain service.
- 함정 / 주의사항: user-facing "append to end" means end of the scope document Requirements section, not the entire file.
- 테스트 작성 지침: success, missing required input, unknown Scope Map prefix, unknown Target Map target, full optional input preservation, repeated AC, checked AC, status default, verified guard denial, ID collision, discarded ID, CRLF, and dry-run.
- Red 단계: write `test/core/mutation/add-requirement.test.ts`; confirm failures.
- Green 단계: implement add requirement service and renderer.
- Refactor 단계: reuse metadata and section constants from parser/validator.
- 검증 명령: 아래 `acceptance_tests`를 실행한다.
- acceptance_tests:
```json
[
  {"shell":"pwsh","cmd":"npm run test -- test/core/mutation/add-requirement.test.ts","expected_exit":0},
  {"shell":"bash","cmd":"npm run test -- test/core/mutation/add-requirement.test.ts","expected_exit":0}
]
```
- 완료 기준: new requirement insertion is minimal, validated, dry-runnable, and shared by adapter layers.
- rollback: `{strategy: "manual", command: "1. git diff -- src/core/mutation/add-requirement.ts src/core/mutation/render-requirement.ts test/core/mutation/add-requirement.test.ts 를 확인한다. 2. apply_patch로 이 TASK hunks를 제거한다."}`.

## TASK-P3-005: Project Init Bootstrap Service

- 목표: shared init service creates `docs/`, `docs/spec/`, `docs/rule/`, empty index and appendix templates, the bundled canonical `SRS-MD-Rules-v1.0.0.md`, and optional uppercase agent files with duplicate-skip behavior.
- 관련 SRS 요구사항 ID: `FR-NODE-009`, `IR-CLI-003`, `FR-MCP-011`, `FR-FLOW-005`, `SEC-NODE-001`, `REL-NODE-001`.
- 선행 조건: `TASK-P3-004` completed.
- 파일 경로: add `src/core/bootstrap/init-project.ts`, `src/core/bootstrap/templates.ts`, `test/core/bootstrap/init-project.test.ts`.
- 메서드/함수 시그니처: `initProject(root: ProjectRoot, input: InitProjectInput): Promise<MutationResult<InitProjectOutput>>`; `renderIndexTemplate(input: InitTemplateInput): string`; `loadBundledRulesDocument(): string`; `renderAgentInstructionSnippet(options: AgentInstructionOptions): string`; `upsertAgentInstruction(root: string, agentFile: AgentFileMode): Promise<AgentInstructionResult>`.
- 참고 패턴: init SRS requirements and rules agent snippet.
- source_anchors: `["docs/spec/30.cli-interface.srs.md:155-219", "docs/spec/40.mcp-stdio-interface.srs.md:836-896", "docs/rule/SRS-MD-Rules-v1.0.0.md:1766-1788"]`.
- 구현 가이드:
  1. Red tests call init on an empty temp repo and assert directories and files are created.
  2. Red tests assert `docs/rule/SRS-MD-Rules-v1.0.0.md` is the bundled canonical rules document, including canonical title, canonical location, and required agent instruction section.
  3. Red tests call init twice and assert second run reports skipped files without duplicate agent text.
  4. Red tests check uppercase `AGENTS.md` and `CLAUDE.md` modes and verify the generated snippet links to `docs/rule/SRS-MD-Rules-v1.0.0.md`.
  5. Green renders empty index/appendix templates, copies the bundled canonical rules document, and duplicate-detects the rules link.
  6. Refactor file creation through safe path utilities.
- Rationale: CLI and MCP init must share the same bootstrap policy.
- 함정 / 주의사항: init can operate before `docs/spec/00.index.md` exists, but other non-init tools can return `not_initialized`.
- 테스트 작성 지침: empty root, canonical rules copy, existing files, force overwrite, agentFile modes, agent snippet link, duplicate-skip, path boundary denial, and internal result preview without adding a public `init --dry-run` contract.
- Red 단계: write `test/core/bootstrap/init-project.test.ts`; confirm failures.
- Green 단계: implement bootstrap templates and file writes.
- Refactor 단계: keep templates pure and filesystem effects in one service.
- 검증 명령: 아래 `acceptance_tests`를 실행한다.
- acceptance_tests:
```json
[
  {"shell":"pwsh","cmd":"npm run test -- test/core/bootstrap/init-project.test.ts","expected_exit":0},
  {"shell":"bash","cmd":"npm run test -- test/core/bootstrap/init-project.test.ts","expected_exit":0}
]
```
- 완료 기준: init creates required structure, writes the bundled canonical rules document, skips duplicate agent instructions, and reports created/skipped/updated paths.
- rollback: `{strategy: "manual", command: "1. git diff -- src/core/bootstrap test/core/bootstrap/init-project.test.ts 를 확인한다. 2. apply_patch로 이 TASK hunks를 제거한다."}`.
