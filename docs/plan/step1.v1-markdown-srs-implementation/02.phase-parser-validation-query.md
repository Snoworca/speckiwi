# Phase 2: Parser, Validation, Query

| Field | Value |
|---|---|
| Phase | P2 |
| Depends On | P1 |
| Primary Goal | implement Markdown SRS discovery, Requirement Block parsing, validator diagnostics, normalized records, and read-only query service |
| Related SRS | `FR-PARSE-001` to `FR-PARSE-010`, `REL-PARSE-001`, `PERF-PARSE-001`, `FR-ARCH-001`, `FR-ARCH-004`, `FR-NODE-001`, `FR-NODE-007` |

## TASK-P2-001: SRS Discovery and Index Parser

- 목표: `docs/spec/00.index.md`와 `docs/spec/**/*.srs.md` discovery, Target Map, Scope Map, SRS Documents table parser를 구현한다.
- 관련 SRS 요구사항 ID: `FR-PARSE-001`, `FR-PARSE-010`, `FR-ARCH-002`.
- 선행 조건: P1 fixtures and safe path utilities completed.
- 파일 경로: add `src/core/parser/discover.ts`, `src/core/parser/index-parser.ts`, `src/core/parser/table.ts`, `test/core/parser/discovery-index.test.ts`.
- 메서드/함수 시그니처: `discoverSrsFiles(root: ProjectRoot): Promise<SrsFileSet>`; `parseIndexDocument(file: TextFile): IndexDocument`; `parseMarkdownTable(lines: string[], startLine: number): ParsedTable`.
- 참고 패턴: current index document and rules index section.
- source_anchors: `["docs/spec/00.index.md:30-59", "docs/rule/SRS-MD-Rules-v1.0.0.md:235-322"]`.
- 구현 가이드:
  1. Red tests load valid fixture and expect index, scope files, target map, and scope map.
  2. Red tests assert `docs/rule/SRS-MD-Rules-v1.0.0.md` is not treated as a requirement file.
  3. Green implements deterministic path sorting and table parser.
  4. Green emits diagnostics for missing index or malformed target/scope rows.
  5. Refactor table parsing into a reusable parser with line ranges.
- Rationale: all later parsing and validation depend on target/scope metadata.
- 함정 / 주의사항: discovery must ignore appendix and rules files as requirement records.
- 테스트 작성 지침: success, missing index, malformed table, and rule-file exclusion cases.
- Red 단계: create `test/core/parser/discovery-index.test.ts`; confirm parser imports fail.
- Green 단계: implement discovery and index table parsing.
- Refactor 단계: expose line range data for validator diagnostics.
- 검증 명령: 아래 `acceptance_tests`를 실행한다.
- acceptance_tests:
```json
[
  {"shell":"pwsh","cmd":"npm run test -- test/core/parser/discovery-index.test.ts","expected_exit":0},
  {"shell":"bash","cmd":"npm run test -- test/core/parser/discovery-index.test.ts","expected_exit":0}
]
```
- 완료 기준: file discovery and maps are deterministic and diagnostics include file path plus line.
- rollback: `{strategy: "manual", command: "1. git diff -- src/core/parser test/core/parser/discovery-index.test.ts 를 확인한다. 2. apply_patch로 이 TASK hunks만 제거한다."}`.

## TASK-P2-002: Requirement Block Heading and Metadata Parser

- 목표: `### {RequirementID} — {Title}` heading과 metadata table을 line range 보존 방식으로 파싱한다.
- 관련 SRS 요구사항 ID: `FR-PARSE-002`, `FR-PARSE-003`, `FR-PARSE-008`.
- 선행 조건: `TASK-P2-001` completed.
- 파일 경로: add `src/core/parser/block-scanner.ts`, `src/core/parser/metadata-table.ts`, `test/core/parser/block-metadata.test.ts`.
- 메서드/함수 시그니처: `scanRequirementBlocks(file: TextFile): RequirementBlockRange[]`; `parseRequirementHeading(line: string): RequirementHeading | Diagnostic`; `parseMetadataTable(block: RequirementBlockRange, lines: string[]): RequirementMetadata`.
- 참고 패턴: SRS-MD heading and metadata rules.
- source_anchors: `["docs/rule/SRS-MD-Rules-v1.0.0.md:457-610", "docs/spec/10.product-architecture.srs.md:42-83"]`.
- 구현 가이드:
  1. Red tests parse valid heading, malformed heading, duplicate headings, and title with Korean text.
  2. Red tests parse required metadata fields and missing fields as diagnostics.
  3. Green implements heading regex and block boundary detection.
  4. Green stores line numbers for heading, metadata rows, and full block.
  5. Refactor metadata enum coercion through shared constants from P1.
- Rationale: mutation targets metadata rows, so precise line locations are mandatory.
- 함정 / 주의사항: block body cannot contain nested `###`; a later `###` starts the next block.
- 테스트 작성 지침: success, malformed, boundary, and duplicate ID fixtures.
- Red 단계: write `test/core/parser/block-metadata.test.ts`; confirm failures before parser exists.
- Green 단계: implement scanner and metadata parser.
- Refactor 단계: remove duplicate regexes from tests and production by exporting a named constant.
- 검증 명령: 아래 `acceptance_tests`를 실행한다.
- acceptance_tests:
```json
[
  {"shell":"pwsh","cmd":"npm run test -- test/core/parser/block-metadata.test.ts","expected_exit":0},
  {"shell":"bash","cmd":"npm run test -- test/core/parser/block-metadata.test.ts","expected_exit":0}
]
```
- 완료 기준: valid metadata becomes typed data and invalid metadata produces diagnostics without stopping other blocks.
- rollback: `{strategy: "manual", command: "1. git diff -- src/core/parser/block-scanner.ts src/core/parser/metadata-table.ts test/core/parser/block-metadata.test.ts 를 확인한다. 2. apply_patch로 이 TASK hunks를 제거한다."}`.

## TASK-P2-003: Fixed Sections, AC, Evidence, and Trace Parser

- 목표: fixed `####` sections, AC task list, Verification Evidence table, Trace Links table을 파싱한다.
- 관련 SRS 요구사항 ID: `FR-PARSE-004`, `FR-PARSE-005`, `FR-PARSE-006`, `FR-PARSE-007`.
- 선행 조건: `TASK-P2-002` completed.
- 파일 경로: add `src/core/parser/section-parser.ts`, `src/core/parser/ac-parser.ts`, `src/core/parser/evidence-parser.ts`, `src/core/parser/trace-parser.ts`, `test/core/parser/sections-ac-evidence-trace.test.ts`.
- 메서드/함수 시그니처: `parseRequirementSections(block: RequirementBlockRange, lines: string[]): RequirementSections`; `parseAcceptanceCriteria(section: SectionRange): AcceptanceCriterion[]`; `parseEvidenceTable(section: SectionRange): EvidenceRow[]`; `parseTraceLinksTable(section: SectionRange): TraceLink[]`.
- 참고 패턴: rules sections, AC, evidence, trace tables.
- source_anchors: `["docs/rule/SRS-MD-Rules-v1.0.0.md:774-1043", "docs/spec/20.parser-validation.srs.md:213-431"]`.
- 구현 가이드:
  1. Red tests assert required section names and order are recognized.
  2. Red tests parse checked and unchecked AC items into IDs and boolean state.
  3. Red tests parse evidence and trace tables with empty placeholder rows.
  4. Green implements section splitter and table readers.
  5. Refactor parsers into pure functions that accept line slices.
- Rationale: validation, status guards, evidence mutation, and trace mutation depend on these structured pieces.
- 함정 / 주의사항: empty evidence placeholder row must not count as verification evidence.
- 테스트 작성 지침: success, missing section, duplicate section, malformed AC, empty evidence, and malformed trace cases.
- Red 단계: write `test/core/parser/sections-ac-evidence-trace.test.ts`; confirm failures.
- Green 단계: implement section, AC, evidence, trace parsers.
- Refactor 단계: share markdown table parser from `TASK-P2-001`.
- 검증 명령: 아래 `acceptance_tests`를 실행한다.
- acceptance_tests:
```json
[
  {"shell":"pwsh","cmd":"npm run test -- test/core/parser/sections-ac-evidence-trace.test.ts","expected_exit":0},
  {"shell":"bash","cmd":"npm run test -- test/core/parser/sections-ac-evidence-trace.test.ts","expected_exit":0}
]
```
- 완료 기준: section and substructure parsing is complete and malformed parts return diagnostics with preserved records.
- rollback: `{strategy: "manual", command: "1. git diff -- src/core/parser test/core/parser/sections-ac-evidence-trace.test.ts 를 확인한다. 2. apply_patch로 이 TASK hunks를 제거한다."}`.

## TASK-P2-004: Workspace Parser and Normalized Records

- 목표: discovered files into `RequirementRecord[]` and workspace diagnostics with partial parsing behavior.
- 관련 SRS 요구사항 ID: `FR-PARSE-008`, `REL-PARSE-001`, `FR-NODE-007`, `FR-ARCH-004`.
- 선행 조건: `TASK-P2-001` to `TASK-P2-003` completed.
- 파일 경로: add `src/core/parser/workspace-parser.ts`, `src/core/query/records.ts`, `test/core/parser/workspace-records.test.ts`.
- 메서드/함수 시그니처: `parseWorkspace(root: ProjectRoot): Promise<ParsedWorkspace>`; `toRequirementRecord(parsed: ParsedRequirement): RequirementRecord`; `getRequirementMarkdown(workspace: ParsedWorkspace, id: string): string | undefined`.
- 참고 패턴: research synthesis normalized record guidance.
- source_anchors: `["docs/research/00.implementation-research-synthesis.md:112-160", "docs/spec/20.parser-validation.srs.md:434-487"]`.
- 구현 가이드:
  1. Red tests parse valid fixture into normalized records.
  2. Red tests parse invalid fixture and still return valid neighboring records.
  3. Green composes discovery and parsers into a workspace parse service.
  4. Green stores markdown block text only when requested by query layer.
  5. Refactor record normalization to a pure conversion function.
- Rationale: adapters should consume a stable read model rather than parser internals.
- 함정 / 주의사항: JSON records must not contain circular references or raw file handles.
- 테스트 작성 지침: success, partial failure, includeMarkdown, and JSON serialization cases.
- Red 단계: write `test/core/parser/workspace-records.test.ts`; confirm failures.
- Green 단계: implement workspace parser and record conversion.
- Refactor 단계: keep diagnostic severity ordering deterministic.
- 검증 명령: 아래 `acceptance_tests`를 실행한다.
- acceptance_tests:
```json
[
  {"shell":"pwsh","cmd":"npm run test -- test/core/parser/workspace-records.test.ts","expected_exit":0},
  {"shell":"bash","cmd":"npm run test -- test/core/parser/workspace-records.test.ts","expected_exit":0}
]
```
- 완료 기준: workspace parse returns records and diagnostics together, and records serialize to JSON.
- rollback: `{strategy: "manual", command: "1. git diff -- src/core/parser/workspace-parser.ts src/core/query/records.ts test/core/parser/workspace-records.test.ts 를 확인한다. 2. apply_patch로 이 TASK hunks를 제거한다."}`.

## TASK-P2-005: Validation Rule Registry

- 목표: SRS-MD validation rules를 registry로 구현하고 verified guard, target map, scope map, trace reference를 검증한다.
- 관련 SRS 요구사항 ID: `FR-PARSE-009`, `FR-PARSE-010`, `FR-PARSE-005`, `FR-PARSE-006`.
- 선행 조건: `TASK-P2-004` completed.
- 파일 경로: add `src/core/validator/rule-registry.ts`, `src/core/validator/rules.ts`, `src/core/validator/validate-workspace.ts`, `test/core/validator/validation-rules.test.ts`.
- 메서드/함수 시그니처: `validateWorkspace(workspace: ParsedWorkspace): ValidationResult`; `registerValidationRule(rule: ValidationRule): void`; `isVerifiedRequirementValid(record: RequirementRecord): boolean`.
- 참고 패턴: rules validate-spec diagnostics and research validator summary.
- source_anchors: `["docs/rule/SRS-MD-Rules-v1.0.0.md:1392-1426", "docs/research/00.implementation-research-synthesis.md:128-160"]`.
- 구현 가이드:
  1. Red tests create fixtures for duplicate ID, missing metadata, enum violation, missing sections, invalid verified state, and broken trace.
  2. Red tests cover Target Map and Scope Map rules: unregistered `Target` warning, ID scope segment not registered in Scope Map error, and Scope Map document reference missing from index warning.
  3. Red tests assert error code and severity are stable.
  4. Green implements a registry with deterministic rule ordering.
  5. Green implements verified guard using parsed AC and evidence data.
  6. Refactor rule helpers by domain group.
- Rationale: validation is both a CLI/MCP feature and a mutation safety gate.
- 함정 / 주의사항: validation errors must not rewrite SRS files.
- 테스트 작성 지침: include one success case, duplicate ID, missing metadata, enum violation, missing sections, verified guard, broken trace, unregistered target, unregistered scope segment, and missing scope document cases.
- Red 단계: write `test/core/validator/validation-rules.test.ts`; confirm failures.
- Green 단계: implement validation registry and rules.
- Refactor 단계: isolate rule code lists from human message text.
- 검증 명령: 아래 `acceptance_tests`를 실행한다.
- acceptance_tests:
```json
[
  {"shell":"pwsh","cmd":"npm run test -- test/core/validator/validation-rules.test.ts","expected_exit":0},
  {"shell":"bash","cmd":"npm run test -- test/core/validator/validation-rules.test.ts","expected_exit":0}
]
```
- 완료 기준: validation diagnostics cover SRS-E style structural errors, verified guard failures, Target Map warnings, Scope Map errors, and missing scope document warnings.
- rollback: `{strategy: "manual", command: "1. git diff -- src/core/validator test/core/validator/validation-rules.test.ts 를 확인한다. 2. apply_patch로 이 TASK hunks를 제거한다."}`.

## TASK-P2-006: Query, Summary, Link Check, and Performance

- 목표: read-only query services for lookup, filters, target/scope summaries, links check, and 500-block performance.
- 관련 SRS 요구사항 ID: `FR-ARCH-003`, `FR-ARCH-004`, `FR-PARSE-008`, `PERF-PARSE-001`, `OBS-FLOW-001`, `FR-FLOW-003`.
- 선행 조건: `TASK-P2-005` completed.
- 파일 경로: add `src/core/query/lookup.ts`, `src/core/query/filter.ts`, `src/core/query/summary.ts`, `src/core/query/links.ts`, `test/core/query/query-summary-links.test.ts`, `test/perf/parser-performance.test.ts`.
- 메서드/함수 시그니처: `listRequirements(workspace: ParsedWorkspace, filter: RequirementFilter): RequirementRecord[]`; `getRequirement(workspace: ParsedWorkspace, id: string, options?: { includeMarkdown?: boolean }): RequirementDetail`; `summarizeTarget(workspace: ParsedWorkspace, target?: string): TargetSummary`; `resolveActiveTarget(workspace: ParsedWorkspace): string`; `checkLinks(workspace: ParsedWorkspace): LinkCheckResult`.
- 참고 패턴: research query strategy and SRS appendix command reference.
- source_anchors: `["docs/research/00.implementation-research-synthesis.md:146-176", "docs/spec/90.appendix.md:30-49"]`.
- 구현 가이드:
  1. Red tests cover filters by target, status, type, scope, and tag.
  2. Red tests cover exact lookup with and without markdown.
  3. Red tests cover target summary counts, implemented-not-verified list, and omitted target defaulting to the active target.
  4. Red tests split link checking into local Markdown path existence, Requirement reference existence, and GitHub URL format-only validation with no network call.
  5. Red perf test builds 500-block fixture, asserts parse plus validation completes under 2 seconds on Node LTS baseline, and writes or prints the measured runtime and environment.
  6. Green implements read-only services without file writes.
  7. Refactor summary count logic into one shared helper.
- Rationale: CLI and MCP read commands must return consistent views.
- 함정 / 주의사항: do not add advanced BM25 search in v1.0.0 because it is outside the current SRS.
- 테스트 작성 지침: success, empty result, unknown target, omitted active target, broken local Markdown path, broken Requirement reference, GitHub URL no-network format check, and performance cases.
- Red 단계: write query and performance tests; confirm failures before services exist.
- Green 단계: implement query services.
- Refactor 단계: share summary builder across CLI and MCP adapters through core only.
- 검증 명령: 아래 `acceptance_tests`를 실행한다.
- acceptance_tests:
```json
[
  {"shell":"pwsh","cmd":"npm run test -- test/core/query/query-summary-links.test.ts","expected_exit":0},
  {"shell":"pwsh","cmd":"npm run test -- test/perf/parser-performance.test.ts","expected_exit":0},
  {"shell":"bash","cmd":"npm run test -- test/core/query/query-summary-links.test.ts","expected_exit":0},
  {"shell":"bash","cmd":"npm run test -- test/perf/parser-performance.test.ts","expected_exit":0}
]
```
- 완료 기준: read-only services are deterministic, never mutate files, omitted target uses the active target, link checks do not call the network for GitHub URLs, and 500-block parse plus validation meets the 2-second target with environment evidence.
- rollback: `{strategy: "manual", command: "1. git diff -- src/core/query test/core/query test/perf 를 확인한다. 2. apply_patch로 이 TASK hunks를 제거한다."}`.
