# Phase 4: CLI Interface

| Field | Value |
|---|---|
| Phase | P4 |
| Depends On | P2, P3 |
| Primary Goal | expose core read and write services through the `speckiwi` CLI with stable options, output, help, and exit codes |
| Related SRS | `IR-CLI-001` to `IR-CLI-016`, `IR-CLI-018`, `IR-CLI-019`, `OPS-CLI-001`, `FR-NODE-006`, `FR-ARCH-003`, `FR-ARCH-004` |

## TASK-P4-001: CLI Framework, Common Options, Output, and Exit Codes

- 목표: Commander-based CLI adapter parses common options, exposes a command registrar extension point, routes stdout/stderr, formats human/JSON output, and maps exit codes.
- 관련 SRS 요구사항 ID: `IR-CLI-001`, `IR-CLI-002`, `IR-CLI-018`, `OPS-CLI-001`, `FR-NODE-006`, `FR-ARCH-004`.
- 선행 조건: P2 and P3 core services available.
- 파일 경로: add `src/cli/command.ts`, `src/cli/options.ts`, `src/cli/exit.ts`, `src/cli/formatters.ts`, `src/core/format/json.ts`, `src/core/format/text.ts`, `test/cli/common-options-exit.test.ts`.
- 메서드/함수 시그니처: `buildCommand(context: CliContext, registrars?: CliCommandRegistrar[]): Command`; `export type CliCommandRegistrar = (command: Command, context: CliContext) => void`; `parseCommonOptions(raw: unknown): CommonOptions`; `mapResultToExitCode(result: CoreResult<unknown>): number`; `formatJsonOutput(value: unknown): string`; `formatHumanOutput(value: CoreDisplayModel): string`; `writeJson(io: CliIo, value: unknown): void`.
- 참고 패턴: appendix command reference and current package bin.
- source_anchors: `["docs/spec/90.appendix.md:11-49", "package.json:6-10"]`.
- 구현 가이드:
  1. Red tests run CLI in-process for `--help`, `--version`, `--root`, `--json`, `--no-color`, and `--quiet`.
  2. Red tests assert `--json` stdout parses as JSON and stderr contains human errors only.
  3. Green implements command builder with a registrar extension point so later MCP CLI wiring does not modify `src/cli/command.ts`.
  4. Green routes JSON/human formatting through `src/core/format`.
  5. Green maps validation, usage, I/O, parse, mutation, and MCP fatal cases to SRS exit codes.
  6. Refactor output functions to accept injected streams for tests.
- Rationale: every CLI command shares options and exit behavior.
- 함정 / 주의사항: no core service may import Commander, and CLI must not duplicate core formatter logic.
- 테스트 작성 지침: success, usage error, JSON purity, command registrar extension injection, and exit code mapping.
- Red 단계: write `test/cli/common-options-exit.test.ts`; confirm failures.
- Green 단계: implement CLI framework adapter.
- Refactor 단계: keep CLI stream writing separate from `src/core/format` pure formatter functions.
- 검증 명령: 아래 `acceptance_tests`를 실행한다.
- acceptance_tests:
```json
[
  {"shell":"pwsh","cmd":"npm run test -- test/cli/common-options-exit.test.ts","expected_exit":0},
  {"shell":"bash","cmd":"npm run test -- test/cli/common-options-exit.test.ts","expected_exit":0}
]
```
- 완료 기준: common options and exit codes match SRS, JSON stdout is pure, and command extension registration works without later ownership changes to `src/cli/command.ts`.
- rollback: `{strategy: "manual", command: "1. git diff -- src/cli test/cli/common-options-exit.test.ts 를 확인한다. 2. apply_patch로 이 TASK hunks를 제거한다."}`.

## TASK-P4-002: Read-Only CLI Commands

- 목표: implement `validate`, `extract`, `list`, `show`, `targets`, `scopes`, `summary`, and `links check`.
- 관련 SRS 요구사항 ID: `IR-CLI-004`, `IR-CLI-005`, `IR-CLI-006`, `IR-CLI-007`, `IR-CLI-008`, `IR-CLI-009`, `IR-CLI-015`, `IR-CLI-016`, `FR-FLOW-003`, `OBS-FLOW-001`.
- 선행 조건: `TASK-P4-001` and P2 query services completed.
- 파일 경로: add `src/cli/commands/read.ts`, `src/cli/commands/links.ts`, `test/cli/read-commands.test.ts`.
- 메서드/함수 시그니처: `registerReadCommands(command: Command, context: CliContext): void`; `runValidate(options: CommonOptions): Promise<CliCommandResult>`; `runList(options: ListOptions): Promise<CliCommandResult>`; `runSummary(options: SummaryOptions): Promise<CliCommandResult>`.
- 참고 패턴: CLI command reference.
- source_anchors: `["docs/spec/90.appendix.md:30-100", "docs/spec/30.cli-interface.srs.md:221-944"]`.
- 구현 가이드:
  1. Red subprocess tests run each command against valid and invalid fixtures.
  2. Red tests assert command-specific option matrix: `validate --fail-on-warning`, `extract --include-markdown`, `list --format table`, `list --format plain`, `list --json`, `show --markdown`, and `summary --markdown`.
  3. Red tests assert human output contains key fields and JSON output matches core DTOs.
  4. Green registers read commands and delegates to core parse, validate, query, and links services.
  5. Green ensures read commands never invoke mutation services.
  6. Refactor shared root loading into `CliContext`.
- Rationale: read-only CLI is the human-visible query surface.
- 함정 / 주의사항: `validate` may return exit code 1 while stdout JSON remains valid when `--json` is passed.
- 테스트 작성 지침: success, empty filter result, invalid SRS, unknown requirement, read command option/output matrix, broken local Markdown path, broken Requirement reference, and GitHub URL format-only no-network checks.
- Red 단계: write `test/cli/read-commands.test.ts`; confirm command failures.
- Green 단계: implement command registrations.
- Refactor 단계: deduplicate output table rendering.
- 검증 명령: 아래 `acceptance_tests`를 실행한다.
- acceptance_tests:
```json
[
  {"shell":"pwsh","cmd":"npm run test -- test/cli/read-commands.test.ts","expected_exit":0},
  {"shell":"bash","cmd":"npm run test -- test/cli/read-commands.test.ts","expected_exit":0}
]
```
- 완료 기준: every read command returns SRS-defined data, command-specific options are covered, and no command mutates fixture bytes.
- rollback: `{strategy: "manual", command: "1. git diff -- src/cli/commands/read.ts src/cli/commands/links.ts test/cli/read-commands.test.ts 를 확인한다. 2. apply_patch로 이 TASK hunks를 제거한다."}`.

## TASK-P4-003: Mutation CLI Commands

- 목표: implement `init`, `update-status`, `check-ac`, `uncheck-ac`, `add-evidence`, `add-trace`, and `add-requirement` through shared core mutations.
- 관련 SRS 요구사항 ID: `IR-CLI-003`, `IR-CLI-010`, `IR-CLI-011`, `IR-CLI-012`, `IR-CLI-013`, `IR-CLI-014`, `IR-CLI-019`, `FR-NODE-008`, `FR-NODE-009`.
- 선행 조건: `TASK-P4-001` and P3 mutation services completed.
- 파일 경로: add `src/cli/commands/mutations.ts`, `test/cli/mutation-commands.test.ts`.
- 메서드/함수 시그니처: `registerMutationCommands(command: Command, context: CliContext): void`; `runAddRequirement(options: AddRequirementCliOptions): Promise<CliCommandResult>`; `runInit(options: InitCliOptions): Promise<CliCommandResult>`.
- 참고 패턴: mutation command reference and core mutation tests.
- source_anchors: `["docs/spec/90.appendix.md:53-134", "docs/spec/30.cli-interface.srs.md:555-1176"]`.
- 구현 가이드:
  1. Red subprocess tests copy mutation fixtures and run each command.
  2. Red tests assert `check-ac --all` and `uncheck-ac --all` map to the core all-AC selector.
  3. Red tests assert `add-requirement` required flag validation and full optional flag mapping: `--status`, `--priority`, `--tags`, `--risk`, `--stability`, `--verification-method`, `--github-issue`, `--related-docs`, `--rationale`, `--implementation-notes`, `--research`, `--change-notes`, `--evidence`, `--trace`, repeated `--ac`, `--checked-ac`, status default, and verified guard.
  4. Red tests assert dry-run returns patch summary without writing.
  5. Red tests assert verified guard failure and adapter-level input validation exit with code 5 or usage code as specified and leave files unchanged.
  6. Green maps CLI flags to core input schemas only.
  7. Refactor mutation result rendering to show created, skipped, updated, and diagnostics consistently.
- Rationale: CLI mutation commands are thin adapters around shared write services.
- 함정 / 주의사항: do not parse Markdown in command handlers.
- 테스트 작성 지침: success, required flag failure, full optional flag mapping, repeated `--ac`, `--checked-ac`, `--all`, dry-run, validation denial, unknown ID, adapter-level no-write-on-failure, and JSON output cases.
- Red 단계: write `test/cli/mutation-commands.test.ts`; confirm failures.
- Green 단계: implement mutation command registrations.
- Refactor 단계: share CLI option parsing helpers by command family.
- 검증 명령: 아래 `acceptance_tests`를 실행한다.
- acceptance_tests:
```json
[
  {"shell":"pwsh","cmd":"npm run test -- test/cli/mutation-commands.test.ts","expected_exit":0},
  {"shell":"bash","cmd":"npm run test -- test/cli/mutation-commands.test.ts","expected_exit":0}
]
```
- 완료 기준: all mutation CLI commands delegate to core and preserve no-write-on-failure.
- rollback: `{strategy: "manual", command: "1. git diff -- src/cli/commands/mutations.ts test/cli/mutation-commands.test.ts 를 확인한다. 2. apply_patch로 이 TASK hunks를 제거한다."}`.
