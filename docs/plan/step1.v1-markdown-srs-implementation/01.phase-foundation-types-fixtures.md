# Phase 1: Foundation, Types, Fixtures

| Field | Value |
|---|---|
| Phase | P1 |
| Depends On | none |
| Primary Goal | create a compilable TypeScript ESM skeleton, shared contracts, safe filesystem primitives, and SRS fixtures |
| Related SRS | `CON-NODE-001`, `CON-NODE-002`, `FR-NODE-001`, `FR-NODE-003`, `FR-NODE-004`, `FR-NODE-007`, `REL-NODE-001`, `SEC-NODE-001`, `OPS-NODE-001`, `FR-ARCH-001`, `FR-ARCH-002`, `CON-ARCH-001`, `CON-ARCH-002` |

## TASK-P1-001: Package Runtime, Bin, and Scripts

- 목표: `package.json`, `tsconfig.json`, `bin/speckiwi`, empty `src/` and `test/` skeleton을 v1.0.0 Node.js TypeScript ESM 기준으로 고정한다.
- 관련 SRS 요구사항 ID: `CON-NODE-001`, `CON-NODE-002`, `OPS-NODE-001`, `IR-CLI-001`.
- 선행 조건: deleted `src/` and `test/` files are not restored; current package files are read.
- 파일 경로: modify `package.json`, `tsconfig.json`; add `bin/speckiwi`, `src/index.ts`, `src/cli/index.ts`, `test/smoke/package.test.ts`.
- 메서드/함수 시그니처: `export function main(argv: string[], io: CliIo): Promise<number>`; `export interface CliIo { stdout: NodeJS.WriteStream; stderr: NodeJS.WriteStream }`.
- 참고 패턴: package metadata example in appendix and current package metadata.
- source_anchors: `["package.json:1-95", "tsconfig.json:1-22", "docs/spec/90.appendix.md:389-438"]`.
- 구현 가이드:
  1. Red test asserts `engines.node` is `>=22`, `type` is `module`, bin points at built CLI output, and scripts exist.
  2. Red test asserts importing `src/index.ts` exposes version-safe public exports without loading CLI or MCP side effects.
  3. Red test asserts `test:integration` runs CLI and MCP integration tests and `perf:srs` runs `test/perf/parser-performance.test.ts`.
  4. Green updates package metadata and creates minimal CLI bootstrap.
  5. Green keeps package metadata free of YAML-era wording and dependencies that are not used by SRS implementation.
  6. Refactor isolates executable startup in `bin/speckiwi` and typed CLI function in `src/cli/index.ts`.
- Rationale: runtime and package contract must be stable before parser, CLI, and MCP tests can execute.
- 함정 / 주의사항: do not resurrect deleted YAML implementation paths or schema package exports.
- 테스트 작성 지침: `test/smoke/package.test.ts` must cover success, stale YAML wording failure, bin path boundary, `test:integration`, and `perf:srs` script routing.
- Red 단계: write `test/smoke/package.test.ts`; run the task acceptance test and confirm it fails on current `>=20` and YAML description.
- Green 단계: update package and skeleton until the smoke test passes.
- Refactor 단계: remove duplicate package-reading helpers from tests by using a small local helper inside the smoke test.
- 검증 명령: 아래 `acceptance_tests`를 실행한다.
- acceptance_tests:
```json
[
  {"shell":"pwsh","cmd":"npm run test -- test/smoke/package.test.ts","expected_exit":0},
  {"shell":"bash","cmd":"npm run test -- test/smoke/package.test.ts","expected_exit":0}
]
```
- 완료 기준: smoke test passes, `npm run build` can resolve `src/cli/index.ts`, package metadata no longer advertises YAML requirements, `test:integration` exists, and `perf:srs` points at the planned parser performance test.
- rollback: `{strategy: "manual", command: "1. git diff -- package.json tsconfig.json bin/speckiwi src/index.ts src/cli/index.ts test/smoke/package.test.ts 로 변경 범위를 확인한다. 2. apply_patch로 이 TASK에서 추가한 hunks만 제거한다."}`.

## TASK-P1-002: Shared Core Types and JSON Contracts

- 목표: parser, validator, query, mutation, CLI, MCP가 공유할 TypeScript DTO와 JSON serializable contracts를 정의한다.
- 관련 SRS 요구사항 ID: `FR-NODE-001`, `FR-NODE-007`, `FR-PARSE-008`, `FR-ARCH-001`, `FR-ARCH-004`, `IR-MCP-002`.
- 선행 조건: `TASK-P1-001` completed.
- 파일 경로: add `src/core/types.ts`, `src/core/result.ts`, `src/core/diagnostic.ts`, `src/core/schema.ts`, `test/core/types/contracts.test.ts`.
- 메서드/함수 시그니처: `export interface RequirementRecord`; `export interface Diagnostic`; `export interface TargetSummary`; `export interface MutationResult`; `export function ok<T>(value: T): Result<T>`; `export function fail(code: string, message: string, diagnostics?: Diagnostic[]): Result<never>`.
- 참고 패턴: no surviving implementation pattern; contract comes from appendix normalized record shape.
- source_anchors: `["docs/spec/90.appendix.md:1-60", "docs/spec/50.nodejs-implementation.srs.md:493-530"]`.
- 구현 가이드:
  1. Red tests serialize and deserialize every DTO with `JSON.stringify` and `JSON.parse`.
  2. Red tests assert all public status, type, priority, risk, and stability enums match appendix and rules.
  3. Green defines type-only modules with no filesystem or CLI imports.
  4. Green exports stable DTOs from `src/index.ts`.
  5. Refactor splits diagnostic and result helpers only after tests pass.
- Rationale: CLI JSON and MCP structured outputs share these contracts, so divergence must be blocked early.
- 함정 / 주의사항: do not import Commander, MCP SDK, or Node streams from `src/core/types.ts`.
- 테스트 작성 지침: test success serialization, enum rejection helper behavior, and type-only import from CLI/MCP-free modules.
- Red 단계: write `test/core/types/contracts.test.ts`; confirm imports fail before modules exist.
- Green 단계: add DTO modules and exports.
- Refactor 단계: keep enum arrays as readonly constants and derive union types from them.
- 검증 명령: 아래 `acceptance_tests`를 실행한다.
- acceptance_tests:
```json
[
  {"shell":"pwsh","cmd":"npm run test -- test/core/types/contracts.test.ts","expected_exit":0},
  {"shell":"bash","cmd":"npm run test -- test/core/types/contracts.test.ts","expected_exit":0}
]
```
- 완료 기준: DTOs are JSON serializable, core type modules have no adapter imports, and enum values match SRS rules.
- rollback: `{strategy: "manual", command: "1. git diff -- src/core src/index.ts test/core/types/contracts.test.ts 를 확인한다. 2. apply_patch로 이 TASK의 files and export hunks를 제거한다."}`.

## TASK-P1-003: Root Resolution, UTF-8 Read, Newline, and Safe Path

- 목표: repository root resolution, UTF-8 read/write preparation, newline detection, path boundary checks를 core filesystem utility로 만든다.
- 관련 SRS 요구사항 ID: `FR-ARCH-002`, `CON-ARCH-002`, `REL-NODE-001`, `SEC-NODE-001`, `SEC-MCP-001`.
- 선행 조건: `TASK-P1-002` completed.
- 파일 경로: add `src/core/fs/read-text.ts`, `src/core/fs/newline.ts`, `src/core/fs/safe-path.ts`, `src/core/project-root.ts`, `test/core/fs/root-path.test.ts`.
- 메서드/함수 시그니처: `resolveProjectRoot(start: string, explicitRoot?: string): Promise<ProjectRoot>`; `readUtf8File(path: string): Promise<TextFile>`; `detectNewline(text: string): "\\n" | "\\r\\n"`; `resolveInsideRoot(root: string, candidate: string): Promise<string>`.
- 참고 패턴: no surviving implementation pattern; use Node `fs/promises` and `path` APIs.
- source_anchors: `["docs/spec/10.product-architecture.srs.md:211-254", "docs/spec/50.nodejs-implementation.srs.md:549-646"]`.
- 구현 가이드:
  1. Red tests create temp repositories with `.git`, with `docs/spec/00.index.md`, and without either marker.
  2. Red tests attempt `..` traversal and symlink escape where platform support exists.
  3. Green implements realpath-based boundary check and UTF-8 reads with explicit encoding.
  4. Green returns structured diagnostics instead of throwing for expected root failures.
  5. Refactor separates pure path normalization from async realpath checks.
- Rationale: every CLI and MCP call depends on safe root detection and file boundary enforcement.
- 함정 / 주의사항: core must not execute shell commands to inspect files.
- 테스트 작성 지침: include success, failure, and boundary tests; fixture temp paths must be created per test.
- Red 단계: write `test/core/fs/root-path.test.ts`; confirm missing modules fail.
- Green 단계: implement filesystem utilities.
- Refactor 단계: keep Windows and POSIX path cases in one table-driven test.
- 검증 명령: 아래 `acceptance_tests`를 실행한다.
- acceptance_tests:
```json
[
  {"shell":"pwsh","cmd":"npm run test -- test/core/fs/root-path.test.ts","expected_exit":0},
  {"shell":"bash","cmd":"npm run test -- test/core/fs/root-path.test.ts","expected_exit":0}
]
```
- 완료 기준: root detection covers explicit root, `.git`, and `docs/spec/00.index.md`; path escape attempts are denied without file mutation.
- rollback: `{strategy: "manual", command: "1. git diff -- src/core/fs src/core/project-root.ts test/core/fs/root-path.test.ts 를 검토한다. 2. apply_patch로 이 TASK hunks를 제거한다."}`.

## TASK-P1-004: SRS Fixture Workspace Set

- 목표: parser, validator, mutation, CLI, MCP 테스트가 공유하는 Markdown-only fixture workspaces를 만든다.
- 관련 SRS 요구사항 ID: `FR-NODE-003`, `FR-NODE-004`, `CON-ARCH-001`, `FR-PARSE-001`, `REL-PARSE-001`.
- 선행 조건: `TASK-P1-003` completed.
- 파일 경로: add `test/fixtures/workspaces/valid-basic/`, `invalid-structure/`, `duplicate-id/`, `missing-metadata/`, `verified-guard-failure/`, `mutation-target/`, `crlf-basic/`, `large-500/fixture-builder.ts`, `test/fixtures/fixture-utils.ts`.
- 메서드/함수 시그니처: `copyFixtureWorkspace(name: string): Promise<string>`; `buildLargeSrsFixture(count: number, targetDir: string): Promise<void>`.
- 참고 패턴: current SRS documents and rules examples define valid Requirement Block shape.
- source_anchors: `["docs/spec/00.index.md:1-115", "docs/rule/SRS-MD-Rules-v1.0.0.md:417-456"]`.
- 구현 가이드:
  1. Red tests assert fixture loader copies files to temp dirs and preserves CRLF bytes.
  2. Red tests assert valid fixture contains `00.index.md` and at least one `*.srs.md`.
  3. Red tests assert `duplicate-id/` and `missing-metadata/` fixtures exist as separate named fixtures, not hidden inside a generic invalid fixture.
  4. Green creates small hand-written fixtures and one builder for 500 generated blocks.
  5. Green keeps fixtures Markdown-only.
  6. Refactor common copy and cleanup helper.
- Rationale: deterministic fixtures are cheaper than using project SRS files for every low-level parser test.
- 함정 / 주의사항: fixtures must not include YAML front matter or `.speckiwi` state.
- 테스트 작성 지침: fixture utility tests cover valid copy, missing fixture error, duplicate ID fixture existence, metadata missing fixture existence, and CRLF preservation.
- Red 단계: write `test/fixtures/fixture-utils.test.ts`; confirm fixture copy fails before files exist.
- Green 단계: add fixture tree and utility.
- Refactor 단계: centralize temp cleanup under Vitest `afterEach`.
- 검증 명령: 아래 `acceptance_tests`를 실행한다.
- acceptance_tests:
```json
[
  {"shell":"pwsh","cmd":"npm run test -- test/fixtures/fixture-utils.test.ts","expected_exit":0},
  {"shell":"bash","cmd":"npm run test -- test/fixtures/fixture-utils.test.ts","expected_exit":0}
]
```
- 완료 기준: valid, invalid structure, duplicate ID, missing metadata, guard failure, mutation, CRLF, and 500-block fixtures are available and copied with UTF-8-safe reads.
- rollback: `{strategy: "manual", command: "1. git diff -- test/fixtures 를 확인한다. 2. apply_patch로 이 TASK에서 추가한 fixture files와 helper exports를 제거한다."}`.
