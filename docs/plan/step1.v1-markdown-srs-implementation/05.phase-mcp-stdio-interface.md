# Phase 5: MCP Stdio Interface

| Field | Value |
|---|---|
| Phase | P5 |
| Depends On | P2, P3, P4 |
| Primary Goal | expose shared core services through a stdio MCP server with tools, resources, metadata, path safety, and structured errors |
| Related SRS | `IR-MCP-001`, `IR-MCP-002`, `FR-MCP-001` to `FR-MCP-011`, `SEC-MCP-001`, `REL-MCP-001`, `FR-NODE-005`, `FR-ARCH-001`, `IR-CLI-017` |

## TASK-P5-001: MCP SDK Adapter, Server, and Metadata

- 목표: implement stdio MCP server startup with official TypeScript SDK isolated inside `src/mcp`.
- 관련 SRS 요구사항 ID: `IR-MCP-001`, `IR-MCP-002`, `FR-NODE-005`.
- 선행 조건: P2 read services and P3 mutation services completed.
- 파일 경로: add `src/mcp/server.ts`, `src/mcp/adapter.ts`, `src/mcp/metadata.ts`, `test/mcp/server-metadata.test.ts`.
- 메서드/함수 시그니처: `startMcpServer(options: McpServerOptions): Promise<void>`; `createMcpServer(deps: McpDependencies): McpServerHandle`; `getServerMetadata(pkg: PackageInfo): McpServerMetadata`.
- 참고 패턴: MCP SRS server and metadata requirements.
- source_anchors: `["docs/spec/40.mcp-stdio-interface.srs.md:47-159", "package.json:79-85"]`.
- 구현 가이드:
  1. Red tests assert metadata name is `speckiwi` and version matches package version.
  2. Red tests assert server advertises tools and resources capability, and read-only tools have read-only annotation or equivalent metadata.
  3. Red tests assert SDK imports are contained under `src/mcp`.
  4. Green creates adapter boundary and server startup.
  5. Refactor dependency injection for testable transport setup.
- Rationale: SDK churn must not leak into core or CLI domains.
- 함정 / 주의사항: no logs on stdout before or during MCP JSON-RPC communication.
- 테스트 작성 지침: metadata, tools/resources capability, read-only tool metadata, SDK import boundary, startup error to stderr, and protocol initialization smoke.
- Red 단계: write `test/mcp/server-metadata.test.ts`; confirm failures.
- Green 단계: implement server and metadata.
- Refactor 단계: isolate package metadata reader from server creation.
- 검증 명령: 아래 `acceptance_tests`를 실행한다.
- acceptance_tests:
```json
[
  {"shell":"pwsh","cmd":"npm run test -- test/mcp/server-metadata.test.ts","expected_exit":0},
  {"shell":"bash","cmd":"npm run test -- test/mcp/server-metadata.test.ts","expected_exit":0}
]
```
- 완료 기준: server metadata and capabilities are stable, read-only tool metadata is exposed, and MCP SDK is adapter-local.
- rollback: `{strategy: "manual", command: "1. git diff -- src/mcp test/mcp/server-metadata.test.ts 를 확인한다. 2. apply_patch로 이 TASK hunks를 제거한다."}`.

## TASK-P5-002: Read-Only MCP Tools and Resources

- 목표: implement `list_requirements`, `get_requirement`, `validate_spec`, `summarize_target`, and resources using core read services.
- 관련 SRS 요구사항 ID: `FR-MCP-001`, `FR-MCP-002`, `FR-MCP-003`, `FR-MCP-008`, `FR-MCP-009`, `FR-ARCH-001`.
- 선행 조건: `TASK-P5-001` and P2 query services completed.
- 파일 경로: add `src/mcp/tools/read-tools.ts`, `src/mcp/resources.ts`, `src/mcp/schemas.ts`, `test/mcp/read-tools-resources.test.ts`.
- 메서드/함수 시그니처: `registerReadTools(server: McpServerHandle, deps: McpDependencies): void`; `registerResources(server: McpServerHandle, deps: McpDependencies): void`; `toMcpToolResult(result: CoreResult<unknown>): McpToolResult`.
- 참고 패턴: MCP tool summary and appendix tool list.
- source_anchors: `["docs/spec/40.mcp-stdio-interface.srs.md:161-660", "docs/spec/90.appendix.md:207-238"]`.
- 구현 가이드:
  1. Red tests call tool handlers directly with fixture roots and verify output shape.
  2. Red tests assert `includeMarkdown` returns raw block only for get requirement.
  3. Red tests assert `validate_spec` accepts `strict` and `failOnWarning` and returns diagnostics with the expected exit-equivalent severity.
  4. Red tests assert `summarize_target` omits target and uses active target.
  5. Red tests assert resources expose exactly the SRS-defined URI families: `speckiwi://index`, `speckiwi://requirements/{id}`, `speckiwi://targets/{target}`, and `speckiwi://scopes/{scope}`.
  6. Green maps tool input schemas to core query calls.
  7. Refactor structured content conversion.
- Rationale: read-only MCP tools must be behaviorally equivalent to CLI read commands.
- 함정 / 주의사항: resource payload may be Markdown or JSON, but it is still a derived view.
- 테스트 작성 지침: success, unknown ID, `validate_spec` strict/failOnWarning schema, active target summary default, index resource, requirement resource, target resource, scope resource, resource not found, and CLI/MCP parity fixtures.
- Red 단계: write `test/mcp/read-tools-resources.test.ts`; confirm failures.
- Green 단계: implement read tools and resources.
- Refactor 단계: share schemas without importing core internals into tool code beyond public DTOs.
- 검증 명령: 아래 `acceptance_tests`를 실행한다.
- acceptance_tests:
```json
[
  {"shell":"pwsh","cmd":"npm run test -- test/mcp/read-tools-resources.test.ts","expected_exit":0},
  {"shell":"bash","cmd":"npm run test -- test/mcp/read-tools-resources.test.ts","expected_exit":0}
]
```
- 완료 기준: every read-only MCP tool delegates to core and resources resolve inside root.
- rollback: `{strategy: "manual", command: "1. git diff -- src/mcp/tools src/mcp/resources.ts src/mcp/schemas.ts test/mcp/read-tools-resources.test.ts 를 확인한다. 2. apply_patch로 이 TASK hunks를 제거한다."}`.

## TASK-P5-003: Mutation MCP Tools, Security, and Structured Errors

- 목표: implement `update_status`, `check_acceptance_criteria`, `add_verification_evidence`, `add_trace_link`, `add_requirement`, `init_project` plus structured errors and root boundary enforcement.
- 관련 SRS 요구사항 ID: `FR-MCP-004`, `FR-MCP-005`, `FR-MCP-006`, `FR-MCP-007`, `FR-MCP-010`, `FR-MCP-011`, `SEC-MCP-001`, `REL-MCP-001`, `FR-NODE-008`, `FR-NODE-009`.
- 선행 조건: P3 mutation services completed and `TASK-P5-001` server boundary exists.
- 파일 경로: add `src/mcp/tools/mutation-tools.ts`, `src/mcp/errors.ts`, `test/mcp/mutation-tools-errors.test.ts`, `test/mcp/stdio-purity.test.ts`.
- 메서드/함수 시그니처: `registerMutationTools(server: McpServerHandle, deps: McpDependencies): void`; `toStructuredError(error: CoreError): McpStructuredError`; `resolveMcpRoot(inputRoot: string | undefined, sessionRoot: string): Promise<ProjectRoot>`.
- 참고 패턴: MCP mutation requirements and security boundary.
- source_anchors: `["docs/spec/40.mcp-stdio-interface.srs.md:327-896", "docs/spec/50.nodejs-implementation.srs.md:716-845"]`.
- 구현 가이드:
  1. Red tests call each mutation tool against temp fixture copies and assert file diff or no-write-on-failure.
  2. Red tests assert `check_acceptance_criteria` accepts `acIds: ["all"]` and `checked: true|false`.
  3. Red tests assert `add_requirement` optional fields, status default, normalized record output, generated ID, and patch summary output.
  4. Red tests assert `init_project` returns created, skipped, updated paths and diagnostics in a structured result.
  5. Red tests assert path escape and not-initialized cases return structured errors.
  6. Red subprocess test starts server and verifies stdout contains only JSON-RPC frames.
  7. Green maps tool input schemas to core mutation services.
  8. Green converts domain failure to tool result errors without crashing the protocol.
  9. Refactor structured error taxonomy.
- Rationale: MCP is the AI-agent mutation path, so it needs the same guards as CLI plus stdio purity.
- 함정 / 주의사항: protocol errors and domain errors must be distinguishable.
- 테스트 작성 지침: success for every mutation, `acIds: ["all"]`, denied verified transition, `add_requirement` optional/default/output schema, `init_project` structured result, init before index, root escape, stdout purity, and stderr logging.
- Red 단계: write `test/mcp/mutation-tools-errors.test.ts` and `test/mcp/stdio-purity.test.ts`; confirm failures.
- Green 단계: implement mutation tools and errors.
- Refactor 단계: share output conversion with read tools while keeping core unaware of MCP.
- 검증 명령: 아래 `acceptance_tests`를 실행한다.
- acceptance_tests:
```json
[
  {"shell":"pwsh","cmd":"npm run test -- test/mcp/mutation-tools-errors.test.ts","expected_exit":0},
  {"shell":"pwsh","cmd":"npm run test -- test/mcp/stdio-purity.test.ts","expected_exit":0},
  {"shell":"bash","cmd":"npm run test -- test/mcp/mutation-tools-errors.test.ts","expected_exit":0},
  {"shell":"bash","cmd":"npm run test -- test/mcp/stdio-purity.test.ts","expected_exit":0}
]
```
- 완료 기준: MCP mutation tools share core services, enforce root boundary, return structured errors, and keep stdout protocol-clean.
- rollback: `{strategy: "manual", command: "1. git diff -- src/mcp test/mcp/mutation-tools-errors.test.ts test/mcp/stdio-purity.test.ts 를 확인한다. 2. apply_patch로 이 TASK hunks를 제거한다."}`.

## TASK-P5-004: `speckiwi mcp` CLI Wiring

- 목표: wire the CLI `speckiwi mcp` command to the completed MCP stdio server while keeping stdout reserved for JSON-RPC messages.
- 관련 SRS 요구사항 ID: `IR-CLI-017`, `IR-MCP-001`, `FR-NODE-005`, `REL-MCP-001`.
- 선행 조건: `TASK-P4-001` command framework and `TASK-P5-001` MCP server completed.
- 파일 경로: add `src/cli/commands/mcp.ts`, add `test/cli/mcp-command.test.ts`. `src/cli/command.ts` is not modified in this task; it uses the registrar extension point created by `TASK-P4-001`.
- 메서드/함수 시그니처: `registerMcpCommand(command: Command, context: CliContext, starter: McpServerStarter): void`; `runMcp(options: McpCliOptions, starter: McpServerStarter): Promise<number>`; `export type McpServerStarter = (options: McpServerOptions) => Promise<void>`.
- 참고 패턴: MCP command reference.
- source_anchors: `["docs/spec/90.appendix.md:177-185", "docs/spec/30.cli-interface.srs.md:946-1000"]`.
- 구현 가이드:
  1. Red tests run `speckiwi mcp --transport invalid` and expect usage exit.
  2. Red tests run `speckiwi mcp --transport stdio` with injected fake starter and assert stdout is untouched by startup logs.
  3. Red tests run the real starter in a subprocess smoke case and assert stdout contains only MCP JSON-RPC frames.
  4. Green registers the command through the P4 registrar extension point after MCP server exists and delegates through the `McpServerStarter` interface.
  5. Refactor startup injection so tests do not need to import SDK internals.
- Rationale: CLI owns process startup while MCP protocol logic remains in `src/mcp`, and this order avoids Phase 4/Phase 5 ownership conflict.
- 함정 / 주의사항: startup warnings go to stderr and `src/mcp/*` remains owned by MCP tasks.
- 테스트 작성 지침: valid stdio, invalid transport, stderr logging, exit code 6 for fatal server startup, and stdout purity.
- Red 단계: write `test/cli/mcp-command.test.ts`; confirm failures.
- Green 단계: implement command with server dependency injection.
- Refactor 단계: keep transport enum shared with MCP adapter types without moving SDK imports into CLI.
- 검증 명령: 아래 `acceptance_tests`를 실행한다.
- acceptance_tests:
```json
[
  {"shell":"pwsh","cmd":"npm run test -- test/cli/mcp-command.test.ts","expected_exit":0},
  {"shell":"bash","cmd":"npm run test -- test/cli/mcp-command.test.ts","expected_exit":0}
]
```
- 완료 기준: `speckiwi mcp` uses stdio by default, delegates to the completed MCP server, and never prints human text to stdout.
- rollback: `{strategy: "manual", command: "1. git diff -- src/cli/commands/mcp.ts test/cli/mcp-command.test.ts 를 확인한다. 2. apply_patch로 이 TASK hunks를 제거한다."}`.
