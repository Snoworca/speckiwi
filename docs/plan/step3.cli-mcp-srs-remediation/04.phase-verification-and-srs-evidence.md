# Phase P4: Verification and SRS Evidence

| Field | Value |
|---|---|
| Phase | P4 |
| Plan ID | plan-20260508-cli-mcp-srs-remediation |
| Status | approved |
| Primary Output | regression tests, cross-platform gate record, SRS evidence updates |

## 1. 목표

P1~P3의 remediation이 CLI/MCP SRS gap을 실제로 닫았는지 검증하고, 검증 증거를 관련 SRS Requirement Block에 반영한다.

## 2. 선행 조건

- P1, P2, P3의 TASK acceptance_tests가 통과한다.
- Windows PowerShell gate가 통과한다.
- POSIX runtime에서 dependency를 설치한 환경을 준비한다.

## 3. TASK 목록

### TASK-P4-001 — Todo integration test를 full remediation smoke로 확장

| Field | Value |
|---|---|
| 관련 SRS 참조 | `IR-CLI-006`, `IR-CLI-007`, `IR-CLI-015`, `IR-CLI-018`, `FR-MCP-003`, `FR-MCP-008`, `FR-MCP-010`, `REL-MCP-001` |
| supporting SRS | `IR-CLI-004` |
| 파일 경로 | 수정: `test/integration/todo-installable-workflow.test.ts` |
| 메서드/함수 시그니처 | existing Vitest cases in `todo-installable-workflow.test.ts` |
| 참고 패턴 | `test/integration/todo-installable-workflow.test.ts:505-529` CLI workflow and `test/integration/todo-installable-workflow.test.ts:531-570` MCP workflow |
| source_anchors | `["test/integration/todo-installable-workflow.test.ts:505-529", "test/integration/todo-installable-workflow.test.ts:531-570"]` |
| 예상 소요 | 3시간 |
| turn_estimate | 5 |
| TDD order | Red: Todo smoke assertions for enhanced CLI/MCP contracts 작성. Green: integration expectations and covered command behavior 수정. Refactor: helper assertions를 재사용하고 full parity snapshot을 만들지 않는다. |

#### 구현 가이드

1. Red: CLI summary assertion expects `countsByType.functional === 2` and `blocked` array.
2. Red: CLI list `--format plain` and `--format table` return non-JSON text with IDs.
3. Red: missing show assertion checks exact exit code 2.
4. Red: MCP missing get returns `error.code === "not_found"`.
5. Red: MCP add_requirement result includes patch summary and record.
6. Red: MCP summarize_target after mutation returns total 4 and type counts.
7. Keep this file as smoke coverage. Detailed edge cases remain in `test/cli`, `test/mcp`, and `test/core`.

#### Rationale

The Todo integration test is the closest user workflow. It should prove that remediation improvements are visible across core, CLI, and MCP without claiming full response parity.

#### 함정 / 주의사항

- Do not assert entire serialized CLI/MCP equality.
- Do not cover every SRS ID in this integration file; use focused smoke assertions.
- Keep generated Todo fixture deterministic.

#### 테스트 작성 지침

- 성공: enhanced summary and mutation payload fields are visible in the generated Todo workflow.
- 실패: missing requirement exact CLI/MCP error semantics are asserted.
- 경계: text formats are checked by content and non-JSON parse behavior, not by brittle whitespace snapshots.

#### 검증 명령어

```powershell
npx vitest run test/integration/todo-installable-workflow.test.ts --hookTimeout 60000 --testTimeout 60000 --no-file-parallelism
```

#### acceptance_tests

```yaml
acceptance_tests:
  - {shell: "pwsh", cmd: "npx vitest run test/integration/todo-installable-workflow.test.ts --hookTimeout 60000 --testTimeout 60000 --no-file-parallelism", expected_exit: 0}
  - {shell: "bash", cmd: "npx vitest run test/integration/todo-installable-workflow.test.ts --hookTimeout 60000 --testTimeout 60000 --no-file-parallelism", expected_exit: 0}
```

#### DoD

- Todo integration verifies enhanced CLI summary, CLI format, exact CLI error, MCP error taxonomy, MCP patch summary, and post-mutation summary.
- Test remains deterministic and temp-root isolated.
- No full CLI/MCP response parity assertion is introduced.

#### rollback

```yaml
rollback: {strategy: "manual", command: "1) Remove only the new assertions from todo-installable-workflow.test.ts. 2) Re-run the target integration test and keep the core remediation tests for debugging."}
```

### TASK-P4-002 — Release regression gate and POSIX dependency note

| Field | Value |
|---|---|
| 관련 SRS 참조 | `IR-CLI-018`, `SEC-MCP-001` |
| supporting SRS | `FR-NODE-004`, `IR-MCP-001` |
| 파일 경로 | 생성/수정: `docs/ci/cross-platform-test-notes.md` |
| 메서드/함수 시그니처 | N/A — documentation and command verification |
| 참고 패턴 | `package.json:45-52` scripts and prior POSIX optional dependency failure record |
| source_anchors | `["package.json:45-52", "docs/pm/pm-state.2026-05-08.14_55_33.json:36-40"]` |
| 예상 소요 | 1시간 |
| turn_estimate | 3 |
| TDD order | Red: gate commands and POSIX native dependency failure mode are documented before evidence update. Green: PowerShell and POSIX-local command results are recorded. Refactor: CI note stays command-oriented and does not duplicate SRS evidence rows. |

#### 구현 가이드

1. Run the PowerShell pre-commit gate.
2. Create or select a POSIX-local checkout/worktree.
3. In that POSIX worktree, run `npm ci` before any Vitest command.
4. Run the POSIX gate from that POSIX worktree with the same project revision.
5. Document that WSL must not reuse Windows `node_modules` for native optional dependency packages.
6. Create `docs/ci/cross-platform-test-notes.md` with the setup command, gate commands, and known failure mode.
7. Do not change package dependencies in this TASK.

#### Rationale

The previous bash Vitest failure was caused by a platform-mismatched `node_modules`, not by test assertions. The project needs a clear verification note so future POSIX gates are meaningful.

#### 함정 / 주의사항

- Do not reinstall dependencies in the Windows workspace for POSIX verification.
- This TASK records environment prerequisites; it does not hide failing code tests.
- Keep CI note short and command-oriented.

#### 테스트 작성 지침

- 성공: PowerShell gate passes.
- 실패: POSIX runtime without matching dependencies documents the native binding startup error.
- 경계: POSIX runtime with matching dependencies runs the same npm scripts.

#### 검증 명령어

```powershell
npm run build
npm run typecheck
npm run lint
npm test
npm run test:integration
npm run release:acceptance
```

```bash
npm ci
npm run build
npm run typecheck
npm run lint
npm test
npm run test:integration
npm run release:acceptance
```

#### acceptance_tests

```yaml
acceptance_tests:
  - {shell: "pwsh", cmd: "npm run build", expected_exit: 0}
  - {shell: "pwsh", cmd: "npm run typecheck", expected_exit: 0}
  - {shell: "pwsh", cmd: "npm run lint", expected_exit: 0}
  - {shell: "pwsh", cmd: "npm test", expected_exit: 0}
  - {shell: "pwsh", cmd: "npm run test:integration", expected_exit: 0}
  - {shell: "pwsh", cmd: "npm run release:acceptance", expected_exit: 0}
  - {shell: "bash", cmd: "npm ci", expected_exit: 0}
  - {shell: "bash", cmd: "npm run build", expected_exit: 0}
  - {shell: "bash", cmd: "npm run typecheck", expected_exit: 0}
  - {shell: "bash", cmd: "npm run lint", expected_exit: 0}
  - {shell: "bash", cmd: "npm test", expected_exit: 0}
  - {shell: "bash", cmd: "npm run test:integration", expected_exit: 0}
  - {shell: "bash", cmd: "npm run release:acceptance", expected_exit: 0}
```

#### DoD

- PowerShell gate passes, including full `npm test`.
- POSIX gate has an environment note that explains native optional dependency requirements.
- CI notes do not claim POSIX pass unless commands were run in a matching POSIX dependency environment.

#### rollback

```yaml
rollback: {strategy: "manual", command: "1) Remove docs/ci/cross-platform-test-notes.md or revert only its added section. 2) Re-run PowerShell gate to confirm code tests remain unaffected."}
```

### TASK-P4-003 — SRS verification evidence update

| Field | Value |
|---|---|
| 관련 SRS 참조 | all remediated CLI/MCP IDs in `00.index.md` trace table |
| 파일 경로 | 수정: `docs/spec/30.cli-interface.srs.md`, `docs/spec/40.mcp-stdio-interface.srs.md` |
| 메서드/함수 시그니처 | N/A — SRS Markdown update |
| 참고 패턴 | Requirement Blocks listed in `00.index.md` trace table |
| source_anchors | `["docs/spec/30.cli-interface.srs.md:281-337", "docs/spec/30.cli-interface.srs.md:338-394", "docs/spec/30.cli-interface.srs.md:395-451", "docs/spec/30.cli-interface.srs.md:452-507", "docs/spec/30.cli-interface.srs.md:508-677", "docs/spec/30.cli-interface.srs.md:678-733", "docs/spec/30.cli-interface.srs.md:734-790", "docs/spec/30.cli-interface.srs.md:791-846", "docs/spec/30.cli-interface.srs.md:847-1018", "docs/spec/30.cli-interface.srs.md:1019-1134", "docs/spec/30.cli-interface.srs.md:1135-1200", "docs/spec/40.mcp-stdio-interface.srs.md:105-275", "docs/spec/40.mcp-stdio-interface.srs.md:276-331", "docs/spec/40.mcp-stdio-interface.srs.md:332-446", "docs/spec/40.mcp-stdio-interface.srs.md:447-503", "docs/spec/40.mcp-stdio-interface.srs.md:504-559", "docs/spec/40.mcp-stdio-interface.srs.md:560-615", "docs/spec/40.mcp-stdio-interface.srs.md:616-672", "docs/spec/40.mcp-stdio-interface.srs.md:673-729", "docs/spec/40.mcp-stdio-interface.srs.md:730-786", "docs/spec/40.mcp-stdio-interface.srs.md:787-849", "docs/spec/40.mcp-stdio-interface.srs.md:850-910"]` |
| 예상 소요 | 2시간 |
| turn_estimate | 4 |
| TDD order | Red: release acceptance detects malformed evidence before edit. Green: evidence and Change Notes rows are added only for remediated blocks. Refactor: SRS edits are minimal, UTF-8, and do not rewrite unrelated sections. |

#### 구현 가이드

1. Run all P4 verification commands first.
2. For each remediated Requirement Block, add a new Verification Evidence row or update the existing VE row notes with the exact test command references.
3. Add Change Notes rows dated `2026-05-08` for the remediation.
4. Do not change Requirement IDs.
5. Do not change status unless every AC and evidence condition remains satisfied. Current status is already `verified`; preserve it after evidence is added.
6. Run `speckiwi validate --json` and the MCP `validate_spec` coverage in `test/mcp/read-tools-resources.test.ts` after SRS edits.
7. Do not add evidence rows to supporting-only SRS IDs unless their Requirement Blocks were directly changed by the implementation.

#### Rationale

The SRS already marks the affected items verified, but the previous implementation review found gaps. The remediation must leave traceable evidence that the new implementation closes those gaps.

#### 함정 / 주의사항

- Evidence must reference actual tests that were run.
- Do not invent POSIX evidence if POSIX gate was not run in a matching dependency environment.
- Modify only relevant Requirement Blocks.

#### 테스트 작성 지침

- 성공: `validate_spec` has zero errors after evidence updates.
- 실패: malformed evidence table or invalid links are caught by release acceptance tests.
- 경계: evidence updates do not rewrite whole SRS documents.

#### 검증 명령어

```powershell
npm run build
node bin/speckiwi validate --json
npx vitest run test/mcp/read-tools-resources.test.ts --hookTimeout 60000 --testTimeout 60000 --no-file-parallelism
npm run release:acceptance
```

#### acceptance_tests

```yaml
acceptance_tests:
  - {shell: "pwsh", cmd: "npm run build", expected_exit: 0}
  - {shell: "pwsh", cmd: "node bin/speckiwi validate --json", expected_exit: 0}
  - {shell: "pwsh", cmd: "npx vitest run test/mcp/read-tools-resources.test.ts --hookTimeout 60000 --testTimeout 60000 --no-file-parallelism", expected_exit: 0}
  - {shell: "pwsh", cmd: "npm run release:acceptance", expected_exit: 0}
  - {shell: "bash", cmd: "npm run build", expected_exit: 0}
  - {shell: "bash", cmd: "node bin/speckiwi validate --json", expected_exit: 0}
  - {shell: "bash", cmd: "npx vitest run test/mcp/read-tools-resources.test.ts --hookTimeout 60000 --testTimeout 60000 --no-file-parallelism", expected_exit: 0}
  - {shell: "bash", cmd: "npm run release:acceptance", expected_exit: 0}
```

#### DoD

- Each remediated CLI/MCP SRS ID has evidence tied to a passing command.
- `speckiwi validate --json` returns zero errors and MCP `validate_spec` coverage remains passing in the MCP read-tool tests.
- `npm run release:acceptance` passes.

#### rollback

```yaml
rollback: {strategy: "manual", command: "1) Revert only the evidence and Change Notes rows added to the affected Requirement Blocks. 2) Run npm run release:acceptance to confirm SRS structure is valid."}
```
