# Phase P3: MCP SRS Remediation

| Field | Value |
|---|---|
| Phase | P3 |
| Plan ID | plan-20260508-cli-mcp-srs-remediation |
| Status | approved |
| Primary Output | MCP tool, schema, error, resource contract updates |

## 1. 목표

MCP SRS에서 확인된 structured error taxonomy, read tool payload, mutation response, optional input, resource, init_project gap을 닫는다.

## 2. 선행 조건

- P1의 summary payload와 root boundary helper가 구현되어 있다.
- P2의 CLI-facing core mutation changes가 적용되어 있다.
- `test/mcp/read-tools-resources.test.ts`, `test/mcp/mutation-tools-errors.test.ts`, `test/mcp/stdio-purity.test.ts`가 현재 통과하는 기준선을 확인한다.

## 3. TASK 목록

### TASK-P3-001 — MCP structured error taxonomy와 result envelope 정규화

| Field | Value |
|---|---|
| 관련 SRS 참조 | `REL-MCP-001`, `FR-MCP-004`, `FR-MCP-010` |
| 파일 경로 | 수정: `src/mcp/errors.ts`, `src/mcp/tools/read-tools.ts`, `src/mcp/tools/mutation-tools.ts`, `test/mcp/mutation-tools-errors.test.ts`, `test/mcp/read-tools-resources.test.ts` |
| 메서드/함수 시그니처 | `function resultToMcp<T>(result: Result<T> | MutationResult<T>): unknown`; `function toStructuredError(error: { code: string; message: string }): McpStructuredError` |
| 참고 패턴 | `src/mcp/errors.ts:8-15` current result mapper and `src/mcp/tools/read-tools.ts:15-20` not found branch |
| source_anchors | `["src/mcp/errors.ts:8-15", "src/mcp/tools/read-tools.ts:15-23", "src/mcp/tools/mutation-tools.ts:37-95", "test/mcp/mutation-tools-errors.test.ts:13-38"]` |
| 예상 소요 | 3시간 |
| turn_estimate | 5 |
| TDD order | Red: lower-case structured error and patch/diagnostics preservation tests 작성. Green: MCP mapper와 tool catch paths 수정. Refactor: read/mutation tools가 같은 `resultToMcp` envelope를 사용하도록 중복 제거. |

#### 구현 가이드

1. Red: missing `get_requirement` returns `{ ok:false, error:{ code:"not_found" } }`.
2. Red: verified guard failure returns `{ ok:false, error:{ code:"status_transition_denied" } }`.
3. Red: malformed SRS content reached through `validate_spec` or a read tool returns a structured parse error result containing diagnostics instead of throwing an unhandled exception.
4. Add `mapMcpErrorCode(code: string): string`.
5. Map `NOT_FOUND` to `not_found`.
6. Map `MUTATION_DENIED` from verified guard messages to `status_transition_denied`; other mutation denial can map to `mutation_denied`.
7. Map parser failures with diagnostics to `parse_error`.
8. Map `USAGE` to `invalid_request`.
9. Map `ROOT_BOUNDARY` to `access_denied`.
10. `resultToMcp()` must preserve `diagnostics` and `patch` when present.
11. Read-tool catch blocks should use the same error mapper.

#### Rationale

SRS defines MCP outward error taxonomy. Core error codes can remain internal, but MCP response must expose the SRS contract.

#### 함정 / 주의사항

- Existing tests that assert uppercase codes must be migrated.
- Do not throw JSON-RPC protocol errors for domain failures; keep `{ ok:false, error }`.
- Diagnostics and patch belong at the top-level MCP result unless the final implementation chooses a documented stable nested shape. Tests must lock that shape.

#### 테스트 작성 지침

- 성공: valid mutation returns `ok:true` with value and patch.
- 실패: not found and verified guard return lower-case structured codes.
- 실패: parse errors return structured `parse_error` with diagnostics.
- 경계: usage error returns lower-case invalid request code and no file write.

#### 검증 명령어

```powershell
npx vitest run test/mcp/mutation-tools-errors.test.ts test/mcp/read-tools-resources.test.ts --hookTimeout 60000 --testTimeout 60000 --no-file-parallelism
```

#### acceptance_tests

```yaml
acceptance_tests:
  - {shell: "pwsh", cmd: "npx vitest run test/mcp/mutation-tools-errors.test.ts test/mcp/read-tools-resources.test.ts --hookTimeout 60000 --testTimeout 60000 --no-file-parallelism", expected_exit: 0}
  - {shell: "bash", cmd: "npx vitest run test/mcp/mutation-tools-errors.test.ts test/mcp/read-tools-resources.test.ts --hookTimeout 60000 --testTimeout 60000 --no-file-parallelism", expected_exit: 0}
```

#### DoD

- MCP outward error codes use lower-case SRS taxonomy.
- MCP result envelope preserves diagnostics and patch summary.
- REL-MCP-001 AC-1~AC-4 are covered: `not_found`, `status_transition_denied`, parse errors with diagnostics, and stderr-only logging discipline through the stdio purity tests.
- Existing domain failures remain structured tool results.

#### rollback

```yaml
rollback: {strategy: "manual", command: "1) Restore src/mcp/errors.ts and MCP tests to uppercase-code expectations. 2) Re-run MCP tests to confirm the prior behavior before retrying the taxonomy change."}
```

### TASK-P3-002 — validate_spec와 summarize_target read tool payload 완성

| Field | Value |
|---|---|
| 관련 SRS 참조 | `FR-MCP-003`, `FR-MCP-008` |
| 파일 경로 | 수정: `src/mcp/server.ts`, `src/mcp/tools/read-tools.ts`, `src/core/validator/validate-workspace.ts`, `test/mcp/read-tools-resources.test.ts`, `test/integration/todo-installable-workflow.test.ts` |
| 메서드/함수 시그니처 | `validate_spec(input: { scope?: string; target?: string; strict?: boolean; failOnWarning?: boolean })`; `summarize_target(input: { target?: string })` |
| 참고 패턴 | `src/mcp/server.ts:23-33` schema, `src/mcp/tools/read-tools.ts:22-23` validate/summary handlers |
| source_anchors | `["src/mcp/server.ts:23-33", "src/mcp/tools/read-tools.ts:22-23", "src/core/validator/validate-workspace.ts:1-10", "src/core/query/summary.ts:7-20", "test/integration/todo-installable-workflow.test.ts:542-546"]` |
| 예상 소요 | 3시간 |
| turn_estimate | 5 |
| TDD order | Red: validate_spec input/summary and summarize_target payload tests 작성. Green: schemas and handlers 확장. Refactor: validation summary helper를 deterministic core function으로 분리하고 diagnostics passthrough 유지. |

#### 구현 가이드

1. Red: MCP schema includes `scope` and `target` for `validate_spec`.
2. Red: `validate_spec({ failOnWarning:true })` returns a summary that exposes whether warnings would fail.
3. Red: calling `validate_spec({ strict:true, failOnWarning:true })` leaves every `docs/spec/**/*.md` file byte-for-byte unchanged.
4. Red: the MCP tool registry marks `validate_spec` as read-only where the local SDK/test adapter exposes read-only annotations.
5. Red: `summarize_target()` response includes `countsByType` and `blocked`.
6. Extend `toolSchemas.validate_spec` with `scope` and `target`.
7. In handler, run full validation, then add a deterministic `summary` object: `errorCount`, `warningCount`, `failed`, `filteredScope`, `filteredTarget`.
8. Filtering can apply to diagnostics by file/record scope when determinable. If not determinable, summary fields must still echo requested filters and full diagnostics must remain available.
9. Ensure the handler uses parser/validator read paths only and never invokes mutation helpers, write helpers, or `initProject`.
10. `summarize_target` should use P1 `TargetSummary` as-is.

#### Rationale

MCP clients need machine-readable validation status without parsing arrays. Summary output must match the enhanced core summary used by CLI.

#### 함정 / 주의사항

- Do not hide full diagnostics when filters are used unless the filter logic is deterministic.
- `strict` can be accepted and echoed even if no strict-only rules exist in v1.0.0.
- `failOnWarning` changes the `failed` summary boolean, not the process exit code.

#### 테스트 작성 지침

- 성공: baseline valid workspace returns summary errorCount 0 and failed false.
- 실패: workspace with warnings and `failOnWarning:true` returns failed true.
- 경계: validate_spec read-only behavior is asserted by before/after file snapshots and read-only tool metadata when available.
- 경계: `summarize_target` with omitted target uses active target.

#### 검증 명령어

```powershell
npx vitest run test/mcp/read-tools-resources.test.ts test/integration/todo-installable-workflow.test.ts --hookTimeout 60000 --testTimeout 60000 --no-file-parallelism
```

#### acceptance_tests

```yaml
acceptance_tests:
  - {shell: "pwsh", cmd: "npx vitest run test/mcp/read-tools-resources.test.ts test/integration/todo-installable-workflow.test.ts --hookTimeout 60000 --testTimeout 60000 --no-file-parallelism", expected_exit: 0}
  - {shell: "bash", cmd: "npx vitest run test/mcp/read-tools-resources.test.ts test/integration/todo-installable-workflow.test.ts --hookTimeout 60000 --testTimeout 60000 --no-file-parallelism", expected_exit: 0}
```

#### DoD

- `validate_spec` accepts scope, target, strict, failOnWarning.
- `validate_spec` returns diagnostics and summary.
- `validate_spec` covers FR-MCP-003 AC-1~AC-3: optional inputs, diagnostics plus summary output, and read-only no-write behavior.
- `summarize_target` returns status count, type count, blocked, implemented-not-verified, missing-evidence.

#### rollback

```yaml
rollback: {strategy: "manual", command: "1) Restore read-tools validate_spec and summarize_target handlers to their previous body. 2) Remove new read-tool payload assertions and rerun MCP read tests."}
```

### TASK-P3-003 — MCP mutation payload, optional input, and notes propagation

| Field | Value |
|---|---|
| 관련 SRS 참조 | `FR-MCP-004`, `FR-MCP-006`, `FR-MCP-007`, `FR-MCP-010`, `SEC-MCP-001` |
| supporting SRS | `FR-NODE-008` |
| 파일 경로 | 수정: `src/mcp/server.ts`, `src/mcp/tools/mutation-tools.ts`, `src/mcp/errors.ts`, `src/core/mutation/update-status.ts`, `src/core/mutation/add-evidence.ts`, `src/core/mutation/add-trace.ts`, `src/core/mutation/add-requirement.ts`, `test/mcp/mutation-tools-errors.test.ts` |
| 메서드/함수 시그니처 | `registerMutationTools(server: McpServerHandle, deps: McpDependencies): void`; `AddRequirementInput`; `AddEvidenceInput`; `AddTraceInput` |
| 참고 패턴 | `src/mcp/tools/mutation-tools.ts:42-55` evidence/trace handlers, `src/mcp/tools/mutation-tools.ts:55-85` add requirement handler |
| source_anchors | `["src/mcp/tools/mutation-tools.ts:42-55", "src/mcp/tools/mutation-tools.ts:55-85", "src/core/mutation/add-evidence.ts:16-25", "src/core/mutation/add-trace.ts:17-28", "src/core/mutation/add-requirement.ts:115-156", "test/mcp/mutation-tools-errors.test.ts:13-38"]` |
| 예상 소요 | 5시간 |
| turn_estimate | 8 |
| TDD order | Red: mutation payload, notes, traceLinks, patch summary, local evidence root-boundary tests 작성. Green: schema/handler/core outputs 수정. Refactor: MCP aliases and core mutation result shapes를 공유 helper로 정리하고 no-write failure tests 유지. |

#### 구현 가이드

1. Red: `add_verification_evidence` schema accepts `id`, `type`, `reference`, `covers`, `notes`; `type`, `reference`, and `covers` are required.
2. Red: missing `type`, missing `reference`, and missing `covers` each return invalid request and leave the SRS file unchanged.
3. Red: successful `add_verification_evidence` auto-generates the next Evidence ID and returns the added row including type, reference, covers, and notes.
4. Red: `add_trace_link` schema accepts `id`, `type`, `reference`, `relation`, `notes`.
5. Red: `add_trace_link` rejects a missing Requirement reference with structured error and no file change.
6. Red: successful `add_trace_link` returns the added trace row including notes.
7. Red: `add_requirement` accepts all required SRS input fields: `type`, `scope`, `target`, `title`, `requirement`, `acceptanceCriteria`.
8. Red: `add_requirement` accepts optional SRS inputs: `status`, `priority`, `tags`, `risk`, `stability`, `verificationMethod`, `rationale`, `implementationNotes`, `relatedDocs`, `githubIssue`, `evidence`, `traceLinks`, `research`, `changeNotes`.
9. Red: `add_requirement` omitted status creates a normalized record with `status:"planned"`.
10. Red: `add_requirement` appends the new block to the target scope document `## 4. Requirements` section end.
11. Red: `add_requirement` with `status:"verified"` fails unless all AC are checked and at least one evidence row has a non-empty reference; failure returns structured error and no file change.
12. Red: `add_requirement` accepts `traceLinks` as SRS input name and maps it to core trace rows while retaining `trace` compatibility alias.
13. Red: `add_requirement` response includes generated normalized record and patch summary.
14. Red: `update_status` schema requires both `id` and `status`; missing either input returns structured invalid request and leaves the SRS file unchanged.
15. Red: successful `update_status` returns the changed normalized record and patch summary.
16. Red: `update_status` rejects invalid status, unknown id, and verified guard failures with structured errors and no file write.
17. Red: `add_verification_evidence` rejects a root-outside local file reference and a root-inside symlink that resolves outside; both return `{ ok:false, error:{ code:"access_denied" } }` and leave the SRS file unchanged.
18. Red: local file evidence validation uses Node.js fs APIs only; it does not pass user input to shell execution.
19. Update MCP schema to expose `traceLinks` while retaining `trace` compatibility alias.
20. Forward notes from MCP handlers into core mutation services.
21. Core mutation outputs should include enough row data for MCP response tests without reparsing in the adapter.
22. Use `resultToMcp()` from TASK-P3-001 to preserve patch and diagnostics.

#### Rationale

MCP is the agent-facing write surface. Agents need patch summaries and normalized records to understand what changed without reading the whole file again.

#### 함정 / 주의사항

- Existing `trace` input remains accepted to avoid breaking current tests.
- SRS name `traceLinks` must be documented by schema and tested.
- Evidence local file reference existence and root boundary must use P1 safe path helper for `type: "local_file"` or equivalent local file evidence rows.
- Do not call `child_process`, `exec`, `spawn`, or shell wrappers for validation of MCP mutation inputs.

#### 테스트 작성 지침

- 성공: evidence and trace notes round-trip through MCP.
- 성공: update_status required inputs, changed record, and patch summary are asserted.
- 실패: evidence missing type, missing reference, and missing covers are invalid requests with no file change.
- 실패: update_status missing input, invalid status, unknown id, and verified guard failures are structured errors with no file change.
- 실패: trace Requirement reference not found is a structured error with no file change.
- 경계: add_requirement dryRun returns patch summary with `dryRun:true` and `written:false`.
- 보안: root 밖 local evidence reference와 symlink escape reference는 structured access-denied error와 no-write assertion을 검증한다.

#### 검증 명령어

```powershell
npx vitest run test/mcp/mutation-tools-errors.test.ts test/core/mutation/add-requirement.test.ts test/core/mutation/status-ac.test.ts --hookTimeout 60000 --testTimeout 60000 --no-file-parallelism
```

#### acceptance_tests

```yaml
acceptance_tests:
  - {shell: "pwsh", cmd: "npx vitest run test/mcp/mutation-tools-errors.test.ts test/core/mutation/add-requirement.test.ts test/core/mutation/status-ac.test.ts --hookTimeout 60000 --testTimeout 60000 --no-file-parallelism", expected_exit: 0}
  - {shell: "bash", cmd: "npx vitest run test/mcp/mutation-tools-errors.test.ts test/core/mutation/add-requirement.test.ts test/core/mutation/status-ac.test.ts --hookTimeout 60000 --testTimeout 60000 --no-file-parallelism", expected_exit: 0}
```

#### DoD

- MCP mutation responses preserve patch summary.
- `update_status` covers FR-MCP-004 AC-1~AC-4: required `id` and `status`, verified guard, changed record plus patch summary on success, and no-write failure behavior.
- `add_requirement` supports `traceLinks`.
- evidence/trace notes are not dropped.
- `add_verification_evidence` covers FR-MCP-006 AC-1~AC-4: required fields, auto Evidence ID, and returned row.
- `add_trace_link` covers FR-MCP-007 AC-1~AC-3: input fields, Requirement reference existence, and returned row.
- `add_requirement` covers FR-MCP-010 AC-1~AC-7 at MCP adapter level, including optional inputs, default planned status, append location, verified guard, normalized record, patch summary, structured error, and no-write failures.
- missing required evidence fields return structured invalid request and no file write.
- local file evidence reference existence and root-boundary validation is tested.
- MCP mutation input handling is covered by a static security assertion that no shell command execution API is imported or invoked in `src/core` and `src/mcp`.

#### rollback

```yaml
rollback: {strategy: "manual", command: "1) Restore mutation-tools handlers and mutation output types to the previous result shape. 2) Remove new MCP mutation response assertions and rerun MCP mutation tests."}
```

### TASK-P3-004 — MCP resources, init_project result, and server metadata consistency

| Field | Value |
|---|---|
| 관련 SRS 참조 | `IR-MCP-002`, `FR-MCP-009`, `FR-MCP-011`, `SEC-MCP-001` |
| supporting SRS | `IR-MCP-001`, `FR-NODE-009` |
| 파일 경로 | 수정: `src/mcp/server.ts`, `src/mcp/resources.ts`, `src/mcp/metadata.ts`, `src/mcp/tools/mutation-tools.ts`, `test/mcp/read-tools-resources.test.ts`, `test/mcp/stdio-purity.test.ts`, `test/mcp/mutation-tools-errors.test.ts` |
| 메서드/함수 시그니처 | `createMcpServer(deps: McpDependencies): McpServerHandle`; `startMcpServer(options: McpServerOptions): Promise<void>`; `registerResources(server: McpServerHandle, deps: McpDependencies): void` |
| 참고 패턴 | `src/mcp/server.ts:76-119` SDK metadata and resource bridge, `src/mcp/resources.ts:7-24` resource registration, `src/mcp/tools/mutation-tools.ts:88-94` init_project tool |
| source_anchors | `["src/mcp/server.ts:76-119", "src/mcp/resources.ts:7-24", "src/mcp/metadata.ts:12-14", "src/mcp/tools/mutation-tools.ts:88-94", "test/mcp/read-tools-resources.test.ts:8-30", "test/mcp/stdio-purity.test.ts:11-47"]` |
| 예상 소요 | 3시간 |
| turn_estimate | 5 |
| TDD order | Red: metadata/resource/init_project result tests 작성. Green: metadata source, resource error wrapper, init envelope 수정. Refactor: SDK bridge와 test adapter가 같은 metadata/resource helpers를 사용하게 정리하고 stdout purity 유지. |

#### 구현 가이드

1. Red: MCP metadata exposes server name `speckiwi`.
2. Red: MCP metadata version matches package version read from package metadata helper.
3. Red: MCP metadata/capabilities expose tools and resources; prompts capability may be absent or explicitly optional.
4. Red: resource list/templates include `speckiwi://index`, `speckiwi://requirements/{id}`, `speckiwi://targets/{target}`, and `speckiwi://scopes/{scope}`.
5. Red: each resource returns a valid structured success payload for valid input.
6. Red: resource missing requirement returns structured error instead of unhandled throw.
7. Red: `init_project` schema accepts optional `target`, `scope`, `agentFile`, `force`.
8. Red: `init_project` creates or reports `docs/`, `docs/spec/`, `docs/rule/`, `docs/spec/00.index.md`, `docs/spec/90.appendix.md`, `docs/rule/SRS-MD-Rules-v1.0.0.md`, `AGENTS.md`, and `CLAUDE.md` according to input.
9. Red: `init_project` returns created, skipped, updated, and diagnostics.
10. Red: `init_project` is idempotent: existing agent instruction/rules link is not duplicated and existing files are not overwritten when `force:false`.
11. Red: `init_project` failure returns structured error and does not overwrite existing files.
12. Ensure `registerResources()` catches domain errors and returns `{ ok:false, error }`.
13. Ensure `init_project` uses P1 safe write behavior and resultToMcp envelope.
14. Replace hard-coded SDK version with package metadata source or a single package version constant.
15. Keep stdout purity tests green.

#### Rationale

Resources and initialization are part of MCP SRS, not auxiliary behavior. They must share structured error and metadata contracts with tools.

#### 함정 / 주의사항

- The SDK server bridge must not log to stdout.
- Resource handler result content should stay JSON serializable.
- Version source should not create an async file read inside hot tool path.

#### 테스트 작성 지침

- 성공: resource templates return ok true for valid id/target/scope.
- 성공: index resource and MCP metadata capabilities are asserted.
- 실패: resource requirement missing returns ok false structured error.
- 경계: package version change is represented by metadata helper in tests.
- 경계: init_project both-agent, idempotency, no-overwrite, and structured result paths are asserted through the MCP adapter.

#### 검증 명령어

```powershell
npx vitest run test/mcp/read-tools-resources.test.ts test/mcp/stdio-purity.test.ts test/mcp/mutation-tools-errors.test.ts --hookTimeout 60000 --testTimeout 60000 --no-file-parallelism
```

#### acceptance_tests

```yaml
acceptance_tests:
  - {shell: "pwsh", cmd: "npx vitest run test/mcp/read-tools-resources.test.ts test/mcp/stdio-purity.test.ts test/mcp/mutation-tools-errors.test.ts --hookTimeout 60000 --testTimeout 60000 --no-file-parallelism", expected_exit: 0}
  - {shell: "bash", cmd: "npx vitest run test/mcp/read-tools-resources.test.ts test/mcp/stdio-purity.test.ts test/mcp/mutation-tools-errors.test.ts --hookTimeout 60000 --testTimeout 60000 --no-file-parallelism", expected_exit: 0}
```

#### DoD

- MCP resources return structured errors for missing domain objects.
- MCP resources cover FR-MCP-009 AC-1~AC-4, including `speckiwi://index`.
- MCP server metadata covers IR-MCP-002 AC-1~AC-5, including name, package version, tools capability, resources capability, and optional prompts capability.
- init_project covers FR-MCP-011 AC-1~AC-11 at MCP adapter level, including optional input schema, generated structure, agent files, idempotency, no-overwrite behavior, structured result, and structured failure.
- init_project response includes created, skipped, updated, diagnostics in the MCP result envelope.
- Static security assertion confirms MCP/core source does not provide shell command execution for user tool input.

#### rollback

```yaml
rollback: {strategy: "manual", command: "1) Restore MCP resource and server metadata files to their previous behavior. 2) Remove new resource error and metadata assertions and rerun MCP tests."}
```
