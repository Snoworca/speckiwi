# Phase P1: Todo Installable Integration Workflow

| Field | Value |
|---|---|
| Phase | P1 |
| Plan ID | plan-20260508-todo-srs-integration |
| Status | approved |
| Primary Output | `test/integration/todo-installable-workflow.test.ts` |

## 1. 목표

동적 임시 프로젝트에 설치형 Todo 리스트 관리 웹서비스 SRS를 생성하고, SpecKiwi core, CLI, MCP read, MCP mutation 흐름을 하나의 통합 테스트에서 검증한다.

## 2. 선행 조건

- `npm install --include=dev` 또는 동등한 dev dependency 설치가 완료되어 있다.
- `npm run build`, `npm run typecheck`, 기존 `npm run test:integration`이 작업 시작 전 기준선으로 통과한다.
- SRS-MD authoring rules는 `docs/rule/SRS-MD-Rules-v1.0.0.md`를 따른다.

## 3. TASK 목록

### TASK-P1-001 — Todo SRS 동적 workspace helper 작성

| Field | Value |
|---|---|
| 관련 SRS 참조 (partial) | `FR-NODE-003`, `FR-NODE-004` |
| Coverage Type | partial-reference |
| 파일 경로 | 생성: `test/integration/todo-installable-workflow.test.ts` |
| 메서드/함수 시그니처 | `async function createTodoWorkspace(options?: { omitScopeMap?: boolean }): Promise<string>`; `function renderIndex(options?: { omitScopeMap?: boolean }): string`; `function renderScopeDocument(input: ScopeDocumentInput): string`; `function renderRequirementBlock(input: RequirementBlockInput): string` |
| 참고 패턴 | `test/integration/full-workflow.test.ts:11`의 `mkdtemp` pattern과 `test/fixtures/fixture-utils.ts:23`의 generated fixture write pattern을 따른다. |
| source_anchors | `["test/integration/full-workflow.test.ts:11", "test/fixtures/fixture-utils.ts:23"]` |
| 예상 소요 | 1시간 |

#### 구현 가이드

1. `mkdtemp(path.join(tmpdir(), "speckiwi-todo-installable-"))`로 temp root를 만든다.
2. `mkdir(path.join(root, "docs", "spec"), { recursive: true })`를 호출한다.
3. `mkdir(path.join(root, "docs", "rule"), { recursive: true })`를 호출한다.
4. `writeFile(..., "utf8")`로 `docs/spec/00.index.md`, `docs/spec/10.server.srs.md`, `docs/spec/20.web-page.srs.md`, `docs/rule/SRS-MD-Rules-v1.0.0.md`를 생성한다.
5. rule file은 최소한 `# SRS-MD Authoring Rules v1.0.0` heading과 `Fixture copy for integration testing.` 본문을 포함한다.
6. index의 target은 `todo-installable-v1`로 고정한다.
7. Scope Map에는 `Server` prefix `SRV`, `Web Page` prefix `WEB`을 등록한다.
8. Server scope에는 `FR-SRV-001`, `IR-SRV-001`을 둔다.
9. Web Page scope에는 `FR-WEB-001`을 둔다.
10. 모든 Requirement Block은 metadata table, Requirement, Rationale, Acceptance Criteria, Verification Evidence, Trace Links, Research / Analysis, Implementation Notes, Change Notes section을 포함한다.

index template은 SRS-MD index 필수 구조를 모두 포함해야 한다. 아래 template을 `docs/spec/00.index.md`의 exact skeleton으로 사용한다.

```md
# 설치형 Todo 리스트 관리 웹서비스 SRS Index

| Field | Value |
|---|---|
| Document Type | srs_index |
| Product | 설치형 Todo 리스트 관리 웹서비스 |
| Version | 1.0.0 |
| Last Updated | 2026-05-08 |
| Rules | [SRS-MD Authoring Rules v1.0.0](../rule/SRS-MD-Rules-v1.0.0.md) |

## 1. Purpose

설치형 Todo 리스트 관리 웹서비스의 Server와 Web Page 요구사항을 정의한다.

## 2. SRS Documents

| Scope | Document | Prefix | Description |
|---|---|---|---|
| Server | [10.server.srs.md](./10.server.srs.md) | SRV | 설치형 Todo 서버 |
| Web Page | [20.web-page.srs.md](./20.web-page.srs.md) | WEB | Todo 관리 웹 페이지 |

## 3. Target Map

| Target | Type | Status | Description |
|---|---|---|---|
| todo-installable-v1 | release | active | 설치형 Todo v1 |

## 4. Scope Map

| Scope | Prefix | Primary Document | Notes |
|---|---|---|---|
| Server | SRV | [10.server.srs.md](./10.server.srs.md) | 설치형 Todo 서버 |
| Web Page | WEB | [20.web-page.srs.md](./20.web-page.srs.md) | Todo 관리 웹 페이지 |

## 5. Status Summary

| Status | Count |
|---|---:|
| planned | 3 |
| in_progress | 0 |
| blocked | 0 |
| implemented | 0 |
| verified | 0 |
| discarded | 0 |

## 6. Requirement Type Summary

| Type | Prefix | Count |
|---|---|---:|
| functional | FR | 2 |
| interface | IR | 1 |

## 7. Cross-scope Dependencies

| From | To | Relation | Notes |
|---|---|---|---|
| WEB | SRV | depends_on | Todo 웹 페이지는 서버 API 계약을 사용한다. |

## 8. Open Questions

| ID | Question | Impact | Status |
|---|---|---|---|

## 9. Reference Documents

- [SRS-MD Authoring Rules v1.0.0](../rule/SRS-MD-Rules-v1.0.0.md)

## 10. Change Notes

| Date | Change | Reason |
|---|---|---|
| 2026-05-08 | Created | integration fixture |
```

scope document skeleton은 SRS-MD scope 필수 구조를 모두 포함해야 한다. `renderScopeDocument()`는 Requirement Blocks를 `## 4. Requirements` 바로 아래에 삽입하고, 그 뒤에 `## 5. Cross-scope Dependencies`부터 footer section을 붙인다. 아래 template을 `10.server.srs.md`의 exact skeleton으로 사용한다.

```md
# Server SRS

| Field | Value |
|---|---|
| Document Type | scope_srs |
| Scope | SRV |
| Scope Name | Server |
| Version | 1.0.0 |
| Last Updated | 2026-05-08 |

## 1. Scope Overview

설치형 Todo 서버 요구사항을 정의한다.

## 2. Scope Boundaries

### In Scope

- Todo CRUD API

### Out of Scope

- 외부 동기화

## 3. Assumptions and Constraints

- 설치형 배포는 단일 사용자 로컬 환경을 기본으로 한다.

## 4. Requirements

{{REQUIREMENT_BLOCKS}}

## 5. Cross-scope Dependencies

| From | To | Relation | Notes |
|---|---|---|---|

## 6. Open Questions

| ID | Question | Impact | Status |
|---|---|---|---|

## 7. Change Notes

| Date | Change | Reason |
|---|---|---|
| 2026-05-08 | Created | integration fixture |
```

`20.web-page.srs.md`는 `# Web Page SRS`, `Scope | WEB |`, `Scope Name | Web Page |` 값을 사용하고 같은 section 순서를 유지한다. Web Page 문서의 `## 5. Cross-scope Dependencies` table에는 `| WEB | SRV | depends_on | Todo 웹 페이지는 Todo REST API 계약을 사용한다. |` row를 둔다.

Requirement Block template은 모든 고정 section을 포함한다. 아래 block을 `FR-SRV-001`의 exact template으로 사용한다.

```md
### FR-SRV-001 — Todo 서버 CRUD 제공

| Field | Value |
| --- | --- |
| Type | functional |
| Target | todo-installable-v1 |
| Status | planned |
| Priority | high |
| Tags | todo, server |
| Risk | medium |
| Stability | stable |
| Verification Method | test |
| GitHub Issue | - |
| Related Docs | - |

#### Requirement

설치형 Todo 서버는 Todo 항목을 생성, 조회, 수정, 삭제할 수 있어야 한다.

#### Rationale

설치형 Todo 서비스의 서버는 웹 페이지가 사용할 기본 데이터 동작을 제공해야 한다.

#### Acceptance Criteria

- [ ] AC-1: 새 Todo 항목을 생성할 수 있다.
- [ ] AC-2: 저장된 Todo 항목 목록을 조회할 수 있다.
- [ ] AC-3: 기존 Todo 항목의 제목과 완료 여부를 수정할 수 있다.
- [ ] AC-4: 기존 Todo 항목을 삭제할 수 있다.

#### Verification Evidence

| Evidence ID | Type | Reference | Covers | Notes |
| --- | --- | --- | --- | --- |

#### Trace Links

| Type | Reference | Relation | Notes |
| --- | --- | --- | --- |

#### Research / Analysis

- -

#### Implementation Notes

- Todo 항목은 설치형 로컬 저장소에 보관한다고 가정한다.

#### Change Notes

| Date | Change | Reason |
| --- | --- | --- |
| 2026-05-08 | Created | integration fixture |
```

나머지 초기 block 입력은 아래 표의 값을 그대로 사용한다.

| ID | Title | Type | Requirement | Acceptance Criteria | Trace Links |
|---|---|---|---|---|---|
| `IR-SRV-001` | Todo REST API 계약 제공 | `interface` | 설치형 Todo 서버는 웹 페이지가 호출할 Todo REST API 계약을 제공해야 한다. | `AC-1: GET /todos는 Todo 목록을 반환한다.`; `AC-2: POST /todos는 새 Todo를 생성한다.` | `Requirement FR-SRV-001 supports server behavior` |
| `FR-WEB-001` | Todo 목록과 생성 UI 제공 | `functional` | Todo 웹 페이지는 Todo 목록을 표시하고 새 Todo를 생성할 수 있어야 한다. | `AC-1: 사용자는 Todo 목록을 볼 수 있다.`; `AC-2: 사용자는 새 Todo 제목을 입력해 항목을 추가할 수 있다.` | `Requirement IR-SRV-001 depends_on API contract` |

두 block 모두 `Rationale`, `Verification Evidence`, `Research / Analysis`, `Implementation Notes`, `Change Notes` section을 위 template과 같은 형식으로 채운다. `IR-SRV-001`과 `FR-WEB-001`의 Change Notes row도 `2026-05-08 | Created | integration fixture`로 고정한다.

#### Red 단계

`createTodoWorkspace()`를 호출해 세 SRS Markdown 파일, rule file, UTF-8 한글 문자열을 기대하는 테스트를 먼저 작성한다. helper가 없으므로 `ReferenceError` 또는 import/compile failure로 실패해야 한다.

#### Green 단계

테스트 파일 내부에 helper와 template renderer를 추가해 temp workspace를 생성하고, `omitScopeMap` 옵션까지 구현한다.

#### Refactor 단계

중복 문자열을 `TODO_TARGET`, `SERVER_SCOPE`, `WEB_SCOPE` 같은 테스트 파일 내부 상수로 정리한다. production source file은 수정하지 않는다.

#### Rationale

동적 workspace는 사용자가 요청한 가상 프로젝트 생성 흐름과 일치한다. 고정 fixture는 장기 회귀 자산으로는 유효하지만, 이 작업에서는 temp root 격리와 UTF-8 write path를 함께 검증하는 편이 더 직접적이다.

#### 함정 / 주의사항

- Requirement heading의 구분자는 ASCII hyphen이 아니라 em dash `—`다.
- `Scope Map`의 Prefix와 ID 중간 segment가 다르면 validator가 error를 낸다.
- 초기 요구사항 status는 `planned`로 둔다. `verified`는 checked AC와 evidence를 요구한다.
- `Trace Links`에서 `Requirement` reference를 쓰면 존재하는 ID만 적는다.

#### 테스트 작성 지침

테스트 파일 `test/integration/todo-installable-workflow.test.ts`에 다음 성공, 실패, 경계 시나리오를 넣는다.

- 성공: temp root에 세 SRS Markdown 파일과 `docs/rule/SRS-MD-Rules-v1.0.0.md`가 생성되고 한글 제품명이 UTF-8로 읽힌다.
- 실패: `createTodoWorkspace({ omitScopeMap: true })`가 Scope Map 없는 index를 만들 수 있어야 하며, 이 변형은 TASK-P1-002의 validation error assertion에서 사용한다.
- 경계: Server와 Web Page 문서가 모두 `*.srs.md` 확장자를 가져 discovery 대상에 들어간다.

#### 검증 명령어

```powershell
npx vitest run test/integration/todo-installable-workflow.test.ts --hookTimeout 60000 --testTimeout 60000 --no-file-parallelism
```

#### acceptance_tests

```yaml
acceptance_tests:
  - {shell: "pwsh", cmd: "npx vitest run test/integration/todo-installable-workflow.test.ts --hookTimeout 60000 --testTimeout 60000 --no-file-parallelism", expected_exit: 0}
  - {shell: "bash", cmd: "npx vitest run test/integration/todo-installable-workflow.test.ts --hookTimeout 60000 --testTimeout 60000 --no-file-parallelism", expected_exit: 0}
```

#### DoD

- `createTodoWorkspace()`가 temp root path를 반환한다.
- 생성된 index 파일은 `설치형 Todo 리스트 관리 웹서비스` 문자열을 UTF-8로 보존한다.
- 세 SRS 파일 경로가 모두 `docs/spec/` 아래에 존재하고 rule file은 `docs/rule/` 아래에 존재한다.

#### rollback

```yaml
rollback: {strategy: "manual", command: "1) Inspect git diff for test/integration/todo-installable-workflow.test.ts. 2) Remove only the new test file changes with the editor, then run npm run test:integration."}
```

### TASK-P1-002 — Core parser, validator, query baseline 검증

| Field | Value |
|---|---|
| 관련 SRS 참조 (partial) | `FR-PARSE-001`, `FR-PARSE-002`, `FR-PARSE-003`, `FR-PARSE-004`, `FR-PARSE-005`, `FR-PARSE-006`, `FR-PARSE-007`, `FR-PARSE-008`, `FR-PARSE-009`, `FR-PARSE-010` |
| Coverage Type | partial-reference |
| 파일 경로 | 생성/수정: `test/integration/todo-installable-workflow.test.ts` |
| 메서드/함수 시그니처 | `it("parses and validates a generated two-scope Todo SRS workspace", async () => Promise<void>)` |
| 참고 패턴 | `src/core/parser/discover.ts:26`, `src/core/parser/index-parser.ts:14`, `src/core/parser/block-scanner.ts:15`, `src/core/validator/rules.ts:26`, `src/core/query/lookup.ts:8` |
| source_anchors | `["src/core/parser/discover.ts:26", "src/core/parser/index-parser.ts:14", "src/core/parser/block-scanner.ts:15", "src/core/validator/rules.ts:26", "src/core/query/lookup.ts:8"]` |
| 예상 소요 | 1시간 |

#### 구현 가이드

1. `const projectRoot = await resolveProjectRoot(rootPath, rootPath)`를 호출한다.
2. `const workspace = await parseWorkspace(projectRoot)`를 호출한다.
3. `workspace.index.targets`에 `todo-installable-v1`이 있는지 검증한다.
4. `workspace.index.scopes`에 `SRV`, `WEB` prefix가 있는지 검증한다.
5. `workspace.records.map((record) => record.id)`가 `FR-SRV-001`, `IR-SRV-001`, `FR-WEB-001`을 포함하는지 검증한다.
6. `FR-SRV-001` record의 `type`, `target`, `status`가 각각 `functional`, `todo-installable-v1`, `planned`인지 검증한다.
7. `FR-SRV-001` record의 `requirement` text가 `Todo 항목을 생성, 조회, 수정, 삭제` 문구를 포함하는지 검증한다.
8. `FR-SRV-001` record의 `acceptanceCriteria`가 4개이고 첫 항목이 unchecked `AC-1: 새 Todo 항목을 생성할 수 있다.`인지 검증한다.
9. `FR-SRV-001` record의 `verificationEvidence`가 빈 배열인지 검증해 empty evidence table이 parse-safe인지 확인한다.
10. `IR-SRV-001` traceLinks가 `Requirement FR-SRV-001` reference를 포함하고, `FR-WEB-001` traceLinks가 `Requirement IR-SRV-001` reference를 포함하는지 검증한다.
11. `validateWorkspace(workspace).errors` 길이가 0인지 검증한다.
12. `listRequirements(workspace, { scope: "WEB" })`가 `FR-WEB-001`만 반환하고 해당 normalized record의 `id`, `scope`, `target`, `status`가 기대값인지 검증한다.
13. `listRequirements(workspace, { scope: "SRV" })`가 `FR-SRV-001`, `IR-SRV-001`을 반환하고 각 normalized record의 `id`, `scope`, `target`, `status`가 기대값인지 검증한다.

#### Red 단계

`parseWorkspace`, `validateWorkspace`, `listRequirements` assertion을 먼저 추가하고 target/scope/ID/metadata/AC/evidence/trace/negative Scope Map 검증이 실패하는 상태를 확인한다.

#### Green 단계

TASK-P1-001 helper의 template을 보완해 core parser가 세 요구사항의 metadata, requirement text, AC, evidence table, trace links와 Scope Map missing diagnostic을 모두 관찰하도록 만든다.

#### Refactor 단계

`expectRequirementIds(records, expected)`와 `expectValidWorkspace(rootPath)` 같은 테스트 내부 helper로 반복 assertion을 줄인다.

#### Rationale

CLI와 MCP 검증 전에 core parser와 validator가 동일 SRS를 올바르게 해석하는지 고정해야 한다. core 검증이 먼저 실패하면 adapter assertion은 원인 파악을 흐린다.

#### 함정 / 주의사항

- `listRequirements`는 ID 기준 정렬을 수행한다. 반환 순서를 assert할 때 이 정렬을 기준으로 둔다.
- validator warning은 이 테스트의 실패 조건이 아니다. errors가 0인지 먼저 확인하고, warnings는 등록되지 않은 target 같은 회귀가 나오지 않도록 빈 배열을 기대해도 된다.
- Requirement `Scope` metadata는 생략 가능하지만 ID segment와 Scope Map prefix로 scope가 계산된다.

#### 테스트 작성 지침

- 성공: 세 요구사항이 정상 파싱되고 metadata, requirement text, AC, empty evidence table, trace links, normalized list fields가 기대값이며 validation errors가 0이다.
- 실패: `createTodoWorkspace({ omitScopeMap: true })`로 만든 workspace에서 `validateWorkspace(...).errors` 또는 parser diagnostics가 `SRS-E014` Scope Map missing error를 포함하는지 검증한다.
- 경계: 두 scope 문서가 모두 discovery 대상이며 scope filter가 정확히 분리된다.

#### 검증 명령어

```powershell
npx vitest run test/integration/todo-installable-workflow.test.ts --hookTimeout 60000 --testTimeout 60000 --no-file-parallelism
```

#### acceptance_tests

```yaml
acceptance_tests:
  - {shell: "pwsh", cmd: "npx vitest run test/integration/todo-installable-workflow.test.ts --hookTimeout 60000 --testTimeout 60000 --no-file-parallelism", expected_exit: 0}
  - {shell: "bash", cmd: "npx vitest run test/integration/todo-installable-workflow.test.ts --hookTimeout 60000 --testTimeout 60000 --no-file-parallelism", expected_exit: 0}
```

#### DoD

- core parsing assertion이 target, scope, ID, metadata, requirement text, AC, empty evidence table, trace links, scope filter를 모두 검증한다.
- `listRequirements` assertion이 returned record의 `id`, `scope`, `target`, `status` normalized fields를 검증한다.
- `validateWorkspace(workspace).errors`가 0이다.
- production source file 변경 없이 테스트 파일만으로 baseline 검증이 가능하다.

#### rollback

```yaml
rollback: {strategy: "manual", command: "1) Inspect git diff for test/integration/todo-installable-workflow.test.ts. 2) Remove only the new test file changes with the editor, then run npm run test:integration."}
```

### TASK-P1-003 — CLI read workflow 검증

| Field | Value |
|---|---|
| 관련 SRS 참조 (partial) | `IR-CLI-004`, `IR-CLI-006`, `IR-CLI-007`, `IR-CLI-015`, `IR-CLI-018` |
| Coverage Type | partial-reference |
| 파일 경로 | 생성/수정: `test/integration/todo-installable-workflow.test.ts` |
| 메서드/함수 시그니처 | `async function runCliJson(args: string[]): Promise<{ code: number; json: unknown; stdout: string; stderr: string }>`; `it("reads the generated Todo SRS through CLI JSON commands", async () => Promise<void>)` |
| 참고 패턴 | `src/cli/index.ts:11`, `src/cli/formatters.ts:5`, `test/cli/read-commands.test.ts:6` |
| source_anchors | `["src/cli/index.ts:11", "src/cli/formatters.ts:5", "test/cli/read-commands.test.ts:6"]` |
| 예상 소요 | 1시간 |

#### 구현 가이드

1. `Writable` 또는 `PassThrough` 기반 capture stream을 만든다.
2. `runCliJson(["--root", rootPath, "validate", "--json"])`를 실행하고 exit code 0과 JSON parse 성공을 검증한다.
3. `list --scope WEB --json` 결과가 `FR-WEB-001`을 반환하는지 검증한다.
4. `show FR-SRV-001 --json --markdown` 결과에 `markdown`이 있고 Todo server requirement text가 들어 있는지 검증한다.
5. `summary --target todo-installable-v1 --json` 결과 total이 3이고 planned count가 3인지 검증한다.
6. `show MISSING --json`이 non-zero exit code와 stderr message를 반환하고 stdout JSON을 만들지 않는지 검증한다.

#### Red 단계

CLI command assertion을 먼저 작성해 `runCliJson` helper 부재와 missing ID error capture 부재로 실패하는지 확인한다.

#### Green 단계

chunk 누적 stdout/stderr capture helper를 구현하고 validate/list/show/summary/missing show command를 모두 실행한다.

#### Refactor 단계

CLI JSON payload access를 좁은 type guard 또는 local cast helper로 정리하고, stderr assertion을 한 곳에서 수행한다.

#### Rationale

CLI는 사용자가 가장 쉽게 재현할 수 있는 interface다. core 검증과 같은 temp root를 사용해 adapter가 core service를 우회하지 않는지 확인한다.

#### 함정 / 주의사항

- stdout capture는 JSON 한 줄만 전제하지 않고 chunk를 누적한다.
- stderr가 비어 있거나 command error가 없는지 함께 확인한다.
- CLI command의 root option은 모든 호출에 포함한다.

#### 테스트 작성 지침

- 성공: validate/list/show/summary JSON이 모두 parse되고 expected domain values를 반환한다.
- 실패: 없는 ID를 `show`하면 exit code가 0이 아니고 stderr에 `Requirement not found`가 포함된다.
- 경계: `show --markdown`이 raw Markdown을 포함하는지 확인한다.

#### 검증 명령어

```powershell
npx vitest run test/integration/todo-installable-workflow.test.ts --hookTimeout 60000 --testTimeout 60000 --no-file-parallelism
```

#### acceptance_tests

```yaml
acceptance_tests:
  - {shell: "pwsh", cmd: "npx vitest run test/integration/todo-installable-workflow.test.ts --hookTimeout 60000 --testTimeout 60000 --no-file-parallelism", expected_exit: 0}
  - {shell: "bash", cmd: "npx vitest run test/integration/todo-installable-workflow.test.ts --hookTimeout 60000 --testTimeout 60000 --no-file-parallelism", expected_exit: 0}
```

#### DoD

- CLI validate/list/show/summary commands are covered in the Todo integration test.
- Successful CLI command assertions check exit code 0 and parseable JSON; the missing-ID assertion checks non-zero exit and stderr.
- The test does not compare CLI and MCP full serialized responses.

#### rollback

```yaml
rollback: {strategy: "manual", command: "1) Inspect git diff for test/integration/todo-installable-workflow.test.ts. 2) Remove only the new test file changes with the editor, then run npm run test:integration."}
```

### TASK-P1-004 — MCP read and add_requirement workflow 검증

| Field | Value |
|---|---|
| 관련 SRS 참조 (partial) | `FR-MCP-001`, `FR-MCP-002`, `FR-MCP-003`, `FR-MCP-008`, `FR-MCP-010`, `FR-NODE-008` |
| Coverage Type | partial-reference |
| 파일 경로 | 생성/수정: `test/integration/todo-installable-workflow.test.ts` |
| 메서드/함수 시그니처 | `function createTodoMcpServer(root: string): McpServerHandle`; `it("reads, mutates, and revalidates the generated Todo SRS through MCP tools", async () => Promise<void>)` |
| 참고 패턴 | `src/mcp/adapter.ts:15`, `src/mcp/tools/read-tools.ts:13`, `src/mcp/tools/mutation-tools.ts:37`, `src/core/mutation/add-requirement.ts:26`, `src/core/mutation/add-requirement.ts:115` |
| source_anchors | `["src/mcp/adapter.ts:15", "src/mcp/tools/read-tools.ts:13", "src/mcp/tools/mutation-tools.ts:37", "src/core/mutation/add-requirement.ts:26", "src/core/mutation/add-requirement.ts:115"]` |
| 예상 소요 | 1.5시간 |

#### 구현 가이드

1. `const server = createTestMcpServer({ root: rootPath })`를 만든다.
2. `registerReadTools(server, { root: rootPath })`와 `registerMutationTools(server, { root: rootPath })`를 호출한다.
3. `list_requirements` with `{ scope: "SRV" }`가 `FR-SRV-001`, `IR-SRV-001`을 반환하는지 검증한다.
4. `get_requirement` with `{ id: "FR-WEB-001", includeMarkdown: true }`가 markdown을 포함하는지 검증한다.
5. `validate_spec`이 errors 0을 반환하는지 검증한다.
6. `summarize_target` with `{ target: "todo-installable-v1" }`이 total 3을 반환하는지 검증한다.
7. `get_requirement` with `{ id: "MISSING", includeMarkdown: true }`가 `{ ok: false, error: { message: string } }` shape를 반환하는지 검증한다. 이 계획은 MCP canonical error code taxonomy를 검증하지 않는다.
8. invalid `add_requirement` without `requirement`를 호출해 `{ ok: false, error: { message: string } }` shape를 반환하고 `20.web-page.srs.md`가 변경되지 않는지 검증한다.
9. `add_requirement` with the exact input below를 호출한다.
10. mutation result의 `value.requirementId`가 `FR-WEB-002`, `value.filePath`가 `docs/spec/20.web-page.srs.md`, `value.written`이 true인지 검증한다.
11. `20.web-page.srs.md`를 UTF-8로 읽어 `### FR-WEB-002 — 완료 필터 UI 제공` heading과 fixed Change Notes row가 있는지 확인한다.

```ts
{
  type: "functional",
  scope: "WEB",
  target: "todo-installable-v1",
  title: "완료 필터 UI 제공",
  requirement: "Todo 웹 페이지는 완료 여부에 따라 Todo 항목을 필터링할 수 있어야 한다.",
  acceptanceCriteria: ["완료된 Todo만 볼 수 있다."],
  rationale: "설치형 Todo 사용자는 완료된 작업과 남은 작업을 분리해 확인해야 한다.",
  changeNotes: "2026-05-08 | Added | integration test"
}
```

#### Red 단계

MCP read/mutation assertion을 먼저 작성해 `createTodoMcpServer` helper와 full add input이 없어서 실패하는 상태를 확인한다.

#### Green 단계

`createTestMcpServer`, `registerReadTools`, `registerMutationTools`를 등록하고 read, negative get, invalid add, valid add assertions을 통과시킨다.

#### Refactor 단계

MCP result narrowing helper를 테스트 내부에 두어 `{ ok: true }`와 `{ ok: false }` branch 검증을 반복하지 않게 만든다.

#### Rationale

MCP read and mutation tools are the agent-facing interface. The test must prove they share the same generated SRS workspace and can write a new Requirement Block into the selected scope.

#### 함정 / 주의사항

- MCP dependencies root is fixed at server creation time. Tool input `root` should not be used to switch roots.
- `add_requirement` full response is not identical to CLI mutation JSON because MCP omits patch and diagnostics.
- `changeNotes` must be set to `2026-05-08 | Added | integration test` so the written file is date-stable.

#### 테스트 작성 지침

- 성공: MCP read tools return baseline values and `add_requirement` creates `FR-WEB-002`.
- 실패: missing requirement lookup and invalid `add_requirement` both return `{ ok: false, error: { message: string } }`; invalid `add_requirement` does not change `20.web-page.srs.md`.
- 경계: mutation target must be Web Page document, not Server document.

#### 검증 명령어

```powershell
npx vitest run test/integration/todo-installable-workflow.test.ts --hookTimeout 60000 --testTimeout 60000 --no-file-parallelism
```

#### acceptance_tests

```yaml
acceptance_tests:
  - {shell: "pwsh", cmd: "npx vitest run test/integration/todo-installable-workflow.test.ts --hookTimeout 60000 --testTimeout 60000 --no-file-parallelism", expected_exit: 0}
  - {shell: "bash", cmd: "npx vitest run test/integration/todo-installable-workflow.test.ts --hookTimeout 60000 --testTimeout 60000 --no-file-parallelism", expected_exit: 0}
```

#### DoD

- MCP read tools cover list, get, validate, and summarize.
- MCP mutation adds exactly `FR-WEB-002` to `docs/spec/20.web-page.srs.md`.
- The written requirement includes fixed Change Notes text.
- The test avoids full CLI/MCP response parity assertion.

#### rollback

```yaml
rollback: {strategy: "manual", command: "1) Inspect git diff for test/integration/todo-installable-workflow.test.ts. 2) Remove only the new test file changes with the editor, then run npm run test:integration."}
```

### TASK-P1-005 — Mutation 후 validation과 후속 제외 항목 고정

| Field | Value |
|---|---|
| 관련 SRS 참조 (partial) | `FR-MCP-003`, `FR-MCP-008`, `FR-MCP-010`, `FR-NODE-008` |
| Coverage Type | partial-reference |
| 파일 경로 | 생성/수정: `test/integration/todo-installable-workflow.test.ts` |
| 메서드/함수 시그니처 | `async function expectValidWorkspace(rootPath: string): Promise<void>`; implemented inside the same `it("reads, mutates, and revalidates the generated Todo SRS through MCP tools", async () => Promise<void>)` used by TASK-P1-004 |
| 참고 패턴 | `src/mcp/errors.ts:12`, `src/core/mutation/render-requirement.ts:95`, `test/integration/cli-mcp-parity.test.ts:6` |
| source_anchors | `["src/mcp/errors.ts:12", "src/core/mutation/render-requirement.ts:95", "test/integration/cli-mcp-parity.test.ts:6"]` |
| 예상 소요 | 45분 |

#### 구현 가이드

1. TASK-P1-004와 같은 `it` 안에서 MCP mutation 후 `parseWorkspace`를 다시 호출한다. 별도 `it`에서 TASK-P1-004의 temp root 상태에 의존하지 않는다.
2. `validateWorkspace(nextWorkspace).errors`가 0인지 검증한다.
3. `listRequirements(nextWorkspace, { scope: "WEB" })`가 `FR-WEB-001`, `FR-WEB-002`를 반환하는지 검증한다.
4. `summarize_target` 또는 CLI `summary --target todo-installable-v1 --json` 결과 total이 4인지 검증한다.
5. 테스트 이름이나 assertion message에 full response parity를 요구하지 않는다고 명시한다.
6. 파일 주석은 남기지 않는다. 후속 제외 항목은 계획 문서에만 둔다.

#### Red 단계

MCP mutation 직후 재파싱과 target summary total 4 assertion을 먼저 추가해 post-validation helper 부재로 실패하는지 확인한다.

#### Green 단계

TASK-P1-004와 같은 `it` 안에서 valid add 후 `parseWorkspace`, `validateWorkspace`, `listRequirements`, `summarizeTarget`을 다시 실행한다.

#### Refactor 단계

post-mutation assertion을 `expectValidWorkspace(rootPath)`로 묶되, 같은 test case 안의 root만 인자로 넘겨 cross-test state를 만들지 않는다.

#### Rationale

mutation 성공만 확인하면 written block이 전체 SRS set을 깨뜨렸는지 놓칠 수 있다. mutation 후 재검증이 있어야 add-requirement workflow의 실제 가치가 보인다.

#### 함정 / 주의사항

- `FR-MCP-010`은 성공 시 patch summary 반환을 요구하지만, 이 Todo 통합 테스트는 해당 AC를 커버하지 않는다. 이 계획은 generated record와 file side effect만 검증한다.
- MCP `add_verification_evidence.notes` 전달 누락은 별도 후속으로 둔다.
- 한 테스트 안에서 같은 temp root에 여러 add-requirement를 반복하면 generated ID가 증가한다. `FR-WEB-002` assertion은 mutation 호출 횟수를 하나로 유지해야 안정적이다.

#### 테스트 작성 지침

- 성공: mutation 후 workspace가 valid이고 total count가 4다.
- 실패: invalid `add_requirement` 결과가 ok false이고, invalid call 전후 `20.web-page.srs.md` content가 동일한지 확인한다.
- 경계: generated ID numbering이 `FR-WEB-002`로 고정되는지 확인한다.

#### 검증 명령어

```powershell
npx vitest run test/integration/todo-installable-workflow.test.ts --hookTimeout 60000 --testTimeout 60000 --no-file-parallelism
```

#### acceptance_tests

```yaml
acceptance_tests:
  - {shell: "pwsh", cmd: "npx vitest run test/integration/todo-installable-workflow.test.ts --hookTimeout 60000 --testTimeout 60000 --no-file-parallelism", expected_exit: 0}
  - {shell: "bash", cmd: "npx vitest run test/integration/todo-installable-workflow.test.ts --hookTimeout 60000 --testTimeout 60000 --no-file-parallelism", expected_exit: 0}
```

#### DoD

- Mutation 후 core validation errors remain 0.
- Web Page scope has exactly two requirements after mutation.
- Target summary total is 4 after mutation.
- The test file does not count MCP evidence notes, canonical error taxonomy, or `add_requirement` patch summary AC as covered.

#### rollback

```yaml
rollback: {strategy: "manual", command: "1) Inspect git diff for test/integration/todo-installable-workflow.test.ts. 2) Remove only the new test file changes with the editor, then run npm run test:integration."}
```

## 4. Phase 테스트 전략

Mock을 사용하지 않는다. The test calls production parser, validator, query, CLI entrypoint, and MCP test adapter directly. The generated Todo SRS is a real Markdown SRS set on disk under a temp root.

## 5. Phase 완료 조건

- All TASK DoD items pass.
- `npx vitest run test/integration/todo-installable-workflow.test.ts --hookTimeout 60000 --testTimeout 60000 --no-file-parallelism` exits 0.
- `npm run test:integration` exits 0.
- `npm run lint` exits 0 after the new test file is added.

## 6. 후속 제외 항목

| Excluded Item | Reason |
|---|---|
| MCP `add_verification_evidence.notes` propagation | Separate mutation parity bug or enhancement |
| MCP `add_requirement` patch summary AC and diagnostics preservation | `FR-MCP-010`에 존재하는 계약이지만, 이 Todo workflow의 부분 통합 범위 밖이다. 전용 MCP response-contract 테스트 또는 remediation 계획에서 다룬다. |
| Fixed Todo fixture directory | Dynamic workspace gives stronger isolation for this request |
