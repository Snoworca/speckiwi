# Local/generated archives
*.zip

# Dependencies and local execution state
node_modules/
.snoworca/
kiwi/.pipeline-path
kiwi/.status.json
kiwi/.srs.lock
kiwi/.srs.lock.guard
# Orchestrator machine state: `git clean -fd` may remove it and `git add -A` must never stage it.
kiwi/waves.jsonl
kiwi/orchestrator/

# Build output (regenerated via `npm run build` / `prepack`)
dist/

# OS/editor noise
.DS_Store
Thumbs.db

# Local runtime/session state and tool caches
.kiwi/
.serena/

# Atomic-write scratch files left beside an SRS document when a mutation races another
# writer. Committed twice by `git add -A` before this entry existed.
docs/spec/.speckiwi-*.tmp
